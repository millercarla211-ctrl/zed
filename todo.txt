IN PROGRESS:
- None

PENDING:
- Manual validation with `just run` for the merge
- Test all custom features (space carousel, web preview, liquid glass)
- Test upstream features (terminal actions, history toggle, bottom dock layouts)

COMPLETED:
- ✅ Renamed old main branch to deprecated-4
- ✅ Created new main branch tracking upstream/main
- ✅ Merged main (upstream) into forge with all conflicts resolved
- ✅ Kept BOTH local and remote changes in all conflicts
- ✅ Documented all merge resolutions in changelog.txt
- ✅ Created git-workflow.md steering file documenting the branch workflow
- ✅ Committed merge: "Merge main (upstream) into forge: resolved all conflicts keeping both changes"
- ✅ Fixed Windows drag-drop compilation errors in gpui_windows crate
- ✅ Removed the deprecated Windows simple-Wry web preview path and render-mode toggle
- ✅ Restored native WebView2 focus, keyboard, and IME routing for the hole-punched Windows preview
- ✅ Forwarded Windows OLE drag-and-drop events into the composition-hosted WebView2 preview
- ✅ Removed the unused `web_preview_windows` workspace crate and stale host-input shim references
- ✅ Reviewed macOS/Linux host activation and focus paths for parity risks

BLOCKED:
- Windows touch/pen forwarding is still a larger follow-up because the current host layer has no `WM_POINTER` -> `SendPointerInput` bridge
