# DX/Zed Launch Worker Notes

Date: 2026-05-21
Launch target: 2026-05-22

## Worker Role

This checkout is the Zed/DX editor surface. Worker chats here should focus on GPUI integration, AI panel polish, DX Agents bridge hooks, token meters, source rails, progress rails, and launch-safe runtime wiring.

## Required Reading

- `G:\Dx\WORKER_PROMPTS.md`
- `G:\Dx\DX.md`
- `G:\Dx\zed\AGENTS.md`
- `G:\Dx\zed\PLAN.md`

## Hard Rules

- Use `[@superpowers](plugin://superpowers@openai-curated)` in new worker chats.
- Preserve existing Zed AI behavior.
- Do not create dummy UI.
- Do not start local servers.
- Do not run `just run`, full Cargo builds, or expensive workspace checks until the user opens the governed final validation window.
- Prefer code reading, targeted implementation, `git diff --check`, conflict-marker search, and narrow checks after coherent milestones.
- Update `todo.txt`, `changelog.txt`, and this file when changing launch status.

## Current Launch Targets

- Add `Agent` alongside existing AI actions like `Write` and `Ask`.
- Add left Sources rail and right Progress/Git/Background Tasks rail.
- Add New Chat, Search, Plugins, and Automations actions.
- Add Pinned and All Chats workspace groups.
- Wire DX token/RLM/serializer receipts into token/tool meters.
- Wire DX Agents CLI receipts, social/account readiness, automations, and provider/model catalog readiness into GPUI status surfaces.

## Current Worker Update

- Hardened DX Studio operation picker numeric safety: `crates/web_preview/src/dx_studio_bridge/capture.ts` now requires the parsed edit-operation index to be a safe JavaScript integer before selecting an operation, so oversized digit strings are refused instead of relying on rounded `parseInt` output. Extended `script/dx-source-quality.test.ts` with a red/green source guard that keeps blank, malformed, and unsafe numeric answers on the refusal/restore path while still parsing the assembled bridge script. Passed: red/green `node --test script/dx-source-quality.test.ts`. Skipped by policy: Cargo build/check/test, `just run`, local servers, browser automation, live WebView runtime, cloud writes, and deploy execution. Exact next action: verify the operation picker refusal labels and receipt-backed overlay preservation in the governed native Web Preview proof window.

- Hardened DX Studio operation picker cancellation: `crates/web_preview/src/dx_studio_bridge/capture.ts` now trims edit-operation prompt answers, restores the previous receipt-backed selection for blank/whitespace answers, and refuses malformed numeric text instead of silently treating it as operation `0`. Extended `script/dx-source-quality.test.ts` with a source guard that also parses the assembled bridge script. Passed: red/green `node --test script/dx-source-quality.test.ts`. Skipped by policy: Cargo build/check/test, `just run`, local servers, browser automation, and live WebView runtime proof. Exact next action: verify the edit-operation prompt preserves the current overlay/receipt state in the governed native Web Preview proof window.

- Split DX launch context prompt ownership: `crates/agent_ui/src/dx_launch_prompts/context.rs` now owns launch status, contract, readiness, audit, DX-WWW evidence, source-audit, receipt-review, and bounded-list summary formatting, while `crates/agent_ui/src/dx_launch_prompts.rs` stays as a 193-line launch prompt coordinator. Bounded prompt lists now append `+N more` instead of silently dropping overflow rows. Expanded `script/dx-launch-prompts-source.test.ts` so future workers keep context formatting, Forge, source-action, and runtime-proof wording out of the parent prompt file. Passed: red/green `node --test script/dx-launch-prompts-source.test.ts` and targeted `rustfmt --edition 2024 --check`. Skipped by policy: Cargo build/check/test, `just run`, local servers, browser automation, and live editor runtime. Exact next action: keep this guard green when launch receipt fields or source-owned audit summary wording changes.

- Added direct edge-case unit coverage for Deploy receipt root keys: `crates/agent_ui/src/dx_deploy_root_key.rs` now collapses repeated Windows separators while preserving drive roots and UNC prefixes, and carries focused `rustc --test` coverage for mixed slashes, repeated separators, drive-root preservation, UNC-prefix preservation, and non-Windows case-sensitive trailing-slash trimming. This keeps long-running Zed Deploy/Check receipt scans from treating `G:\Dx`, `G://Dx`, and similar spellings as separate roots. Passed: red/green direct `rustc --edition=2024 --test crates\agent_ui\src\dx_deploy_root_key.rs` and targeted `rustfmt --edition 2024`. Skipped by policy: Cargo build/check/test, `just run`, local servers, browser automation, live editor runtime, cloud writes, and deploy execution. Exact next action: add fixture-backed ordering coverage for duplicate workspace/hub check and deploy receipt roots when an approved targeted Cargo/native proof window is open.

- Hardened DX Deploy receipt root de-duplication: `crates/agent_ui/src/dx_deploy_root_key.rs` now owns the shared platform-aware root key used by both `dx_deploy_check_roots.rs` and `dx_deploy_receipt_roots.rs`, so workspace/DX hub/www `.dx\receipts\check` and `.dx\receipts\deploy` roots avoid duplicate IO without two normalization copies. Windows folds case, slash spelling, and trailing separators; non-Windows stays case-sensitive and trims only trailing `/`. Expanded `script/dx-deploy-panel-source.test.ts` with the shared de-dupe source guard. Passed: `node --test script/dx-deploy-panel-source.test.ts` and targeted `rustfmt --edition 2024`. Skipped by policy: Cargo build/check/test, `just run`, local servers, browser automation, live editor runtime, cloud writes, and deploy execution. Exact next action: add fixture-backed ordering coverage for duplicate workspace/hub check and deploy receipt roots during an approved targeted Cargo/native proof window.

- Split DX launch Forge prompt ownership: `crates/agent_ui/src/dx_launch_prompts/forge.rs` now owns Forge proof, restore approval, and Forge history summary prompt context, while `crates/agent_ui/src/dx_launch_prompts.rs` stays as a 529-line launch prompt coordinator and re-exports the existing Forge prompt helpers. Expanded `script/dx-launch-prompts-source.test.ts` so future workers keep Forge, source-action, and runtime-proof wording out of the parent prompt file. Passed: red/green `node --test script/dx-launch-prompts-source.test.ts` and targeted `rustfmt --edition 2024 --check`. Skipped by policy: Cargo build/check/test, `just run`, local servers, browser automation, and live editor runtime. Exact next action: keep this guard green when Forge history receipts, restore approval wording, or restore-target proof language changes.

- Split DX launch source prompt ownership: `crates/agent_ui/src/dx_launch_prompts/source.rs` now owns source action icons, labels, source receipt review text, and source attachment/review prompt wording, while `crates/agent_ui/src/dx_launch_prompts.rs` stays as the launch prompt coordinator that re-exports the existing public helpers. Expanded `script/dx-launch-prompts-source.test.ts` so future workers keep Forge, source-action, and runtime-proof wording out of the parent prompt file. Passed: red/green `node --test script/dx-launch-prompts-source.test.ts` and targeted `rustfmt --edition 2024 --check`. Skipped by policy: Cargo build/check/test, `just run`, local servers, browser automation, and live editor runtime. Exact next action: keep this guard green when source attachment kinds, receipt drilldowns, or guarded source prompt wording changes.

- Split DX launch runtime-proof prompt ownership: `crates/agent_ui/src/dx_launch_prompts/runtime_proof.rs` now owns the runtime proof handoff, import, evidence-template, receipt-context, and guarded requirements wording, while `crates/agent_ui/src/dx_launch_prompts.rs` stays as the launch prompt coordinator that re-exports the existing public prompt helpers. Added `script/dx-launch-prompts-source.test.ts` so future workers keep runtime-proof wording out of the parent prompt file. Passed: red/green `node --test script/dx-launch-prompts-source.test.ts` and targeted `rustfmt --edition 2024 --check`. Skipped by policy: Cargo build/check/test, `just run`, local servers, browser automation, and live editor runtime. Exact next action: keep this source guard green when runtime proof receipt fields or governed validation wording changes.

- Wired Deploy gate quick fixes and launch-check receipt roots to the real DX ecosystem contract: `crates/agent_ui/src/dx_deploy_check_roots.rs` now reads workspace plus DX hub/www check receipts through the same `DX_HOME`, `DX_ROOT`, `D:\Dx`, and `G:\Dx` hub discovery path as Deploy receipts, `crates/agent_ui/src/dx_deploy_receipt_summary.rs` preserves `dx.deploy.zed_handoff.v1` quick-fix command, `risk_level`, approval requirement, and receipt-write flags from receipts written by the root `dx deploy` lane, and `crates/agent_ui/src/dx_deploy_gate_rail.rs` renders those safe/network actions as metadata only. This keeps native Zed as a consumer of `dx deploy` and `dx check` receipts, not the owner of deploy proof. Passed: red/green `node --test script/dx-deploy-panel-source.test.ts`. Skipped by policy: Cargo build/check/test, local servers, browser automation, cloud writes, and live deploy. Exact next action: add a tiny fixture-backed parser test for `dx.deploy.zed_handoff.v1` quick fixes during an approved targeted Cargo window.

- Split DX Check panel source ownership: `crates/agent_ui/src/dx_check_panel.rs` is now a 348-line cache/type facade, `crates/agent_ui/src/dx_check_panel/reader.rs` owns bounded receipt discovery and JSON IO, and `crates/agent_ui/src/dx_check_panel/parser.rs` owns `dx.check.zed_panel.v1` plus shared DX-WWW view-model parsing/defaults. Added `script/dx-check-panel-source.test.ts` so future workers keep receipt IO and parser logic out of the facade. Passed: red/green `node --test script/dx-check-panel-source.test.ts`, existing DX source guards, and targeted `rustfmt --edition 2024 --check`. Skipped by policy: Cargo build/check/test, `just run`, local servers, browser automation, and live editor runtime. Exact next action: keep this guard green whenever native Check rail receipt fields change, then verify the rail against a real `check-latest.json` during the governed native proof window.

- Split and hardened DX Deploy receipt root ownership: `crates/agent_ui/src/dx_deploy_hub_roots.rs` now owns DX hub discovery through `DX_HOME`, `DX_ROOT`, then the launch-rule `D:\Dx` / `G:\Dx` fallback, while `crates/agent_ui/src/dx_deploy_receipt_roots.rs` owns deploy receipt assembly, skips empty roots, and normalizes duplicate Windows root path spellings without lowercasing non-Windows paths before feeding workspace plus DX hub/cli/www receipt roots into the Deploy capability scanner. `crates/agent_ui/src/dx_deploy_capabilities.rs` stays focused on receipt discovery, ranking, parsing, and provider rows. Passed: `node --test script/dx-deploy-panel-source.test.ts`. Skipped by policy: Cargo build/check/test, local servers, browser automation, cloud writes, and live deploy. Exact next action: add fixture-backed receipt ordering coverage when a targeted Cargo test window is approved.

- Migrated the current DX Deploy panel slice from the stale `G:\Zed` checkout into canonical `G:\Dx\zed` only, preserving relative paths for the Deploy launch evidence, quick actions, bucket scores, launch outcome, launch scope, approval evidence, source guard, and status docs. No whole-repo copy, Cargo command, local server, or live deploy ran. Exact next action: continue all Zed/DX editor work from `G:\Dx\zed`.

- Added DX Deploy approval evidence rows: Zed Deploy now reads bounded `source_ready.evidence`, `runtime_approved.evidence`, and `launch_approved.evidence` arrays through `crates/agent_ui/src/dx_deploy_launch_approval_evidence.rs`, renders source runtime and launch evidence rows in the Deploy launch gate, and carries `approval_evidence=` in deploy readiness prompts. This shows which proof signals are present, such as deploy dry-run reviewed, while still keeping missing runtime proof and blocked launch approval honest. Exact next action: compare approval evidence rows against `G:\Dx\.dx\receipts\check\check-launch-latest.json` during the governed proof window.

- Added DX Deploy bucket-score breakdown: Zed Deploy now reads bounded dx-check launch buckets from `bucket_scores` through `crates/agent_ui/src/dx_deploy_launch_buckets.rs`, renders each launch bucket score/status/estimated state in the Deploy launch gate, and carries `launch_buckets=` in deploy readiness prompts. This makes the current `49/100 estimated` status explain itself through real launch buckets such as code-quality, structure, web-performance, DX framework health, and test-readiness instead of hiding behind one aggregate number. Exact next action: compare the launch buckets against `G:\Dx\.dx\receipts\check\check-launch-latest.json` during the governed proof window.

- Added DX Deploy launch scope and checked paths: Zed Deploy now reads the dx-check launch scoring profile, scoring-config status, config-present state, next action, and bounded checked paths from the selected launch receipt through `crates/agent_ui/src/dx_deploy_launch_scope.rs`. The Deploy launch gate renders Scope and Checked rows, and deploy readiness prompts include `launch_scope=` plus `checked_paths=` so the scoring profile and workspace scope are visible beside outcome counts without growing the launch-gate reader. Exact next action: compare the Scope and Checked rows against `G:\Dx\.dx\receipts\check\check-launch-latest.json` during the governed proof window.

- Split DX Deploy launch outcome ownership: launch pass/fail/warn/skipped counts, duration, and skipped expensive check names now live in `crates/agent_ui/src/dx_deploy_launch_outcome.rs` instead of growing the launch-gate reader. The Deploy launch gate still renders Outcome and Duration, now also renders the bounded skipped expensive checks, and deploy readiness prompts include `skipped_checks=` beside `launch_outcome=`. Exact next action: compare the skipped expensive checks row with `G:\Dx\.dx\receipts\check\check-launch-latest.json` during the governed proof window.

- Added DX Deploy launch outcome counts: Zed Deploy now reads dx-check `pass_count`, `fail_count`, `warn_count`, `skipped_count`, and `duration_ms` from the selected launch receipt, renders compact Outcome and Duration rows in the Deploy launch gate, and includes `launch_outcome=` plus `duration_ms=` in deploy readiness prompts. This keeps deploy promotion blocked for the right reason while showing the exact aggregate proof posture behind the current `49/100 estimated` status. Exact next action: compare Outcome and Duration against `G:\Dx\.dx\receipts\check\check-launch-latest.json` during the governed proof window.

- Added DX Deploy evidence-source blocker text: Zed Deploy now preserves bounded blocker text from each `launch_evidence_sources` row, renders the first source-owned blocker messages in the Deploy launch evidence rows, and includes `source_blockers=` in deploy readiness prompts. This keeps missing code, web-metrics, web-probe, and E2E proof reasons visible beside their stable evidence ids instead of only showing aggregate blocker counts. Exact next action: compare the per-source blocker text with `G:\Dx\.dx\receipts\check\check-launch-latest.json` during the governed proof window.

- Added DX Deploy warning evidence paths: Zed Deploy now preserves bounded dx-check launch warning rows with `severity` and `evidence_path`, renders the first warning details in the Deploy launch gate, and includes `launch_warnings=` in deploy readiness prompts. This makes missing README/TODO and large-source warning evidence visible from the same receipt path as blocker evidence without opening the raw launch receipt. Exact next action: compare the first warning rows with `G:\Dx\.dx\receipts\check\check-launch-latest.json` during the governed proof window.

- Added DX Deploy source blocker evidence paths: Zed Deploy now preserves `severity` and `evidence_path` from dx-check launch blocker rows, renders them in the Deploy launch-gate blocker detail, and includes `severity=` plus `evidence_path=` in deploy readiness prompts. This makes the current giant-source-file blockers traceable to exact source-owned paths such as the dx-agents files without opening the raw receipt. Exact next action: compare the first blocker rows with `G:\Dx\.dx\receipts\check\check-launch-latest.json` during the governed proof window.

- Hardened DX Deploy quick-action identities: the Deploy rail now carries the stable `quick-fix-*` action id from `dx check launch --json` beside each bounded launch quick action, and deploy readiness prompts include `action_id=` plus `next_action=` for the first actions. This keeps receipt-write refreshes and approval-required proof runners traceable back to the DX launch receipt instead of relying only on mutable labels. Exact next action: in the governed native proof window, compare the rendered quick-action identities with `G:\Dx\.dx\receipts\check\check-launch-latest.json`.

- Corrected DX Deploy quick-action totals: the Deploy rail now reads `quick_fix_count` from the dx-check launch receipt and labels bounded quick-action rows as shown-of-total, so the current 5 rendered rows are not mistaken for all 22 available launch actions. Deploy readiness prompts also include `quick_action_count=` beside the action summaries. Exact next action: keep this count aligned with `zed.quick_fix_count` whenever the launch receipt shape changes.

- Split DX Deploy launch prompt ownership again so the launch-gate, five-source launch-evidence, 100-point status, and launch quick-action wording now live in `crates/agent_ui/src/dx_deploy_launch_prompts.rs`; `crates/agent_ui/src/dx_deploy_prompts.rs` stays as the stable deploy readiness wrapper and re-exports the launch-gate helper for existing DX launch review prompts. This keeps the Deploy prompt surface small, source-owned, and connected to `.dx\receipts\deploy` plus `dx check launch` without mixing provider receipts, launch approval, and quick-action text in one file. Exact next action: keep the focused launch prompt source guard green whenever deploy status, launch evidence, or action wording changes.

- Added DX Deploy launch quick actions: Zed Deploy now parses bounded `zed.quick_fixes` from the dx-check launch receipt into `crates/agent_ui/src/dx_deploy_launch_actions.rs`, renders the first safe operator actions through `crates/agent_ui/src/dx_deploy_launch_actions_rail.rs`, and carries command, risk, approval, quick-action identities, and receipt-write metadata into deploy readiness prompts. Current receipt quick actions include `receipt-write` refresh actions that write receipts but do not run live deploys, plus approval-required runner actions for code, web metrics, and E2E proof. Exact next action: in the governed native proof window, verify the Deploy rail shows launch quick actions without executing them.

- Added a 100-point deploy status row: Zed Deploy now normalizes the dx-check launch score through `crates/agent_ui/src/dx_deploy_launch_score.rs`, preserves the structured `score_estimated` flag, renders `Status score` in the Deploy launch-gate card, and includes `status_score=` in deploy readiness prompts. Current refreshed receipt status is `245/500`, so the operator-facing 100-point deploy status is `49/100 estimated`; this is a blocked launch status, not live deploy approval. Exact next action: keep this normalized row beside the raw score when the governed native proof window compares Zed against `G:\Dx\.dx\receipts\check\check-launch-latest.json`.

- Hardened DX Deploy launch evidence identity: Zed Deploy now preserves the stable evidence id and receipt freshness timestamp from `launch_evidence_sources`, renders them in the Deploy rail, and carries them into deploy readiness prompts. The launch-chain blocker list is still bounded but now covers the full five-source launch evidence set, so code, web metrics, web probe, E2E, and deploy dry-run gaps can be compared directly against `G:\Dx\.dx\receipts\check\check-launch-latest.json`. Exact next action: in the governed native proof window, confirm the rail shows `deploy` as the only ready evidence id with receipt freshness while the four runtime proof ids remain missing.

- Added DX Deploy launch evidence-source rows: `crates/agent_ui/src/dx_deploy_launch_evidence.rs` now parses the aggregate `dx check launch --json` `launch_evidence_sources` plus `launch_chain` contracts, `crates/agent_ui/src/dx_deploy_launch_evidence_rail.rs` renders compact five-source launch evidence, launch-chain rows, and chain blocker text, and deploy readiness prompts include the same evidence-source summary. This connects the Zed Deploy rail and agent handoff prompt to the same DX ecosystem evidence chain that blocks or approves launch without running checks from Zed or pretending dry-run equals live deployment. Current refreshed receipt status: 5 launch evidence sources, 1 ready deploy dry-run source, 4 missing runtime proof sources, and `245/500` launch score with source/runtime/launch approvals still false. Exact next action: in the governed native proof window, compare the rendered evidence rows against `G:\Dx\.dx\receipts\check\check-launch-latest.json`.

- Added the DX Deploy panel source guard: `script/dx-deploy-panel-source.test.ts` now checks the native Deploy rail module registration, deploy prompt ownership, dry-run/launch-approval wording, launch-gate receipt precedence, rail render order, deploy source file sizes, and deploy status docs. This makes the no-build Deploy panel contract repeatable for future DX workers without launching Zed, running Cargo, starting servers, or touching cloud providers. Current source/config status remains 100/100 for the Zed Deploy panel slice. Exact next action: keep this guard green whenever deploy provider rows, launch-gate wording, or deploy prompt ownership changes.

- Split Deploy prompt ownership out of the large launch prompt file: `crates/agent_ui/src/dx_deploy_prompts.rs` now owns deploy readiness prompts, deploy receipt bucket prompt summaries, and canonical deploy capability prompt summaries, while `crates/agent_ui/src/dx_deploy_launch_prompts.rs` owns dx-check launch-gate, launch-evidence, status-score, and quick-action prompt summaries. `crates/agent_ui/src/dx_launch_prompts.rs` imports only the deploy prompt helpers it needs for receipt review, and `crates/agent_ui/src/agent_panel.rs` imports `deploy_readiness_prompt` from the deploy prompt module directly. This keeps deploy handoffs connected to `.dx\receipts\deploy` plus the aggregate dx-check launch gate while reducing the launch prompt file from 1158 to 969 lines and keeping `dx_deploy_prompts.rs` at 125 lines. Verification passed: red/green deploy prompt extraction source guard and targeted rustfmt. Exact next action: keep future provider-specific receipt wording inside `dx_deploy_prompts.rs` and future launch-gate wording inside `dx_deploy_launch_prompts.rs`.

- Added the DX Deploy launch-gate card: `crates/agent_ui/src/dx_deploy_launch_gate.rs` now reads bounded `check-launch-latest.json` / `check-latest.json` receipts from the active workspace and canonical `G:\Dx` / `G:\Dx\www` check roots, preferring launch-specific receipts before generic check receipts; `crates/agent_ui/src/dx_deploy_launch_gate_rail.rs` renders source/runtime/launch approval status, score, blockers, warning count, next action, and receipt source inside the Deploy rail; and `crates/agent_ui/src/dx_deploy_targets.rs` carries that launch gate beside deploy plan/status/provider-gate receipts. This connects the provider dry-run panel to the aggregate dx-check launch approval path without running Zed, running checks, or claiming live deploy readiness. Verification passed: red/green deploy launch-gate source guard, targeted rustfmt, and source review. Exact next action: in the governed native proof window, confirm the Deploy rail shows the current `245/500` blocked launch gate from `G:\Dx\.dx\receipts\check\check-launch-latest.json` beside the Vercel dry-run gate.
- Added the DX Deploy right-rail capability matrix and provider-gate connection: `crates/agent_ui/src/dx_deploy_capabilities.rs` now reads canonical `.dx\receipts\deploy` plan, status, provider dry-run gate, and `provider-capability-matrix.json` receipts from the active workspace plus `G:\Dx`, `G:\Dx\cli`, and `G:\Dx\www`; `crates/agent_ui/src/dx_deploy_receipt_summary.rs` owns the JSON receipt summary structs and parsers; `crates/agent_ui/src/dx_deploy_receipt_rank.rs` makes receipt precedence explicit so plan/status/matrix prefer active workspace then DX-WWW, while provider promotion gates prefer the hub receipt; `crates/agent_ui/src/dx_deploy_target_detection.rs` owns deploy config/link detection; `crates/agent_ui/src/dx_deploy_receipt_buckets.rs` owns the older `tools/dx-deploy` bucket state; `crates/agent_ui/src/dx_deploy_receipt_files.rs` owns receipt filesystem walking; `crates/agent_ui/src/dx_deploy_receipt_extract.rs` owns bucket receipt headline/status extraction; `crates/agent_ui/src/dx_deploy_local_files.rs` owns bounded local JSON/path helpers; `crates/agent_ui/src/dx_deploy_targets.rs` is now a 98-line cached snapshot orchestrator; `crates/agent_ui/src/dx_deploy_matrix_rail.rs` owns plan/status/provider matrix rendering; `crates/agent_ui/src/dx_deploy_gate_rail.rs` owns the provider-gate handoff rows; `crates/agent_ui/src/dx_deploy_rail_ui.rs` owns shared compact row helpers; `crates/agent_ui/src/dx_deploy_rail.rs` is the 182-line top-level Deploy rail coordinator; `crates/agent_ui/src/dx_launch_workspace.rs` now only calls the focused deploy rail module; `crates/agent_ui/src/dx_launch_prompts.rs` routes deploy review prompts to canonical `.dx\receipts\deploy` before legacy `tools/dx-deploy`; and `crates/agent_ui/src/dx_receipts.rs` counts Deploy as a first-class hub receipt bucket. Configured live DX-WWW dry-run receipts from `G:\Dx\www` with the root CLI, writing `G:\Dx\www\.dx\receipts\deploy\deploy-plan-latest.json`, `deploy-status-latest.json`, retained snapshots, and `provider-capability-matrix.json`. The receipts report `schema_version=dx.deploy.plan` and `schema_version=dx.deploy.status`, `status=ready`, `dry_run=true`, `deploy_ran=false`, 6 providers, 6 ready dry-run plans, and 0 blocked plans. Public installed CLI verification now passes from `G:\Dx\www`: `G:\Dx\bin\dx.exe deploy plan/status --json --compact` refreshed plan/status receipts without falling through to DX-WWW. Also refreshed the hub-level Vercel deploy gate from `G:\Dx` with `G:\Dx\bin\dx.exe deploy vercel --dry-run --json --compact`; it wrote `G:\Dx\.dx\receipts\deploy\deploy-vercel-latest.json` as `schema_version=dx.deploy.vercel_dry_run.v1`, `dry_run=true`, `deploy_ran=false`, and honestly `blocked` until launch approval is green. Zed now parses that receipt's `deploy_ready`, blocker/warning counts, `zed.panel_kind="deploy-gate"`, source/runtime/launch approval rows, quick-fix count, and next action so the Deploy rail can show promotion readiness without claiming live deployment. The deploy source files are split by scanner, summary, ranking, receipt bucket state, receipt file walking, target detection, matrix rail, gate rail, shared rail UI, and top-level rail ownership; every deploy-specific file is now under 300 lines. No cloud provider command, credential read, build, local server, live deploy, Cargo check/test/build, or `just run` ran. Verification passed: red/green deploy-capability, deploy-provider-gate, deploy-gate-rail split, deploy-receipt-precedence, deploy-parser-extraction, deploy-matrix-rail split, deploy-rail split, and deploy-target/receipt ownership source guards, `rustfmt --edition 2024 --check` on touched Rust files, targeted `git diff --check`, conflict-marker scan, installed root CLI dry-run plan/status, and hub Vercel dry-run gate. Current source/config status for the Zed Deploy panel slice: 100/100. Remaining non-source proof: governed native Zed visual/runtime validation, intentionally skipped by lane policy.
- Added DX Agents flat recovery-control count consumption: `crates/agent_ui/src/dx_agent_bridge.rs` now parses `recovery_controls.required_intent_count`, `action_count`, and `check_count` into typed import-summary/release-gate counts, and `crates/agent_ui/src/agent_configuration.rs` plus `crates/agent_ui/src/dx_launch_workspace.rs` render the compact `6 intent / 10 action / 5 check` contract without walking the full action-map payload. Verification passed: red source guard for missing flat counter parsing, `rustfmt --edition 2024 --check`, source guards, targeted `git diff --check`, and conflict-marker scan. Skipped: Cargo tests/builds, live editor runtime, local servers, browser/WebView proof, package installs, deploys, and `just run`. Exact next action: in the governed proof window, refresh `dx agents import-summary --json` and `dx agents release-gate --json`, then confirm Zed shows the flat recovery-control counts from receipts without opening action-map detail.
- Added DX Studio reorder-direction refusal: `crates/web_preview/src/dx_studio_bridge.rs` now trims the reorder prompt value, accepts only `up` or `down`, and redraws the selected surface with `reorder direction refused` without posting a source-edit request for typos, whitespace-only input, or unexpected prompt text. Verification passed: red/green source guard for the missing reorder guard, injected bridge JavaScript parse, and targeted rustfmt check. Skipped: Cargo tests/builds, live WebView proof, local servers, browser automation, package installs, deploys, and `just run`. Exact next action: add a compact guard for blank/whitespace operation-picker answers so an empty edit-operation prompt restores the previous receipt-backed state instead of silently selecting operation 0.
- Added DX Studio value-prompt cancellation preservation: `crates/web_preview/src/dx_studio_bridge.rs` now redraws the selected DX Studio surface with receipt-backed status when text, token, replacement-token, or reorder-direction prompts are cancelled or submitted as no-ops. This prevents value-entry cancellation from leaving stale `text edit`, `writing token source`, or reorder intent labels, and still posts no edit request. Verification passed: red/green source guard for value-prompt cancellation branches, injected bridge JavaScript parse, and targeted rustfmt check. Skipped: Cargo tests/builds, live WebView proof, local servers, browser automation, package installs, and `just run`. Exact next action: add a compact guard for invalid reorder direction values so unexpected prompt text is refused locally without posting an edit request.
- Added DX Studio cancelled-prompt state preservation: `crates/web_preview/src/dx_studio_bridge.rs` now snapshots the current selection, status label, receipt, and hot-reload panel state before hover capture, and restores that same state when target selection, Escape, or the ambiguous operation picker is cancelled. This prevents cancelled ambiguous target/operation prompts from clearing the overlay, selecting an unintended surface, or leaving a fake `edit operation` outcome. Verification passed: red/green source guard for the cancelled-prompt contract, injected bridge JavaScript parse, and targeted rustfmt check. Skipped: Cargo tests/builds, live WebView proof, local servers, browser automation, package installs, and `just run`. Exact next action: add a compact guard for cancelled text/token/reorder value prompts so value-entry cancellation also returns to receipt-backed selection state without posting an edit request.
- Added DX Studio bridge selection-restore status preservation: `crates/web_preview/src/dx_studio_bridge.rs` now routes restored-selection labels through the current source-edit receipt, so failed/stale/refused receipts keep their real refusal status on the dashed overlay while successful `source_updated` reloads can still show `selection restored`. Verification passed: red/green Node source guard for the restore-status contract, injected bridge JavaScript parse, and targeted rustfmt check. Skipped: Cargo tests/builds, live WebView proof, local servers, browser automation, package installs, and `just run`. Exact next action: add a compact guard for cancelled ambiguous target/operation prompts so cancelling a target choice preserves the current overlay and receipt state without creating a fake edit outcome.
- Added dx-check outcome and duration visibility in the native Zed Check rail: `crates/agent_ui/src/dx_check_panel.rs` now carries bounded `pass_count`, `fail_count`, `warn_count`, `skipped_count`, and `duration_ms` from native Zed-panel payloads, shared DX-WWW view-model payloads, or the top-level dx-check receipt, and `crates/agent_ui/src/dx_launch_workspace.rs` renders compact `Outcome` and `Duration` rows before scope/skipped details. This keeps the rail honest about what passed, failed, warned, skipped, and how long the lightweight check took without running any expensive tool. Verification passed: red source guard for the missing outcome-count contract, rustfmt --check, source guards, fixture outcome smoke, and targeted conflict-marker scan. Skipped: Cargo tests/builds, live editor runtime, local servers, browser/WebView proof, package installs, and running `dx check`. Exact next action: in the governed proof window, compare the Zed `Outcome` and `Duration` rows against a real `check-latest.json`, especially a failing receipt where `fail_count > 0`.
- Added dx-check skipped-expensive-check visibility in the native Zed Check rail: `crates/agent_ui/src/dx_check_panel.rs` now carries bounded `skipped_expensive_checks` from native Zed-panel payloads, shared DX-WWW view-model payloads, or the top-level dx-check receipt, and `crates/agent_ui/src/dx_launch_workspace.rs` renders a compact `Skipped` count plus the first two skipped expensive checks. This keeps Lighthouse/CDP/E2E/lint/format skips honest in the launch rail without running those expensive tools. Verification passed: red source guard for the missing skipped-check contract, rustfmt --check, source guard, and targeted conflict-marker scan. Skipped: Cargo tests/builds, live editor runtime, local servers, browser/WebView proof, package installs, and running `dx check`. Exact next action: in the governed proof window, compare the Zed `Skipped` row against a real `check-latest.json` receipt and confirm no skipped check is presented as passed.
- Added dx-check checked-path visibility in the native Zed Check rail: `crates/agent_ui/src/dx_check_panel.rs` now carries bounded `checked_paths` from either `dx.check.zed_panel.v1`, `dx.www.check_panel_view_model.v1`, or the top-level receipt contract, and `crates/agent_ui/src/dx_launch_workspace.rs` renders a compact `Checked` count plus the first two paths. This makes fallback receipts and workspace-scoped receipts visibly honest without reading large report bodies. Verification passed: red source guard for the missing checked-path contract, rustfmt --check, source guard, and targeted conflict-marker scan. Skipped: Cargo tests/builds, live editor runtime, local servers, browser/WebView proof, package installs, and running `dx check`. Exact next action: in the governed proof window, verify the Zed Check rail `Checked` row against a real workspace receipt and confirm the canonical `G:\Dx` fallback does not look like a workspace-local receipt.
- Added dx-check last-run label parity in the native Zed Check rail: `crates/agent_ui/src/dx_check_panel.rs` now carries a `last_run_label` snapshot field, prefers the shared DX-WWW `view_model.last_run_label` when available, and otherwise synthesizes an honest fallback from `generated_at_unix_ms` or the missing-receipt state. `crates/agent_ui/src/dx_launch_workspace.rs` now renders `Last Run` from that label instead of showing only a bare generated timestamp. Verification passed: red source guard for the missing snapshot contract, rustfmt --check, source guard, and targeted conflict-marker scan. Skipped: Cargo tests/builds, live editor runtime, local servers, browser/WebView proof, package installs, and running `dx check`. Exact next action: in the governed proof window, compare the Zed Check rail `Last Run` row against the website `/check` panel for the same receipt and verify native Zed receipts still show a clear generated-time fallback.
- Added explicit dx-check fallback warning visibility in the native Zed Check rail: when Zed renders the shared `dx.www.check_panel_view_model.v1` because the native `dx.check.zed_panel.v1` payload is missing or unsupported, `crates/agent_ui/src/dx_check_panel.rs` now inserts a `zed-panel-fallback-view-model` warning before the receipt's own warning rows. This keeps the compatibility path visible without blocking the shared panel or fabricating a score. Verification passed: rustfmt --check, targeted `git diff --check`, targeted conflict-marker scan, source guard, and malformed fixture shape smoke. Skipped: Cargo tests/builds, live editor runtime, local servers, browser/WebView proof, package installs, and running `dx check`. Exact next action: in the governed proof window, verify the fallback warning appears once for the unsupported-Zed fixture and not for normal native Zed-panel receipts.
- Added DX-WWW check panel fallback consumption in the native Zed Check rail: Zed still prefers `dx.check.zed_panel.v1`, but `crates/agent_ui/src/dx_check_panel.rs` can now safely render `dx.www.check_panel_view_model.v1` when the Zed-specific payload is absent or unsupported. The fallback maps `score_meter`, `bucket_rows`, `blocker_rows`, `warning_rows`, `quick_fix_rows`, `primary_action`, `secondary_action`, and `scoring_config` into the same rail snapshot without fabricating a score. Verification passed: rustfmt --check, targeted `git diff --check`, targeted conflict-marker scan, DX-WWW fixture shape smoke, and source guard. Skipped: Cargo tests/builds, live editor runtime, local servers, browser/WebView proof, and running `dx check`. Exact next action: during the governed proof window, compare the native Zed Check rail and website `/check` rendering against the same receipt fixture.
- Added native dx-check receipt-panel consumption in the Zed launch rail: `crates/agent_ui/src/dx_check_panel.rs` reads bounded `dx.check.zed_panel.v1` receipts from workspace `.dx\receipts\check\check-latest.json` or the canonical `G:\Dx` fallback, refuses unsupported schemas without claiming a score, and renders score, 500-point profile, scoring-config posture, sections, warnings, blockers, quick fixes, and refresh/detail commands in the existing Check rail. Verification passed: rustfmt, rustfmt --check, targeted `git diff --check`, and conflict-marker scan. Skipped: Cargo tests/builds, live editor runtime, local servers, browser/WebView proof, and running `dx check`, because this lane is lightweight. Exact next action: generate or copy a real `check-latest.json` receipt during the governed proof window and visually tune the native rail against that live receipt.
- Final DX Agents bridge validation update: the installed root `dx` launcher now exposes the current public `dx agents ... --json` bridge. Live CLI checks from `G:\Dx` pass for status, social list, automate list, snapshot, run, receipts, receipts list, contract audit, provider/model list, release gate, and import summary, with receipts under `G:\Dx\.dx\receipts\agents`. Zed visual/runtime proof is still pending because no `just run` or local editor runtime was launched in this lane.
- Added DX Agents receipt-root contract consumption in Zed: GPUI now reads `receipt_root_present` from `dx agents receipts list --json` output, marks the background receipt surface as missing-config when the runtime reports a missing root, and shows that state in the launch rail and Agent Configuration instead of relying only on local folder inference.
- Added DX Agents receipt-inbox review in Zed: Agent Configuration now has a metadata-only `dx agents receipts --json` action that caches `receipts-inbox-latest.json`, and the launch rail renders malformed/missing/stale/expired inbox counts separately from the runtime-owned `receipts-list-latest.json` refresh.
- Aligned the DX Agents CLI path setting with the public bridge: `agent.dx_agents.cli_path` now defaults to the root `dx` executable and all GPUI actions spawn that configured CLI path for fixed `dx agents ... --json` commands, instead of displaying one CLI while hardcoding another.
- Added source-owned DX Agents action-error receipts in Zed: failed GPUI bridge actions now write redacted `action-error-latest.json` metadata and the Agent Configuration plus launch rail render the last failure, covering missing CLI/path/runtime launch failures without exposing stderr secrets. Zed also parses the action-error redaction booleans and raises review state if a stale or malformed packet ever claims to export secrets, provider credentials, or receipt bodies.
- Added DX Studio manifest-backed selection enrichment: selected `data-dx-*` DOM surfaces are now matched against loaded `public/preview-manifest.json` and source TypeScript `dx-studio-edit-contract.ts` / `dx.studio.launch_edit_contract.v1` surface contracts before source-edit planning, so Zed inherits source files, materialized files, operation IDs, package IDs, and operation summaries from the real DX-WWW contract instead of relying only on runtime attributes.
- Added DX Studio ambiguous manifest refusal: when JSON/TypeScript edit contracts expose multiple equally strong source surfaces for one selected DOM marker, Zed now records the candidate surfaces and refuses the source write instead of choosing the first selector.
- Added source freshness snapshots to DX Studio edit plans: Zed now carries source file length/modified-time from selection into edit requests and refuses stale writes when the owning file changed after selection.
- Added structured DX Studio source-edit refusal receipts: stale source, ambiguous source, readonly/generated/runtime refusal, missing source template, non-DX project, operation-not-declared, and responsive breakpoint mismatch now flow back to the WebView overlay with the current source-edit plan.
- Hardened the DX Studio responsive edit contract: breakpoint-prefixed class tokens are inspectable, but only become editable when the selected source surface declares `update_design_token`; responsive token writes preserve their breakpoint prefix and carry `data-dx-style-surface` / responsive metadata through selection and source-edit planning.
- Split the DX Studio source-edit implementation into focused modules: a small public facade, guarded path policy, marker range lookup, shared value helpers, an operation router, and separate text/token/reorder/insert/safety modules. Behavior stays source-backed and conservative while the code is easier to review before live WebView proof.
- Hardened DX Studio source ownership resolution: real source-owned candidates from manifest `sourceFile`, `data-dx-source-file`, source metadata, and matching edit contracts now outrank generated/runtime/materialized preview files, and materialized fallback writes still require explicit manifest permission.
- Hardened DX Studio overlay stability: selected overlays now re-resolve the same `data-dx-edit-id`, text marker, or selector on scroll/resize and redraw from a fresh DOM rect so selected/parent/root dashed borders stay attached to the real source-owned surface during normal preview movement.
- Hardened DX Studio write readiness: the source mutation path now refuses declared operations unless the selected surface also has the required marker, slot, token/class, reorder group, or manifest source template; `data-dx-edit-kind` is carried into source-edit plans and receipts for clearer target inspection without treating it as write permission by itself.
- Hardened DX Studio source-policy evidence: source-edit plans and receipts now report whether the resolved source is source-owned, generated/runtime, or materialized fallback, whether manifest permission allows generated edits, and whether the path policy permits the write before mutation.
- Hardened DX Studio freshness trust: edit requests now discard any page-supplied `expected_source_snapshot` and attach only the snapshot captured by Zed's latest matching DX Studio selection receipt, keeping stale-source protection native-owned instead of WebView-owned.
- Hardened DX Studio stale-write refusal: source edits now require a trusted Zed source snapshot whose source file identity matches the resolved write target, and native selection matching covers insert/media/reorder/token/component/section markers so non-text operations still receive freshness evidence.
- Hardened DX Studio hot-reload selection restore: the WebView bridge now persists and reselects stable insert/media/reorder/token/component/section/style/source markers after reload, instead of restoring only text/edit-id selections.
- Hardened DX Studio project detection from source markers: the Rust DX-WWW detector now recognizes bounded source-folder `data-dx-route`, `data-dx-source`, `data-dx-component`, `data-dx-section`, `data-dx-edit-id`, `data-dx-edit-ops`, and hot-reload markers even when a manifest has not been generated yet.
- Added real DX Studio visual style metrics: selected Web Preview surfaces now carry DOM-computed size, margin, padding, border width, layout, typography, and visual values into the overlay panel, with an honest missing state when computed style is unavailable.
- Added DX Studio source-backed style edit planning: selected surfaces now carry a `zed.web_preview.dx_studio_style_edit_plan.v1` object that marks computed CSS as read-only unless the real DX-WWW surface declares `update_design_token` plus a token/scope/style/responsive marker, giving the overlay and Rust source-edit plan a safe handoff for future dx-style token edits.
- Added DX Studio style-token edit prefill wiring: the WebView token edit prompt now uses the source-backed `style_edit_plan` to prefill candidate tokens, show computed size/margin/padding/display/background context, and send a compact prefill receipt with the source-edit request while keeping Rust-side marker/source-policy validation authoritative.
- Added DX Studio style-token receipt context: successful and refused token edits now carry `zed.web_preview.dx_studio_style_edit_receipt_context.v1` metadata with prefill status, declared-contract evidence, computed summary, and no-inline-style policy, and overlay/session status text can report that source-backed style context.
- Added bounded DX Studio style-token receipt shape fixtures: the source-edit facade now has local unit coverage for declared dx-style token-contract context, refused/missing-contract context, and unrelated non-style edits returning no style receipt context. Cargo execution remains deferred by the launch-lane rule.
- Added DX Studio `apply_source_edit` fixture coverage for token receipts: a temporary DX-WWW root fixture now exercises a real `update_design_token` write against a source-owned `data-dx-design-token` marker, verifies the returned source-edit receipt carries style context, source policy, and hot-reload metadata, and verifies stale trusted snapshots refuse without mutating the file.
- Added DX Studio Web Preview refusal receipt assembly coverage: the Web Preview IPC path now builds failed source-edit receipts through a focused helper with source-edit plan, `status_detail`, source policy, and style-edit context kept together, and compact session summaries now expose the same refusal bundle for Agent/source diagnostics.
- Added DX Studio bridge receipt status labels: the injected WebView `afterSourceEdit` renderer now classifies real receipt metadata for stale-source refusal, missing dx-style token-contract refusal, and source-updated style context, so the selected dashed label and status panel row show the actual source-backed outcome instead of a generic edit failure. Verification passed: red/green Node source guard, injected bridge JavaScript parse, and targeted rustfmt check. Skipped: Cargo tests/builds, live WebView proof, local servers, browser automation, and `just run`. Exact next action: add a bridge-level source guard that renders source-policy and rollback/refusal metadata in the bottom panel before the governed live WebView proof.
- Added DX Studio bridge policy receipt rows: the injected WebView bottom panel now merges receipt-level `source_policy` with `source_edit_plan.source_policy` and renders real source kind, policy allow/refuse state, generated/materialized fallback state, failed-before-confirmed-write, rollback guard, and trusted stale-snapshot guard rows when the receipt carries them. Verification passed: red/green Node source guard, injected bridge JavaScript parse, and targeted rustfmt check. Skipped: Cargo tests/builds, live WebView proof, local servers, browser automation, and `just run`. Exact next action: add a compact bridge guard for hot-reload refresh-needed/no-version states so successful edits without DX-WWW version polling show an honest source-updated-refresh-needed panel row.
- Added DX Studio bridge hot-reload status rows: the injected WebView bridge now records source-updated hot-reload polling state, clears stale reload state on each new edit, and renders panel rows for polling, version endpoint unavailable, version poll timeout/failure, and version seen outcomes from the actual `/_dx/hot-reload/version` polling path. Verification passed: red/green Node source guard, injected bridge JavaScript parse, and targeted rustfmt check. Skipped: Cargo tests/builds, live WebView proof, local servers, browser automation, and `just run`. Exact next action: add a tiny guard that stale/non-source-updated edit receipts clear any previous hot-reload panel state while preserving refusal policy/style rows.
- Added DX Studio bridge failed-receipt reload clearing: the injected WebView `afterSourceEdit` path now funnels every receipt through a focused renderer that clears stale hot-reload panel state, stores the new receipt, redraws refusal policy/style rows from that receipt, and only starts hot-reload polling for `source_updated` receipts. Verification passed: red/green Node source guard, injected bridge JavaScript parse, and targeted rustfmt check. Skipped: Cargo tests/builds, live WebView proof, local servers, browser automation, and `just run`. Exact next action: add a compact source guard for selection-restore status so failed receipts keep the selected dashed overlay attached without showing a misleading "selection restored" success state.
- Refined the source-backed DX Studio Web Preview editing slice: the bridge now has a general Edit DX Studio Surface path, captures `data-dx-source-file`, insert slot, reorder group, design token, token scope, media slot, class tokens, and responsive tokens from the selected real DOM surface, and records a pre-write hot-reload version baseline before source mutations.
- Added real non-text source write coverage for declared contracts: `update_design_token` can replace selected source token references or class tokens, and `move_reorder_section` can move a selected source-owned section within its declared `data-dx-reorder-group`; both keep generated/runtime file refusal, stale/ambiguous-source refusal, and rollback-on-write-error behavior.
- Kept insert component and icon/media source edits honest: the operation IPC and plan states are wired, but actual writes require a manifest-declared source snippet/template so Zed does not create dummy UI or random inline source.
- Implemented the source-backed DX Studio Web Preview editing slice: WebView2 now installs a DX Studio bridge for nearest editable `data-dx-*` selection, selected/parent/route dashed overlays, responsive breakpoint panel metadata, editable-parent target choice, source-edit IPC, source-owned text update receipts, hot-reload version polling, and selection restoration; the Rust side resolves owning files and refuses ambiguous, stale, readonly, generated/runtime, non-DX, or unsupported operation writes.
- Added DX Studio source-edit honesty: direct `update_text_content` writes are real but limited to direct source literals under declared `data-dx-editable-text` markers, `update_design_token` and `move_reorder_section` now have guarded source transformers, and insert component/icon/media writes stay template-gated until DX-WWW manifests provide safe source snippets.
- Added DX launch UX/manifest polish: Web Preview's DX Studio parser now accepts DX-WWW preview routes from top-level routes, preview route arrays, or page arrays, and extracts route/source/package/asset/marker surfaces from both string rows and object rows.
- Added parsed DX Studio session target metadata: Web Preview session snapshots now include the default preview target and up to six preview targets with source file, Forge package, asset, `data-dx` marker, and hot-reload counts for easier route/source/package inspection.
- Polished the DX AI workspace rail organization: the left rail now shows a source-backed Chat/Tasks/Sources/Agent block from current Agent thread state, retained background tasks, source-set counts, and DX Agents bridge state; the right rail separates DX Agents Automations from Social Accounts.
- Polished DX onboarding preview target selection: first-run onboarding now honors manifest-declared static preview file paths before falling back to conventional `public`, `out`, and `dist` route files, while keeping explicit credential import and Skip behavior unchanged.
- Added first-class DX Agents launch rail treatment for Automations: the right rail now renders an Automations section with `dx agents automate list --json` as the explicit entrypoint, while Social Accounts stays focused on account and QR/connect state.
- Added DX Agents run-receipt hardening in the GPUI reader: receipt rows can now show task lifecycle state, duration state, provider/model readiness, and safe retry/cancel posture from the metadata-only `dx agents run --json` contract, while unsafe or redacted rows stay constrained to safe summary fields.
- Added DX Agents receipt-row social/automation counters in the GPUI reader: background task rows can now show social connected/needs-auth and automation enabled/warning counts from the runtime-owned receipt index without opening run receipt bodies.
- Added DX Agents `needs_auth` summary parsing in the GPUI reader while preserving fallback to older `needs_connection` receipts, so the Social Accounts rail can render launch-native auth-needed state without guessing.
- Recorded the DX Agents task-state review contract: runtime and CLI receipt indexes now flag unknown non-redacted run `task_state` values as unsafe or malformed metadata, and the existing receipt rail warning path should render those rows for operator review.
- Recorded the DX Agents contract-audit receipt classification: runtime and cached CLI receipt indexes now expose `contract-audit-latest.json` as bridge contract-audit metadata, so the receipt rail can distinguish audit evidence from generic cached rows.
- Hardened DX Agents bridge-contract redaction review: Zed now treats task payload, transcript, and provider credential export flags as review-triggering leak classes alongside secrets, account targets, automation bodies, and tool payloads.
- Tightened DX Studio edit-contract parsing so Web Preview requires explicit manifest or operation metadata before marking an edit surface as write-capable or source-owned; missing fields now stay conservative while normal Web Preview behavior and the no-node-modules default remain unchanged.
- Tightened DX Studio Web Preview session metadata so missing source-owned edit contracts no longer advertise write-file readiness; the session snapshot now marks edit contract loaded state explicitly and keeps mutation fields false until a real manifest is present.
- Aligned DX Agents row-action parsing with the latest bridge contract: Zed now carries and validates `public_command` / `public_refresh_command` metadata for social and automation rows, requires those fields to advertise the public root `dx agents ... --json` bridge, uses the public command metadata in prompts and rail summaries, and still executes only typed fixed command variants.
- Tightened DX Agents social row-action metadata validation: Zed now only accepts rendered `dx agents social connect/disconnect --platform ... --json` action descriptors when the platform token passes the same safe platform rule as executable bridge actions.
- Hardened the DX Agents action bridge: Agent Configuration now builds public `dx agents ... --json` actions from typed command variants and metadata command variants instead of raw argument vectors, keeping social connect/disconnect platform validation, receipt-cache writes for import-summary/release-gate, and fixed public command routing centralized in `dx_agent_bridge`.
- Added DX Agents receipt-index rendering: Zed now reads `G:\Dx\.dx\receipts\agents\receipts-list-latest.json`, exposes returned/known receipt counts, active task count, latest receipt path, redaction/unsafe-row state, and typed background receipt rows in both Agent Configuration and the DX launch right rail while keeping raw receipt payloads and unsafe rows out of the UI.
- Routed the DX Agents receipt-index refresh through the public root CLI command `dx agents receipts list --json`, so Zed can refresh the runtime-owned `receipts-list-latest.json` contract without calling the raw `dx-agents` binary directly.
- Aligned Agent Configuration controls with the public `dx agents ... --json` command surface for status, social, automation, and run receipt actions while keeping `dx-agents` runtime receipt metadata accepted and requiring safe explicit platform arguments for connect/disconnect.
- Hardened DX Agents receipt scalar filtering so Zed redacts bearer headers, authorization labels, bare `sk-` values, provider-key labels, password markers, private-token strings, and OAuth token names from cached metadata before rendering receipt rows or bridge action labels.
- Added crowded coding layout activity-bar behavior: when the active workspace has code editor and Web Preview items open with the Agent panel visible, the far-left AI sidebar now renders as a 48px icon-first activity bar for New Chat, Search, Agents, Sources, Plugins, Automations, Background Tasks, Settings, project switching, and sidebar toggle controls; full sidebar/AI modes remain available outside that tri-pane layout or when explicitly expanded.
- Tightened crowded coding layout detection so the compact activity bar only activates when editor and Web Preview/browser items are active in visible center panes with the Agent panel visible, avoiding collapse from hidden background preview tabs.
- Split the compact coding activity bar shell into a dedicated sidebar module while keeping the action wiring in the existing sidebar owner, reducing GPUI layout bulk without changing behavior.
- Hardened the compact activity bar for short windows by keeping secondary controls fixed and letting the primary action stack scroll inside the 48px rail instead of overflowing into the editor/Web Preview layout.
- Retargeted the compact activity bar Background Tasks action to the live thread list: it now expands the sidebar and selects the first running, waiting, or retained Agent task row when one is visible, with normal sidebar search focus as the empty state.
- Added compact Agent dock sizing for crowded coding layouts: when visible center panes include both editor and Web Preview/browser items, the Agent panel uses a fixed compact coding width instead of flexible proportional width, while no-editor/full-width AI mode and explicit zoom behavior remain available.
- Wired the compact activity bar Sources action to draft a real DX source attachment/review prompt from the current source-set snapshot in the Agent panel, instead of expanding generic thread chrome in crowded coding layouts.
- Added a dock-level optional max-size hook and capped the Agent dock in crowded editor + Web Preview layouts so persisted wide Agent widths cannot crush the center coding/Web Preview surface; the cap is inactive outside the compact coding posture.
- Wired the compact activity bar Automations action to draft a DX Agents automation receipt review from the Agent bridge snapshot, keeping compact coding layouts in place while avoiding direct automation execution, shell calls, or secret/social-login flows.
- Consolidated the compact activity bar AgentPanel update path so Sources and Automations share one focused draft-routing helper, keeping the coding layout behavior consistent while preserving the existing expanded sidebar task controls.
- Removed an unused screen-carousel width persistence experiment from `workspace.rs`, leaving the active edge-dwell screen switching path intact and reducing pre-test dead-code surface.
- Polished the expanded sidebar screen grid so it renders real files, folders, and site shortcuts plus one honest Add Folder action instead of repeated filler tiles, and removed dead current-space heading helpers from the sidebar source.
- Split the DX receipt-root scanner out of the large launch workspace view into `dx_receipts.rs`, keeping `G:\Dx\.dx\receipts` bucket counting, latest receipt labels, and cache behavior unchanged while reducing GPUI view ownership.
- Replaced bare token/tool meter placeholder values with receipt-root-aware pending, missing, or ready states so the AI workspace meter rows stay honest until governed token receipts exist.
- Added DX onboarding static preview manifest selection: the Web Preview hero now reads `public/preview-manifest.json`, prefers `/launch` then `/`, resolves real static/export HTML targets such as `.dx\vercel-landing\index.html`, `public\launch.html`, `out\launch\index.html`, or `dist\launch\index.html`, and scans bounded `G:\WWW\www\examples` roots when they expose a manifest or renderable preview.
- Added the DX Studio Web Preview contract slice: Zed Web Preview now detects DX-WWW workspaces from the root `dx`/route/Forge shape, opens the DX route origin by default for DX projects, exposes the `dx www preview-manifest --json`, `dx www routes --json`, and `dx forge packages --json` command contract, and collects `data-dx-*` route/source/Forge/hot-reload markers in page diagnostics without affecting normal websites.
- Completed the DX Studio edit/drag manifest slice: Web Preview session snapshots now expose source-owned Studio/Edit manifest candidates, real DX-WWW read contracts, and a metadata-only drag-to-preview contract while page diagnostics collect manifest links plus `data-dx-edit-target` / drag / drop markers without executing CLI commands or changing non-DX sites.
- Added DX Studio public manifest/edit-operation discovery: Web Preview now treats `public/preview-manifest.json` and source-owned `dx.studio.launch_edit_contract.v1` manifests as first-class candidates, advertises the explicit edit operation/marker contract in session and page diagnostics, and keeps all mutation commands disabled until a governed operator action exists.
- Added DX Studio manifest field compatibility: Web Preview now reads the current DX-WWW public manifest's camelCase route fields (`sourceFile`, `forgePackages`, `dataDxMarkers`, `hotReloadTarget`) and loads the source-owned edit contract summary with source, route, operation IDs, marker attributes, surface count, write policy, node_modules posture, and absolute-positioning posture.
- Split the DX Studio session metadata builder into a focused Web Preview module so the large GPUI view delegates manifest/edit/drag contract snapshot assembly while preserving the existing Web Preview behavior and DX-only detection gates.
- Added the DX launch onboarding Web Preview hero slice: onboarding now creates a real Web Preview child for the hero canvas, prefers a detected launchable `G:\WWW\www` DX WWW page on first paint, supports explicit `DX_ONBOARDING_PREVIEW_URL`, and falls back to a bundled original animated DX page without local servers or secret import.
- Added DX onboarding provider readiness: the launch hero now shows native authenticated provider readiness, DX Agents provider/model/contract receipt presence, and DX catalog artifact presence from source-owned paths/environment hooks, while keeping all credential setup behind the existing Provider Settings explicit-consent action.
- Added DX onboarding WWW target discovery: the launch hero now supports `DX_WWW_WORKSPACE` / `DX_WWW_ROOT`, scans bounded recent `G:\WWW\www\.dx\cache\*\launch-app` and `G:\WWW\.dx\*` evidence roots for renderable Forge/Web Preview HTML, keeps `DX_ONBOARDING_PREVIEW_URL` as the exact override, and only uses a `dx dev` route URL when the operator explicitly selects a DX WWW root.
- Added DX onboarding preview-contract visibility: the launch hero now renders source-backed Preview Contract rows for the selected target, hook, and DX WWW command contract (`dx www preview-manifest --json`, `dx www routes --json`, `dx forge packages --json`) or a graceful bundled fallback state without running the CLI.
- Completed DX onboarding demo action coverage: the launch hero quick actions now route to the selected Web Preview target, bundled 3D scene, Agent panel, Automations surface, Provider Settings, and Skip without adding secret import, social login, CLI execution, or dummy screens.
- Added DX launch status aggregate visibility: the AI workspace now reads the Zed-facing `dx.launch.status.v1` receipt from `G:\Dx\.dx\receipts\launch\status-latest.json`, renders agent/token/discovery readiness plus `dx www templates --json` and `dx forge packages --json` bridge commands in the right rail, and reports the exact missing receipt state without running the CLI.
- Added DX launch handoff visibility: the AI workspace now reads source-owned `G:\Dx\cli\fixtures\launch-examples\import-manifest.json` and `handoff.json` metadata, renders packet/action/polling/no-fanout posture in the right rail, and adds a Launch Handoff guided card that drafts the safe operator review without running CLI commands.
- Added DX launch gate visibility: the AI workspace now reads source-owned `G:\Dx\cli\fixtures\launch-examples\import-summary-*.json`, `release-gate-*.json`, and `fallback-drill-*.json` parser fixtures, renders import/release/fallback readiness and cached receipt fallback states in the right rail, and adds a Launch Gate guided card without running CLI commands.
- Added DX launch audit visibility: the AI workspace now reads source-owned `G:\Dx\cli\fixtures\launch-examples\schemas.json`, `fixtures.json`, `smoke.json`, and `status.json`, renders command matrix, startup polling, fixture render-state, smoke check, fanout, and redaction posture in the right rail, and adds a Launch Audit guided card without running CLI commands.
- Added DX-WWW launch evidence visibility: the AI workspace now reads the selected DX WWW project or `G:\WWW\www` fallback for `.dx\forge\template-readiness` and `.dx\forge\release` launch evidence handoff artifacts, renders release/restart/acceptance packet coverage in the right rail, and adds a WWW Evidence guided card that drafts the next safe DX-WWW operator sequence without running Forge or CLI commands.
- Added DX source audit visibility: the AI workspace now reads the hub-owned `G:\Dx\.dx\audit\launch-source\latest.json` and DX Studio QA audit receipt metadata, renders cross-repo coordination, worker-output ledger, template trust, risk-review blockers, and next Friday action in the right rail, and adds a Source Audit guided card without touching `G:\Dx\www` package work or running audit helpers.
- Added DX launch receipt review visibility: the AI workspace now scans `G:\Dx\.dx\receipts\launch` for the protected `status-latest.json` receipt and retained `launch-status-*.json` snapshots, renders freshness/malformed/stale/expired metadata in the right rail, and routes missing or stale states to the documented `dx launch status --json` / `dx launch receipts --json` contracts without reading receipt bodies or running the CLI.
- Added DX metadata binary-cache bridge visibility: `dx_catalog` now has a stable metadata-only receipt cache artifact contract with rkyv serialization and a memmap read path, and the AI workspace right rail reports provider catalog, launch receipt, global receipt-index, and token/tool meter cache readiness from real paths while keeping JSON receipts authoritative until a governed producer writes the binary artifacts.
- Completed the DX receipt-cache producer bridge: `dx_catalog` now includes a bounded metadata-only JSON receipt-folder builder plus an explicit artifact writer so governed tools can materialize `DXRCP001` caches for launch/agent/token/Forge/source/media/RLM/serializer/deploy/runtime-proof receipts without changing Zed startup or JSON receipt authority.
- Added the standard DX receipt-cache root contract so governed producers can materialize the same launch/agent/token/Forge/source/media/RLM/serializer/deploy/runtime-proof buckets that Zed rails already surface, without duplicating the mapping outside `dx_catalog`.
- Added DX receipt-cache summary helpers so panels and governed producers can read root counts, missing roots, freshness buckets, malformed entries, latest receipt timestamps, root-filtered entries, kind-filtered entries, and latest-per-root records from the stable manifest contract without reparsing receipt folders.
- Added DX receipt-cache per-kind summaries so Agent/token/Forge/source/media/RLM/serializer/deploy/runtime-proof rail buckets can read entry counts, freshness counts, malformed counts, latest timestamps, and total metadata size from the binary-cache manifest contract without bespoke panel logic.
- Added DX receipt-cache health classification so manifest and per-kind summary consumers share one stable ready/partial/stale/expired/malformed/missing/empty/unknown interpretation without duplicating rail-specific status rules.
- Wired the AI workspace Binary Cache rail to read DX receipt-cache artifacts through the shared `dx_catalog` rkyv/memmap contract when present, show manifest/bucket health for launch receipts and token/RLM/serializer meter buckets, gracefully fall back to the existing JSON-authoritative states when artifacts are missing, and surface malformed artifact states without treating them as ready.
- Added Binary Cache rail artifact-review status so malformed, expired, partial, missing-roots, or unknown receipt-cache artifact states route operators to regenerate metadata-only artifacts while keeping JSON receipts authoritative.
- Added the DX Agents GPUI bridge surface: Zed now reads redacted Agent status/social/automation/provider/model receipts from `G:\Dx\.dx\receipts\agents`, shows DX Agents status in Agent Configuration and the launch right rail, honors settings gates for CLI actions/managed providers/rail visibility, and runs only fixed public `dx agents ... --json` actions for contract, status, social list/actions, automations, runs, receipt list, provider list, model list, and provider catalog regeneration without importing `dx-agents` crates, secrets, or social-login flows into Zed.
- Added DX Agents social action controls: Agent Configuration now exposes per-account connect/disconnect receipt actions through fixed `dx agents social connect/disconnect --platform <platform> --json` command vectors, and Agent Configuration plus the right rail render redacted QR/link/revoke readiness from the latest social action receipts without storing payloads.
- Completed DX Agents row-action consumption: Zed now parses `accounts[].actions[]` and `automations[].actions[]`, validates action ids, receipt filenames, refresh commands, and `secrets_exposed=false`, renders action readiness in Agent Configuration and the launch right rail, and still executes only fixed safe bridge command vectors.
- Added DX Agents bridge-contract visibility so Agent Configuration and the right rail can show `dx-agents agents contract --json` status, fixed command coverage, receipt contracts, provider-catalog recovery guidance, and redaction posture from metadata-only receipts.
- Added gated provider/model/catalog refresh actions on the managed provider catalog row so the Zed surface exposes the public `dx agents providers list --json`, `dx agents models list --json`, and `dx agents providers catalog regenerate --json` bridge commands without credential import.
- Added DX Agents import-summary visibility so Agent Configuration and the launch right rail can run the fixed `dx agents import-summary --json` metadata action, cache validated stdout as the latest agents import-summary receipt, and surface release gates, action maps, recovery controls, command-fanout posture, warnings/blockers, and the next safe recovery command without starting `dx-agents`.
- Added DX Agents release-gate visibility so Agent Configuration and the launch right rail can run the fixed `dx agents release-gate --json` metadata action, cache validated stdout as the latest agents release-gate receipt, and surface final acceptance counts, receipt inbox state, action-map readiness, command-fanout posture, warnings/blockers, and first acceptance row without starting `dx-agents`.
- Implemented the DX launch workspace chrome around existing Agent thread rendering when the panel is zoomed/full-workspace: left Sources/receipts rail, right Progress/Git/Background Tasks rail, and token/tool meter slots.
- Added receipt hooks for `G:\Dx\.dx\receipts` with graceful missing/empty states and no CLI execution from Zed.
- Added an Agent action beside the existing Write/Ask mode and model controls while preserving the current model picker, profile selector, token usage, and thread behavior.
- Added New Chat, Search, Plugins, and Automations sidebar actions plus Pinned/All Chats group headers; the compact crowded-layout Automations action now drafts from DX Agents automation receipt metadata while expanded sidebar task controls remain available through existing Zed project task behavior.
- Made the Agent panel default to full-width/zoomed when the workspace has no active editor item; user zoom toggles still override that default.
- Added `execute_dx_media_tool`, a permissioned Agent tool that consumes approved media runner gates, executes ffmpeg/ffprobe via no-shell argument vectors, refuses overwrites and path traversal, records stdout/stderr previews, hashes produced files, and writes managed DX media execution receipts.
- Added a cached workspace tool-history scanner for `tools/dx-forge` and `tools/dx-media/executions`, then rendered Forge History and Media Executions in the DX right rail with missing/empty states.
- Added the first durable Sources rail model for workspace roots, DX metasearch source-pack receipts, produced media files, and Forge restore previews.
- Added `prepare_dx_source_attachment`, a permissioned Agent tool that packages selected workspace roots, metasearch source-pack receipts, produced media files, and Forge restore previews into managed source attachment receipts for later Agent context.
- Wired `prepare_dx_metasearch_context` to accept source attachment manifests/receipts, embed metasearch source-pack receipt text into compact cited context, and warn on path-only media/restore references instead of embedding binary payloads.
- Added `list_dx_launch_demo_recipes`, a permissioned read-only Agent tool that lists metasearch-to-context, media-output-to-sources, and Forge-restore-preview-to-sources demo flows with required tool chains, receipt gates, safety notes, and workspace receipt-root status.
- Added `gate_dx_serializer_rlm_runner`, a permissioned Agent tool that validates serializer/RLM execution plans or receipts, requires explicit runner approval, requires persisted execution receipts by default, separates RLM model-call approval, and can write managed runner-gate receipts without running serializer/RLM code.
- Added `write_dx_serializer_rlm_reduced_context`, a permissioned Agent tool that consumes runner-gate receipts and context bundles, writes deterministic reduced-context receipts with cited source summaries, and does not run external serializer/RLM crates, Cargo, network, browser input, or model calls.
- Surfaced reduced-context receipts in the launch workspace: the Sources rail now has a Reduced Context set, and the right Tool History rail scans workspace `tools/dx-serializer-rlm` receipts.
- Added guided DX sidebar actions for Demo Recipe and Review Receipts; they prepare permission-safe Agent drafts for the flagship metasearch-to-reduced-context receipt chain without auto-running tools.
- Added right-rail Media Proof and Forge Proof cards that prepare permission-safe Agent drafts for proof flows without running tools automatically.
- Added source attachment readiness to the Sources rail and a typed read-only Check score to the right rail, both backed by actual workspace/source/receipt/tool-history state.
- Added restore-preview warning labels to Forge source rows and a read-only Deploy rail backed by detected workspace deploy config files.
- Added source-derived action prompt cards and deploy readiness receipt counts/latest entries, still drafting only permission-safe Agent follow-ups.
- Added Deploy rail receipt buckets for readiness, env, logs, and rollback under `tools/dx-deploy`, with compact missing/fresh/stale/old states and safer deploy-readiness prompts that include the bucket summary.
- Added validation and visual proof freshness rows backed by managed workspace proof roots, plus produced-file proof lines for media outputs from execution receipts.
- Added URL/status deploy receipt buckets plus a Runtime Proof handoff card that drafts the next safe validation plan from Check score, proof freshness, deploy target, and receipt state without running tools.
- Added Restore Approval and Reducer Guard handoff cards plus sharper source prompts for restore-preview and reduced-context receipt rows.
- Added runtime proof freshness/status buckets for managed runtime proof, import, and runtime-status receipt roots.
- Added `plan_dx_runtime_proof`, a permissioned Agent tool that prepares governed manual validation checklists and managed plan receipts without running `just run`, Cargo, local servers, browser automation, deploys, external reducers, or restore-to-target actions.
- Split runtime proof planning from imported runtime proof in the Proof Freshness rail and Check score so plan receipts show readiness without counting as runtime-green proof evidence.
- Added compact Proof Freshness drilldowns with bucket descriptions and up to two latest receipt paths, making runtime plan/import state visible without opening raw receipt folders.
- Updated the Runtime Proof handoff card so drafted Agent prompts include current Proof Freshness bucket counts, statuses, descriptions, and latest receipt paths.
- Updated the Review Receipts action so drafted Agent prompts include live DX receipt buckets, latest receipt paths, tool-history buckets, proof freshness, and deploy receipt state.
- Expanded the Runtime Proof handoff card so drafted Agent prompts include live Check score items, blockers, receipt root/latest receipts, deploy targets, and deploy receipt buckets.
- Added a dedicated Import Proof guided card that drafts the exact operator-evidence contract for `import_dx_runtime_proof` without running validation from Zed.
- Added content-aware runtime proof status scanning so the right rail and Check score distinguish missing evidence, blocked imports, unpaired status copies, and paired runtime-green candidates.
- Added content-aware runtime proof plan drilldowns so the right rail and Agent handoff prompts show the latest governed plan command, checklist step counts, requirements, and blockers without treating plan receipts as runtime-green proof.
- Added row-level source controls so visible Sources rail rows can draft real Attach or Review Agent prompts without relying only on the separate Source Actions cards.
- Added row-level receipt drilldowns so Sources rail rows show the managed source-pack, reduced-context, media execution, or restore receipt that produced each visible source.
- Expanded source receipt drilldowns with receipt type/size metadata and added row-level Receipt handoff buttons that draft source-specific receipt review prompts.
- Added `import_dx_runtime_proof`, a permissioned Agent tool that captures operator-supplied runtime proof summaries, evidence, blockers, import receipts, and operator status copy receipts under managed runtime-proof roots.
- Added `capture_dx_forge_restore_approval`, a permissioned Agent tool that captures operator restore approval evidence, target path, rollback status, overwrite posture, blockers, and managed approval receipts under `tools/dx-forge/restore-approvals` without mutating target paths.
- Added `plan_dx_forge_restore_target`, a permissioned Agent tool that consumes restore approval receipts, inspects target/previews read-only, writes managed restore-target dry-run plan receipts, and keeps live target mutation in a separate governed future window.
- Added `preview_dx_serializer_rlm_reducer_execution`, a permissioned Agent tool that captures dry-run external reducer execution previews under `tools/dx-serializer-rlm/execution-previews` without running external reducers, Cargo, model calls, shell commands, network, or unmanaged writes.
- Added `execute_dx_serializer_rlm_reducer`, a permissioned Agent tool that runs only approved no-shell absolute serializer/RLM reducer command vectors under approved DX roots, feeds deterministic reduced-context text by stdin when requested, captures stdout/stderr previews and hashes, and writes managed external-execution receipts.
- Added content-aware Deploy receipt drilldowns so deploy buckets can surface latest receipt headline, status, target, URL, and blocker counts in the right rail and Agent handoff prompts.
- Added restore approval visibility to the read-only Forge history contract so `inspect_dx_forge_history` reports `restore_approval` entries, approval readiness, evidence counts, blockers, target paths, and preview roots.
- Added content-aware Forge history drilldowns so the right rail and Forge/Restore Approval prompts surface latest restore-target plan status, approval readiness, target path, preview root, and blockers without opening raw receipt JSON.
- Added a Runtime Evidence Form guided card plus plan-derived evidence metadata in the Runtime Proof rail, so operators can draft the exact `import_dx_runtime_proof` fields from the latest plan before any proof import.
- Added content-aware runtime proof import drilldowns so the Runtime Proof rail and Agent handoff prompts show imported proof summary, final command, source, and evidence samples from managed import receipts.
- Advanced the current non-mutating DX Native Tool Execution/Restore/Panels/Demos coding lane and the editor-lane launch UI to 100/100 coding-complete before governed runtime proof; the remaining launch gate is runtime evidence/import plus upstream source-audit reconciliation in the DX-WWW lane and any future restore-to-target mutation after preview/rollback evidence is ready.

## Remaining Proof

- The source-backed DX Studio Web Preview editing slice has rustfmt, targeted rustfmt check, targeted diff hygiene, conflict-marker validation, ASCII scan, source review, DX-WWW JSON/TypeScript manifest-contract inspection, and injected-bridge JS parse validation only so far; live WebView overlay behavior, prompt/edit flow, hot-reload timing, selection restoration after an actual DX-WWW reload, Cargo checks, local servers, and `just run` remain deferred under the launch validation rules.
- The May 22 DX launch UX/manifest polish has rustfmt, targeted rustfmt check, targeted diff hygiene, and conflict-marker validation only so far; live WebView route/source/package inspection, onboarding visual proof, Cargo checks, local servers, and `just run` remain deferred under the repo's launch validation rules.
- The DX Studio edit-contract parser hardening has formatting/diff/conflict/source-reference validation only so far; live WebView diagnostics and DX-WWW manifest producer proof remain deferred under the repo's launch validation rules.
- The DX onboarding and DX Studio Web Preview contracts have diff/conflict/source-level validation only so far; live route opening, hot-reload timing, Cargo checks, `just run`, and local server proof remain deferred under the repo's launch validation rules.
- The DX Studio edit/drag manifest, public edit-operation contract, and public-manifest field compatibility slice has diff/conflict/source-level validation only so far; live page marker proof, drag/drop runtime behavior, Cargo checks, `just run`, and DX-WWW CLI producer proof remain deferred under the repo's launch validation rules.
- The DX Studio session module split has formatting/diff/conflict/source-reference validation only so far; Cargo checks, `just run`, and runtime WebView proof remain deferred under the repo's launch validation rules.
- The DX Studio write-contract metadata hardening has formatting/diff/conflict/source-reference validation only so far; live WebView diagnostics and DX-WWW manifest producer proof remain deferred under the repo's launch validation rules.
- The DX onboarding Web Preview hero has formatting, diff, conflict-marker, and source-reference validation only so far; `just run` visual proof and native WebView2 runtime proof remain deferred under the repo's launch validation rules.
- The DX onboarding provider readiness slice has formatting/source-reference validation only so far; live onboarding visual proof, provider settings click proof, Cargo checks, local servers, and credential import proof remain deferred under the repo's launch validation rules.
- The DX onboarding WWW target discovery slice has formatting/source-reference validation only so far; live WebView2 navigation proof, `DX_WWW_WORKSPACE` route proof, Cargo checks, local servers, and `just run` remain deferred under the repo's launch validation rules.
- The DX onboarding preview-contract visibility slice has formatting/source-reference validation only so far; live visual proof, command-contract refresh proof, Cargo checks, local servers, and `just run` remain deferred under the repo's launch validation rules.
- The DX onboarding demo-action coverage slice has formatting/diff/conflict/source-reference validation only so far; live click proof, Cargo checks, local servers, and `just run` remain deferred under the repo's launch validation rules.
- The DX Agents bridge has formatting/diff/conflict/source-reference validation only so far; live `dx-agents ... --json` command proof, Cargo checks, provider calls, local servers, and runtime rail visual proof remain deferred under the repo's launch validation rules.
- The DX Agents public-command bridge has formatting/diff/conflict/source-review validation only so far; live `dx agents ... --json` command proof, Cargo checks, and runtime UI proof remain deferred under the repo's launch validation rules.
- The DX Agents receipt-index row slice has rustfmt/diff/conflict/source-reference validation only so far; live `dx agents receipts list --json` proof, Cargo checks, local servers, and runtime rail visual proof remain deferred under the repo's launch validation rules.
- The DX Agents social action receipt slice has formatting/diff/conflict/source-reference validation only so far; live social connect/disconnect command proof, Cargo checks, and runtime visual proof remain deferred under the repo's launch validation rules.
- The DX Agents import-summary slice has formatting/diff/conflict/source-reference validation only so far; live `dx agents import-summary --json` proof, Cargo checks, local servers, and runtime rail visual proof remain deferred under the repo's launch validation rules.
- The DX Agents release-gate slice has formatting/diff/conflict/source-reference validation only so far; live `dx agents release-gate --json` proof, Cargo checks, local servers, and runtime rail visual proof remain deferred under the repo's launch validation rules.
- The DX launch status aggregate slice has formatting/diff/conflict/source-reference validation only so far; live `dx launch status --json` proof, Cargo checks, local servers, and runtime rail visual proof remain deferred under the repo's launch validation rules.
- The DX launch handoff slice has formatting/diff/conflict/source-reference validation only so far; live `dx launch import-manifest --json`, `dx launch handoff --json`, Cargo checks, local servers, and runtime rail visual proof remain deferred under the repo's launch validation rules.
- The DX launch gate slice has formatting/source-reference validation only so far; live `dx launch import-summary --json`, `dx launch release-gate --json`, `dx launch fallback-drill --json`, Cargo checks, local servers, and runtime rail visual proof remain deferred under the repo's launch validation rules.
- The DX launch audit slice has formatting/source-reference validation only so far; live `dx launch schemas --json`, `dx launch fixtures --json`, `dx launch smoke --json`, Cargo checks, local servers, and runtime rail visual proof remain deferred under the repo's launch validation rules.
- The DX-WWW launch evidence slice has formatting/source-reference validation only so far; live `dx forge launch-readiness-bundle`, release packet, restart, acceptance, Friday baton commands, Cargo checks, local servers, and runtime rail visual proof remain deferred under the repo's launch validation rules.
- The DX source audit slice has formatting/source-reference validation only so far; live source-audit helper execution, cross-repo risk reconciliation, Cargo checks, local servers, and runtime rail visual proof remain deferred under the repo's launch validation rules.
- The DX launch receipt review slice has formatting/diff/conflict/source-reference validation only so far; live `dx launch receipts --json` proof, Cargo checks, local servers, and runtime rail visual proof remain deferred under the repo's launch validation rules.
- The DX metadata binary-cache bridge slice has formatting/source-level validation only so far; live receipt-cache producer proof, Cargo checks, local servers, and runtime rail visual proof remain deferred under the repo's launch validation rules.
- The DX receipt-cache producer bridge has formatting/diff/conflict/source validation only so far; governed CLI producer execution, artifact round-trip proof, Cargo checks, `just run`, and performance measurements remain deferred under the repo's launch validation rules.
- The standard DX receipt-cache root/summary/kind-summary/health contract and Binary Cache rail consumption/review path have formatting/diff/conflict/source-reference validation only so far; governed producer execution, artifact round-trip proof, Cargo checks, `just run`, and performance measurements remain deferred under the repo's launch validation rules.
- Runtime visual proof is still pending because this launch lane forbids local servers, `just run`, full Cargo builds, and heavy validation without explicit permission.
- The crowded coding layout activity bar has formatting/diff/conflict/source-reference validation only so far; live tri-pane visual proof, panel resize behavior, Cargo checks, and `just run` remain deferred under the repo's launch validation rules.
- The visible-pane activity-bar trigger refinement has formatting/diff/conflict/source-review validation only so far; live split-pane proof remains deferred under the repo's launch validation rules.
- The compact activity-bar module extraction has formatting/diff/conflict/source-reference validation only so far; Cargo checks and live visual proof remain deferred under the repo's launch validation rules.
- The compact activity-bar overflow hardening has formatting/diff/conflict/source-review validation only so far; live small-window proof remains deferred under the repo's launch validation rules.
- The compact activity-bar Background Tasks retargeting has formatting/diff/conflict/source-review validation only so far; live retained-thread selection proof remains deferred under the repo's launch validation rules.
- The compact activity-bar Sources/Automations shared routing cleanup has formatting/diff/conflict/source-review validation only so far; live click proof remains deferred under the repo's launch validation rules.
- The screen-carousel dead-code cleanup has formatting/diff/conflict/source-reference validation only so far; live screen switching proof remains deferred under the repo's launch validation rules.
- The expanded sidebar screen-grid polish has formatting/diff/conflict/source-reference validation only so far; live click and layout proof remains deferred under the repo's launch validation rules.
- The DX receipt scanner module split has formatting/diff/conflict/source-reference validation only so far; live receipt refresh and rail visual proof remain deferred under the repo's launch validation rules.
- The token/tool meter state cleanup has formatting/diff/conflict/source-reference validation only so far; live receipt visual proof and governed token/tool receipt producer proof remain deferred under the repo's launch validation rules.
- The DX CLI receipt producers remain external to Zed; this slice reads receipt files and reports missing or empty receipt states only.
- The media runner source slice has rustfmt/diff/conflict validation only so far; Cargo check/test and runtime ffmpeg proof remain deferred under the repo's launch validation rules.
- The reduced-context writer has formatting/diff/conflict validation only so far; Cargo check/test and runtime Agent proof remain deferred under the repo's launch validation rules.
- The reduced-context rail slice has formatting/diff/conflict validation only so far; runtime visual proof remains deferred under the repo's launch validation rules.
- The guided action slice has formatting/diff/conflict validation only so far; runtime click proof remains deferred under the repo's launch validation rules.
- The guided proof card slice has formatting/diff/conflict validation only so far; runtime click proof remains deferred under the repo's launch validation rules.
- The source attachment and Check score slice has formatting/diff/conflict validation only so far; runtime visual proof remains deferred under the repo's launch validation rules.
- The restore-warning and Deploy registry slice has formatting/diff/conflict validation only so far; runtime visual proof remains deferred under the repo's launch validation rules.
- The source action and deploy readiness receipt slice has formatting/diff/conflict validation only so far; runtime click proof remains deferred under the repo's launch validation rules.
- The deploy receipt bucket slice has formatting/diff/conflict validation only so far; runtime visual proof remains deferred under the repo's launch validation rules.
- The deploy receipt drilldown slice has formatting/diff/conflict validation only so far; runtime visual proof remains deferred under the repo's launch validation rules.
- The proof freshness and produced-file card slice has formatting/diff/conflict validation only so far; runtime visual proof remains deferred under the repo's launch validation rules.
- The deploy URL/status and runtime-proof handoff slice has formatting/diff/conflict validation only so far; runtime visual proof remains deferred under the repo's launch validation rules.
- The restore approval and reducer guard handoff slice has formatting/diff/conflict validation only so far; runtime visual proof remains deferred under the repo's launch validation rules.
- The runtime proof status bucket slice has formatting/diff/conflict validation only so far; runtime visual proof remains deferred under the repo's launch validation rules.
- The runtime proof plan slice has formatting/diff/conflict validation only so far; actual `just run` runtime evidence and proof import remain deferred under the repo's launch validation rules.
- The runtime proof plan visibility slice has formatting/diff/conflict validation only so far; runtime visual proof and proof import remain deferred under the repo's launch validation rules.
- The proof freshness drilldown slice has formatting/diff/conflict validation only so far; runtime visual proof remains deferred under the repo's launch validation rules.
- The runtime proof handoff-context slice has formatting/diff/conflict validation only so far; runtime visual proof remains deferred under the repo's launch validation rules.
- The live receipt-review draft slice has formatting/diff/conflict validation only so far; runtime click proof remains deferred under the repo's launch validation rules.
- The runtime proof live-context slice has formatting/diff/conflict validation only so far; runtime click proof remains deferred under the repo's launch validation rules.
- The runtime proof import handoff slice has formatting/diff/conflict validation only so far; runtime click proof and actual operator evidence import remain deferred under the repo's launch validation rules.
- The runtime proof status scanner slice has formatting/diff/conflict validation only so far; runtime visual proof and actual operator evidence import remain deferred under the repo's launch validation rules.
- The runtime proof plan drilldown slice has formatting/diff/conflict validation only so far; runtime visual proof and actual operator evidence import remain deferred under the repo's launch validation rules.
- The runtime proof evidence-form slice has formatting/diff/conflict validation only so far; runtime visual proof and actual operator evidence import remain deferred under the repo's launch validation rules.
- The runtime proof import drilldown slice has formatting/diff/conflict validation only so far; runtime visual proof and actual operator evidence import remain deferred under the repo's launch validation rules.
- The row-level source control slice has formatting/diff/conflict validation only so far; runtime click proof remains deferred under the repo's launch validation rules.
- The row-level receipt drilldown slice has formatting/diff/conflict validation only so far; runtime visual proof remains deferred under the repo's launch validation rules.
- The source receipt review handoff slice has formatting/diff/conflict validation only so far; runtime click proof remains deferred under the repo's launch validation rules.
- The runtime proof import capture slice has formatting/diff/conflict validation only so far; Cargo check/test and runtime proof remain deferred under the repo's launch validation rules.
- The restore approval capture slice has formatting/diff/conflict validation only so far; Cargo check/test and restore-to-target runtime proof remain deferred under the repo's launch validation rules.
- The restore approval history visibility slice has formatting/diff/conflict validation only so far; Cargo check/test and restore-to-target runtime proof remain deferred under the repo's launch validation rules.
- The restore-target dry-run plan slice has formatting/diff/conflict validation only so far; Cargo check/test and restore-to-target mutation remain deferred under the repo's launch validation rules.
- The Forge history drilldown slice has formatting/diff/conflict validation only so far; Cargo check/test and restore-to-target runtime proof remain deferred under the repo's launch validation rules.
- The serializer/RLM execution preview slice has formatting/diff/conflict validation only so far; Cargo check/test and external reducer runtime proof remain deferred under the repo's launch validation rules.
- The serializer/RLM external execution slice has formatting/diff/conflict validation only so far; Cargo check/test and actual reducer runtime proof remain deferred under the repo's launch validation rules.

## dx-check Quick-Fix Risk Metadata - 2026-05-22

- Native Zed dx-check snapshots now carry quick-fix `risk_level`, `requires_user_approval`, and `writes_receipts` fields from the shared receipt contract.
- Missing older metadata is derived from the command shape so `dx check ...` actions render as receipt-writing actions and opt-in runner/probe actions stay visibly approval-sensitive.
- The launch rail now shows risk, approval, and receipt-write state next to each dx-check quick fix instead of rendering command text with no operator risk context.
- Verification: `rustfmt --check crates/agent_ui/src/dx_check_panel.rs crates/agent_ui/src/dx_launch_workspace.rs` passed; targeted `cargo test -p agent_ui dx_check_panel --lib` was attempted twice but exceeded the timeout on this busy machine.
