# DX/Zed Launch Worker Notes

Date: 2026-05-29
Launch target: historical 2026-05-22 planning target; current production readiness is source-audited only until the governed runtime proof window opens.
Current branch handoff: `dev`, tracking `origin/dev` when this audit started.

## Worker Role

This checkout is the Zed/DX editor surface. Worker chats here should focus on GPUI integration, AI panel polish, DX Agents bridge hooks, token meters, source rails, progress rails, and launch-safe runtime wiring.

## Required Reading

- `G:\Dx\WORKER_PROMPTS.md`
- `G:\Dx\DX.md`
- `G:\Dx\zed\AGENTS.md`
- `G:\Dx\zed\PLAN.md`

## Hard Rules

- Use `[@superpowers](plugin://superpowers@openai-curated)` in new worker chats.
- Preserve existing Zed AI behavior.
- Do not create dummy UI.
- Do not start local servers.
- Do not run `just run`, full Cargo builds, or expensive workspace checks until the user opens the governed final validation window.
- Prefer code reading, targeted implementation, `git diff --check`, conflict-marker search, and narrow checks after coherent milestones.
- Update `todo.txt`, `changelog.txt`, and this file when changing launch status.

## Current Verification Lane

- This production-readiness pass is no-`just run` and no-Cargo by direct instruction.
- Allowed proof for this pass is release-hygiene inspection with `git status`, targeted `rg`, and `git diff --check`.
- Existing `100/100`, "complete", and "production" notes in older handoffs mean source/code-complete for their scoped lane unless a later governed runtime proof says otherwise.
- Do not claim runtime-green, production-ready, or launch-ready from these docs alone.

## Current Style Sidebar Direction

- The DX Style sidebar must use GPUI as the native dock/status shell and Web Preview as the visual generator canvas. Do not hand-build complex gradient, shadow, animation, grid/flex, or shape generators as GPUI widgets.
- Current source-only slice: the Agent launch workspace Style rail and right-dock DX Style panel read the real `G:\Dx\style` plan, grouped-class two-way contract, grouped-class editor read model, first-25 visual generator catalog, readiness files, the DX Style Web Preview generator surface, and the review-only source-apply session bridge opened by Zed.
- The dock panel reports active file style relevance and cursor-scoped static class/className token context from the active workspace item, including explicit source path, human-readable span, structured byte span, source digest, and read-only source state.
- Web Preview owns the first local DX Style generator surface behind `dx_style::OpenGeneratorPreview`; GPUI panels only dispatch that real action and report source/readiness state. Catalog, control, recipe, source-apply, group-context, and CSS declaration hint metadata alignment is surfaced in source checks, recipe runtime value keys come from the source-owned DX Style fixture, and Apply stays gated when those source-owned fixtures or recipe placeholders drift. Zed also sends a bounded static class-list token summary and grouped-class context when available, so Web Preview can prioritize the right generator from inline group utilities, the surrounding className, or CSS declaration hints while honestly marking alias-only `button()` calls as requiring a project group registry receipt before expansion. Source Apply also requires the source-owned `dx.style.grouped-class-source-apply-contract`, expected Zed context schema, a real native Web Preview apply handler, and dedicated source-path and structured source-span context fields, with explicit `source_apply_blocker` and `source_apply_payload_diagnostic` output for review, so future receipt readiness cannot accidentally turn the button into an enabled no-op or infer source identity from display text. The first native handler is review-only: it can produce a bounded `dx-style-source-apply` IPC receipt with the contract schema and IPC kind, but it does not mutate source until the source-owned contract and editor write bridge both expose mutation capability.
- Source-apply review IPC is now tied to the Web Preview generator surface that Zed opened. The native view creates a short source-apply session token, embeds it into the local generator page, and refuses `dx-style-source-apply` messages whose envelope and request body do not echo that active trusted session.
- Source-apply session tokens now include UUID-backed nonce material, are hidden behind a stable `zed://dx-style/generator` display URL, are not copied into split panes, and are consumed only after a trusted `dx-style-source-apply` request reaches native review. Refused/untrusted packets do not burn the valid session, and the Web Preview IPC bridge caps aggregate queued IPC bytes in addition to per-message and message-count limits.
- Native source-apply review now revalidates trusted Web Preview review packets against open editor source identity before preserving review evidence. The native side must find a singleton editor for the requested source path and match the structured byte span, active source length, and bounded `fnv1a64:` digest; missing editors, stale spans, digest drift, and unsupported buffers stay blocked while source mutation remains disabled.
- Native source-apply review is now also bound to the exact source identity that launched the trusted Web Preview generator session. Zed stores a bounded session source identity beside the native session token, rejects malformed launch identity up front, and requires request path, span, source length, digest, session source, and live editor source to agree before preserving a matched review receipt.
- Native source-apply review now nests same-session native editor identity evidence under `native_active_editor_source_revalidation.session_source.native_editor`. Zed captures the source editor item id, editor entity id, active buffer entity id, active buffer remote id, multi-buffer id, worktree id, project path, and singleton buffer kind at generator launch, then refuses matched review if that original editor/buffer identity is no longer open or no longer matches.
- DX Style's editor write-bridge preflight now mirrors that stricter review evidence before any future source writer can be considered: trusted Web Preview session, session-bound source identity, active source length, native active-editor revalidation, same-session native editor identity, trusted dry-run receipt, cursor-scoped structured edit preview, reverse-delta provenance, CSS dry-run receipt, explicit apply, and authorized runtime validation. Zed reads and copies this as preflight evidence while `can_apply`, `can_mutate_source`, and source mutation remain disabled.
- Source-write readiness is now explicit evidence instead of an inference: DX Style's source-apply contract names `source_write_readiness`, Zed's Web Preview copied review packet and native review receipt both emit bounded missing-requirement codes, and refused/untrusted packets also carry a fail-closed readiness block. This remains source-only evidence; no WebView/runtime/build proof or mutation-capable writer proof has been run.
- The Web Preview generator cockpit also has a source-only script parse guard in `script/dx-style-panel-source.test.ts`; it substitutes fixture-shaped JSON into the embedded Rust raw-string browser script and verifies syntax without opening Zed, a WebView, Cargo, or `just run`.
- Alias-only grouped calls now have a real receipt path: `button()` remains unresolved without a trusted `dx.style.grouped-class-registry-receipt`, but when DX Style emits the latest `.dx/receipts/style/grouped-class-registry-latest.json` snapshot with `registry_entries_verified` and `source_owned`, Zed can pass the receipt path and bounded atomics into the Web Preview generator context without enabling source writes. Zed looks for project-local receipts near the active file before the DX fallback roots, and reads the freshest trusted snapshot as a whole so older receipts cannot keep removed aliases alive. DX Style also emits the sibling `.dx/receipts/style/grouped-class-reverse-css-map-latest.json` receipt; Zed reads its bounded review status into the Web Preview context, but generated-CSS-to-source projection stays disabled until source digest, editor bridge, and declaration-delta guards exist.
- Reverse CSS delta previews now carry grouped-class provenance through Web Preview and native review receipts, including the alias, trusted registry receipt, reverse CSS map receipt/status, group expansion state, source state, and grouped utility count. This keeps visual generator output connected to source-owned grouped atomics instead of becoming detached generated CSS.
- Reverse CSS delta review now covers effect generator output as source-owned review evidence: `box-shadow` and `text-shadow` can map to arbitrary shadow utilities, `filter: drop-shadow(...)` can map to `drop-shadow-[...]`, and `backdrop-filter: blur(...)` can map to `backdrop-blur-[...]` while preserving color utilities and keeping mutation disabled.
- Reverse CSS delta review now also covers layout generator output for review-only grouped atomics: `align-items`, `justify-content`, `grid-template-columns`, `grid-template-rows`, `column-gap`, and `row-gap` can map to `items-*`, `justify-*`, `grid-cols-*`, `grid-rows-*`, `gap-x-*`, and `gap-y-*` when the generated CSS value matches a source-owned strategy.
- Reverse CSS delta review now covers deterministic spacing, sizing, display, and flex-content generator output: `display`, `padding*`, `margin*`, `width`, `height`, `min-*`, `max-*`, and `align-content` can map back to source-owned grouped atomics, including negative margin utilities such as `-mt-4`. Web Preview treats `display` as a fallback so richer grid/flex declarations are reviewed first, and source mutation remains disabled.
- Reverse CSS delta review now tries every source-owned strategy for a matching declaration before refusing it, so generators that emit literal CSS such as `gap: 16px`, `padding: clamp(...)`, `width: 320px`, or `border-radius: 14px` can fall back to arbitrary grouped atomics like `gap-[16px]` after token-specific strategies fail.
- Reverse CSS delta review now covers the remaining color-token declarations advertised by the Style hint catalog: `outline-color`, `accent-color`, and `caret-color` map to `outline-*`, `accent-*`, and `caret-*`, with outline width/style utilities preserved during review.
- The Style CSS declaration hint catalog now points `outline-color` at `outline-*` instead of `border-*`, keeping cursor-driven generator selection aligned with the source-owned reverse-delta mapping.
- Reverse CSS delta review now covers transition generator output: `transition-property`, `transition-duration`, `transition-delay`, and `transition-timing-function` can map back to `transition-*`, `duration-*`, `delay-*`, and `ease-*`, with known easing curves normalized before arbitrary `ease-[...]` fallback. Web Preview treats `transition-property` as a fallback so duration/easing edits from the same generator output can be reviewed first.
- The easing/cubic-bezier generator metadata now matches that transition review path: DX Style and Zed advertise `[transition-timing-function:...]` and `ease-*` for the easing generator instead of pointing cursor routing at animation timing while the recipe emits transition timing.
- Reverse CSS delta review now covers `background-image` generator output as conservative grouped atomics: gradient/image declarations can map to `bg-[...]`, while existing color utilities such as `bg-primary` are preserved instead of being mistaken for background-image utilities.
- Native source-apply review now checks that reverse CSS delta preview provenance still matches the active Zed group context. A stale or forged alias, registry receipt, reverse CSS map receipt/status, expansion state, or utility count becomes a named review refusal before any future mutation-capable writer can trust the preview.
- DX Style now owns that provenance rule in both source contracts: `dx.style.grouped-class-reverse-css-delta-contract` lists the required preview provenance fields, and `dx.style.grouped-class-source-apply-contract` requires the same provenance-match guard before source apply can graduate beyond review.
- Zed native review enforces the required provenance fields from the DX Style contract and refuses unsupported required field names as contract drift. This keeps reverse-delta trust source-owned even as DX Style adds or tightens provenance requirements.
- The Web Preview generator cockpit shows the same reverse-delta contract diagnostics before IPC, including missing provenance guards, missing required provenance fields, and unsupported provenance field names.
- Web Preview also reports live preview provenance diagnostics from the same required field list, separating contract drift from stale or missing active group context before native source-apply review.
- The context-aware Web Preview generator action now bounds, parses, and script-safely embeds the GPUI-provided editor context before loading the local generator page. Oversized or malformed context becomes a read-only blocked `zed.dx_style.active_context.v1` payload instead of an unbounded data URL or silent null state.
- The Web Preview generator cockpit now has a source-catalog-backed filter for the visual generator list, so the first 25 generators remain navigable and future generator growth does not require GPUI-owned controls.
- DX Style recipe metadata now carries source-owned preview kind, a supported preview-part list, and per-recipe preview anatomy parts (`hero-card`, `text-card`, `swatch-pair`, `layout-items`, `timeline`, plus their swatch/timeline/label children) so Web Preview can validate and render richer previews for layout, motion, text, and color generators without hardcoding generator IDs or preview structure in Zed browser code.
- Web Preview now forwards and displays DX Style catalog `preferred_output` and `source_edit_safety` metadata for each generator, so the cockpit can distinguish atomic-utility output, DX-owned CSS, safe static source edits, and read-only generators from source-owned catalog policy.
- Web Preview now has wired copy actions for generated class output, generated CSS, and a structured review packet containing generator metadata, active source context, source-apply state, reverse CSS delta preview, and metadata diagnostics. This improves handoff usability without enabling source mutation.
- Web Preview now separates native source review from source mutation: `Review source` can send a bounded `dx-style-source-apply` request to Zed's review-only native handler when source context and contract checks are valid, while `Apply` remains disabled until the source-owned contract, trusted receipt, editor write bridge, and native writer all expose mutation capability.
- Native source-apply review receipts now preserve a source-owned `dry_run_review` block with trusted receipt presence, match state, receipt path, receipt summary, and mismatch diagnostics. DX Style's source-apply contract also names `dry_run_review` and `mutation_ready` as expected review receipt fields, so copied Web Preview review packets and native receipts carry the same dry-run evidence without implying mutation.
- Native source-apply review receipts now also preserve bounded `preview_output` metadata from the Web Preview generator output, including source-owned preview kind and preview anatomy parts, so review evidence matches the visual state being inspected.
- DX Style's source-apply contract now owns preview output caps for preview kind, preview anatomy part bytes, and preview anatomy part count. Web Preview reports those limits before IPC, and native review validates the same caps before preserving `preview_output`.
- Source-apply review now requires the active context to carry a bounded `fnv1a64:` source digest before Web Preview can send native review, and native receipts validate the same digest prefix and contract byte limit. Path/span-only payloads are treated as incomplete source identity.
- CSS declaration dry-run review now preflights and validates the source-owned path, span, and digest caps from `dx.style.css-declaration-dry-run-contract` in both Web Preview review output and native receipts, keeping CSS-file generator evidence aligned with the same source identity limits used by future apply paths.
- Native review now names CSS declaration dry-run contract cap drift explicitly, so generator handoffs can tell CSS dry-run policy mismatches apart from grouped-class source-apply contract drift.
- The Style sidebar readiness gate now checks the DX Style Web Preview generator surface plus review-only native source-apply handler directly with a bounded scan for the large Web Preview host file, keeping GPUI as a shell and Web Preview as the real generator host before enabling the context-aware generator action.
- Trusted dry-run receipt summaries now keep bounded structured edit previews in addition to human-readable preview lines, carrying source path, byte span, and replacement text through the apply gate, copied Web Preview review packet, and native review receipt. Native review also records a dedicated `dry_run_edit_review` block that must find a trusted structured edit preview scoped to the active source span before any future writer can treat the dry-run receipt as cursor-ready.
- Active style context now carries a machine-readable context kind (`class_token`, `class_list`, or `css_declaration`) plus DX Style-owned CSS declaration source-edit safety. Web Preview and native review receipts accept CSS declaration contexts for generator review, but source mutation remains limited to class-token contexts and CSS declarations report `css_declaration_requires_css_dry_run_receipt` until a CSS-specific dry-run/apply contract exists.
- DX Style now provides that source-owned CSS declaration dry-run contract as `dx.style.css-declaration-dry-run-contract`. Zed mirrors it into Web Preview, displays its schema/source/required fields/accepted safety values, includes it in review packets and native receipts, and keeps CSS declaration mutation blocked until a trusted CSS declaration dry-run receipt exists.
- Web Preview now derives a structured CSS declaration dry-run preview from generator output for CSS declaration contexts and forwards the proposed declaration into review packets and native source-apply review receipts. This makes CSS review concrete without turning the Style panel into a CSS writer.
- Web Preview and native review now enforce the CSS declaration dry-run contract's `max_declaration_bytes` cap before preserving a proposed declaration, so CSS review evidence stays bounded by DX Style-owned policy.
- Next source slice: keep the editor write-bridge preflight aligned with the source-owned review evidence, then move toward mutation only after trusted spans, receipts, editor identity, the native writer, and the editor bridge are proven in an authorized runtime window.

## Lightweight Source Guard Registry

These guards are source-contract checks only. They keep source ownership, handoff wording, receipt parsing, and file-size boundaries discoverable, but they do not prove native runtime behavior, WebView/browser behavior, Cargo health, live deploys, or production readiness. Run the narrowest guard that matches the owned lane, and keep `just run`, Cargo, local servers, and runtime proof gated until the user explicitly opens that window.

Current handoff and production-readiness guards:
- `node --test script/dx-handoff-source-guard-registry.test.ts` - handoff docs, guard registry, and source-only claim hygiene.
- `node --test script/dx-windows-reliability-source.test.ts` - Windows scanner/minidump source contracts.
- `node --test script/zed-builtin-schema-source.test.ts` - built-in JSON schema render byte-boundary contract.
- `node --test script/zed-platform-source.test.ts` - app-bundle platform copy failure display boundaries.
- `node --test script/zed-startup-reliability-source.test.ts` - startup theme/keymap and reliability enumeration boundaries.
- `node --test script/dx-project-agent-registry-source.test.ts` - ACP registry HTTP response byte and error-display boundaries.
- `node --test script/dx-agent-registry-ui-source.test.ts` - Agent Registry UI query and result materialization boundaries.
- `node --test script/dx-completion-provider-source.test.ts` - Agent prompt completion candidate materialization boundaries.
- `node --test script/dx-project-search-source.test.ts` - project search multiline file-read byte boundaries.
- `node --test script/dx-prompt-store-source.test.ts` - prompt metadata fuzzy candidate materialization boundaries.
- `node --test script/dx-task-inventory-source.test.ts` - task inventory template, debug scenario, and validation-list boundaries.
- `node --test script/dx-lsp-aggregation-source.test.ts` - document link and symbol LSP response aggregation boundaries.
- `node --test script/dx-language-model-selector-source.test.ts` - language model picker provider/model/result materialization and favorite-cycle boundaries.
- `node --test script/dx-model-selector-source.test.ts` - agent model selector grouped/fuzzy materialization and favorite-cycle boundaries.
- `node --test script/dx-agent-ui-selector-source.test.ts` - Agent UI model selector stale fuzzy candidate-id boundaries.
- `node --test script/zed-open-listener-source.test.ts` - CLI/open-listener path, diff, item, and fanout boundaries.
- `node --test script/dx-context-server-store-source.test.ts` - context server configuration, command, OAuth, and fanout boundaries.
- `node --test script/dx-worktree-event-source.test.ts` - worktree filesystem event, metadata, and changed-path boundaries.
- `node --test script/dx-entry-view-state-source.test.ts` - agent entry-view tool-call display materialization and stale-entry replacement boundaries.
- `node --test script/dx-profile-selector-source.test.ts` - profile picker candidate, fuzzy-match, and render materialization boundaries.
- `node --test script/dx-terminal-thread-metadata-source.test.ts` - terminal thread metadata DB, cache, path-list, and serialization boundaries.
- `node --test script/dx-terminal-display-source.test.ts` - terminal display/search/hyperlink materialization boundaries.
- `node --test script/dx-editor-git-source.test.ts` - editor diff-review overlay stale-index boundaries.
- `node --test script/dx-editor-search-source.test.ts` - editor search match stale-index boundaries.
- `node --test script/dx-editor-items-source.test.ts` - editor searchable item match activation and navigation stale-index boundaries.
- `node --test script/dx-editor-context-menu-source.test.ts` - editor completion navigation, markdown, and documentation cache stale-index boundaries.
- `node --test script/dx-editor-lsp-ui-source.test.ts` - editor code-action, code-lens, and completion fanout boundaries.
- `node --test script/dx-editor-element-source.test.ts` - editor element layout stale visible-row and popover materialization boundaries.
- `node --test script/dx-editor-line-ops-source.test.ts` - editor line-rotation selection remap stale-row boundaries.
- `node --test script/dx-language-registry-source.test.ts` - language registry/settings and WASM metadata materialization boundaries.
- `node --test script/zed-app-fanout-source.test.ts` - app-level quit, channel-note, serialization, and action dump fanout boundaries.
- `node --test script/dx-terminal-view-source.test.ts` - terminal view persistence, restore, selection, and render materialization boundaries.
- `node --test script/dx-file-finder-source.test.ts` - file finder history, query, channel, worktree, and result materialization boundaries.
- `node --test script/dx-agent-diff-source.test.ts` - agent diff changed-buffer, hunk, undo, and workspace item materialization boundaries.
- `node --test script/dx-editor-navigation-source.test.ts` - editor navigation, signature-help, linked-editing, and LSP extension fanout boundaries.
- `node --test script/dx-editor-inlay-semantic-source.test.ts` - editor inlay hint and semantic-token request/result materialization boundaries.
- `node --test script/dx-editor-input-source.test.ts` - editor newline cursor remap stale-row boundaries.
- `node --test script/dx-workspace-ui-state-source.test.ts` - workspace dock, history, jump-list, and item project-handle materialization boundaries.
- `node --test script/dx-title-bar-source.test.ts` - title-bar application menu stale entry activation boundaries.
- `node --test script/dx-project-panel-source.test.ts` - project panel tree, selection, visible-range, edit-state, drag/drop, sticky-row, undo, and lazy inline media-preview materialization boundaries.
- `node --test script/dx-diagnostics-ui-source.test.ts` - diagnostics grouping, excerpt, markdown, hint, copy, and status-label materialization boundaries.
- `node --test script/dx-git-ui-source.test.ts` - Git branch, stash, repository, status, history, remote, and worktree picker materialization boundaries.
- `node --test script/dx-settings-keymap-source.test.ts` - settings search/picker rows and keymap stale candidate-id materialization boundaries.
- `node --test script/dx-settings-ui-source.test.ts` - Settings UI font, theme, and icon-theme picker stale fuzzy candidate-id boundaries.
- `node --test script/dx-extensions-ui-source.test.ts` - extensions search/list/card/suggestion/version-selector materialization boundaries.
- `node --test script/dx-outline-theme-source.test.ts` - outline stale candidate-id, outline-panel, theme/icon-theme selector, and file-icon suffix materialization boundaries.
- `node --test script/dx-command-palette-source.test.ts` - command palette stale-selection and command materialization boundaries.
- `node --test script/dx-picker-core-source.test.ts` - picker core stale-selection, selectable fallback, and reveal-scroll boundaries.
- `node --test script/dx-search-ui-source.test.ts` - project-search active-match label and navigation index boundaries.
- `node --test script/dx-debugger-ui-source.test.ts` - debugger console DAP completion materialization boundaries.
- `node --test script/dx-debugger-panel-source.test.ts` - debugger restart missing-binary boundaries.
- `node --test script/dx-debugger-run-terminal-source.test.ts` - debugger run-in-terminal missing-binary cwd fallback boundaries.
- `node --test script/dx-debugger-stack-frame-source.test.ts` - debugger stack-frame filtered-row and entry render boundaries.
- `node --test script/dx-debugger-module-list-source.test.ts` - debugger module-list stale-row render boundaries.
- `node --test script/dx-collab-panel-source.test.ts` - collab panel stale fuzzy candidate-id materialization boundaries.
- `node --test script/dx-collab-notifications-source.test.ts` - collab contact finder async-result selection boundaries.
- `node --test script/dx-markdown-preview-source.test.ts` - remote directory listing response materialization boundaries.
- `node --test script/dx-activity-indicator-source.test.ts` - activity indicator status, tooltip, and pending-work menu materialization boundaries.
- `node --test script/dx-project-symbols-source.test.ts` - project symbols UTF-8 highlight and stale fuzzy candidate-id boundaries.
- `node --test script/dx-snippets-source.test.ts` - snippet scope candidate, existing scope-file, and stale candidate-id boundaries.
- `node --test script/dx-repl-ui-source.test.ts` - REPL kernel picker stale-selection and selected-kernel materialization boundaries.
- `node --test script/dx-preview-surfaces-source.test.ts` - CSV preview rendered-index metric materialization boundaries.
- `node --test script/dx-component-preview-source.test.ts` - Component Preview list/sidebar stale-row render boundaries.
- `node --test script/dx-acp-tools-source.test.ts` - ACP tool request-id chip materialization boundaries.
- `node --test script/dx-breadcrumbs-source.test.ts` - breadcrumb segment materialization boundaries.
- `node --test script/dx-icon-picker-source.test.ts` - icon picker TSV sample and representative preview materialization boundaries.
- `node --test script/dx-language-selector-source.test.ts` - language selector candidate, match, stale-selection, and confirm boundaries.
- `node --test script/dx-toolchain-selector-source.test.ts` - toolchain selector match, stale-selection, and stale candidate-id boundaries.
- `node --test script/dx-dev-container-source.test.ts` - Dev Container template/feature picker stale match-row boundaries.
- `node --test script/dx-recent-projects-ui-source.test.ts` - recent projects candidate, path label, result, and stale-selection boundaries.
- `node --test script/dx-notifications-source.test.ts` - status toast message and action label materialization boundaries.
- `node --test script/dx-encoding-selector-source.test.ts` - encoding selector match caps, stale-selection, and candidate-id boundaries.
- `node --test script/dx-line-ending-selector-source.test.ts` - line ending selector stale-selection boundaries.
- `node --test script/dx-settings-profile-selector-source.test.ts` - settings profile selector row caps, active-profile preservation, stale-selection, and candidate-id boundaries.
- `node --test script/dx-tasks-modal-source.test.ts` - tasks modal candidate, match, tooltip tag, stale-selection, and confirm boundaries.
- `node --test script/dx-feedback-source.test.ts` - feedback installed-extension clipboard and prompt materialization boundaries.
- `node --test script/dx-font-panel-source.test.ts` - font panel custom web-font, element-id, and preview-file materialization boundaries.
- `node --test script/dx-agent-config-options-source.test.ts` - agent config option row, current overflow option, fuzzy, stale-selection, and stale next-index boundaries.
- `node --test script/dx-agent-tool-picker-source.test.ts` - agent tool picker row cap, context-server header, and stale-selection boundaries.
- `node --test script/dx-debugger-attach-modal-source.test.ts` - debugger attach process candidate, command text, fuzzy row, and stale-selection boundaries.
- `node --test script/dx-debugger-new-process-source.test.ts` - debugger scenario candidate, prompt command, stale-selection, and render candidate-id boundaries.
- `node --test script/dx-open-path-prompt-source.test.ts` - open path prompt directory, fuzzy, user-input suffix, and stale-selection boundaries.
- `node --test script/dx-tab-switcher-source.test.ts` - all-pane tab switcher collection, fuzzy label, result mapping, and stale-selection boundaries.
- `node --test script/dx-agent-server-store-source.test.ts` - agent server discovery and archive-cache enumeration boundaries.
- `node --test script/dx-workspace-reentrant-source.test.ts` - active-workspace event payload and reentrant update contracts.
- `node --test script/dx-workspace-persistence-source.test.ts` - workspace persistence KVP, JSON column byte, and stale-index boundaries.
- `node --test script/dx-workspace-enumeration-source.test.ts` - workspace history and local discovery enumeration boundaries.
- `node --test script/dx-source-quality.test.ts` - DX Studio source/edit manifest and bridge source contracts.
- `node --test script/dx-style-panel-source.test.ts` - DX Style plan/read-model, Web Preview generator split modules, trusted source-apply session, CSS declaration dry-run review contracts, and Zed Style panel source-truth contracts.
- `node G:\Dx\style\scripts\sync_zed_visual_generator_fixtures.mjs --check` - DX Style source fixture to Zed embedded visual-generator mirror freshness.
- `node --test script/dx-studio-project-source.test.ts` - DX Studio project detection and source-edit bounded file-read contracts.
- `node --test script/dx-buffer-codegen-source.test.ts` - inline assistant rewrite source-selection byte boundaries.
- `node --test script/dx-inline-prompt-source.test.ts` - inline assistant prompt materialization, linked-group byte, and prompt-history navigation boundaries.
- `node --test script/dx-agent-panel-input-source.test.ts` - Agent Panel editor text and draft materialization byte boundaries.
- `node --test script/dx-agent-panel-clipboard-source.test.ts` - native-agent thread clipboard payload size boundaries.
- `node --test script/dx-agent-configuration-source.test.ts` - Agent Configuration settings editor insertion boundaries.
- `node --test script/dx-agent-configuration-modal-source.test.ts` - Agent Configuration modal text-input byte boundaries.
- `node --test script/dx-agent-persisted-state-source.test.ts` - Agent Panel and draft prompt persisted KVP JSON boundaries.
- `node --test script/dx-agent-thread-view-source.test.ts` - Agent thread view thinking-effort stale-cycle boundaries.
- `node --test script/dx-mention-set-source.test.ts` - external skill mention file bounded reads.
- `node --test script/dx-message-editor-source.test.ts` - pasted markdown mention-link parsing size boundaries.
- `node --test script/dx-thread-metadata-source.test.ts` - persisted remote-connection metadata JSON boundaries.
- `node --test script/dx-thread-archive-source.test.ts` - thread import, archive, bounded replacement, and project-picker materialization boundaries.

Deploy and check guards:
- `node --test script/dx-deploy-panel-source.test.ts`
- `node --test script/dx-deploy-receipts-source.test.ts`
- `node --test script/dx-deploy-launch-gate-source.test.ts`
- `node --test script/dx-deploy-launch-evidence-source.test.ts`
- `node --test script/dx-check-panel-source.test.ts`
- `script/dx-deploy-source-guard.ts` is the shared helper for deploy source guards, not a standalone test command.

Launch workspace and receipt guards:
- `node --test script/dx-launch-workspace-source.test.ts`
- `node --test script/dx-launch-audit-source.test.ts`
- `node --test script/dx-launch-audit-fixtures.test.ts`
- `node --test script/dx-launch-binary-cache-source.test.ts`
- `node --test script/dx-launch-contracts-source.test.ts`
- `node --test script/dx-launch-contracts-fixtures.test.ts`
- `node --test script/dx-launch-prompts-source.test.ts`
- `node --test script/dx-launch-readiness-source.test.ts`
- `node --test script/dx-launch-readiness-fixtures.test.ts`
- `node --test script/dx-launch-receipts-source.test.ts`
- `node --test script/dx-launch-source-audit-source.test.ts`
- `node --test script/dx-launch-status-source.test.ts`
- `node --test script/dx-receipt-history-source.test.ts`
- `node --test script/dx-receipt-directory-source.test.ts`
- `node --test script/dx-runtime-proof-status-source.test.ts`
- `node --test script/dx-source-sets-source.test.ts`

Adjacent source guards:
- `node --test script/dx-agent-bridge-source.test.ts`
- `node --test script/web-preview-payload-source.test.ts`
- `node --test script/dx-www-launch-evidence-source.test.ts`
- `node --test script/web-preview-platform-lifecycle.test.ts`

## Current Launch Targets

- Add `Agent` alongside existing AI actions like `Write` and `Ask`.
- Add left Sources rail and right Progress/Git/Background Tasks rail.
- Add New Chat, Search, Plugins, and Automations actions.
- Add Pinned and All Chats workspace groups.
- Wire DX token/RLM/serializer receipts into token/tool meters.
- Wire DX Agents CLI receipts, social/account readiness, automations, and provider/model catalog readiness into GPUI status surfaces.

## Current Worker Update

- DX Style sidebar architecture is locked to a native-shell plus Web Preview split: GPUI owns the right-dock panel shell, active editor context, status, receipts, trust gates, and commands, while Web Preview owns the actual visual CSS generator cockpit, live previews, generated class/CSS output, and read-only patch review UI. Trusted dry-run receipts are treated as applicable only after matching the active source path, cursor token span, and shared dx-style `fnv1a64:` source digest; trusted-but-mismatched receipts now surface bounded path/span/digest mismatch reasons plus the closest trusted receipt candidate for review only. Do not rebuild the generator controls as hand-authored GPUI components.

- Completed a six-agent Project Panel media preview lane without `just run` or Cargo. Expanded folders now build lazy, bounded, cached media preview data from direct child entries only, preserving normal tree explorer behavior for code and document files plus existing selection, rename, drag/drop, context-menu, reveal, keyboard, sticky-row, uniform-list scroll, and panel behavior. Image files render as direct local visual previews in compact rectangle cards inside the existing row end slot; video files use same-stem image sidecars as representative frame previews when available and fall back to a lightweight play card otherwise; audio files render an honest `Duration unavailable` card with the file name available in the tooltip until off-UI-path duration extraction is authorized. Extended `script/dx-project-panel-source.test.ts` for lazy expanded-folder probing, scan/item caps, negative-result caching, inline-card caps, direct image/video/audio card contracts, variable-height uniform-list regression prevention, and UI-path filesystem-probe avoidance. Passed source-only checks: `node --test script\dx-handoff-source-guard-registry.test.ts script\dx-project-panel-source.test.ts` (13/13 passing), `rustfmt --edition 2024 --check --config skip_children=true crates\project_panel\src\project_panel.rs crates\project_panel\src\media_preview.rs`, line-anchored conflict-marker scan over touched lane files, and `git diff --check -- crates/project_panel/src/project_panel.rs crates/project_panel/src/media_preview.rs script/dx-project-panel-source.test.ts DX.md todo.txt changelog.txt docs/superpowers/plans/2026-05-29-project-panel-media-preview-lane-1.md`. Skipped by direct instruction: Cargo build/check/test/clippy, `just run`, local servers/apps, browser automation, thumbnail/duration extraction workers, and live editor runtime proof.

- Completed a six-agent editor/project-panel/agent-ui stale-state hardening pass without `just run` or Cargo. Editor searchable item activation/navigation, completion context-menu navigation and markdown lookup, Agent entry-view replacement, thread-archive bounded replacement, Project Panel visible-entry range/edit-state materialization, and editor newline cursor remapping now use checked lookups or safe fallbacks instead of stale direct indexes or invariant panics. Added and registered `script/dx-editor-items-source.test.ts` and `script/dx-editor-input-source.test.ts`, and extended `script/dx-editor-context-menu-source.test.ts`, `script/dx-entry-view-state-source.test.ts`, `script/dx-thread-archive-source.test.ts`, and `script/dx-project-panel-source.test.ts`. Passed source-only integration: 26/26 targeted Node source subtests including the handoff registry, targeted rustfmt checks, stale direct-index/panic pattern scan, line-anchored conflict-marker scan, and `git diff --check`. Skipped by direct instruction: Cargo build/check/test/clippy, `just run`, local servers, browser automation, and live editor runtime proof.

- Completed a six-agent editor/workspace/debugger stale-state hardening pass without `just run` or Cargo. Editor element layout navigation labels, inline diagnostics, and cursor popovers now check visible row/layout/popover indexes before materialization; workspace persistence grouping and recent-workspace de-dupe now use checked collection access; Workspace pane removal and pinned-tab movement now verify source/destination item indexes before mutation; line rotation selection remaps preserve stale selections instead of panicking; debugger restart now fails closed when binary state is missing, and run-in-terminal cwd fallback avoids the missing-binary unwrap while continuing without a binary-derived cwd. Added and registered `script/dx-editor-element-source.test.ts`, `script/dx-editor-line-ops-source.test.ts`, `script/dx-debugger-panel-source.test.ts`, and `script/dx-debugger-run-terminal-source.test.ts`, and extended `script/dx-workspace-persistence-source.test.ts` plus `script/dx-workspace-ui-state-source.test.ts`. Passed source-only integration: 23/23 targeted Node source subtests including the handoff registry, targeted rustfmt checks, stale direct-index/unwrap pattern scan, line-anchored conflict-marker scan, and `git diff --check`. Skipped by direct instruction: Cargo build/check/test/clippy, `just run`, local servers, browser automation, and live editor runtime proof.

- Completed a six-agent command/editor/workspace stale-index hardening pass without `just run` or Cargo. Command Palette confirm-time command removal, Editor diff-review overlay comment submission, Editor search match navigation, Editor reference navigation, Editor completion documentation cache promotion, and Workspace Dock panel activation now use checked lookups or checked removals before reading, mutating, activating, or rendering stale indexes. Added and registered `script/dx-editor-git-source.test.ts`, `script/dx-editor-search-source.test.ts`, and `script/dx-editor-context-menu-source.test.ts`, and extended `script/dx-command-palette-source.test.ts`, `script/dx-editor-navigation-source.test.ts`, and `script/dx-workspace-ui-state-source.test.ts`. Passed source-only integration: 19/19 targeted Node source subtests including the handoff registry, targeted rustfmt checks, stale direct-index pattern scan, line-anchored conflict-marker scan, and `git diff --check`. Skipped by direct instruction: Cargo build/check/test/clippy, `just run`, local servers, browser automation, and live editor runtime proof.

- Completed a six-agent stale UI materialization hardening pass for Project Panel, Git Panel, Collab Panel, Settings UI, Agent provider configuration, and Workspace pane tabs without `just run` or Cargo. Project Panel previous-selection navigation and active-indent guide lookup, Git tree/prompt selection, Collab Panel row rendering, Settings navbar/file-filter updates, Add LLM Provider model rendering/toggles/removal, and Workspace pane tab materialization now use checked lookups before reading, mutating, focusing, or rendering stale indexes. Extended `script/dx-project-panel-source.test.ts`, `script/dx-git-ui-source.test.ts`, `script/dx-collab-panel-source.test.ts`, `script/dx-settings-keymap-source.test.ts`, `script/dx-agent-configuration-modal-source.test.ts`, and `script/dx-workspace-ui-state-source.test.ts`. Passed source-only integration: 40/40 targeted Node source subtests including the handoff registry, targeted rustfmt checks, stale direct-index pattern scan, line-anchored conflict-marker scan, and `git diff --check`. Skipped by direct instruction: Cargo build/check/test/clippy, `just run`, local servers, browser automation, and live editor runtime proof.

- Completed a six-agent stale UI navigation index hardening pass for Buffer Search, Terminal Panel, Workspace pane switching, Git remote picker, Sidebar project/thread cycling, and Title Bar application menu activation without `just run` or Cargo. Buffer Search field cycling, Terminal Panel pane cycling, Workspace pane cycling, Git remote selection, Sidebar project/thread cycling, and Title Bar application menu navigation now use checked lookups before focusing, activating, or materializing stale indexes. Added and registered `script/dx-title-bar-source.test.ts`, and extended `script/dx-search-ui-source.test.ts`, `script/dx-terminal-view-source.test.ts`, `script/dx-workspace-ui-state-source.test.ts`, `script/dx-git-ui-source.test.ts`, and `script/dx-workspace-reentrant-source.test.ts`. Passed source-only integration: 31/31 targeted Node source subtests including the handoff registry, targeted rustfmt checks, stale direct-index pattern scan, line-anchored conflict-marker scan, and `git diff --check`. Skipped by direct instruction: Cargo build/check/test/clippy, `just run`, local servers, browser automation, and live editor runtime proof.

- Completed a six-agent stale cycle/render-index hardening pass for Agent Config Options, Agent Model Selector, Language Model Selector, Agent Thread View, Git Stash Picker, and Debugger Module List without `just run` or Cargo. Config option cycling, agent model favorite cycling, language model favorite cycling, and thread thinking-effort cycling now use checked next-index lookups before mutating model/thread/settings state; Git stash row rendering now skips stale picker rows; Debugger module-list rendering now returns an empty row for stale uniform-list indexes. Added and registered `script/dx-agent-thread-view-source.test.ts` and `script/dx-debugger-module-list-source.test.ts`, and extended `script/dx-agent-config-options-source.test.ts`, `script/dx-model-selector-source.test.ts`, `script/dx-language-model-selector-source.test.ts`, and `script/dx-git-ui-source.test.ts`. Passed source-only integration: 29/29 targeted Node source subtests including the handoff registry, targeted rustfmt checks, stale direct-index pattern scan, line-anchored conflict-marker scan, and `git diff --check`. Skipped by direct instruction: Cargo build/check/test/clippy, `just run`, local servers, browser automation, and live editor runtime proof.

- Completed a six-agent stale fuzzy-candidate hardening pass for Keymap Editor, Prompt Store, Project Symbols, Outline, Snippets UI, and Toolchain Selector without `just run` or Cargo. Keymap search/materialization and action completions now skip stale candidate IDs before reading bindings or action names; Prompt Store fuzzy metadata search skips stale cache rows; Project Symbols filters stale symbol IDs before sorting, confirming, or building fake workspace-symbol responses; Outline search guards item/prefix/range lookups and outline helper reads; Snippets scope confirm/render guards stale scope candidates; Toolchain Selector confirm ignores stale toolchain candidates before workspace side effects. Added and registered `script/dx-prompt-store-source.test.ts`, and extended `script/dx-settings-keymap-source.test.ts`, `script/dx-project-symbols-source.test.ts`, `script/dx-outline-theme-source.test.ts`, `script/dx-snippets-source.test.ts`, and `script/dx-toolchain-selector-source.test.ts`. Passed source-only integration: 37/37 targeted Node source subtests including the handoff registry, targeted rustfmt checks, refined stale candidate-id pattern scans, line-anchored conflict-marker scan, and `git diff --check`. Skipped by direct instruction: Cargo build/check/test/clippy, `just run`, local servers, browser automation, and live editor runtime proof.

- Completed a six-agent stale UI row and fuzzy-candidate hardening pass for Collab Panel, Dev Container pickers, Debugger stack frames, Inline Prompt history, Project Search navigation, and Component Preview without `just run` or Cargo. Collab Panel now skips stale fuzzy candidate IDs across pending participants, channels, invites, requests, and contacts; Dev Container template/feature rendering guards stale match rows; Debugger stack-frame rendering fails closed for stale filtered rows and entries; Inline Prompt history navigation and edit comparisons use checked history lookups; Project Search selection/focus navigation guards stale computed indexes; Component Preview list/sidebar rows skip stale render indexes. Added and registered `script/dx-collab-panel-source.test.ts`, `script/dx-dev-container-source.test.ts`, `script/dx-debugger-stack-frame-source.test.ts`, and `script/dx-component-preview-source.test.ts`, and extended `script/dx-inline-prompt-source.test.ts` plus `script/dx-search-ui-source.test.ts`. Passed source-only integration: 20/20 targeted Node source subtests including the handoff registry, targeted rustfmt checks, refined stale candidate/index pattern scans, line-anchored conflict-marker scan, and `git diff --check`. Skipped by direct instruction: Cargo build/check/test/clippy, `just run`, local servers, browser automation, and live editor runtime proof.

- Completed a six-agent stale fuzzy-candidate/index hardening pass for Command Palette, Extensions UI, Agent UI completions/selectors, Settings UI pickers, and Editor completion menus without `just run` or Cargo. Command Palette de-duplication now guards command candidates before action comparison; Extensions row rendering guards filtered dev/remote row indexes and backing entries; Agent completion, symbol, session, skill, and model selector fuzzy results now skip stale candidate IDs while preserving sorted ranking before caps; Settings font/theme/icon-theme pickers guard current-selection lookup; Editor completion menus guard documentation, rendered rows, snippet de-duplication, groups, and sort paths before completion lookup. Added and registered `script/dx-agent-ui-selector-source.test.ts` and `script/dx-settings-ui-source.test.ts`, and extended `script/dx-command-palette-source.test.ts`, `script/dx-extensions-ui-source.test.ts`, `script/dx-completion-provider-source.test.ts`, and `script/dx-editor-lsp-ui-source.test.ts`. Passed source-only integration: 35/35 targeted Node source subtests including the handoff registry and adjacent selector guards, targeted rustfmt checks, stale candidate/index pattern scans, line-anchored conflict-marker scan, and `git diff --check`. Skipped by direct instruction: Cargo build/check/test/clippy, `just run`, local servers, browser automation, and live editor runtime proof.

- Completed a six-agent stale-selection and fuzzy-candidate hardening pass for Outline, Extension Version Selector, File Finder, Editor snippet completions, Git pickers, and Settings UI without `just run` or Cargo. Outline modal now clamps stale match selections and guards outline item candidate IDs before navigation; Extension Version Selector now clamps selection after picker and async match changes and guards confirm-time version lookups; File Finder now skips stale channel fuzzy candidate IDs and clamps selection after match replacement/reset paths; Editor snippet completions now skip stale fuzzy candidates before snippet lookup; Git branch/stash/worktree pickers now guard stale fuzzy IDs and use clamped selected indexes for confirm/delete/footer paths; Settings UI now guards deep-link search entries, filter-table writes, navbar visibility/toggle/open/focus paths, file selector reads, content focus handles, and root-entry lookup against stale indexes. Extended `script/dx-outline-theme-source.test.ts`, `script/dx-extensions-ui-source.test.ts`, `script/dx-file-finder-source.test.ts`, `script/dx-editor-lsp-ui-source.test.ts`, `script/dx-git-ui-source.test.ts`, and `script/dx-settings-keymap-source.test.ts`. Passed source-only integration: 40/40 targeted Node source subtests including the handoff registry, targeted rustfmt checks, stale-index pattern scans, line-anchored conflict-marker scan, and `git diff --check`. Skipped by direct instruction: Cargo build/check/test/clippy, `just run`, local servers, browser automation, and live editor runtime proof.

- Completed a six-agent config option picker, tool picker, debugger attach modal, debugger new-process modal, open-path prompt, and tab switcher hardening pass without `just run` or Cargo. Config options now cap picker/fuzzy rows, guard match IDs, and preserve the current overflow option inside the bounded list; Agent Tool Picker now caps visible tool rows while preserving context-server headers and guards stale selection; Debugger Attach now caps process candidates, command fields, fuzzy rows, and command render text; Debugger New Process now caps scenario candidates/fuzzy rows, guards candidate IDs, and refuses oversized custom commands before shell splitting; Open Path Prompt now caps directory/fuzzy materialization while refusing oversized create suffixes instead of truncating real paths; Tab Switcher now caps all-pane tab collection, fuzzy labels/results, and clamps before preview activation. Added `script/dx-agent-config-options-source.test.ts`, `script/dx-agent-tool-picker-source.test.ts`, `script/dx-debugger-attach-modal-source.test.ts`, `script/dx-debugger-new-process-source.test.ts`, `script/dx-open-path-prompt-source.test.ts`, and `script/dx-tab-switcher-source.test.ts`, and registered the new guards. Passed source-only integration: 28/28 targeted Node source subtests including the handoff registry, targeted rustfmt checks, line-anchored conflict-marker scan, and `git diff --check`. Skipped by direct instruction: Cargo build/check/test/clippy, `just run`, local servers, browser automation, and live editor runtime proof.

- Completed a six-agent encoding selector, line ending selector, settings profile selector, tasks modal, feedback, and font panel hardening pass without `just run` or Cargo. Encoding Selector now caps empty/fuzzy matches, clamps stale selections, and guards stale candidate IDs before encoding lookup; Line Ending Selector now clamps stale selections before setter and confirm paths; Settings Profile Selector now caps configured profiles while preserving Disabled and the active overflow profile, caps string matches, reclamps async replacement, and guards candidate IDs; Tasks Modal now caps candidate/fuzzy rows, bounded tag-label materialization, reclamps async replacement, and guards confirm-time candidate IDs; Feedback now caps installed-extension report rows, field text, and prompt preview before clipboard/prompt display; Font Panel now bounds custom web-font names, element IDs, and preview file stems with stable hashes for oversized values. Added `script/dx-encoding-selector-source.test.ts`, `script/dx-line-ending-selector-source.test.ts`, `script/dx-settings-profile-selector-source.test.ts`, `script/dx-tasks-modal-source.test.ts`, `script/dx-feedback-source.test.ts`, and `script/dx-font-panel-source.test.ts`, and registered the new guards. Passed source-only integration: 29/29 targeted Node source subtests including the handoff registry, targeted rustfmt checks, line-anchored conflict-marker scan, and `git diff --check`. Skipped by direct instruction: Cargo build/check/test/clippy, `just run`, local servers, browser automation, and live editor runtime proof.

- Completed a six-agent breadcrumbs, icon picker, language selector, toolchain selector, recent projects, and notifications hardening pass without `just run` or Cargo. Breadcrumbs now compact oversized segment vectors to a bounded head/tail set before invoking the global breadcrumb renderer; Icon Picker now caps TSV sample-name columns before pack summary storage and ties representative loops to that cap; Language Selector now caps visible candidates and empty/fuzzy matches, preserves the current language inside the cap, clamps stale selections, and guards confirm-time candidate IDs; Toolchain Selector now caps picker rows and reclamps async match replacement; Recent Projects now caps sidebar fuzzy candidates, path labels, rendered tooltip/highlight paths, and stale selections; Status Toast now bounds message and action labels before Label, Button, tooltip, and action/id materialization. Added `script/dx-breadcrumbs-source.test.ts`, `script/dx-icon-picker-source.test.ts`, `script/dx-language-selector-source.test.ts`, `script/dx-toolchain-selector-source.test.ts`, `script/dx-recent-projects-ui-source.test.ts`, and `script/dx-notifications-source.test.ts`, and registered the new guards. Passed source-only integration: 19/19 targeted Node source subtests including the handoff registry, targeted rustfmt checks, line-anchored conflict-marker scan, and `git diff --check`. Skipped by direct instruction: Cargo build/check/test/clippy, `just run`, local servers, browser automation, and live editor runtime proof.

- Completed a six-agent activity indicator, project symbols, snippets, REPL kernel picker, CSV preview, and ACP tools hardening pass without `just run` or Cargo. Activity Indicator now bounds status, tooltip, and pending-work menu labels before UI materialization; Project Symbols now filters stale or non-UTF-8-boundary fuzzy highlight positions before combining render highlights; Snippets now caps scope candidates and existing snippet scope-file discovery; the REPL kernel picker now clamps stale filtered entry indexes and clears stale selected kernels after empty or header-only matches; CSV Preview now caps rendered-index metric collection used by debug overlays; ACP tools now bounds request-id chip labels before rendering. Added `script/dx-activity-indicator-source.test.ts`, `script/dx-project-symbols-source.test.ts`, `script/dx-snippets-source.test.ts`, `script/dx-repl-ui-source.test.ts`, `script/dx-preview-surfaces-source.test.ts`, and `script/dx-acp-tools-source.test.ts`, and registered the new guards. Passed source-only integration: 14/14 targeted Node source subtests including the handoff registry, targeted rustfmt checks, line-anchored conflict-marker scan, and `git diff --check`. Skipped by direct instruction: Cargo build/check/test/clippy, `just run`, local servers, browser automation, and live editor runtime proof.

- Completed a six-agent command palette, picker core, search UI, debugger console, collab contact finder, and remote-directory listing hardening pass without `just run` or Cargo. Command Palette now clamps stale selected indexes after match updates and before command materialization; Picker core now bounds stale selections before selectable fallback loops and reveal scrolling; Project Search now saturates active-match labels and clamps stale navigation indexes; Debugger console now caps DAP completion rows and marks truncated responses incomplete; Contact Finder now clamps stale async search selections; remote directory listing responses now cap entry materialization while keeping `entry_info` aligned. Added `script/dx-command-palette-source.test.ts`, `script/dx-picker-core-source.test.ts`, `script/dx-search-ui-source.test.ts`, `script/dx-debugger-ui-source.test.ts`, `script/dx-collab-notifications-source.test.ts`, and `script/dx-markdown-preview-source.test.ts`, and registered the new guards. Passed source-only integration: 11/11 targeted Node source subtests including the handoff registry, targeted rustfmt checks, line-anchored conflict-marker scan, and `git diff --check`. Skipped by direct instruction: Cargo build/check/test/clippy, `just run`, local servers, browser automation, and live editor runtime proof.

- Completed a six-agent project-panel, diagnostics, Git UI, settings/keymap, extensions, and outline/theme/icon hardening pass without `just run` or Cargo. Project Panel now caps visible worktrees/entries, expanded directory ids, selection and drag/drop fanout, remote download lists, sticky parents, sibling sorting, undo batches, and display labels; diagnostics now cap buffer diagnostics, diagnostic groups/blocks/excerpts, markdown/copy payloads, hint links, refresh paths, and status labels; Git UI now caps branch/stash/repository candidates, status rows, commit history, prompt options, and labels; settings/keymap now caps settings pages/search rows/picker options plus keymap bindings/actions/matches/conflict scans/action completions; extensions UI now caps search text, list/result rows, fuzzy candidates, card labels/descriptions/authors/version/provides chips, suggestions, and version selector rows; outline/theme/icon code now caps outline items/search expansion/panel rows/location scans, theme/icon-theme candidates, and file-icon suffix expansion. Added `script/dx-project-panel-source.test.ts`, `script/dx-diagnostics-ui-source.test.ts`, `script/dx-git-ui-source.test.ts`, `script/dx-settings-keymap-source.test.ts`, `script/dx-extensions-ui-source.test.ts`, and `script/dx-outline-theme-source.test.ts`, and registered the new guards. Passed source-only integration: 31/31 targeted Node source subtests, targeted rustfmt checks, line-anchored conflict-marker scan, and `git diff --check`. Skipped by direct instruction: Cargo build/check/test/clippy, `just run`, local servers, browser automation, and live editor runtime proof.

- Completed a six-agent terminal-view, file-finder, agent-diff, editor navigation, inlay/semantic-token, and workspace UI-state hardening pass without `just run` or Cargo. Terminal view persistence/restoration now caps persisted panel JSON, pane/item/flex restoration, terminal restore joins, path paste, stored search matches, and render cell/highlight/background materialization; file finder now caps history task fanout, history items, query bytes, worktree/channel candidates, create-new rows, and search/result vectors; agent diff now caps changed buffers, diff hunk rows, selections, workspace items, registered buffers/editors, and undo buffers before sort/render/action materialization; editor navigation, signature help, linked editing, and LSP extension helpers now cap task joins, grouped references, multibuffer ranges, signature labels/parameters, and runnable/source results; inlay hints and semantic tokens now cap visible buffers/ranges, request tasks, fetched results, label bytes/parts, splice vectors, semantic token responses, and highlight vectors; workspace dock/history/item state now caps panel-size persistence batches, workspace history paths, jump-list removals, delete-id lists, and item project-handle walks. Added `script/dx-terminal-view-source.test.ts`, `script/dx-file-finder-source.test.ts`, `script/dx-agent-diff-source.test.ts`, `script/dx-editor-navigation-source.test.ts`, `script/dx-editor-inlay-semantic-source.test.ts`, and `script/dx-workspace-ui-state-source.test.ts`, and registered the new guards. Passed source-only integration: 31/31 targeted Node source subtests including the handoff registry, targeted rustfmt checks, line-anchored conflict-marker scan, and `git diff --check`. Skipped by direct instruction: Cargo build/check/test/clippy, `just run`, local servers, browser automation, and live editor runtime proof.

- Completed a six-agent profile, terminal, editor LSP UI, language registry, and app fanout hardening pass without `just run` or Cargo. Profile selection now caps profile candidates, fuzzy matches, and rendered entries; terminal thread metadata now bounds DB rows, backpressures queued DB operations instead of dropping full-queue writes, caps path lists/string fields, and bounds remote JSON serialization; terminal display/hyperlink handling now caps event drains, display cells, search/select matches, logical-line strings, and hyperlink path extraction; editor code actions, code lenses, debug scenarios, completion resolution, and markdown parse fanout now have named caps before UI materialization; language registry/settings now cap names, grammar metadata, WASM bytes, settings-backed arrays, and Prettier plugin sets; Zed app-level quit now cancels on pathological window/workspace/flush fanout before draining pending removal tasks, awaits already-collected flush tasks before cancelling, and avoids skipping unchecked close/serialization work, while channel-note, serialization flush, and action dump paths cap fanout before collection or `join_all`. Added `script/dx-profile-selector-source.test.ts`, `script/dx-terminal-thread-metadata-source.test.ts`, `script/dx-terminal-display-source.test.ts`, `script/dx-editor-lsp-ui-source.test.ts`, `script/dx-language-registry-source.test.ts`, and `script/zed-app-fanout-source.test.ts`, and registered the new guards. Passed source-only integration: 31/31 targeted Node source subtests including the handoff registry, targeted rustfmt checks, line-anchored conflict-marker scan, and `git diff --check`. Skipped by direct instruction: Cargo build/check/test/clippy, `just run`, local servers, browser automation, and live editor runtime proof.

- Completed a six-agent picker, open-listener, context-server, worktree-event, and entry-view boundary hardening pass without `just run` or Cargo. Language model and agent model selectors now cap provider/model/group/favorite/fuzzy/render materialization; open-listener request handling now validates URL/path/diff counts and bytes before CLI/default-open fanout, PathBuf materialization, diff view vectors, navigation rows, and item-release waits; context server configuration now caps settings/server-id lists, enabled-server fanout, command args/env fields, URLs, OAuth sessions, and client secrets before command/keychain materialization, and oversized configuration failures remain visible as error statuses; worktree filesystem events now drain in bounded chunks, log before root-rescan fallbacks on pathological event/symlink fanout, chunk metadata fanout, and cap changed-path tracking; entry-view tool-call rows now cap terminal/diff display lists and prune cached views to the capped IDs. Added `script/dx-language-model-selector-source.test.ts`, `script/dx-model-selector-source.test.ts`, `script/zed-open-listener-source.test.ts`, `script/dx-context-server-store-source.test.ts`, `script/dx-worktree-event-source.test.ts`, and `script/dx-entry-view-state-source.test.ts`, and registered the new guards. Passed source-only integration: 25/25 targeted Node source subtests, targeted rustfmt checks, line-anchored conflict-marker scan, and `git diff --check`. Skipped by direct instruction: Cargo build/check/test/clippy, `just run`, local servers, browser automation, and live editor runtime proof.

- Completed a six-agent Agent Registry, completion, search, task inventory, and LSP aggregation boundary hardening pass without `just run` or Cargo. ACP registry index entries, icon fetches, and binary target maps now have named caps before agent/icon/target materialization; Agent Registry UI search now bounds query extraction, filtered indices, and rendered rows; agent prompt completions now cap slash skills/commands, skill mentions, top-recent threads, visible worktrees, empty-file rows, and symbol candidates; multiline project search now uses a sentinel-byte bounded reader before UTF-8 string materialization; task inventory now caps settings-backed task/debug template lists, validation errors, and unknown-variable display lists; document link/symbol LSP aggregation now caps remote response fanout, local response collection, per-server link/symbol payloads, and symbol outline flattening. Added `script/dx-agent-registry-ui-source.test.ts`, `script/dx-completion-provider-source.test.ts`, `script/dx-project-search-source.test.ts`, `script/dx-task-inventory-source.test.ts`, and `script/dx-lsp-aggregation-source.test.ts`, extended `script/dx-project-agent-registry-source.test.ts`, and registered the new guards. Passed source-only integration: 22/22 targeted Node source subtests and targeted rustfmt checks. Skipped by direct instruction: Cargo build/check/test/clippy, `just run`, local servers, browser automation, and live editor runtime proof.

- Completed a six-agent network/discovery/history/clipboard/archive boundary hardening pass without `just run` or Cargo. ACP registry HTTP responses now use sentinel-byte body caps and bounded error display text; agent server discovery caps registry, settings, remote-name, and archive-cache scans; workspace random-space discovery plus history/jump-list materialization now caps filesystem and recent-entry growth, with history/project-picker callers using limited recent-workspace DB queries; MessageEditor paste context now limits clipboard entries and external paths before cloning; thread import/archive/project-picker flows cap agent rows, sessions, workspaces, queries, candidates, branch names, and path labels, while worktree archiving now refuses an oversized linked-thread set instead of reporting success after a partial link; Web Preview clipboard JSON imports now use a dedicated named cap before string materialization and JSON parsing. Added `script/dx-project-agent-registry-source.test.ts`, `script/dx-agent-server-store-source.test.ts`, `script/dx-workspace-enumeration-source.test.ts`, and `script/dx-thread-archive-source.test.ts`, extended the MessageEditor and Web Preview guards, and registered the new guards. Passed source-only integration: 29/29 targeted Node source subtests, targeted rustfmt checks, line-anchored conflict-marker scan, and `git diff --check`. Skipped by direct instruction: Cargo build/check/test/clippy, `just run`, local servers, browser automation, and live editor runtime proof.

- Completed a six-agent persistence/input/enumeration boundary hardening pass without `just run` or Cargo, plus parent integration fixes for pre-clone AgentPanel text checks, the remaining startup scans in `main.rs`, source-guard registry existence checks, and remote minidump metadata caps. Workspace KVP/DB JSON, Agent configuration modal input, AgentPanel editor text/draft materialization, Web Preview local extension/profile discovery, DX receipt directory/latest-candidate scans, Zed theme/keymap startup paths, hang/minidump/build-timing reliability scans, remote crash metadata parsing, and debug language watcher registration now have explicit byte or entry caps before parse, clone, serialization, upload, or watch registration. Added `script/dx-workspace-persistence-source.test.ts`, `script/dx-agent-configuration-modal-source.test.ts`, `script/dx-agent-panel-input-source.test.ts`, `script/dx-receipt-directory-source.test.ts`, and `script/zed-startup-reliability-source.test.ts`, extended adjacent Web Preview, receipt-history, and Windows reliability guards, and registered the new guards. Passed source-only integration: 53/53 targeted Node source subtests, targeted rustfmt checks, line-anchored conflict-marker scan, and `git diff --check`. Skipped by direct instruction: Cargo build/check/test/clippy, `just run`, local servers, browser automation, and live editor runtime proof.

- Completed a six-agent inline prompt and DX Studio source-boundary hardening pass without `just run` or Cargo. Inline assistant codegen now rejects oversized selected source before request construction or streaming text materialization, empty rejected alternatives no longer rematerialize batch diffs, inline prompt submission and linked prompt-group unlinking now size-check before cloning prompt text, and DX Studio manifest-derived route/detail/ambiguity summaries are capped before UI/IPC exposure. Added `script/dx-buffer-codegen-source.test.ts` and `script/dx-inline-prompt-source.test.ts`, extended `script/dx-source-quality.test.ts`, and registered the new guards. Passed source-only integration: 37/37 targeted Node source subtests, targeted rustfmt checks, line-anchored conflict-marker scan, and `git diff --check`. Skipped by direct instruction: Cargo build/check/test/clippy, `just run`, local servers, browser automation, and live editor runtime proof.

- Completed a six-agent prompt/IPC/receipt boundary hardening pass without `just run` or Cargo. Web Preview IPC messages are byte-capped before raw browser-event queueing, deferred queueing, and serde parsing, with bounded raw/deferred bridge queues; minidump multipart upload bodies are sentinel-byte capped before `AsyncBody`; Agent Configuration settings insertion refuses oversized buffers before `snapshot.text()`; DX Agent bridge receipt writes check post-serialization bytes and bound action-error display fields; launch prompt/context lists and workspace labels compact whitespace and cap individual receipt-derived rows. Added `script/dx-agent-configuration-source.test.ts`, extended the Web Preview, Windows reliability, Agent bridge, launch prompt, and launch workspace guards, and registered the new guard in the handoff registry. Passed source-only integration: 51/51 targeted Node source subtests, targeted rustfmt checks, line-anchored conflict-marker scan, and `git diff --check`. Skipped by direct instruction: Cargo build/check/test/clippy, `just run`, local servers, browser automation, and live editor runtime proof.

- Completed a six-agent file/response-boundary hardening pass without `just run` or Cargo. Web Preview durable JSON/status/manifest/bookmark/screenshot reads now use sentinel-byte bounded helpers before serde parse, image creation, or attachment encoding; Agent Panel skill URL clipboard import now bounds text before trim/filter; built-in JSON schema rendering now rejects oversized schema payloads before parse/pretty-print; minidump upload response bodies and app-copy stderr now compact/cap display text before logs/errors; fetched mention client-error bodies now use capped compact text. Added source guards `script/zed-builtin-schema-source.test.ts` and `script/zed-platform-source.test.ts`, and extended `script/web-preview-payload-source.test.ts`, `script/dx-agent-panel-clipboard-source.test.ts`, `script/dx-mention-set-source.test.ts`, `script/dx-windows-reliability-source.test.ts`, and the handoff registry. Passed source-only integration: 28/28 targeted Node source subtests, targeted rustfmt checks, line-anchored conflict-marker scan, and `git diff --check`. Skipped by direct instruction: Cargo build/check/test/clippy, `just run`, local servers, browser automation, and live editor runtime proof.

- Completed a six-agent local input-surface hardening pass without `just run` or Cargo. Previous minidump metadata/payload reads, Web Preview final-proof clipboard imports, external mention image and fetched-body ingestion, DX Studio source snapshot digesting, Agent Panel/draft prompt persisted KVP JSON, and failed DX Agents bridge stderr rendering now reject, skip, or compact oversized input before parse, digest, upload, image conversion, or UI/log display. Added source guard `script\dx-agent-persisted-state-source.test.ts` and extended `script\dx-windows-reliability-source.test.ts`, `script\web-preview-payload-source.test.ts`, `script\dx-mention-set-source.test.ts`, `script\dx-source-quality.test.ts`, and `script\dx-agent-bridge-source.test.ts`. Passed source-only integration: 35/35 targeted Node source subtests, targeted rustfmt checks, line-anchored conflict-marker scan, and `git diff --check`. Skipped by direct instruction: Cargo build/check/test/clippy, `just run`, local servers, browser automation, and live editor runtime proof.

- Completed a six-agent external payload-boundary hardening pass without `just run` or Cargo. Native-agent thread clipboard export/import, Web Preview agent-browser payload imports, reliability build-timing JSON reads, external skill mention files, pasted markdown mention-link parsing, and persisted remote-connection metadata JSON now reject or skip oversized external text/file payloads before text concatenation, base64/zstd decode, parse, prompt insertion, or deserialization. Added source guards `script\dx-agent-panel-clipboard-source.test.ts`, `script\web-preview-payload-source.test.ts`, `script\dx-mention-set-source.test.ts`, `script\dx-message-editor-source.test.ts`, and `script\dx-thread-metadata-source.test.ts`, and extended `script\dx-windows-reliability-source.test.ts`. Passed source-only integration: 20/20 targeted Node source subtests, targeted rustfmt checks, line-anchored conflict-marker scan, and `git diff --check`. Skipped by direct instruction: Cargo build/check/test/clippy, `just run`, local servers, browser automation, and live editor runtime proof.

- Completed a six-agent receipt display-string hardening pass without `just run` or Cargo. DX Check panel parser, Runtime Proof status summaries, Agent bridge receipts, Launch Audit packets, Launch Readiness packets, and Launch Handoff contracts now compact, bound, and where applicable redact receipt/packet-derived strings before snapshot or prompt-facing use. Added focused `dx_agent_bridge/receipts/receipt_strings.rs` ownership and extended source guards across `script\dx-agent-bridge-source.test.ts`, `script\dx-check-panel-source.test.ts`, `script\dx-runtime-proof-status-source.test.ts`, `script\dx-launch-audit-source.test.ts`, `script\dx-launch-readiness-source.test.ts`, and `script\dx-launch-contracts-source.test.ts`. Passed source-only integration: targeted Node source guards plus handoff registry, targeted rustfmt checks, conflict-marker scan, and `git diff --check`. Skipped by direct instruction: Cargo build/check/test/clippy, `just run`, local servers, browser automation, and live editor runtime proof.

- Completed a six-agent bounded-read hardening pass without `just run` or Cargo. Remaining DX launch packet readers, DX Check panel receipt reads, DX-WWW launch evidence JSON reads, DX Studio route/config reads, DX Studio project detection scans, and DX Studio source-edit source-file reads now use sentinel-byte bounded reads before parsing or UTF-8 decoding. Source guards were extended across `script\dx-launch-audit-source.test.ts`, `script\dx-launch-readiness-source.test.ts`, `script\dx-launch-source-audit-source.test.ts`, `script\dx-launch-contracts-source.test.ts`, `script\dx-www-launch-evidence-source.test.ts`, `script\dx-check-panel-source.test.ts`, `script\dx-source-quality.test.ts`, and `script\dx-studio-project-source.test.ts`. Skipped by direct instruction: Cargo build/check/test/clippy, `just run`, local servers, browser automation, and live editor runtime proof.

- Completed a six-agent receipt and manifest read-bound hardening pass without `just run` or Cargo. Agent bridge receipts, runtime-proof receipts, source-set receipts, extensionless `dx` config reads, receipt-history JSON IO, launch receipt/status reads, and DX Studio manifest/session/policy reads now use sentinel-byte bounded reads before parsing so oversized files cannot be accepted as truncated proof. Source guards were extended across `script\dx-agent-bridge-source.test.ts`, `script\dx-runtime-proof-status-source.test.ts`, `script\dx-source-sets-source.test.ts`, `script\dx-receipt-history-source.test.ts`, `script\dx-launch-receipts-source.test.ts`, `script\dx-launch-status-source.test.ts`, and `script\dx-source-quality.test.ts`. Skipped by direct instruction: Cargo build/check/test/clippy, `just run`, local servers, browser automation, and live editor runtime proof.

- Worker 6 handoff/source-guard hygiene: added the `Lightweight Source Guard Registry` and `script/dx-handoff-source-guard-registry.test.ts` so new DX source guards are discoverable without reading the whole changelog. The guard also protects the no-`just run`/no-Cargo runtime-proof boundary in `DX.md` and `AGENTS.md`. Reworded current Deploy-panel 100/100 handoff lines to say source/config or dry-run evidence only, not runtime, launch, or production readiness. Passed: `node --test script\dx-handoff-source-guard-registry.test.ts`. Skipped by direct instruction: Cargo build/check/test/clippy, `just run`, local servers, browser automation, commits, pushes, and live editor runtime proof.

- Completed a six-agent source-level production-hardening pass without `just run` or Cargo. The branch now surfaces malformed dx-deploy receipts in the Deploy rail and readiness prompt, exposes DX Studio invalid/unreadable/malformed manifest candidates plus TypeScript edit-contract summary loading, extends active-workspace event/cached-handle usage in Sidebar and Call, and tightens Windows scanner/minidump noise handling. Passed: targeted rustfmt checks, the integrated Node source guard set (`script\dx-windows-reliability-source.test.ts`, `script\dx-source-quality.test.ts`, `script\dx-deploy-receipts-source.test.ts`, `script\dx-deploy-panel-source.test.ts`, `script\dx-workspace-reentrant-source.test.ts`, `script\dx-deploy-launch-gate-source.test.ts`, `script\dx-deploy-launch-evidence-source.test.ts`), conflict-marker scan, and `git diff --check`. Skipped by direct instruction: Cargo build/check/test/clippy, `just run`, local servers, browser automation, and live editor runtime proof. Exact next action: only open native runtime proof when the user explicitly authorizes `just run`.

- Added DX Windows reliability source guard coverage: `script\dx-windows-reliability-source.test.ts` now guards Windows scanner metadata handling for expected protected/locked system entries, local/dev missing-commit minidump logging, and remote minidump upload gating so crash-file requests stay behind diagnostics and endpoint checks. Passed: red/green `node --test script\dx-windows-reliability-source.test.ts` plus adjacent source guards. Skipped by direct instruction: Cargo build/check/test/clippy, `just run`, local servers, browser automation, and live editor runtime proof. This remains a no-Cargo/no-`just run` production-readiness source guard.

- Added production-readiness source audit hardening: `crates/zed/src/reliability.rs` now avoids requesting remote crash files when no minidump endpoint is configured, `crates/workspace/src/multi_workspace.rs` carries active workspace payloads for active-workspace changes and removals, Sidebar/Zed/Call integration consume those event payloads instead of re-reading risky active workspace state, and the Deploy launch gate now surfaces malformed latest dx-check launch receipts as explicit blockers. `script/dx-workspace-reentrant-source.test.ts` guards AgentPanel's deferred `DxWorkspaceSnapshot`, active workspace event payloads, Sidebar cached active workspace helpers, and Call integration. Passed: targeted rustfmt check, `node --test script\dx-workspace-reentrant-source.test.ts`, `node --test script\dx-deploy-launch-gate-source.test.ts`, conflict-marker scan, and `git diff --check`. Skipped by direct instruction: Cargo build/check/test/clippy, `just run`, local servers, browser automation, and live editor runtime proof. Exact next action: keep these guards in the lightweight source set and verify native startup/runtime behavior only in the governed runtime proof window.

- Split DX Agent bridge detail ownership: `crates/agent_ui/src/dx_launch_workspace/agents/bridge/details.rs` now owns latest-receipt, failed-command, action-error redaction, contract command/catalog-regeneration, import recovery-command, and gate acceptance-row details in the original display order. `crates/agent_ui/src/dx_launch_workspace/agents/bridge.rs` now delegates summary, availability prompts, detail rows, and review rows through focused helpers and stays focused on the bridge rail shell at 49 lines. `script/dx-launch-workspace-source.test.ts` guards detail module registration, shell delegation, detail row ownership, redaction detail ownership, and tighter bridge/detail budgets. Passed: red/green workspace source guard, full lightweight source guard set (30/30), targeted rustfmt check, and line-count budgets. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, provider calls, cloud writes, and deploy execution. Exact next action: verify native Agent bridge detail rows against real DX Agents latest receipt, action-error, contract, import-summary, and release-gate receipts in the governed native Zed proof window.

- Split DX Agent bridge gate recovery ownership: `crates/agent_ui/src/dx_launch_workspace/agents/bridge/summary/gate/recovery.rs` now owns release-gate action-map, recovery-control, and command-fanout rows, while `summary/gate.rs` keeps the release-gate metrics and receipt-index rows in the same display order at 58 lines. `script/dx-launch-workspace-source.test.ts` guards nested recovery module registration, gate-shell delegation, recovery/fanout ownership, and tighter gate/recovery budgets. Passed: red/green workspace source guard, full lightweight source guard set (30/30), targeted rustfmt check, and line-count budgets. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, provider calls, cloud writes, and deploy execution. Exact next action: verify native Agent bridge gate recovery rows against real DX Agents release-gate action-map, recovery-control, fanout, and receipt-index receipts in the governed native Zed proof window.

- Split DX Agent receipt summary/footer ownership: `crates/agent_ui/src/dx_launch_workspace/agents/receipts/summary.rs` now owns receipt redacted/unsafe counts, receipt index/root/inbox/returned/active summary rows, and latest-receipt eligibility, while `receipts/footer.rs` owns the inbox review row and next-action footer. `crates/agent_ui/src/dx_launch_workspace/agents/receipts.rs` now delegates summary, warning, row, latest, and footer ownership through focused helpers and stays focused on receipt rail composition plus missing/renderable states at 54 lines. `script/dx-launch-workspace-source.test.ts` guards summary/footer module registration, shell delegation, count ownership, inbox footer ownership, latest-row ownership, and tighter receipt shell/summary/footer budgets. Passed: red/green workspace source guard, targeted rustfmt check, and line-count budgets. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, provider calls, cloud writes, and deploy execution. Exact next action: verify native Agent receipt summary/footer rows against real DX Agents receipt-index and inbox receipts in the governed native Zed proof window.

- Split DX Sources row detail ownership: `crates/agent_ui/src/dx_launch_workspace/sources/signals.rs` now owns bounded source proof and warning signal rows, while `sources/drilldowns.rs` now owns bounded source receipt drilldown row collection beside the individual drilldown card renderer. `crates/agent_ui/src/dx_launch_workspace/sources/rows.rs` now delegates receipt drilldowns plus proof/warning signals through focused helpers and stays focused on source row shell layout, source kind icon placement, row control insertion, and source label/detail/path rows at 62 lines. `script/dx-launch-workspace-source.test.ts` guards signal module registration, row-shell delegation, source proof/warning ID ownership, drilldown collection ownership, and tighter Sources row/signal/drilldown budgets. Passed: red/green workspace source guard, targeted rustfmt check, and line-count budgets. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, provider calls, cloud writes, and deploy execution. Exact next action: verify native Sources row detail rendering against real source-pack, media, reduced-context, receipt, and restore-preview rows in the governed native Zed proof window.

- Split DX Agent bridge review helper ownership: `crates/agent_ui/src/dx_launch_workspace/agents/bridge/review/warnings.rs` now owns action-error redaction, release-gate blocker/fanout/warning, import-summary blocker/fanout/warning, contract-redaction, and bridge-error warning rows, while `review/next_action.rs` owns the final release/import/contract/default next-action fallback. `crates/agent_ui/src/dx_launch_workspace/agents/bridge/review.rs` now delegates warning and next-action selection through focused helpers and stays focused on final review-row composition at 22 lines. `script/dx-launch-workspace-source.test.ts` guards helper module registration, review-shell delegation, warning ID ownership, next-action ownership, and tighter bridge-review helper budgets. Passed: red/green workspace source guard, targeted rustfmt check, and line-count budgets. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, provider calls, cloud writes, and deploy execution. Exact next action: verify native Agent bridge review helper rows against real DX Agents action-error, contract, import-summary, release-gate, and receipt-index receipts in the governed native Zed proof window.

- Split DX Agent automation row ownership: `crates/agent_ui/src/dx_launch_workspace/agents/automations/rows.rs` now owns individual automation row rendering, including enabled/paused state labels, schedule/source rows, next-action rows, and compact action-line rows. `crates/agent_ui/src/dx_launch_workspace/agents/automations.rs` now delegates automation rows through the focused helper and stays focused on automation summary metrics plus list-empty fallback at 36 lines. `script/dx-launch-workspace-source.test.ts` guards row module registration, shell delegation, row-helper ownership, action-line ownership, and tighter automation/automation-row budgets. Passed: red/green workspace source guard, targeted rustfmt check, and line-count budgets. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, provider calls, cloud writes, and deploy execution. Exact next action: verify native Agent automation rows against real `dx agents automate list --json` receipts in the governed native Zed proof window.

- Split DX Launch Gate summary ownership: `crates/agent_ui/src/dx_launch_workspace/readiness/summary.rs` now owns Launch Gate summary metrics, import/release/fallback packet counts, acceptance row counts, bounded freshness/fallback/recovery rows, and command-fanout labels. `crates/agent_ui/src/dx_launch_workspace/readiness.rs` now delegates summary, status, warning, and example ownership through focused helpers and stays focused on rail composition at 26 lines. `script/dx-launch-workspace-source.test.ts` guards summary module registration, shell delegation, summary metric ownership, and tighter Launch Gate shell/summary budgets. Passed: red/green workspace source guard, targeted rustfmt check, and line-count budgets. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, provider calls, cloud writes, and deploy execution. Exact next action: verify native Launch Gate summary rows against real import-summary, release-gate, and fallback-drill packets in the governed native Zed proof window.

- Split DX Launch Status summary ownership: `crates/agent_ui/src/dx_launch_workspace/launch_status/summary.rs` now owns Launch Status summary metrics for agent account readiness, active agent work, token budget state, discovery readiness, and template/package command labels. `crates/agent_ui/src/dx_launch_workspace/launch_status.rs` now delegates summary, status, and valid-detail rows through focused helpers and stays focused on rail composition at 25 lines. `script/dx-launch-workspace-source.test.ts` guards summary module registration, shell delegation, summary metric ownership, launch-status label ownership, and tighter Launch Status shell/summary budgets. Passed: red/green workspace source guard, targeted rustfmt check, and line-count budgets. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, provider calls, cloud writes, and deploy execution. Exact next action: verify native Launch Status summary rows against real `G:\Dx\.dx\receipts\launch\status-latest.json` receipts in the governed native Zed proof window.

- Split DX Launch Audit summary ownership: `crates/agent_ui/src/dx_launch_workspace/audit/summary.rs` now owns Launch Audit summary metrics, command/safety/fixture/smoke counts, example state rows, and bounded command/fixture/smoke row labels. `crates/agent_ui/src/dx_launch_workspace/audit.rs` now delegates summary, status, and warning ownership through focused helpers and stays focused on rail composition plus next-action fallback at 33 lines. `script/dx-launch-workspace-source.test.ts` guards summary module registration, shell delegation, summary metric ownership, and tighter Launch Audit shell/summary budgets. Passed: red/green workspace source guard, targeted rustfmt check, and line-count budgets. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, provider calls, cloud writes, and deploy execution. Exact next action: verify native Launch Audit summary rows against real `G:\Dx\cli\fixtures\launch-examples` audit packets in the governed native Zed proof window.

- Split DX Source Audit summary ownership: `crates/agent_ui/src/dx_launch_workspace/source_audit/summary.rs` now owns Source Audit summary metrics, coordination readiness, repo/review/DX Studio/template counts, bounded repo/delta/blocker rows, and next-target row. `crates/agent_ui/src/dx_launch_workspace/source_audit.rs` now delegates summary, status, and warning ownership through focused helpers and stays focused on rail composition at 29 lines. `script/dx-launch-workspace-source.test.ts` guards summary module registration, shell delegation, metric ownership, and tighter Source Audit shell/summary budgets. Passed: red/green workspace source guard, targeted rustfmt check, and line-count budgets. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, provider calls, cloud writes, and deploy execution. Exact next action: verify native Source Audit summary rows against real latest source-audit, markdown, and DX Studio QA receipts in the governed native Zed proof window.

- Split DX Runtime Proof plan detail ownership: `crates/agent_ui/src/dx_launch_workspace/proof/runtime_rows/plan_details.rs` now owns optional runtime-proof plan detail rows for expected final command, evidence requirements, blockers, and next-action fallback. `proof/runtime_rows/plan.rs` now delegates those detail rows through the focused helper and stays focused on the plan card shell, plan metric, requirements summary, and plan label at 50 lines. `script/dx-launch-workspace-source.test.ts` guards plan-detail module registration, shell delegation, optional-detail ownership, and tighter plan/detail budgets. Passed: red/green workspace source guard, targeted rustfmt check, and line-count budgets. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, provider calls, cloud writes, and deploy execution. Exact next action: verify native Runtime Proof plan detail rows against real runtime proof plan receipts in the governed native Zed proof window.

- Split DX Runtime Proof receipt detail ownership: `crates/agent_ui/src/dx_launch_workspace/proof/runtime_rows/receipt_details.rs` now owns optional runtime-proof receipt detail rows for headline, proof summary, final command, source, and first evidence sample. `proof/runtime_rows/receipt.rs` now delegates those detail rows through the focused helper and stays focused on the receipt card shell, claim-state metric, operator/evidence summary, and receipt label at 49 lines. `script/dx-launch-workspace-source.test.ts` guards receipt-detail module registration, shell delegation, optional-detail ownership, and tighter receipt/detail budgets. Passed: red/green workspace source guard, targeted rustfmt check, and line-count budgets. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, provider calls, cloud writes, and deploy execution. Exact next action: verify native Runtime Proof receipt detail rows against real runtime proof import/status receipts in the governed native Zed proof window.

- Split DX-WWW Evidence status/warning row ownership: `crates/agent_ui/src/dx_launch_workspace/www_evidence/status.rs` now owns WWW Evidence metric rows, bounded latest/missing/next rows, and missing project/release-root cards, while `www_evidence/warnings.rs` owns first-issue and partial-evidence warning selection. `crates/agent_ui/src/dx_launch_workspace/www_evidence.rs` now delegates through both focused helpers and stays focused on right-rail composition at 27 lines. `script/dx-launch-workspace-source.test.ts` guards status/warning module registration, shell delegation, missing-root ownership, warning-ID ownership, and tighter WWW Evidence budgets. Passed: red/green workspace source guard, targeted rustfmt check, and line-count budgets. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, provider calls, cloud writes, and deploy execution. Exact next action: verify native WWW Evidence status and warning rows against real DX-WWW release evidence packets in the governed native Zed proof window.

- Split DX Launch Gate status row ownership: `crates/agent_ui/src/dx_launch_workspace/readiness/status.rs` now owns missing launch-example root cards, warning row selection, and next-action fallback rows. `crates/agent_ui/src/dx_launch_workspace/readiness.rs` now delegates status rows through the focused helper and stays focused on Launch Gate metrics, bounded freshness/fallback/recovery rows, and example rows at 85 lines. `script/dx-launch-workspace-source.test.ts` guards status module registration, shell delegation, missing-root/warning/next-action ownership, and tighter readiness/status budgets. Passed: red/green workspace source guard, targeted rustfmt check, and line-count budgets. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, provider calls, cloud writes, and deploy execution. Exact next action: verify native Launch Gate status rows against real import-summary, release-gate, and fallback-drill packets in the governed native Zed proof window.

- Split DX Tool History summary row ownership: `crates/agent_ui/src/dx_launch_workspace/tool_history/summary_rows.rs` now owns receipt summary cards, including target path, restore preview path, blocker warning, and receipt label rows. `crates/agent_ui/src/dx_launch_workspace/tool_history/rows.rs` now delegates summary cards through the focused helper and stays focused on bucket-card state plus latest receipt source rows at 54 lines. `script/dx-launch-workspace-source.test.ts` guards summary-row module registration, bucket-helper delegation, receipt-summary ownership, and tighter Tool History row/summary budgets. Passed: red/green workspace source guard, targeted rustfmt check, and line-count budgets. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, provider calls, cloud writes, and deploy execution. Exact next action: verify native Tool History summary cards against real Forge/media/serializer/RLM receipt history in the governed native Zed proof window.

- Split DX Agent social row ownership: `crates/agent_ui/src/dx_launch_workspace/agents/social/rows.rs` now owns individual social account row rendering, including connected/configured/QR-ready state labels, next-action rows, and compact action-line rows. `crates/agent_ui/src/dx_launch_workspace/agents/social.rs` now delegates social account rows through the focused helper and stays focused on social summary metrics plus connect/disconnect receipt rows at 62 lines. `script/dx-launch-workspace-source.test.ts` guards social row module registration, shell delegation, row-helper ownership, action-line ownership, and tighter social/social-row budgets. Passed: red/green workspace source guard, targeted rustfmt check, and line-count budgets. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, provider calls, cloud writes, and deploy execution. Exact next action: verify native Agent social rows against real connected-account, connect, and disconnect receipts in the governed native Zed proof window.

- Split DX Launch Status status row ownership: `crates/agent_ui/src/dx_launch_workspace/launch_status/status.rs` now owns missing receipt-root cards, missing latest-receipt cards, warning rows, and next-action fallback rows. `crates/agent_ui/src/dx_launch_workspace/launch_status.rs` now delegates those status rows through the focused helper and stays focused on Launch Status summary metrics and schema-valid detail rows at 87 lines. `script/dx-launch-workspace-source.test.ts` guards status module registration, shell delegation, warning/next-action ownership, and tighter Launch Status/status budgets. Passed: red/green workspace source guard, targeted rustfmt check, and line-count budgets. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, provider calls, cloud writes, and deploy execution. Exact next action: verify native Launch Status missing/latest/warning rows against real `G:\Dx\.dx\receipts\launch\status-latest.json` receipts in the governed native Zed proof window.

- Split DX Launch Audit status row ownership: `crates/agent_ui/src/dx_launch_workspace/audit/status.rs` now owns missing launch-example root cards plus missing schemas, fixtures, smoke, and status packet cards. `crates/agent_ui/src/dx_launch_workspace/audit.rs` now delegates those status rows through the focused helper and stays focused on Launch Audit metrics, bounded command/fixture/smoke rows, warning selection, and next-action fallback at 90 lines. `script/dx-launch-workspace-source.test.ts` guards status module registration, shell delegation, missing-packet ownership, and tighter audit/status budgets. Passed: red/green workspace source guard, targeted rustfmt check, and line-count budgets. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, provider calls, cloud writes, and deploy execution. Exact next action: verify native Launch Audit missing-packet cards against real `G:\Dx\cli\fixtures\launch-examples` audit packets in the governed native Zed proof window.

- Split DX Agent receipt detail row ownership: `crates/agent_ui/src/dx_launch_workspace/agents/receipts/row_details.rs` now owns secondary receipt detail rows for task state, duration, provider/model, action, social, automation, schema, error, and next-action lines. `agents/receipts/rows.rs` now stays focused on the receipt card shell and state metric at 31 lines, while `agents/receipts.rs` registers the focused helper beside labels, rows, and status. `script/dx-launch-workspace-source.test.ts` guards row-detail module registration, row-shell delegation, detail ownership, and tighter row/detail budgets. Passed: red/green workspace source guard, targeted rustfmt check, and line-count budgets. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, provider calls, cloud writes, and deploy execution. Exact next action: verify native Agent receipt detail rows against real DX Agents receipt-index and inbox receipts in the governed native Zed proof window.

- Split DX Runtime Proof label ownership: `crates/agent_ui/src/dx_launch_workspace/proof_labels.rs` is now a small facade over `proof_labels/evidence.rs` and `proof_labels/receipt.rs`, while `proof_labels_tests.rs` owns the direct pure-label tests. Evidence/requirement labels now live apart from receipt-state labels, and runtime proof plan/receipt rows keep importing the same source-owned facade. `script/dx-launch-workspace-source.test.ts` guards facade wiring, helper ownership, test ownership, and tighter file budgets. Passed: red/green workspace source guard, direct `rustc --edition=2024 --test crates\agent_ui\src\dx_launch_workspace\proof_labels.rs`, targeted rustfmt check, and line-count budgets. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, provider calls, cloud writes, and deploy execution. Exact next action: verify native Runtime Proof labels against real runtime proof plan/import/status receipts in the governed native Zed proof window.

- Split DX Launch Handoff contract status ownership: `crates/agent_ui/src/dx_launch_workspace/contracts/status.rs` now owns missing import-manifest rows, missing handoff-packet rows, contract warning/redaction/fanout rows, next-action rows, and first-action rows. `crates/agent_ui/src/dx_launch_workspace/contracts.rs` now delegates status rows through the focused helper and stays focused on Launch Handoff summary metrics, bounded command labels, packet counts, and cached/refresh state at 69 lines. `script/dx-launch-workspace-source.test.ts` guards status module registration, shell delegation, status row ownership, warning ID ownership, first-action ownership, and tighter contracts/status budgets. Passed: red/green workspace source guard, targeted rustfmt check, and line-count budgets. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, provider calls, cloud writes, and deploy execution. Exact next action: verify native Launch Handoff contract status rows against real import-manifest and handoff packets in the governed native Zed proof window.

- Split DX Launch Receipts status ownership: `crates/agent_ui/src/dx_launch_workspace/launch_receipts/status.rs` now owns missing-root, missing-latest, latest receipt warning, malformed/stale/schema-review, last-error, next-action, and latest-snapshot status rows. `crates/agent_ui/src/dx_launch_workspace/launch_receipts.rs` now delegates those rows through the focused helper and stays focused on Launch Receipts review summary metrics at 54 lines. `script/dx-launch-workspace-source.test.ts` guards status module registration, shell delegation, status row ownership, warning ID ownership, snapshot-row ownership, and tighter Launch Receipts shell/status budgets. Passed: red/green workspace source guard, targeted rustfmt check, and line-count budgets. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, provider calls, cloud writes, and deploy execution. Exact next action: verify native Launch Receipts status rows against real `G:\Dx\.dx\receipts\launch` latest/snapshot receipts in the governed native Zed proof window.

- Split DX Tool History row ownership: `crates/agent_ui/src/dx_launch_workspace/tool_history/rows.rs` now owns tool-history bucket cards, receipt summary rows, latest receipt source rows, target/preview labels, and blocker warning rows. `crates/agent_ui/src/dx_launch_workspace/tool_history.rs` now delegates row rendering through the focused helper and stays focused on rail iteration/composition at 21 lines. `script/dx-launch-workspace-source.test.ts` guards row module registration, rail delegation, row ownership, receipt-summary ownership, and tighter Tool History shell/row budgets. Passed: red/green workspace source guard, targeted rustfmt check, and line-count budgets. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, provider calls, cloud writes, and deploy execution. Exact next action: verify native Tool History bucket and receipt summary rows against real Forge/media/serializer/RLM receipt history in the governed native Zed proof window.

- Split DX Sources row helper ownership: `crates/agent_ui/src/dx_launch_workspace/sources/drilldowns.rs` now owns source receipt drilldown card rendering, and `crates/agent_ui/src/dx_launch_workspace/sources/kinds.rs` owns source-kind icon policy. `crates/agent_ui/src/dx_launch_workspace/sources/rows.rs` now delegates those details and stays focused on the source item row shell, source row controls, proof rows, and warning rows at 85 lines. `script/dx-launch-workspace-source.test.ts` guards module registration, row delegation, drilldown ownership, source-kind ownership, and tighter Sources row/drilldown/kind file budgets. Passed: red/green workspace source guard, targeted rustfmt check, and line-count budgets. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, provider calls, cloud writes, and deploy execution. Exact next action: verify native Sources row receipt drilldowns and source-kind icons against real source-pack, media, reduced-context, receipt, and restore-preview rows in the governed native Zed proof window.

- Split DX Check summary ownership: `crates/agent_ui/src/dx_launch_workspace/check/summary.rs` now owns Check rail summary metric rows, outcome/duration/path/skip labels, scoring-config state, last-run labels, detail-command rows, and bounded checked/skipped path samples. `crates/agent_ui/src/dx_launch_workspace/check.rs` now delegates summary rows through the focused helper and stays focused on receipt state, panel sections, blockers, warnings, quick fixes, rail score, and top-level blocker composition at 68 lines. `script/dx-launch-workspace-source.test.ts` guards summary module registration, shell delegation, summary label ownership, path/skip sample ownership, and tighter Check/summary file budgets. Passed: red/green workspace source guard, targeted rustfmt check, and line-count budgets. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, provider calls, cloud writes, and deploy execution. Exact next action: verify native Check summary rows against a real `check-latest.json` receipt in the governed native Zed proof window.

- Split DX Source Audit status row ownership: `crates/agent_ui/src/dx_launch_workspace/source_audit/status.rs` now owns missing-root, missing-latest, invalid-schema, missing-markdown, and missing-DX-Studio-QA status rows. `crates/agent_ui/src/dx_launch_workspace/source_audit.rs` now delegates those rows through the focused helper and stays focused on Source Audit summary metrics, bounded row labels, warning selection, and rail composition at 90 lines. `script/dx-launch-workspace-source.test.ts` guards status module registration, parent delegation, status row ownership, invalid-schema row ownership, and tighter Source Audit/status file budgets. Passed: red/green workspace source guard, targeted rustfmt check, and line-count budgets. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, provider calls, cloud writes, and deploy execution. Exact next action: verify native Source Audit status rows against real latest source-audit, markdown, and DX Studio QA receipts in the governed native Zed proof window.

- Split DX Agent receipt status ownership: `crates/agent_ui/src/dx_launch_workspace/agents/receipts/status.rs` now owns receipt-root labels plus root-missing, inbox-root-missing, inbox-malformed, index-error, and unsafe-row warning selection. `crates/agent_ui/src/dx_launch_workspace/agents/receipts.rs` now delegates receipt status rows through the focused helper and stays focused on receipt index/inbox metrics, missing CLI prompt, latest receipt row, renderable receipt rows, inbox review summary, and next-action text at 89 lines. `script/dx-launch-workspace-source.test.ts` guards status module registration, parent delegation, status row ID ownership, root-state ownership, and tighter receipt/status file budgets. Passed: red/green workspace source guard, targeted rustfmt check, and line-count budgets. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, provider calls, cloud writes, and deploy execution. Exact next action: verify native Agent receipt warning/status rows against real DX Agents receipt-index and inbox receipts in the governed native Zed proof window.

- Split DX Agent provider row ownership: `crates/agent_ui/src/dx_launch_workspace/agents/providers/rows.rs` now owns provider and model row rendering, including provider/model state labels, detail labels, row backgrounds, and compact label layout. `crates/agent_ui/src/dx_launch_workspace/agents/providers.rs` now delegates row rendering through the focused helper and stays focused on catalog/provider/model summary state, hidden-managed-provider state, source-hash state, regeneration prompt, and stable row IDs at 69 lines. `script/dx-launch-workspace-source.test.ts` guards row module registration, parent delegation, row ID ownership, row helper label ownership, and tighter provider file budgets. Passed: red/green workspace source guard, targeted rustfmt check, and line-count budgets. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, provider calls, cloud writes, and deploy execution. Exact next action: verify native Agent provider/model rows against real provider catalog receipts in the governed native Zed proof window.

- Split DX Launch Gate warning and example row ownership: `crates/agent_ui/src/dx_launch_workspace/readiness/warnings.rs` now owns first-issue, redaction-review, and command-fanout warning row selection plus the `dx-launch-readiness-*` warning IDs, while `readiness/examples.rs` owns bounded Launch Gate example and next-action rows. `crates/agent_ui/src/dx_launch_workspace/readiness.rs` now delegates both helpers and stays focused on Launch Gate summary metrics, missing-root state, next-action fallback, and rail composition at 104 lines. `script/dx-launch-workspace-source.test.ts` guards helper module registration, parent delegation, warning ID ownership, example row ownership, and tighter Launch Gate file budgets. Passed: red/green workspace source guard, targeted rustfmt check, and line-count budgets. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, provider calls, cloud writes, and deploy execution. Exact next action: verify native Launch Gate warning and example rows against real launch readiness import-summary, release-gate, and fallback-drill packets in the governed native Zed proof window.

- Split DX Sources attachment state ownership: `crates/agent_ui/src/dx_launch_workspace/sources/attachments.rs` now owns attach-ready state, workspace-root/managed-receipt metrics, produced-media and restore-preview rows, and the no-attach-ready source prompt. `crates/agent_ui/src/dx_launch_workspace/sources.rs` now delegates both attachment and receipt state through focused helpers and stays focused on source-set orchestration and source-row control matching at 80 lines. `script/dx-launch-workspace-source.test.ts` guards attachment module registration, parent re-export, attachment summary ownership, and tighter Sources/attachment file budgets. Passed: red/green workspace source guard, targeted rustfmt check, and line-count budgets. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, provider calls, cloud writes, and deploy execution. Exact next action: verify native Sources attachment summary rows against real source-pack, media, and restore receipts in the governed native Zed proof window.

- Split DX Sources receipt state ownership: `crates/agent_ui/src/dx_launch_workspace/sources/receipts.rs` now owns receipt-root missing state, total receipt count rows, latest receipt rows, and the `latest-receipt-*` row IDs. `crates/agent_ui/src/dx_launch_workspace/sources.rs` now delegates receipt-source rendering through the focused helper and stays focused on source-set orchestration, attachment summary, and source-row control matching at 121 lines. `script/dx-launch-workspace-source.test.ts` guards receipt module registration, parent re-export, receipt row ID ownership, receipt snapshot ownership, and tighter Sources/receipt file budgets. Passed: red/green workspace source guard, targeted rustfmt check, and line-count budgets. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, provider calls, cloud writes, and deploy execution. Exact next action: verify native Sources receipt rows against real DX receipt roots and latest receipt labels in the governed native Zed proof window.

- Split DX Agent bridge review ownership: `crates/agent_ui/src/dx_launch_workspace/agents/bridge/review.rs` now owns action-error redaction, release-gate blocker/fanout/warning, import-summary blocker/fanout/warning, contract-redaction, bridge-error, and next-action fallback rows. `crates/agent_ui/src/dx_launch_workspace/agents/bridge.rs` now delegates final review rendering through the focused helper and stays focused on availability, latest receipt/command rows, and bridge summary composition at 85 lines. `script/dx-launch-workspace-source.test.ts` guards review module registration, bridge-shell delegation, review row ID ownership, next-action fallback ownership, and tighter bridge/review file budgets. Passed: red/green workspace source guard, targeted rustfmt check, and line-count budgets. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, provider calls, cloud writes, and deploy execution. Exact next action: verify native Agent bridge review rows against real DX Agents action-error, contract, import-summary, release-gate, and receipt-index receipts in the governed native Zed proof window.

- Split DX Agent receipt label ownership: `crates/agent_ui/src/dx_launch_workspace/agents/receipts/labels.rs` now owns receipt state/detail/provider-model/action/social/automation label decisions, including metadata-redacted, retry/cancel, social-auth, and automation-warning policy. `crates/agent_ui/src/dx_launch_workspace/agents/receipts/rows.rs` now delegates those labels through the focused helper and stays focused on GPUI row layout at 115 lines. `script/dx-launch-workspace-source.test.ts` guards receipt label module registration, row delegation, label ownership, and tighter receipt row/label budgets. Passed: red/green workspace source guard, targeted rustfmt check, and line-count budgets. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, provider calls, cloud writes, and deploy execution. Exact next action: verify native Agent receipt rows against real DX Agents receipt-index and inbox receipts in the governed native Zed proof window.

- Split DX Agent bridge summary section ownership: `crates/agent_ui/src/dx_launch_workspace/agents/bridge/summary.rs` is now a 21-line coordinator over `summary/overview.rs`, `summary/contract.rs`, `summary/import.rs`, and `summary/gate.rs`. Overview rows own Bridge/CLI/account/task/catalog metrics, contract rows own contract/receipt/redaction metrics, import rows own import/release/action-map/recovery/fanout/last-action metrics, and gate rows own release-gate, retention, action, recovery, fanout, and receipt-index metrics. `script/dx-launch-workspace-source.test.ts` guards section module registration, parent delegation, section ownership, and tighter file budgets. Passed: red/green workspace source guard, targeted rustfmt check, and line-count budgets. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, provider calls, cloud writes, and deploy execution. Exact next action: verify native Agent bridge summary rows against real DX Agents contract/import/release-gate/receipt-index receipts in the governed native Zed proof window.

- Split DX Agent provider label ownership: `crates/agent_ui/src/dx_launch_workspace/agents/provider_labels.rs` is now a 13-line facade over focused `provider_labels/detail.rs`, `provider_labels/state.rs`, and `provider_labels/text.rs`, while `provider_labels_tests.rs` owns the direct pure-label tests. Provider/model row rendering still imports the same facade, but provider/model state priority, blank fallback text, provider/model detail labels, compatibility trimming, and overflow disclosure now live in smaller source-owned files. `script/dx-launch-workspace-source.test.ts` guards the facade wiring, detail/state/text ownership, test ownership, and tighter file budgets. Passed: red/green workspace source guard, direct `rustc --edition=2024 --test crates\agent_ui\src\dx_launch_workspace\agents\provider_labels.rs`, targeted rustfmt check, and line-count budgets. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, provider calls, cloud writes, and deploy execution. Exact next action: verify native Agent provider/model labels against real provider catalog receipts in the governed native Zed proof window.

- Split DX Launch Audit warning ownership: `crates/agent_ui/src/dx_launch_workspace/audit/warnings.rs` now owns first-issue, redaction-review, and command-fanout warning selection plus the `dx-launch-audit-warning`, `dx-launch-audit-redaction-review`, and `dx-launch-audit-fanout-review` warning IDs. `crates/agent_ui/src/dx_launch_workspace/audit.rs` now delegates launch-audit warning selection through the focused helper and stays focused on Launch Audit rail metrics, missing-root/missing-packet cards, and next-action fallback at 112 lines. `script/dx-launch-workspace-source.test.ts` guards warning module registration, parent delegation, warning ownership, warning IDs, and Launch Audit/warning file budgets. Passed: red/green workspace source guard, targeted rustfmt check, and line-count budgets. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, provider calls, cloud writes, and deploy execution. Exact next action: verify native Launch Audit warning rows against real `G:\Dx\cli\fixtures\launch-examples` audit packets in the governed native Zed proof window.

- Split DX Source Audit warning ownership: `crates/agent_ui/src/dx_launch_workspace/source_audit/warnings.rs` now owns first-issue, risk-review, template-trust, and DX Studio QA warning selection plus the `dx-source-audit-warning`, `dx-source-audit-risk`, `dx-source-audit-template-trust`, and `dx-source-audit-www-qa` warning IDs. `crates/agent_ui/src/dx_launch_workspace/source_audit.rs` now delegates launch source-audit warning selection through the focused helper and stays focused on Source Audit rail metrics, missing-root/latest/markdown/QA cards, invalid-schema warning, and top-level status rows at 131 lines. `script/dx-launch-workspace-source.test.ts` guards warning module registration, parent delegation, warning ownership, warning IDs, and Source Audit/warning file budgets. Passed: red/green workspace source guard, targeted rustfmt check, and line-count budgets. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, provider calls, cloud writes, and deploy execution. Exact next action: verify native Source Audit warning rows against real `G:\Dx\.dx\audit\launch-source\latest.json` and DX Studio QA receipts in the governed native Zed proof window.

- Split DX Launch Status row and warning ownership: `crates/agent_ui/src/dx_launch_workspace/launch_status/rows.rs` now owns schema-valid Agent/Token/Discovery next-action rows and redaction-summary detail labels, while `launch_status/warnings.rs` owns invalid-schema, redaction-review, and launch-status warning selection. `crates/agent_ui/src/dx_launch_workspace/launch_status.rs` now delegates through both focused helpers and stays focused on the Launch Status rail shell, summary metrics, missing-root/missing-latest cards, next-action fallback, and top-level status rows at 111 lines. `script/dx-launch-workspace-source.test.ts` guards row/warning module registration, parent delegation, helper ownership, warning IDs, and Launch Status/row/warning file budgets. Passed: red/green workspace source guard, targeted rustfmt check, and line-count budgets. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, provider calls, cloud writes, and deploy execution. Exact next action: verify native Launch Status warning and schema-valid detail rows against real `G:\Dx\.dx\receipts\launch\status-latest.json` receipts in the governed native Zed proof window.

- Split DX Launch Receipts row rendering: `crates/agent_ui/src/dx_launch_workspace/launch_receipts/rows.rs` now owns launch receipt card rows, receipt identity row IDs, display-state rows, age/next-action labels, and themed row backgrounds. `crates/agent_ui/src/dx_launch_workspace/launch_receipts.rs` now delegates receipt card rendering through the focused row helper and stays focused on Launch Receipts review state, latest/snapshot status, malformed/stale/schema warnings, missing-directory/missing-latest cards, and next-action fallback at 118 lines. `script/dx-launch-workspace-source.test.ts` guards row module registration, parent delegation, receipt-row ownership, and Launch Receipts/row file budgets. Passed: red/green workspace source guard, targeted rustfmt check, and line-count budgets. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, provider calls, cloud writes, and deploy execution. Exact next action: verify native Launch Receipts latest/snapshot card rows against real `G:\Dx\.dx\receipts\launch` receipts in the governed native Zed proof window.

- Split DX Check rail row rendering: `crates/agent_ui/src/dx_launch_workspace/check/rows.rs` now owns Check section score rows, blocker rows, warning rows plus next-action rows, and quick-fix detail rows including approval/receipt-write/command detail labels. `crates/agent_ui/src/dx_launch_workspace/check.rs` now delegates through the focused row helpers and stays focused on the Check rail shell, receipt state, summary metrics, missing/error state, next action, rail score, and top-level blockers at 126 lines. `script/dx-launch-workspace-source.test.ts` guards Check row module registration, parent delegation, section/warning/quick-fix ownership, and Check/check-row file budgets. Passed: red/green workspace source guard, adjacent agent bridge, deploy panel, and source-set guards, targeted rustfmt check, and line-count budgets. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, provider calls, cloud writes, and deploy execution. Exact next action: verify native Check rail section/warning/quick-fix rows against real `check-latest.json` receipts in the governed native Zed proof window.

- Split DX Check label ownership: `crates/agent_ui/src/dx_launch_workspace/check_labels.rs` is now a 10-line facade, `check_labels/counts.rs` owns checked-path, skipped-check, nonblank-count, and outcome-count labels, `check_labels/run.rs` owns duration and last-run/generated-at labels, and `check_labels_tests.rs` owns the direct unit tests for those helpers. `script/dx-launch-workspace-source.test.ts` guards the Check label facade, production submodules, test-module split, direct label ownership, and file budgets. Passed: red/green workspace source guard, direct `rustc --edition=2024 --test` Check label tests, adjacent agent bridge, deploy panel, and source-set guards, targeted rustfmt check, and line-count budgets. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, provider calls, cloud writes, and deploy execution. Exact next action: verify native Check rail labels against real `check-latest.json` receipts in the governed native Zed proof window.

- Split DX Runtime Proof row ownership again: `crates/agent_ui/src/dx_launch_workspace/proof/runtime_rows.rs` is now a 5-line coordinator, `proof/runtime_rows/plan.rs` owns latest runtime proof plan rows, requirements labels, expected final command labels, evidence detail labels, blocker labels, and next-action fallback, and `proof/runtime_rows/receipt.rs` owns import/status receipt rows, validation state labels, operator/evidence counts, headline/summary labels, final command/source labels, and first evidence sample labels. `script/dx-launch-workspace-source.test.ts` guards runtime row submodule registration, parent re-exports, plan/receipt helper ownership, proof label usage, and plan/receipt file budgets. Passed: red/green workspace source guard, adjacent agent bridge, deploy panel, and source-set guards, targeted rustfmt check, and line-count budgets. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, provider calls, cloud writes, and deploy execution. Exact next action: verify native Runtime Proof plan/import/status rows against real runtime proof receipts in the governed native Zed proof window.

- Split DX Agent bridge summary rows: `crates/agent_ui/src/dx_launch_workspace/agents/bridge/summary.rs` now owns Agent bridge summary metric rows for bridge/CLI/account/automation/task/catalog state, contract/import/release-gate state, recovery-control labels, command-fanout labels, receipt index counts, and gate retention/action/recovery labels. `crates/agent_ui/src/dx_launch_workspace/agents/bridge.rs` now delegates through `summary::dx_agent_bridge_summary_rows` and stays focused on bridge availability prompts, latest/failed command rows, warning priority, redaction warnings, and next-action fallback at 157 lines. `script/dx-launch-workspace-source.test.ts` guards summary module registration, parent delegation, summary row ownership, recovery/fanout/receipt-index label ownership, and bridge/summary file budgets. Passed: red/green workspace source guard, adjacent agent bridge, deploy panel, and source-set guards, targeted rustfmt check, and line-count budgets. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, provider calls, cloud writes, and deploy execution. Exact next action: verify native Agent bridge summary rows against real DX Agents contract/import/release-gate/receipt-index receipts in the governed native Zed proof window.

- Split DX Agent receipt row rendering: `crates/agent_ui/src/dx_launch_workspace/agents/receipts/rows.rs` now owns Agent receipt row rendering, including safe/active/redacted state labels, provider/model labels, retry/cancel labels, social/auth labels, automation warning labels, schema/error/next-action details, and `dx-agent-receipt-{ix}` rows. `crates/agent_ui/src/dx_launch_workspace/agents/receipts.rs` now delegates through `rows::dx_agent_receipt_row` and stays focused on receipt index/inbox state, root status, warnings, and inbox review summary at 131 lines. `script/dx-launch-workspace-source.test.ts` guards receipt row module registration, parent delegation, row helper ownership, retry/social/automation label ownership, and receipt/receipt-row file budgets. Passed: red/green workspace source guard, adjacent agent bridge, deploy panel, and source-set guards, targeted rustfmt check, `git diff --check`, and conflict-marker scan. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, provider calls, cloud writes, and deploy execution. Exact next action: verify native Agent receipt rows against real DX Agents receipt-index and inbox receipts in the governed native Zed proof window.

- Split DX source row rendering: `crates/agent_ui/src/dx_launch_workspace/sources/rows.rs` now owns source item rows, receipt drilldown rows, proof/warning source signals, source-kind icons, and the `source-receipt-*`, `source-proof-*`, and `source-warning-*` row IDs. `crates/agent_ui/src/dx_launch_workspace/sources.rs` now delegates through `rows::source_item_row` and stays focused on source-set orchestration, attachment summary, source-row control matching, and receipt-source state at 155 lines. `script/dx-launch-workspace-source.test.ts` guards source row module registration, parent delegation, row helper ownership, icon mapping, signal row IDs, and source/source-row file budgets. Passed: red/green workspace source guard, adjacent agent bridge, deploy panel, and source-set guards, targeted rustfmt check, `git diff --check`, and conflict-marker scan. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, provider calls, cloud writes, and deploy execution. Exact next action: verify native Sources rail rows against real workspace/source-pack/media/restore receipt inputs in the governed native Zed proof window.

- Split DX runtime proof row rendering: `crates/agent_ui/src/dx_launch_workspace/proof/runtime_rows.rs` now owns latest runtime proof plan and import/status receipt rows, including requirements, evidence detail, receipt validation state, command/source/evidence labels, and `dx-runtime-proof-latest-*` row IDs. `crates/agent_ui/src/dx_launch_workspace/proof.rs` now delegates through `runtime_rows::{runtime_proof_plan_row, runtime_proof_receipt_row}` and stays focused on the runtime proof status shell at 71 lines. `script/dx-launch-workspace-source.test.ts` guards runtime row module registration, parent delegation, row helper ownership, proof/proof-runtime file budgets, and proof label ownership. Passed: red/green workspace source guard, adjacent agent bridge, deploy panel, and source-set guards, targeted rustfmt check, `git diff --check`, and conflict-marker scan. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, provider calls, cloud writes, and deploy execution. Exact next action: verify native Runtime Proof status rows against real runtime proof plan/import/status receipts in the governed native Zed proof window.

- Split DX Proof freshness rendering: `crates/agent_ui/src/dx_launch_workspace/proof/freshness.rs` now owns proof freshness buckets, latest labels, missing-root fallback labels, and the `dx-proof-freshness-{ix}` row IDs. `crates/agent_ui/src/dx_launch_workspace/proof.rs` now delegates through `freshness::proof_freshness_state` and stays focused on runtime proof status at 235 lines. `script/dx-launch-workspace-source.test.ts` guards proof freshness module registration, parent delegation, row helper ownership, and proof/proof-freshness file budgets. Passed: red/green workspace source guard, adjacent agent bridge, deploy panel, and source-set guards, targeted rustfmt check, `git diff --check`, and conflict-marker scan. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, provider calls, cloud writes, and deploy execution. Exact next action: verify native Proof freshness rows against real DX proof freshness receipts in the governed native Zed proof window.

- Split DX Agent bridge summary rendering: `crates/agent_ui/src/dx_launch_workspace/agents/bridge.rs` now owns the Agent bridge summary, contract/import/release-gate rows, missing receipt/root prompts, action-error warnings, redaction warnings, and next-action fallback. `crates/agent_ui/src/dx_launch_workspace/agents.rs` is now a 13-line module index that re-exports the focused bridge, provider, receipt, social, and automation rails. `script/dx-launch-workspace-source.test.ts` guards bridge module registration, parent delegation, warning IDs, helper ownership, and the tiny Agent module budget. Passed: red/green workspace source guard, adjacent agent bridge, deploy panel, and source-set guards, targeted rustfmt check, `git diff --check`, and conflict-marker scan. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, provider calls, cloud writes, and deploy execution. Exact next action: verify native Agent bridge summary rows against real DX Agents contract, import-summary, release-gate, and action-error receipts in the governed native Zed proof window.

- Split DX Agent social and automation rail rendering: `crates/agent_ui/src/dx_launch_workspace/agents/actions.rs` now owns the shared action-line label, `agents/automations.rs` owns automation summary and rows, `agents/social.rs` owns social account summary and rows, and `agents/social_actions.rs` owns connect/disconnect receipt rows. `crates/agent_ui/src/dx_launch_workspace/agents.rs` now delegates through focused social/automation modules and is down to 330 lines while keeping the bridge summary as the coordinator. `script/dx-launch-workspace-source.test.ts` guards module registration, parent delegation, helper ownership, social/action/automation file budgets, and warning-free source structure. Passed: red/green workspace source guard, adjacent agent bridge, deploy panel, and source-set guards, targeted rustfmt check, `git diff --check`, and conflict-marker scan. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, provider calls, cloud writes, and deploy execution. Exact next action: verify native Agent social and automation rail rows against real DX Agents social/action and automation receipts in the governed native Zed proof window.

- Split DX Agent receipt rail rendering: `crates/agent_ui/src/dx_launch_workspace/agents/receipts.rs` now owns Agent Receipts right-rail rendering, including receipt root/inbox status, malformed inbox warning, unsafe row warning, latest receipt path, inbox review counts, receipt row details, provider/model/social/automation receipt labels, and retry/cancel labels. `crates/agent_ui/src/dx_launch_workspace/agents.rs` now delegates through `receipts::dx_agent_receipt_state` and is down to 602 lines while keeping bridge/social/automation rows. `script/dx-launch-workspace-source.test.ts` guards the module registration, parent delegation, receipt row helper ownership, warning IDs, and Agent receipt rail file budget. Passed: red/green workspace source guard, adjacent agent bridge, deploy panel, and source-set guards, targeted rustfmt check, `git diff --check`, and conflict-marker scan. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, provider calls, cloud writes, and deploy execution. Exact next action: verify native Agent Receipts rail rows against real DX Agents receipt-index and inbox receipts in the governed native Zed proof window.

- Split DX Launch Receipts rail rendering: `crates/agent_ui/src/dx_launch_workspace/launch_receipts.rs` now owns Launch Receipts right-rail rendering for latest state, snapshot/malformed/stale/schema counts, latest receipt row, latest snapshot row, malformed warning, stale/expired warning, schema review warning, and receipt scanner warning. `crates/agent_ui/src/dx_launch_workspace.rs` now delegates through `launch_receipts::launch_receipt_review_state` and is down to 464 lines as a coordinator. `script/dx-launch-workspace-source.test.ts` guards the module registration, parent delegation, warning IDs, row helper ownership, and Launch Receipts rail file budget. Passed: red/green workspace source guard, adjacent launch receipts/status/prompt and deploy panel guards, targeted rustfmt check, `git diff --check`, and conflict-marker scan. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, cloud writes, and deploy execution. Exact next action: verify native Launch Receipts rail rows against real `G:\Dx\.dx\receipts\launch` receipts in the governed native Zed proof window.

- Split DX-WWW Evidence rail rendering: `crates/agent_ui/src/dx_launch_workspace/www_evidence.rs` now owns the WWW Evidence right-rail rows for project/release roots, artifact counts, format counts, result buckets, no-execution count, latest/missing/next rows, missing-root cards, warning state, and partial-evidence warning. `crates/agent_ui/src/dx_launch_workspace.rs` now delegates through `www_evidence::www_launch_evidence_state` and is down to 611 lines as a coordinator. `script/dx-launch-workspace-source.test.ts` guards the module registration, parent delegation, warning IDs, and WWW Evidence rail file budget. Passed: red/green workspace source guard, adjacent DX-WWW evidence, launch prompt, launch audit, and deploy panel guards, targeted rustfmt check, `git diff --check`, and conflict-marker scan. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, cloud writes, and deploy execution. Exact next action: verify native WWW Evidence rows against real DX-WWW release evidence packets in the governed native Zed proof window.

- Split DX launch Audit rail rendering: `crates/agent_ui/src/dx_launch_workspace/audit.rs` now owns Launch Audit right-rail rendering for command, safety, fixture, smoke, example, missing-packet, redaction-review, and command-fanout rows. `crates/agent_ui/src/dx_launch_workspace.rs` now delegates through `audit::launch_audit_state` and is down to 699 lines as a coordinator. `script/dx-launch-workspace-source.test.ts` guards the module registration, parent delegation, warning IDs, and audit rail file budget. Passed: red/green workspace source guard, adjacent launch audit source/fixture, prompt, source-audit, and deploy guards, targeted rustfmt check, `git diff --check`, and conflict-marker scan. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, cloud writes, and deploy execution. Exact next action: verify native Launch Audit rows against the real `G:\Dx\cli\fixtures\launch-examples` packets in the governed native Zed proof window.

- Split DX launch Source Audit rail rendering: `crates/agent_ui/src/dx_launch_workspace/source_audit.rs` now owns the Source Audit right-rail rows, missing-receipt cards, schema warnings, risk-review warning, template-trust warning, and DX Studio QA warning. `crates/agent_ui/src/dx_launch_workspace.rs` now delegates through `source_audit::launch_source_audit_state` and is down to 820 lines as a coordinator. `script/dx-launch-workspace-source.test.ts` guards the module registration, parent delegation, warning IDs, and source-audit rail file budget. Passed: red/green workspace source guard, adjacent launch source-audit/prompts/deploy/source-set/receipt-history guards, targeted rustfmt check, `git diff --check`, and conflict-marker scan. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, cloud writes, and deploy execution. Exact next action: verify the native Source Audit rail renders real `G:\Dx\.dx\audit\launch-source\latest.json` rows in the governed native Zed proof window.

- Split DX Launch Receipts helper ownership: `crates/agent_ui/src/dx_launch_receipts.rs` now stays focused on cache, receipt-root scan orchestration, aggregate counts, warning status, and public snapshot fields at 226 lines. `dx_launch_receipts/fields.rs` owns bounded receipt field extraction, `freshness.rs` owns freshness state and operator-summary wording, `paths.rs` owns latest/snapshot path discovery and timestamp helpers, `receipt_io.rs` owns bounded JSON receipt reads, and `summary.rs` owns `DxLaunchReceiptSummary` parsing/display/schema checks. Added `script/dx-launch-receipts-source.test.ts` to guard the module boundary and file budgets. Passed: red/green `node --test script\dx-launch-receipts-source.test.ts`, adjacent launch prompts/workspace/status/contract/audit/readiness guards, and targeted rustfmt check. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, cloud writes, and deploy execution. Exact next action: verify Launch Receipt latest/snapshot freshness, malformed retained rows, and stale/expired warning copy against real `G:\Dx\.dx\receipts\launch` receipts in the governed native Zed proof window.

- Split DX Launch Status helper ownership: `crates/agent_ui/src/dx_launch_status.rs` now stays focused on cache, latest-receipt validation, snapshot assembly, and public snapshot fields at 204 lines. `dx_launch_status/fields.rs` owns JSON field/pointer helpers, `receipts.rs` owns bounded launch-status receipt reads, `review.rs` owns redaction review policy, and `summaries.rs` owns Agents/Tokens/Discovery summary parsing plus empty states. Added `script/dx-launch-status-source.test.ts` to guard the module boundary and file budgets. Passed: red/green `node --test script\dx-launch-status-source.test.ts`, adjacent launch workspace/prompts/contract/audit/readiness guards, and targeted rustfmt check. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, cloud writes, and deploy execution. Exact next action: verify Launch Status latest-receipt rows, redaction review, Agents, Tokens, and Discovery summaries against real `G:\Dx\.dx\receipts\launch\status-latest.json` in the governed native Zed proof window.

- Split DX Launch Contract helper ownership and added fixture coverage: `crates/agent_ui/src/dx_launch_contracts.rs` now stays focused on cache, import-manifest/handoff scan, snapshot assembly, and public snapshot fields at 248 lines. `dx_launch_contracts/fields.rs` owns JSON field helpers, `packets.rs` owns bounded packet reads, and `review.rs` owns redaction review policy. Added `script/dx-launch-contracts-source.test.ts` to guard the module boundary and file budgets, and `script/dx-launch-contracts-fixtures.test.ts` to validate the real `G:\Dx\cli\fixtures\launch-examples\import-manifest.json` and `handoff.json` packets for schemas, ready state, nonblank next actions, metadata-only redaction, zero nested command fanout, packet call order, required commands, polling policy, cached receipt path, and action-map safety. Passed: red/green contract source and fixture guards, adjacent launch workspace/prompts/audit/readiness guards, targeted rustfmt check, `git diff --check`, and conflict-marker scan. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, cloud writes, and deploy execution. Exact next action: verify native Launch Handoff rows and import-manifest packet rows against these real fixtures in the governed Zed proof window.

- Added DX Launch Audit fixture contract coverage: `script/dx-launch-audit-fixtures.test.ts` now validates the real `G:\Dx\cli\fixtures\launch-examples` audit packets (`schemas.json`, `fixtures.json`, `smoke.json`, and `status.json`) for expected schemas, ready status, nonblank next actions, metadata-only redaction, zero nested command fanout, bounded command/fixture/smoke sample rows, and ready Agents/Tokens/Discovery status sections. The guard allows declared totals to exceed rendered sample rows, matching the Zed scanner contract. Passed: red/green `node --test script\dx-launch-audit-fixtures.test.ts`, adjacent launch audit/readiness fixture/source guards, `git diff --check`, and conflict-marker scan. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, cloud writes, and deploy execution. Exact next action: verify the native Launch Audit rail renders the same schema, fixture, smoke, and status packet rows in the governed Zed proof window.

- Added DX Launch Readiness fixture contract coverage: `script/dx-launch-readiness-fixtures.test.ts` now validates the real `G:\Dx\cli\fixtures\launch-examples` import-summary, release-gate, and fallback-drill packet families for expected schemas, ready/non-ready coverage, nonblank next actions, metadata-only `no_command_fanout`, zero nested command fanout, safe redaction flags, and expected recovery-command keys. Passed: `node --test script\dx-launch-readiness-fixtures.test.ts`, adjacent launch readiness/workspace/prompts/audit/agent source guards, `git diff --check`, and conflict-marker scan. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, cloud writes, and deploy execution. Exact next action: verify the native Launch Readiness rail renders these same real fixture states in the governed Zed proof window.

- Split DX Launch Readiness helper ownership: `crates/agent_ui/src/dx_launch_readiness.rs` now stays focused on cache, example discovery, snapshot assembly, and public snapshot fields at 304 lines. `dx_launch_readiness/packet_fields.rs` owns JSON field helpers, `packets.rs` owns bounded packet read/schema validation, `review.rs` owns command-fanout and redaction review policy, `examples.rs` owns recovery/example curation helpers, and `status_counts.rs` owns readiness status-count formatting. Added `script/dx-launch-readiness-source.test.ts` to guard the module boundary and small-file budgets. Passed: red/green `node --test script\dx-launch-readiness-source.test.ts`, adjacent `node --test script\dx-launch-workspace-source.test.ts script\dx-launch-prompts-source.test.ts script\dx-launch-audit-source.test.ts script\dx-agent-bridge-source.test.ts`, targeted rustfmt check, `git diff --check`, and conflict-marker scan. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, cloud writes, and deploy execution. Exact next action: verify Launch Readiness import-summary, release-gate, fallback-drill, fanout, redaction-review, and recovery-command rows against real `G:\Dx\cli\fixtures\launch-examples` packets in the governed native Zed proof window.

- Split DX Launch Audit helper ownership: `crates/agent_ui/src/dx_launch_audit.rs` now stays focused on cache, packet presence, audit status assembly, and public snapshot fields at 329 lines. `dx_launch_audit/packet_fields.rs` owns packet field helpers, `packets.rs` owns bounded packet read/schema validation, `review.rs` owns command-fanout and redaction review policy, and `status_summaries.rs` owns status packet summaries for Agents, Tokens, and Discovery. Added `script/dx-launch-audit-source.test.ts` to guard the module boundary and small-file budgets. Passed: red/green `node --test script\dx-launch-audit-source.test.ts`, adjacent `node --test script\dx-launch-workspace-source.test.ts`, adjacent `node --test script\dx-launch-prompts-source.test.ts`, adjacent `node --test script\dx-agent-bridge-source.test.ts`, adjacent `node --test script\dx-runtime-proof-status-source.test.ts`, and targeted rustfmt check. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, cloud writes, and deploy execution. Exact next action: verify Launch Audit schemas/fixtures/smoke/status rows against real `G:\Dx\cli\fixtures\launch-examples` packets in the governed native Zed proof window.

- Split DX Runtime Proof Status helper ownership: `crates/agent_ui/src/dx_runtime_proof_status.rs` now stays focused on cache, workspace scan, claim-state assembly, and public snapshot types at 264 lines. `dx_runtime_proof_status/receipts.rs` owns bounded receipt counting/discovery/JSON reads, `fields.rs` owns JSON field extraction and compact text helpers, and `summaries.rs` owns plan/import/status receipt parsers. Added `script/dx-runtime-proof-status-source.test.ts` to guard the module boundary and file budgets. Passed: red/green `node --test script\dx-runtime-proof-status-source.test.ts`, adjacent `node --test script\dx-launch-workspace-source.test.ts`, adjacent `node --test script\dx-launch-prompts-source.test.ts`, adjacent `node --test script\dx-agent-bridge-source.test.ts`, and targeted rustfmt check. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, cloud writes, and deploy execution. Exact next action: verify Runtime Proof plan/import/status rows against real `tools/dx-runtime-proof` receipts in the governed native Zed proof window.

- Split DX Source Sets helper ownership: `crates/agent_ui/src/dx_source_sets.rs` now stays focused on source-set snapshot assembly at 408 lines, while `dx_source_sets/receipts.rs` owns bounded receipt discovery/JSON reads, `receipt_fields.rs` owns JSON field extraction, `formatting.rs` owns compact display labels/byte formatting/status labels, and `restore.rs` owns Forge restore warning classification. Added `script/dx-source-sets-source.test.ts` to guard the module boundary and small-file budgets. Passed: red/green `node --test script\dx-source-sets-source.test.ts`, adjacent `node --test script\dx-launch-workspace-source.test.ts`, adjacent `node --test script\dx-deploy-panel-source.test.ts`, adjacent `node --test script\dx-agent-bridge-source.test.ts`, and targeted rustfmt check. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, cloud writes, and deploy execution. Exact next action: verify Sources rail workspace, metasearch, reduced-context, media output, and Forge restore rows against real receipts in the governed native Zed proof window.

- Split DX launch readiness rail rendering: `crates/agent_ui/src/dx_launch_workspace/readiness.rs` now owns Launch Gate/readiness rail rendering, including import-summary, release-gate, fallback, freshness, fanout, recovery, example rows, and warning states. `crates/agent_ui/src/dx_launch_workspace.rs` now delegates through `readiness::launch_readiness_state` and stays under the tighter 1000-line source guard. `script/dx-launch-workspace-source.test.ts` guards the readiness module, parent delegation, and small-file budget. Passed: `node --test script\dx-launch-workspace-source.test.ts`, adjacent `node --test script\dx-deploy-panel-source.test.ts`, adjacent `node --test script\dx-www-launch-evidence-source.test.ts`, adjacent `node --test script\dx-agent-bridge-source.test.ts`, targeted rustfmt check, `git diff --check`, and conflict-marker scan. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, DX-WWW execution, cloud writes, and deploy execution. Exact next action: verify Launch Gate/readiness import-summary, release-gate, fallback, and fanout rows against real launch readiness receipts in the governed native Zed proof window.

- Split DX Deploy source guard ownership: `script/dx-deploy-panel-source.test.ts` now stays focused on panel module registration, deploy prompt ownership, rail ordering, source file-size caps, guard file-size caps, and docs coverage. Focused receipt/root/provider checks now live in `script/dx-deploy-receipts-source.test.ts`, launch receipt/provenance/status/action checks live in `script/dx-deploy-launch-gate-source.test.ts`, and launch outcome/scope/bucket/approval/evidence checks live in `script/dx-deploy-launch-evidence-source.test.ts`, with shared helpers in `script/dx-deploy-source-guard.ts`. Passed red/green source guard split; each deploy source guard is below 260 lines. Exact next action: add future deploy assertions to the guard file that owns that receipt surface, not the panel wrapper.

- Split DX Deploy launch-gate notice parsing: `crates/agent_ui/src/dx_deploy_launch_notices.rs` now owns launch blocker/warning notice structs and bounded parsing, `dx_deploy_receipt_fields.rs` now also owns shared boolean field extraction, and `dx_deploy_launch_gate.rs` is down to 195 lines focused on check-receipt selection and launch-gate snapshot assembly. `dx_deploy_launch_gate_rail.rs` imports notice rows from the focused module while preserving source-owned severity and evidence-path rendering. `script/dx-deploy-panel-source.test.ts` now guards the notice module, shared field-helper ownership, and smaller launch-gate source budget. Passed: red/green `node --test script\dx-deploy-panel-source.test.ts` and targeted `rustfmt --edition 2024` on touched deploy Rust files. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, DX-WWW execution, cloud writes, and deploy execution. Exact next action: verify launch blocker/warning severity and evidence-path rows against real root `dx check` launch receipts in the governed native Zed proof window.

- Split DX launch Handoff rendering and hardened Launch Status labels: `crates/agent_ui/src/dx_launch_workspace/contracts.rs` now owns Launch Handoff rail rendering, `launch_status_labels.rs` owns whitespace-safe Launch Status summary, command, next-action, and optional redaction labels, and `dx_launch_workspace.rs` is down to 1077 lines under the 1120-line source guard. `script/dx-launch-workspace-source.test.ts` now guards the Handoff module, Launch Status label boundary, and blank receipt edge cases. Passed: red/green `node --test script\dx-launch-workspace-source.test.ts`, direct std-only `rustc --edition=2024 --test crates\agent_ui\src\dx_launch_workspace\launch_status_labels.rs`, adjacent `node --test script\dx-www-launch-evidence-source.test.ts`, adjacent `node --test script\dx-agent-bridge-source.test.ts`, targeted rustfmt check, `git diff --check`, and conflict-marker scan. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, DX-WWW execution, cloud writes, and deploy execution. Exact next action: verify Launch Handoff and Launch Status fallback rows against real launch status and handoff receipts in the governed native Zed proof window.

- Split DX Deploy provider-gate receipt parsing: `crates/agent_ui/src/dx_deploy_provider_gate_summary.rs` now owns provider-gate receipt summaries, rows, quick-fix risk metadata, and parsing; `crates/agent_ui/src/dx_deploy_receipt_fields.rs` owns shared deploy receipt JSON field helpers; and `crates/agent_ui/src/dx_deploy_receipt_summary.rs` is back to command receipt plus provider capability rows at 121 lines. `dx_deploy_capabilities.rs` keeps the existing deploy rail facade/re-exports so the Provider Gate and capability matrix rails stay wired to the same DX ecosystem surfaces. `script/dx-deploy-panel-source.test.ts` now guards the new module boundary, shared helper ownership, and small-file budgets. Passed: red/green `node --test script\dx-deploy-panel-source.test.ts` and targeted `rustfmt --edition 2024` on touched deploy Rust files. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, DX-WWW execution, cloud writes, and deploy execution. Exact next action: verify provider-gate quick-fix/risk rows against real root `dx deploy` receipts in the governed native Zed proof window.

- Split DX launch Status and Binary Cache rail rendering: `crates/agent_ui/src/dx_launch_workspace/launch_status.rs` now owns Launch Status rail rendering, `binary_cache.rs` owns Binary Cache rail rendering, `binary_cache_labels.rs` owns blank-safe summary, next-action, detail, and path labels, and `dx_launch_workspace.rs` delegates through focused modules at 1184 lines under the tighter 1280-line source guard. `script/dx-launch-workspace-source.test.ts` guards the module boundaries, line budgets, warning helper, and shared `yes_no` label home. Passed: red/green `node --test script\dx-launch-workspace-source.test.ts`, executed direct std-only label harnesses for `binary_cache_labels.rs` and `list_labels.rs`, adjacent `node --test script\dx-www-launch-evidence-source.test.ts`, adjacent `node --test script\dx-agent-bridge-source.test.ts`, targeted rustfmt check, `git diff --check`, and conflict-marker scan. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, DX-WWW execution, cloud writes, and deploy execution. Exact next action: verify Launch Status and Binary Cache blank/fallback rows against real launch and receipt-cache artifacts in the governed native Zed proof window.

- Hardened DX-WWW launch evidence status labels: `crates/agent_ui/src/dx_www_launch_evidence/evidence_status.rs` now owns the pure packet status classifier, so failed packets stay `blocked`, missing `passed` with findings becomes `warning`, and `passed: true` packets with non-empty findings no longer render as clean `ready`. `dx_www_launch_evidence.rs` delegates status classification through the focused helper, and `script/dx-www-launch-evidence-source.test.ts` guards the status module beside the catalog and score labels. Passed: red/green `node --test script\dx-www-launch-evidence-source.test.ts`, direct `rustc --edition=2024 --test crates\agent_ui\src\dx_www_launch_evidence\evidence_status.rs`, adjacent `node --test script\dx-agent-bridge-source.test.ts`, and targeted rustfmt check. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, DX-WWW execution, cloud writes, and deploy execution. Exact next action: verify warning status rows against real DX-WWW release evidence packets in the governed native Zed proof window.

- Split DX-WWW launch evidence score labels: `crates/agent_ui/src/dx_www_launch_evidence/evidence_labels.rs` now owns pure score/schema labels for evidence rows, rejects impossible scores above 100 as `invalid score N`, trims blank schemas to `unknown`, and keeps score formatting out of the scanner. `script/dx-www-launch-evidence-source.test.ts` now guards the label module beside the expected-artifact catalog. Passed: red/green `node --test script\dx-www-launch-evidence-source.test.ts`, direct `rustc --edition=2024 --test crates\agent_ui\src\dx_www_launch_evidence\evidence_labels.rs`, adjacent launch and bridge source guards, targeted rustfmt check, `git diff --check`, and conflict-marker scan. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, DX-WWW execution, cloud writes, and deploy execution. Exact next action: verify score/schema labels against real generated DX-WWW release artifacts in the governed native Zed proof window.

- Split DX-WWW launch evidence catalog ownership: `crates/agent_ui/src/dx_www_launch_evidence/expected_artifacts.rs` now owns the static expected launch-evidence artifact catalog, while `dx_www_launch_evidence.rs` stays focused on project-root selection, cache/scanning, artifact inspection, and operator summaries. Added `script/dx-www-launch-evidence-source.test.ts` to guard the module boundary and line budgets; scanner dropped from 595 to 386 lines. Passed: red/green `node --test script\dx-www-launch-evidence-source.test.ts`, `node --test script\dx-launch-workspace-source.test.ts`, adjacent `node --test script\dx-agent-bridge-source.test.ts`, targeted rustfmt check, `git diff --check`, and conflict-marker scan. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, cloud writes, and deploy execution. Exact next action: verify DX-WWW launch evidence rows against real generated release artifacts in the governed native Zed proof window.

- Hardened DX Agent bridge local receipt labels: `crates/agent_ui/src/dx_agent_bridge/local_file_labels.rs` now owns receipt-file label filtering, accepts `.json` extension spelling case-insensitively, keeps non-JSON files out of latest receipt rows, and preserves root-relative labels. `local_files.rs` delegates latest-receipt labeling through the focused helper, and `script/dx-agent-bridge-source.test.ts` guards the new ownership boundary plus line budgets. Passed: red/green `node --test script\dx-agent-bridge-source.test.ts`, direct `rustc --edition=2024 --test crates\agent_ui\src\dx_agent_bridge\local_file_labels.rs`, and targeted rustfmt check. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, cloud writes, and deploy execution. Exact next action: add fixture-backed latest-receipt ordering coverage when a governed Cargo/native proof window opens.

- Hardened DX Agent bridge command labels: `crates/agent_ui/src/dx_agent_bridge/command_safety.rs` now redacts secret-like command arguments before rendering bridge command labels, including `--token`, `sk-*`, `--api-key=value`, and the value that follows secret flag arguments. Added direct red/green coverage plus a source guard in `script/dx-agent-bridge-source.test.ts`; production command safety stays under 120 lines and its focused test file stays under 130. Passed: red/green direct `rustc --edition=2024 --test crates\agent_ui\src\dx_agent_bridge\command_safety.rs`, `node --test script\dx-agent-bridge-source.test.ts`, targeted rustfmt check, `git diff --check`, and conflict-marker scan. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, cloud writes, and deploy execution. Exact next action: verify redacted bridge command labels against real failed DX Agents action receipts in the governed native Zed proof window.

- Split DX Agent bridge command safety and local receipt files: `crates/agent_ui/src/dx_agent_bridge/command_safety.rs` now owns secret-marker detection, command redaction, legacy public-command mapping, safe platform validation, and bridge command labels, while `crates/agent_ui/src/dx_agent_bridge/local_files.rs` owns bounded JSON receipt reads, alternate receipt-name lookup, latest receipt listing, and DX_HOME derivation from receipt roots. `dx_agent_bridge.rs` now delegates those helpers and stays under the tighter 820-line source guard, while `script/dx-agent-bridge-source.test.ts` enforces both boundaries. Passed: red/green `node --test script/dx-agent-bridge-source.test.ts`, direct `rustc --edition=2024 --test crates\agent_ui\src\dx_agent_bridge\command_safety.rs`, targeted rustfmt check, and `git diff --check`. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, cloud writes, and deploy execution. Exact next action: verify DX Agents command redaction, receipt-root discovery, and social platform actions against real action-error/social receipts in the governed native Zed proof window.

- Hardened DX launch Agent provider/model labels: `crates/agent_ui/src/dx_launch_workspace/agents/provider_labels.rs` now owns pure provider/model state and detail labels, trims blank compatibility entries, falls back for blank provider/model IDs, and prevents blank status strings from rendering as empty rail states. `agents/providers.rs` delegates label construction through the focused helper, and the launch workspace source guard now enforces the ownership boundary plus the edge-case tests. Passed: red/green `node --test script/dx-launch-workspace-source.test.ts`, direct `rustc --edition=2024 --test crates\agent_ui\src\dx_launch_workspace\agents\provider_labels.rs`, and targeted `rustfmt --edition 2024 --check`. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, cloud writes, and deploy execution. Exact next action: verify Agent provider/model labels against real provider catalog receipts in the governed native Zed proof window.

- Split DX launch Agent provider rows and Proof labels: `crates/agent_ui/src/dx_launch_workspace/agents/providers.rs` now owns Agent provider/model rail rows, and `crates/agent_ui/src/dx_launch_workspace/proof_labels.rs` now owns pure runtime-proof evidence, requirement, and receipt-state labels with direct unit coverage. `agents.rs` is down to 865 lines, `proof.rs` is down to 294 lines, provider rows are 127 lines, and proof labels are 116 lines. Passed: red/green `node --test script/dx-launch-workspace-source.test.ts`, direct `rustc --edition=2024 --test crates\agent_ui\src\dx_launch_workspace\proof_labels.rs`, adjacent launch label tests, `node --test script\dx-deploy-panel-source.test.ts`, targeted rustfmt check, and `git diff --check`. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, cloud writes, and deploy execution. Exact next action: verify Agent provider/model rows and Runtime Proof labels against real DX Agents/runtime-proof receipts in the governed native Zed proof window.

- Split DX launch Proof rail rendering: `crates/agent_ui/src/dx_launch_workspace/proof.rs` now owns Proof Freshness and Runtime Proof right-rail rows, including proof-freshness buckets, latest runtime proof plan details, import/status receipts, blocker badges, governed proof requirements, and evidence hints. `crates/agent_ui/src/dx_launch_workspace.rs` now delegates through `proof::proof_freshness_state` and `proof::runtime_proof_status_state`, keeping the parent coordinator under the tighter 1450-line guard. Passed: red/green `node --test script/dx-launch-workspace-source.test.ts`, targeted rustfmt check, direct `rustc --edition=2024 --test` coverage for adjacent pure launch/deploy label helpers, and `node --test script/dx-deploy-panel-source.test.ts`. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, cloud writes, and deploy execution. Exact next action: verify Proof Freshness and Runtime Proof rows against real runtime-proof receipts in the governed native Zed proof window.

- Split DX launch bounded-list labels: `crates/agent_ui/src/dx_launch_workspace/list_labels.rs` now owns the shared compact receipt-list formatter used by launch workspace receipt rows, trims displayed values, ignores blank/whitespace receipt strings, and counts `+N more` after blank values are removed. `crates/agent_ui/src/dx_launch_workspace.rs` delegates through `list_labels::bounded_items`, and `script/dx-launch-workspace-source.test.ts` guards the ownership boundary plus direct edge-case coverage. Passed: red/green `node --test script/dx-launch-workspace-source.test.ts`, direct `rustc --edition=2024 --test crates\agent_ui\src\dx_launch_workspace\list_labels.rs`, and targeted rustfmt check. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, cloud writes, and deploy execution. Exact next action: verify receipt rows with blank values and overflow counts in the governed native Zed proof window.

- Hardened DX Deploy launch quick-action labels: `crates/agent_ui/src/dx_deploy_launch_action_labels.rs` now owns the pure detail labels for launch quick actions, and `crates/agent_ui/src/dx_deploy_launch_actions_rail.rs` delegates to it instead of building ad hoc GPUI detail strings. Launch action rows now explicitly show safe `no approval required`, `read-only`, unknown approval/receipt-write states, and `metadata only` when receipts omit a runnable command. Passed: red/green `node --test script/dx-deploy-panel-source.test.ts`, direct `rustc --edition=2024 --test crates\agent_ui\src\dx_deploy_launch_action_labels.rs`, and targeted `rustfmt --edition 2024 --check`. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, cloud writes, and deploy execution. Exact next action: verify the Deploy launch quick-action rows against `G:\Dx\.dx\receipts\check\check-launch-latest.json` in the governed native proof window.

- Split DX launch Tool History rail rendering: `crates/agent_ui/src/dx_launch_workspace/tool_history.rs` now owns the right-rail Forge/Media/Tool History receipt bucket rows, latest receipt rows, target/preview summaries, and blocker badges, while `crates/agent_ui/src/dx_launch_workspace.rs` delegates through `tool_history::tool_history_state` and stays under the tighter 1720-line guard. Tightened `script/dx-launch-workspace-source.test.ts` to require the Tool History module and a smaller parent coordinator. Also hardened Check rail count labels so blank checked-path and skipped-check strings are ignored by `crates/agent_ui/src/dx_launch_workspace/check_labels.rs`. Passed: red/green `node --test script/dx-launch-workspace-source.test.ts`, direct `rustc --edition=2024 --test crates\agent_ui\src\dx_launch_workspace\check_labels.rs`, and targeted `rustfmt --edition 2024 --check`. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, cloud writes, and deploy execution. Exact next action: verify Forge/Media Tool History rows and Check count labels against real receipts in the governed native Zed proof window.

- Hardened DX Check rail blank last-run labels: `crates/agent_ui/src/dx_launch_workspace/check_labels.rs` now trims receipt-provided last-run labels and falls back to `Last run Unix ms: <timestamp>` or `Never` when the label is empty, preventing blank receipts from rendering as ` (timestamp)`. Extended the direct std-only Check label unit coverage and kept the launch workspace source guard pointed at the focused label module. Passed: red/green direct `rustc --edition=2024 --test crates\agent_ui\src\dx_launch_workspace\check_labels.rs`, `node --test script/dx-launch-workspace-source.test.ts`, and targeted `rustfmt --edition 2024 --check`. Skipped by policy: Cargo build/check/test/clippy, `just run`, local servers, browser automation, live editor runtime, cloud writes, and deploy execution. Exact next action: verify blank and generated-time Check rail labels against real `check-latest.json` receipts in the governed native proof window.

- Split DX launch Check rail rendering: `crates/agent_ui/src/dx_launch_workspace/check.rs` now owns Check panel score, section, warning, blocker, quick-fix, and rail-score rendering, while `crates/agent_ui/src/dx_launch_workspace/check_labels.rs` owns the pure checked-path, skipped-check, outcome, duration, and last-run labels with focused unit coverage, including blank last-run fallback to generated timestamps or `Never`. `crates/agent_ui/src/dx_launch_workspace.rs` delegates through `check::check_score_state` and stays under the tighter 1800-line source guard. Expanded `script/dx-launch-workspace-source.test.ts` with the Check rail ownership boundary. Passed: red/green `node --test script/dx-launch-workspace-source.test.ts`, direct `rustc --edition=2024 --test crates\agent_ui\src\dx_launch_workspace\check_labels.rs`, and targeted `rustfmt --edition 2024 --check`. Skipped by policy: Cargo build/check/test, `just run`, local servers, browser automation, live editor runtime, cloud writes, and deploy execution. Exact next action: keep Check rail rows in the focused module when dx-check receipt fields change, then verify Check/Deploy/Proof rows in the governed native Zed proof window.

- Hardened DX Studio target picker numeric safety: `crates/web_preview/src/dx_studio_bridge/selection.ts` now trims ambiguous target-prompt answers, treats blank/whitespace answers as cancellation, refuses non-digit text, and requires a safe JavaScript integer before selecting a target. This prevents blank target prompts from silently choosing target `0` and keeps malformed target answers on the receipt-backed restore path. Extended `script/dx-source-quality.test.ts` with a red/green source guard for the target picker while keeping the assembled bridge parse guard green. Passed: red/green `node --test script/dx-source-quality.test.ts`. Skipped by policy: Cargo build/check/test, `just run`, local servers, browser automation, live WebView runtime, cloud writes, and deploy execution. Exact next action: verify ambiguous target prompt cancellation and refusal labels in the governed native Web Preview proof window.

- Hardened DX Studio operation picker numeric safety: `crates/web_preview/src/dx_studio_bridge/capture.ts` now requires the parsed edit-operation index to be a safe JavaScript integer before selecting an operation, so oversized digit strings are refused instead of relying on rounded `parseInt` output. Extended `script/dx-source-quality.test.ts` with a red/green source guard that keeps blank, malformed, and unsafe numeric answers on the refusal/restore path while still parsing the assembled bridge script. Passed: red/green `node --test script/dx-source-quality.test.ts`. Skipped by policy: Cargo build/check/test, `just run`, local servers, browser automation, live WebView runtime, cloud writes, and deploy execution. Exact next action: verify the operation picker refusal labels and receipt-backed overlay preservation in the governed native Web Preview proof window.

- Hardened DX Studio operation picker cancellation: `crates/web_preview/src/dx_studio_bridge/capture.ts` now trims edit-operation prompt answers, restores the previous receipt-backed selection for blank/whitespace answers, and refuses malformed numeric text instead of silently treating it as operation `0`. Extended `script/dx-source-quality.test.ts` with a source guard that also parses the assembled bridge script. Passed: red/green `node --test script/dx-source-quality.test.ts`. Skipped by policy: Cargo build/check/test, `just run`, local servers, browser automation, and live WebView runtime proof. Exact next action: verify the edit-operation prompt preserves the current overlay/receipt state in the governed native Web Preview proof window.

- Split DX launch context prompt ownership: `crates/agent_ui/src/dx_launch_prompts/context.rs` now owns launch status, contract, readiness, audit, DX-WWW evidence, source-audit, receipt-review, and bounded-list summary formatting, while `crates/agent_ui/src/dx_launch_prompts.rs` stays as a 193-line launch prompt coordinator. Bounded prompt lists now append `+N more` instead of silently dropping overflow rows. Expanded `script/dx-launch-prompts-source.test.ts` so future workers keep context formatting, Forge, source-action, and runtime-proof wording out of the parent prompt file. Passed: red/green `node --test script/dx-launch-prompts-source.test.ts` and targeted `rustfmt --edition 2024 --check`. Skipped by policy: Cargo build/check/test, `just run`, local servers, browser automation, and live editor runtime. Exact next action: keep this guard green when launch receipt fields or source-owned audit summary wording changes.

- Added direct edge-case unit coverage for Deploy receipt root keys: `crates/agent_ui/src/dx_deploy_root_key.rs` now collapses repeated Windows separators while preserving drive roots and UNC prefixes, and carries focused `rustc --test` coverage for mixed slashes, repeated separators, drive-root preservation, UNC-prefix preservation, and non-Windows case-sensitive trailing-slash trimming. This keeps long-running Zed Deploy/Check receipt scans from treating `G:\Dx`, `G://Dx`, and similar spellings as separate roots. Passed: red/green direct `rustc --edition=2024 --test crates\agent_ui\src\dx_deploy_root_key.rs` and targeted `rustfmt --edition 2024`. Skipped by policy: Cargo build/check/test, `just run`, local servers, browser automation, live editor runtime, cloud writes, and deploy execution. Exact next action: add fixture-backed ordering coverage for duplicate workspace/hub check and deploy receipt roots when an approved targeted Cargo/native proof window is open.

- Hardened DX Deploy receipt root de-duplication: `crates/agent_ui/src/dx_deploy_root_key.rs` now owns the shared platform-aware root key used by both `dx_deploy_check_roots.rs` and `dx_deploy_receipt_roots.rs`, so workspace/DX hub/www `.dx\receipts\check` and `.dx\receipts\deploy` roots avoid duplicate IO without two normalization copies. Windows folds case, slash spelling, and trailing separators; non-Windows stays case-sensitive and trims only trailing `/`. Expanded `script/dx-deploy-panel-source.test.ts` with the shared de-dupe source guard. Passed: `node --test script/dx-deploy-panel-source.test.ts` and targeted `rustfmt --edition 2024`. Skipped by policy: Cargo build/check/test, `just run`, local servers, browser automation, live editor runtime, cloud writes, and deploy execution. Exact next action: add fixture-backed ordering coverage for duplicate workspace/hub check and deploy receipt roots during an approved targeted Cargo/native proof window.

- Split DX launch Forge prompt ownership: `crates/agent_ui/src/dx_launch_prompts/forge.rs` now owns Forge proof, restore approval, and Forge history summary prompt context, while `crates/agent_ui/src/dx_launch_prompts.rs` stays as a 529-line launch prompt coordinator and re-exports the existing Forge prompt helpers. Expanded `script/dx-launch-prompts-source.test.ts` so future workers keep Forge, source-action, and runtime-proof wording out of the parent prompt file. Passed: red/green `node --test script/dx-launch-prompts-source.test.ts` and targeted `rustfmt --edition 2024 --check`. Skipped by policy: Cargo build/check/test, `just run`, local servers, browser automation, and live editor runtime. Exact next action: keep this guard green when Forge history receipts, restore approval wording, or restore-target proof language changes.

- Split DX launch source prompt ownership: `crates/agent_ui/src/dx_launch_prompts/source.rs` now owns source action icons, labels, source receipt review text, and source attachment/review prompt wording, while `crates/agent_ui/src/dx_launch_prompts.rs` stays as the launch prompt coordinator that re-exports the existing public helpers. Expanded `script/dx-launch-prompts-source.test.ts` so future workers keep Forge, source-action, and runtime-proof wording out of the parent prompt file. Passed: red/green `node --test script/dx-launch-prompts-source.test.ts` and targeted `rustfmt --edition 2024 --check`. Skipped by policy: Cargo build/check/test, `just run`, local servers, browser automation, and live editor runtime. Exact next action: keep this guard green when source attachment kinds, receipt drilldowns, or guarded source prompt wording changes.

- Split DX launch runtime-proof prompt ownership: `crates/agent_ui/src/dx_launch_prompts/runtime_proof.rs` now owns the runtime proof handoff, import, evidence-template, receipt-context, and guarded requirements wording, while `crates/agent_ui/src/dx_launch_prompts.rs` stays as the launch prompt coordinator that re-exports the existing public prompt helpers. Added `script/dx-launch-prompts-source.test.ts` so future workers keep runtime-proof wording out of the parent prompt file. Passed: red/green `node --test script/dx-launch-prompts-source.test.ts` and targeted `rustfmt --edition 2024 --check`. Skipped by policy: Cargo build/check/test, `just run`, local servers, browser automation, and live editor runtime. Exact next action: keep this source guard green when runtime proof receipt fields or governed validation wording changes.

- Wired Deploy gate quick fixes and launch-check receipt roots to the real DX ecosystem contract: `crates/agent_ui/src/dx_deploy_check_roots.rs` now reads workspace plus DX hub/www check receipts through the same `DX_HOME`, `DX_ROOT`, `D:\Dx`, and `G:\Dx` hub discovery path as Deploy receipts, `crates/agent_ui/src/dx_deploy_receipt_summary.rs` preserves `dx.deploy.zed_handoff.v1` quick-fix command, `risk_level`, approval requirement, and receipt-write flags from receipts written by the root `dx deploy` lane, and `crates/agent_ui/src/dx_deploy_gate_rail.rs` renders those safe/network actions as metadata only. This keeps native Zed as a consumer of `dx deploy` and `dx check` receipts, not the owner of deploy proof. Passed: red/green `node --test script/dx-deploy-panel-source.test.ts`. Skipped by policy: Cargo build/check/test, local servers, browser automation, cloud writes, and live deploy. Exact next action: add a tiny fixture-backed parser test for `dx.deploy.zed_handoff.v1` quick fixes during an approved targeted Cargo window.

- Split DX Check panel source ownership: `crates/agent_ui/src/dx_check_panel.rs` is now a 348-line cache/type facade, `crates/agent_ui/src/dx_check_panel/reader.rs` owns bounded receipt discovery and JSON IO, and `crates/agent_ui/src/dx_check_panel/parser.rs` owns `dx.check.zed_panel.v1` plus shared DX-WWW view-model parsing/defaults. Added `script/dx-check-panel-source.test.ts` so future workers keep receipt IO and parser logic out of the facade. Passed: red/green `node --test script/dx-check-panel-source.test.ts`, existing DX source guards, and targeted `rustfmt --edition 2024 --check`. Skipped by policy: Cargo build/check/test, `just run`, local servers, browser automation, and live editor runtime. Exact next action: keep this guard green whenever native Check rail receipt fields change, then verify the rail against a real `check-latest.json` during the governed native proof window.

- Split and hardened DX Deploy receipt root ownership: `crates/agent_ui/src/dx_deploy_hub_roots.rs` now owns DX hub discovery through `DX_HOME`, `DX_ROOT`, then the launch-rule `D:\Dx` / `G:\Dx` fallback, while `crates/agent_ui/src/dx_deploy_receipt_roots.rs` owns deploy receipt assembly, skips empty roots, and normalizes duplicate Windows root path spellings without lowercasing non-Windows paths before feeding workspace plus DX hub/cli/www receipt roots into the Deploy capability scanner. `crates/agent_ui/src/dx_deploy_capabilities.rs` stays focused on receipt discovery, ranking, parsing, and provider rows. Passed: `node --test script/dx-deploy-panel-source.test.ts`. Skipped by policy: Cargo build/check/test, local servers, browser automation, cloud writes, and live deploy. Exact next action: add fixture-backed receipt ordering coverage when a targeted Cargo test window is approved.

- Migrated the current DX Deploy panel slice from the stale `G:\Zed` checkout into canonical `G:\Dx\zed` only, preserving relative paths for the Deploy launch evidence, quick actions, bucket scores, launch outcome, launch scope, approval evidence, source guard, and status docs. No whole-repo copy, Cargo command, local server, or live deploy ran. Exact next action: continue all Zed/DX editor work from `G:\Dx\zed`.

- Added DX Deploy approval evidence rows: Zed Deploy now reads bounded `source_ready.evidence`, `runtime_approved.evidence`, and `launch_approved.evidence` arrays through `crates/agent_ui/src/dx_deploy_launch_approval_evidence.rs`, renders source runtime and launch evidence rows in the Deploy launch gate, and carries `approval_evidence=` in deploy readiness prompts. This shows which proof signals are present, such as deploy dry-run reviewed, while still keeping missing runtime proof and blocked launch approval honest. Exact next action: compare approval evidence rows against `G:\Dx\.dx\receipts\check\check-launch-latest.json` during the governed proof window.

- Added DX Deploy bucket-score breakdown: Zed Deploy now reads bounded dx-check launch buckets from `bucket_scores` through `crates/agent_ui/src/dx_deploy_launch_buckets.rs`, renders each launch bucket score/status/estimated state in the Deploy launch gate, and carries `launch_buckets=` in deploy readiness prompts. This makes the current `49/100 estimated` status explain itself through real launch buckets such as code-quality, structure, web-performance, DX framework health, and test-readiness instead of hiding behind one aggregate number. Exact next action: compare the launch buckets against `G:\Dx\.dx\receipts\check\check-launch-latest.json` during the governed proof window.

- Added DX Deploy launch scope and checked paths: Zed Deploy now reads the dx-check launch scoring profile, scoring-config status, config-present state, next action, and bounded checked paths from the selected launch receipt through `crates/agent_ui/src/dx_deploy_launch_scope.rs`. The Deploy launch gate renders Scope and Checked rows, and deploy readiness prompts include `launch_scope=` plus `checked_paths=` so the scoring profile and workspace scope are visible beside outcome counts without growing the launch-gate reader. Exact next action: compare the Scope and Checked rows against `G:\Dx\.dx\receipts\check\check-launch-latest.json` during the governed proof window.

- Split DX Deploy launch outcome ownership: launch pass/fail/warn/skipped counts, duration, and skipped expensive check names now live in `crates/agent_ui/src/dx_deploy_launch_outcome.rs` instead of growing the launch-gate reader. The Deploy launch gate still renders Outcome and Duration, now also renders the bounded skipped expensive checks, and deploy readiness prompts include `skipped_checks=` beside `launch_outcome=`. Exact next action: compare the skipped expensive checks row with `G:\Dx\.dx\receipts\check\check-launch-latest.json` during the governed proof window.

- Added DX Deploy launch outcome counts: Zed Deploy now reads dx-check `pass_count`, `fail_count`, `warn_count`, `skipped_count`, and `duration_ms` from the selected launch receipt, renders compact Outcome and Duration rows in the Deploy launch gate, and includes `launch_outcome=` plus `duration_ms=` in deploy readiness prompts. This keeps deploy promotion blocked for the right reason while showing the exact aggregate proof posture behind the current `49/100 estimated` status. Exact next action: compare Outcome and Duration against `G:\Dx\.dx\receipts\check\check-launch-latest.json` during the governed proof window.

- Added DX Deploy evidence-source blocker text: Zed Deploy now preserves bounded blocker text from each `launch_evidence_sources` row, renders the first source-owned blocker messages in the Deploy launch evidence rows, and includes `source_blockers=` in deploy readiness prompts. This keeps missing code, web-metrics, web-probe, and E2E proof reasons visible beside their stable evidence ids instead of only showing aggregate blocker counts. Exact next action: compare the per-source blocker text with `G:\Dx\.dx\receipts\check\check-launch-latest.json` during the governed proof window.

- Added DX Deploy warning evidence paths: Zed Deploy now preserves bounded dx-check launch warning rows with `severity` and `evidence_path`, renders the first warning details in the Deploy launch gate, and includes `launch_warnings=` in deploy readiness prompts. This makes missing README/TODO and large-source warning evidence visible from the same receipt path as blocker evidence without opening the raw launch receipt. Exact next action: compare the first warning rows with `G:\Dx\.dx\receipts\check\check-launch-latest.json` during the governed proof window.

- Added DX Deploy source blocker evidence paths: Zed Deploy now preserves `severity` and `evidence_path` from dx-check launch blocker rows, renders them in the Deploy launch-gate blocker detail, and includes `severity=` plus `evidence_path=` in deploy readiness prompts. This makes the current giant-source-file blockers traceable to exact source-owned paths such as the dx-agents files without opening the raw receipt. Exact next action: compare the first blocker rows with `G:\Dx\.dx\receipts\check\check-launch-latest.json` during the governed proof window.

- Hardened DX Deploy quick-action identities: the Deploy rail now carries the stable `quick-fix-*` action id from `dx check launch --json` beside each bounded launch quick action, and deploy readiness prompts include `action_id=` plus `next_action=` for the first actions. This keeps receipt-write refreshes and approval-required proof runners traceable back to the DX launch receipt instead of relying only on mutable labels. Exact next action: in the governed native proof window, compare the rendered quick-action identities with `G:\Dx\.dx\receipts\check\check-launch-latest.json`.

- Corrected DX Deploy quick-action totals: the Deploy rail now reads `quick_fix_count` from the dx-check launch receipt and labels bounded quick-action rows as shown-of-total, so the current 5 rendered rows are not mistaken for all 22 available launch actions. Deploy readiness prompts also include `quick_action_count=` beside the action summaries. Exact next action: keep this count aligned with `zed.quick_fix_count` whenever the launch receipt shape changes.

- Split DX Deploy launch prompt ownership again so the launch-gate, five-source launch-evidence, 100-point status, and launch quick-action wording now live in `crates/agent_ui/src/dx_deploy_launch_prompts.rs`; `crates/agent_ui/src/dx_deploy_prompts.rs` stays as the stable deploy readiness wrapper and re-exports the launch-gate helper for existing DX launch review prompts. This keeps the Deploy prompt surface small, source-owned, and connected to `.dx\receipts\deploy` plus `dx check launch` without mixing provider receipts, launch approval, and quick-action text in one file. Exact next action: keep the focused launch prompt source guard green whenever deploy status, launch evidence, or action wording changes.

- Added DX Deploy launch quick actions: Zed Deploy now parses bounded `zed.quick_fixes` from the dx-check launch receipt into `crates/agent_ui/src/dx_deploy_launch_actions.rs`, renders the first safe operator actions through `crates/agent_ui/src/dx_deploy_launch_actions_rail.rs`, and carries command, risk, approval, quick-action identities, and receipt-write metadata into deploy readiness prompts. Current receipt quick actions include `receipt-write` refresh actions that write receipts but do not run live deploys, plus approval-required runner actions for code, web metrics, and E2E proof. Exact next action: in the governed native proof window, verify the Deploy rail shows launch quick actions without executing them.

- Added a 100-point deploy status row: Zed Deploy now normalizes the dx-check launch score through `crates/agent_ui/src/dx_deploy_launch_score.rs`, preserves the structured `score_estimated` flag, renders `Status score` in the Deploy launch-gate card, and includes `status_score=` in deploy readiness prompts. Current refreshed receipt status is `245/500`, so the operator-facing 100-point deploy status is `49/100 estimated`; this is a blocked launch status, not live deploy approval. Exact next action: keep this normalized row beside the raw score when the governed native proof window compares Zed against `G:\Dx\.dx\receipts\check\check-launch-latest.json`.

- Hardened DX Deploy launch evidence identity: Zed Deploy now preserves the stable evidence id and receipt freshness timestamp from `launch_evidence_sources`, renders them in the Deploy rail, and carries them into deploy readiness prompts. The launch-chain blocker list is still bounded but now covers the full five-source launch evidence set, so code, web metrics, web probe, E2E, and deploy dry-run gaps can be compared directly against `G:\Dx\.dx\receipts\check\check-launch-latest.json`. Exact next action: in the governed native proof window, confirm the rail shows `deploy` as the only ready evidence id with receipt freshness while the four runtime proof ids remain missing.

- Added DX Deploy launch evidence-source rows: `crates/agent_ui/src/dx_deploy_launch_evidence.rs` now parses the aggregate `dx check launch --json` `launch_evidence_sources` plus `launch_chain` contracts, `crates/agent_ui/src/dx_deploy_launch_evidence_rail.rs` renders compact five-source launch evidence, launch-chain rows, and chain blocker text, and deploy readiness prompts include the same evidence-source summary. This connects the Zed Deploy rail and agent handoff prompt to the same DX ecosystem evidence chain that blocks or approves launch without running checks from Zed or pretending dry-run equals live deployment. Current refreshed receipt status: 5 launch evidence sources, 1 ready deploy dry-run source, 4 missing runtime proof sources, and `245/500` launch score with source/runtime/launch approvals still false. Exact next action: in the governed native proof window, compare the rendered evidence rows against `G:\Dx\.dx\receipts\check\check-launch-latest.json`.

- Added the DX Deploy panel source guard: `script/dx-deploy-panel-source.test.ts` now checks the native Deploy rail module registration, deploy prompt ownership, dry-run/launch-approval wording, launch-gate receipt precedence, rail render order, deploy source file sizes, and deploy status docs. This makes the no-build Deploy panel contract repeatable for future DX workers without launching Zed, running Cargo, starting servers, or touching cloud providers. Current scoped source/config score is 100/100 for the Zed Deploy panel slice only; it is not runtime, launch, or production readiness. Exact next action: keep this guard green whenever deploy provider rows, launch-gate wording, or deploy prompt ownership changes.

- Split Deploy prompt ownership out of the large launch prompt file: `crates/agent_ui/src/dx_deploy_prompts.rs` now owns deploy readiness prompts, deploy receipt bucket prompt summaries, and canonical deploy capability prompt summaries, while `crates/agent_ui/src/dx_deploy_launch_prompts.rs` owns dx-check launch-gate, launch-evidence, status-score, and quick-action prompt summaries. `crates/agent_ui/src/dx_launch_prompts.rs` imports only the deploy prompt helpers it needs for receipt review, and `crates/agent_ui/src/agent_panel.rs` imports `deploy_readiness_prompt` from the deploy prompt module directly. This keeps deploy handoffs connected to `.dx\receipts\deploy` plus the aggregate dx-check launch gate while reducing the launch prompt file from 1158 to 969 lines and keeping `dx_deploy_prompts.rs` at 125 lines. Verification passed: red/green deploy prompt extraction source guard and targeted rustfmt. Exact next action: keep future provider-specific receipt wording inside `dx_deploy_prompts.rs` and future launch-gate wording inside `dx_deploy_launch_prompts.rs`.

- Added the DX Deploy launch-gate card: `crates/agent_ui/src/dx_deploy_launch_gate.rs` now reads bounded `check-launch-latest.json` / `check-latest.json` receipts from the active workspace and canonical `G:\Dx` / `G:\Dx\www` check roots, preferring launch-specific receipts before generic check receipts; `crates/agent_ui/src/dx_deploy_launch_gate_rail.rs` renders source/runtime/launch approval status, score, blockers, warning count, next action, and receipt source inside the Deploy rail; and `crates/agent_ui/src/dx_deploy_targets.rs` carries that launch gate beside deploy plan/status/provider-gate receipts. This connects the provider dry-run panel to the aggregate dx-check launch approval path without running Zed, running checks, or claiming live deploy readiness. Verification passed: red/green deploy launch-gate source guard, targeted rustfmt, and source review. Exact next action: in the governed native proof window, confirm the Deploy rail shows the current `245/500` blocked launch gate from `G:\Dx\.dx\receipts\check\check-launch-latest.json` beside the Vercel dry-run gate.
- Added the DX Deploy right-rail capability matrix and provider-gate connection: `crates/agent_ui/src/dx_deploy_capabilities.rs` now reads canonical `.dx\receipts\deploy` plan, status, provider dry-run gate, and `provider-capability-matrix.json` receipts from the active workspace plus `G:\Dx`, `G:\Dx\cli`, and `G:\Dx\www`; `crates/agent_ui/src/dx_deploy_receipt_summary.rs` owns the JSON receipt summary structs and parsers; `crates/agent_ui/src/dx_deploy_receipt_rank.rs` makes receipt precedence explicit so plan/status/matrix prefer active workspace then DX-WWW, while provider promotion gates prefer the hub receipt; `crates/agent_ui/src/dx_deploy_target_detection.rs` owns deploy config/link detection; `crates/agent_ui/src/dx_deploy_receipt_buckets.rs` owns the older `tools/dx-deploy` bucket state; `crates/agent_ui/src/dx_deploy_receipt_files.rs` owns receipt filesystem walking; `crates/agent_ui/src/dx_deploy_receipt_extract.rs` owns bucket receipt headline/status extraction; `crates/agent_ui/src/dx_deploy_local_files.rs` owns bounded local JSON/path helpers; `crates/agent_ui/src/dx_deploy_targets.rs` is now a 98-line cached snapshot orchestrator; `crates/agent_ui/src/dx_deploy_matrix_rail.rs` owns plan/status/provider matrix rendering; `crates/agent_ui/src/dx_deploy_gate_rail.rs` owns the provider-gate handoff rows; `crates/agent_ui/src/dx_deploy_rail_ui.rs` owns shared compact row helpers; `crates/agent_ui/src/dx_deploy_rail.rs` is the 182-line top-level Deploy rail coordinator; `crates/agent_ui/src/dx_launch_workspace.rs` now only calls the focused deploy rail module; `crates/agent_ui/src/dx_launch_prompts.rs` routes deploy review prompts to canonical `.dx\receipts\deploy` before legacy `tools/dx-deploy`; and `crates/agent_ui/src/dx_receipts.rs` counts Deploy as a first-class hub receipt bucket. Configured live DX-WWW dry-run receipts from `G:\Dx\www` with the root CLI, writing `G:\Dx\www\.dx\receipts\deploy\deploy-plan-latest.json`, `deploy-status-latest.json`, retained snapshots, and `provider-capability-matrix.json`. The receipts report `schema_version=dx.deploy.plan` and `schema_version=dx.deploy.status`, `status=ready`, `dry_run=true`, `deploy_ran=false`, 6 providers, 6 ready dry-run plans, and 0 blocked plans. Public installed CLI verification now passes from `G:\Dx\www`: `G:\Dx\bin\dx.exe deploy plan/status --json --compact` refreshed plan/status receipts without falling through to DX-WWW. Also refreshed the hub-level Vercel deploy gate from `G:\Dx` with `G:\Dx\bin\dx.exe deploy vercel --dry-run --json --compact`; it wrote `G:\Dx\.dx\receipts\deploy\deploy-vercel-latest.json` as `schema_version=dx.deploy.vercel_dry_run.v1`, `dry_run=true`, `deploy_ran=false`, and honestly `blocked` until launch approval is green. Zed now parses that receipt's `deploy_ready`, blocker/warning counts, `zed.panel_kind="deploy-gate"`, source/runtime/launch approval rows, quick-fix count, and next action so the Deploy rail can show promotion readiness without claiming live deployment. The deploy source files are split by scanner, summary, ranking, receipt bucket state, receipt file walking, target detection, matrix rail, gate rail, shared rail UI, and top-level rail ownership; every deploy-specific file is now under 300 lines. No cloud provider command, credential read, build, local server, live deploy, Cargo check/test/build, or `just run` ran. Verification passed: red/green deploy-capability, deploy-provider-gate, deploy-gate-rail split, deploy-receipt-precedence, deploy-parser-extraction, deploy-matrix-rail split, deploy-rail split, and deploy-target/receipt ownership source guards, `rustfmt --edition 2024 --check` on touched Rust files, targeted `git diff --check`, conflict-marker scan, installed root CLI dry-run plan/status, and hub Vercel dry-run gate. Current source/config status for the Zed Deploy panel slice is 100/100 by source and dry-run evidence only; it is not runtime, launch, or production readiness. Remaining non-source proof: governed native Zed visual/runtime validation, intentionally skipped by lane policy.
- Added DX Agents flat recovery-control count consumption: `crates/agent_ui/src/dx_agent_bridge.rs` now parses `recovery_controls.required_intent_count`, `action_count`, and `check_count` into typed import-summary/release-gate counts, and `crates/agent_ui/src/agent_configuration.rs` plus `crates/agent_ui/src/dx_launch_workspace.rs` render the compact `6 intent / 10 action / 5 check` contract without walking the full action-map payload. Verification passed: red source guard for missing flat counter parsing, `rustfmt --edition 2024 --check`, source guards, targeted `git diff --check`, and conflict-marker scan. Skipped: Cargo tests/builds, live editor runtime, local servers, browser/WebView proof, package installs, deploys, and `just run`. Exact next action: in the governed proof window, refresh `dx agents import-summary --json` and `dx agents release-gate --json`, then confirm Zed shows the flat recovery-control counts from receipts without opening action-map detail.
- Added DX Studio reorder-direction refusal: `crates/web_preview/src/dx_studio_bridge.rs` now trims the reorder prompt value, accepts only `up` or `down`, and redraws the selected surface with `reorder direction refused` without posting a source-edit request for typos, whitespace-only input, or unexpected prompt text. Verification passed: red/green source guard for the missing reorder guard, injected bridge JavaScript parse, and targeted rustfmt check. Skipped: Cargo tests/builds, live WebView proof, local servers, browser automation, package installs, deploys, and `just run`. Exact next action: add a compact guard for blank/whitespace operation-picker answers so an empty edit-operation prompt restores the previous receipt-backed state instead of silently selecting operation 0.
- Added DX Studio value-prompt cancellation preservation: `crates/web_preview/src/dx_studio_bridge.rs` now redraws the selected DX Studio surface with receipt-backed status when text, token, replacement-token, or reorder-direction prompts are cancelled or submitted as no-ops. This prevents value-entry cancellation from leaving stale `text edit`, `writing token source`, or reorder intent labels, and still posts no edit request. Verification passed: red/green source guard for value-prompt cancellation branches, injected bridge JavaScript parse, and targeted rustfmt check. Skipped: Cargo tests/builds, live WebView proof, local servers, browser automation, package installs, and `just run`. Exact next action: add a compact guard for invalid reorder direction values so unexpected prompt text is refused locally without posting an edit request.
- Added DX Studio cancelled-prompt state preservation: `crates/web_preview/src/dx_studio_bridge.rs` now snapshots the current selection, status label, receipt, and hot-reload panel state before hover capture, and restores that same state when target selection, Escape, or the ambiguous operation picker is cancelled. This prevents cancelled ambiguous target/operation prompts from clearing the overlay, selecting an unintended surface, or leaving a fake `edit operation` outcome. Verification passed: red/green source guard for the cancelled-prompt contract, injected bridge JavaScript parse, and targeted rustfmt check. Skipped: Cargo tests/builds, live WebView proof, local servers, browser automation, package installs, and `just run`. Exact next action: add a compact guard for cancelled text/token/reorder value prompts so value-entry cancellation also returns to receipt-backed selection state without posting an edit request.
- Added DX Studio bridge selection-restore status preservation: `crates/web_preview/src/dx_studio_bridge.rs` now routes restored-selection labels through the current source-edit receipt, so failed/stale/refused receipts keep their real refusal status on the dashed overlay while successful `source_updated` reloads can still show `selection restored`. Verification passed: red/green Node source guard for the restore-status contract, injected bridge JavaScript parse, and targeted rustfmt check. Skipped: Cargo tests/builds, live WebView proof, local servers, browser automation, package installs, and `just run`. Exact next action: add a compact guard for cancelled ambiguous target/operation prompts so cancelling a target choice preserves the current overlay and receipt state without creating a fake edit outcome.
- Added dx-check outcome and duration visibility in the native Zed Check rail: `crates/agent_ui/src/dx_check_panel.rs` now carries bounded `pass_count`, `fail_count`, `warn_count`, `skipped_count`, and `duration_ms` from native Zed-panel payloads, shared DX-WWW view-model payloads, or the top-level dx-check receipt, and `crates/agent_ui/src/dx_launch_workspace.rs` renders compact `Outcome` and `Duration` rows before scope/skipped details. This keeps the rail honest about what passed, failed, warned, skipped, and how long the lightweight check took without running any expensive tool. Verification passed: red source guard for the missing outcome-count contract, rustfmt --check, source guards, fixture outcome smoke, and targeted conflict-marker scan. Skipped: Cargo tests/builds, live editor runtime, local servers, browser/WebView proof, package installs, and running `dx check`. Exact next action: in the governed proof window, compare the Zed `Outcome` and `Duration` rows against a real `check-latest.json`, especially a failing receipt where `fail_count > 0`.
- Added dx-check skipped-expensive-check visibility in the native Zed Check rail: `crates/agent_ui/src/dx_check_panel.rs` now carries bounded `skipped_expensive_checks` from native Zed-panel payloads, shared DX-WWW view-model payloads, or the top-level dx-check receipt, and `crates/agent_ui/src/dx_launch_workspace.rs` renders a compact `Skipped` count plus the first two skipped expensive checks. This keeps Lighthouse/CDP/E2E/lint/format skips honest in the launch rail without running those expensive tools. Verification passed: red source guard for the missing skipped-check contract, rustfmt --check, source guard, and targeted conflict-marker scan. Skipped: Cargo tests/builds, live editor runtime, local servers, browser/WebView proof, package installs, and running `dx check`. Exact next action: in the governed proof window, compare the Zed `Skipped` row against a real `check-latest.json` receipt and confirm no skipped check is presented as passed.
- Added dx-check checked-path visibility in the native Zed Check rail: `crates/agent_ui/src/dx_check_panel.rs` now carries bounded `checked_paths` from either `dx.check.zed_panel.v1`, `dx.www.check_panel_view_model.v1`, or the top-level receipt contract, and `crates/agent_ui/src/dx_launch_workspace.rs` renders a compact `Checked` count plus the first two paths. This makes fallback receipts and workspace-scoped receipts visibly honest without reading large report bodies. Verification passed: red source guard for the missing checked-path contract, rustfmt --check, source guard, and targeted conflict-marker scan. Skipped: Cargo tests/builds, live editor runtime, local servers, browser/WebView proof, package installs, and running `dx check`. Exact next action: in the governed proof window, verify the Zed Check rail `Checked` row against a real workspace receipt and confirm the canonical `G:\Dx` fallback does not look like a workspace-local receipt.
- Added dx-check last-run label parity in the native Zed Check rail: `crates/agent_ui/src/dx_check_panel.rs` now carries a `last_run_label` snapshot field, prefers the shared DX-WWW `view_model.last_run_label` when available, and otherwise synthesizes an honest fallback from `generated_at_unix_ms` or the missing-receipt state. `crates/agent_ui/src/dx_launch_workspace.rs` now renders `Last Run` from that label instead of showing only a bare generated timestamp. Verification passed: red source guard for the missing snapshot contract, rustfmt --check, source guard, and targeted conflict-marker scan. Skipped: Cargo tests/builds, live editor runtime, local servers, browser/WebView proof, package installs, and running `dx check`. Exact next action: in the governed proof window, compare the Zed Check rail `Last Run` row against the website `/check` panel for the same receipt and verify native Zed receipts still show a clear generated-time fallback.
- Added explicit dx-check fallback warning visibility in the native Zed Check rail: when Zed renders the shared `dx.www.check_panel_view_model.v1` because the native `dx.check.zed_panel.v1` payload is missing or unsupported, `crates/agent_ui/src/dx_check_panel.rs` now inserts a `zed-panel-fallback-view-model` warning before the receipt's own warning rows. This keeps the compatibility path visible without blocking the shared panel or fabricating a score. Verification passed: rustfmt --check, targeted `git diff --check`, targeted conflict-marker scan, source guard, and malformed fixture shape smoke. Skipped: Cargo tests/builds, live editor runtime, local servers, browser/WebView proof, package installs, and running `dx check`. Exact next action: in the governed proof window, verify the fallback warning appears once for the unsupported-Zed fixture and not for normal native Zed-panel receipts.
- Added DX-WWW check panel fallback consumption in the native Zed Check rail: Zed still prefers `dx.check.zed_panel.v1`, but `crates/agent_ui/src/dx_check_panel.rs` can now safely render `dx.www.check_panel_view_model.v1` when the Zed-specific payload is absent or unsupported. The fallback maps `score_meter`, `bucket_rows`, `blocker_rows`, `warning_rows`, `quick_fix_rows`, `primary_action`, `secondary_action`, and `scoring_config` into the same rail snapshot without fabricating a score. Verification passed: rustfmt --check, targeted `git diff --check`, targeted conflict-marker scan, DX-WWW fixture shape smoke, and source guard. Skipped: Cargo tests/builds, live editor runtime, local servers, browser/WebView proof, and running `dx check`. Exact next action: during the governed proof window, compare the native Zed Check rail and website `/check` rendering against the same receipt fixture.
- Added native dx-check receipt-panel consumption in the Zed launch rail: `crates/agent_ui/src/dx_check_panel.rs` reads bounded `dx.check.zed_panel.v1` receipts from workspace `.dx\receipts\check\check-latest.json` or the canonical `G:\Dx` fallback, refuses unsupported schemas without claiming a score, and renders score, 500-point profile, scoring-config posture, sections, warnings, blockers, quick fixes, and refresh/detail commands in the existing Check rail. Verification passed: rustfmt, rustfmt --check, targeted `git diff --check`, and conflict-marker scan. Skipped: Cargo tests/builds, live editor runtime, local servers, browser/WebView proof, and running `dx check`, because this lane is lightweight. Exact next action: generate or copy a real `check-latest.json` receipt during the governed proof window and visually tune the native rail against that live receipt.
- Final DX Agents bridge validation update: the installed root `dx` launcher now exposes the current public `dx agents ... --json` bridge. Live CLI checks from `G:\Dx` pass for status, social list, automate list, snapshot, run, receipts, receipts list, contract audit, provider/model list, release gate, and import summary, with receipts under `G:\Dx\.dx\receipts\agents`. Zed visual/runtime proof is still pending because no `just run` or local editor runtime was launched in this lane.
- Added DX Agents receipt-root contract consumption in Zed: GPUI now reads `receipt_root_present` from `dx agents receipts list --json` output, marks the background receipt surface as missing-config when the runtime reports a missing root, and shows that state in the launch rail and Agent Configuration instead of relying only on local folder inference.
- Added DX Agents receipt-inbox review in Zed: Agent Configuration now has a metadata-only `dx agents receipts --json` action that caches `receipts-inbox-latest.json`, and the launch rail renders malformed/missing/stale/expired inbox counts separately from the runtime-owned `receipts-list-latest.json` refresh.
- Aligned the DX Agents CLI path setting with the public bridge: `agent.dx_agents.cli_path` now defaults to the root `dx` executable and all GPUI actions spawn that configured CLI path for fixed `dx agents ... --json` commands, instead of displaying one CLI while hardcoding another.
- Added source-owned DX Agents action-error receipts in Zed: failed GPUI bridge actions now write redacted `action-error-latest.json` metadata and the Agent Configuration plus launch rail render the last failure, covering missing CLI/path/runtime launch failures without exposing stderr secrets. Zed also parses the action-error redaction booleans and raises review state if a stale or malformed packet ever claims to export secrets, provider credentials, or receipt bodies.
- Added DX Studio manifest-backed selection enrichment: selected `data-dx-*` DOM surfaces are now matched against loaded `public/preview-manifest.json` and source TypeScript `dx-studio-edit-contract.ts` / `dx.studio.launch_edit_contract.v1` surface contracts before source-edit planning, so Zed inherits source files, materialized files, operation IDs, package IDs, and operation summaries from the real DX-WWW contract instead of relying only on runtime attributes.
- Added DX Studio ambiguous manifest refusal: when JSON/TypeScript edit contracts expose multiple equally strong source surfaces for one selected DOM marker, Zed now records the candidate surfaces and refuses the source write instead of choosing the first selector.
- Added source freshness snapshots to DX Studio edit plans: Zed now carries source file length/modified-time from selection into edit requests and refuses stale writes when the owning file changed after selection.
- Added structured DX Studio source-edit refusal receipts: stale source, ambiguous source, readonly/generated/runtime refusal, missing source template, non-DX project, operation-not-declared, and responsive breakpoint mismatch now flow back to the WebView overlay with the current source-edit plan.
- Hardened the DX Studio responsive edit contract: breakpoint-prefixed class tokens are inspectable, but only become editable when the selected source surface declares `update_design_token`; responsive token writes preserve their breakpoint prefix and carry `data-dx-style-surface` / responsive metadata through selection and source-edit planning.
- Split the DX Studio source-edit implementation into focused modules: a small public facade, guarded path policy, marker range lookup, shared value helpers, an operation router, and separate text/token/reorder/insert/safety modules. Behavior stays source-backed and conservative while the code is easier to review before live WebView proof.
- Hardened DX Studio source ownership resolution: real source-owned candidates from manifest `sourceFile`, `data-dx-source-file`, source metadata, and matching edit contracts now outrank generated/runtime/materialized preview files, and materialized fallback writes still require explicit manifest permission.
- Hardened DX Studio overlay stability: selected overlays now re-resolve the same `data-dx-edit-id`, text marker, or selector on scroll/resize and redraw from a fresh DOM rect so selected/parent/root dashed borders stay attached to the real source-owned surface during normal preview movement.
- Hardened DX Studio write readiness: the source mutation path now refuses declared operations unless the selected surface also has the required marker, slot, token/class, reorder group, or manifest source template; `data-dx-edit-kind` is carried into source-edit plans and receipts for clearer target inspection without treating it as write permission by itself.
- Hardened DX Studio source-policy evidence: source-edit plans and receipts now report whether the resolved source is source-owned, generated/runtime, or materialized fallback, whether manifest permission allows generated edits, and whether the path policy permits the write before mutation.
- Hardened DX Studio freshness trust: edit requests now discard any page-supplied `expected_source_snapshot` and attach only the snapshot captured by Zed's latest matching DX Studio selection receipt, keeping stale-source protection native-owned instead of WebView-owned.
- Hardened DX Studio stale-write refusal: source edits now require a trusted Zed source snapshot whose source file identity matches the resolved write target, and native selection matching covers insert/media/reorder/token/component/section markers so non-text operations still receive freshness evidence.
- Hardened DX Studio hot-reload selection restore: the WebView bridge now persists and reselects stable insert/media/reorder/token/component/section/style/source markers after reload, instead of restoring only text/edit-id selections.
- Hardened DX Studio project detection from source markers: the Rust DX-WWW detector now recognizes bounded source-folder `data-dx-route`, `data-dx-source`, `data-dx-component`, `data-dx-section`, `data-dx-edit-id`, `data-dx-edit-ops`, and hot-reload markers even when a manifest has not been generated yet.
- Added real DX Studio visual style metrics: selected Web Preview surfaces now carry DOM-computed size, margin, padding, border width, layout, typography, and visual values into the overlay panel, with an honest missing state when computed style is unavailable.
- Added DX Studio source-backed style edit planning: selected surfaces now carry a `zed.web_preview.dx_studio_style_edit_plan.v1` object that marks computed CSS as read-only unless the real DX-WWW surface declares `update_design_token` plus a token/scope/style/responsive marker, giving the overlay and Rust source-edit plan a safe handoff for future dx-style token edits.
- Added DX Studio style-token edit prefill wiring: the WebView token edit prompt now uses the source-backed `style_edit_plan` to prefill candidate tokens, show computed size/margin/padding/display/background context, and send a compact prefill receipt with the source-edit request while keeping Rust-side marker/source-policy validation authoritative.
- Added DX Studio style-token receipt context: successful and refused token edits now carry `zed.web_preview.dx_studio_style_edit_receipt_context.v1` metadata with prefill status, declared-contract evidence, computed summary, and no-inline-style policy, and overlay/session status text can report that source-backed style context.
- Added bounded DX Studio style-token receipt shape fixtures: the source-edit facade now has local unit coverage for declared dx-style token-contract context, refused/missing-contract context, and unrelated non-style edits returning no style receipt context. Cargo execution remains deferred by the launch-lane rule.
- Added DX Studio `apply_source_edit` fixture coverage for token receipts: a temporary DX-WWW root fixture now exercises a real `update_design_token` write against a source-owned `data-dx-design-token` marker, verifies the returned source-edit receipt carries style context, source policy, and hot-reload metadata, and verifies stale trusted snapshots refuse without mutating the file.
- Added DX Studio Web Preview refusal receipt assembly coverage: the Web Preview IPC path now builds failed source-edit receipts through a focused helper with source-edit plan, `status_detail`, source policy, and style-edit context kept together, and compact session summaries now expose the same refusal bundle for Agent/source diagnostics.
- Added DX Studio bridge receipt status labels: the injected WebView `afterSourceEdit` renderer now classifies real receipt metadata for stale-source refusal, missing dx-style token-contract refusal, and source-updated style context, so the selected dashed label and status panel row show the actual source-backed outcome instead of a generic edit failure. Verification passed: red/green Node source guard, injected bridge JavaScript parse, and targeted rustfmt check. Skipped: Cargo tests/builds, live WebView proof, local servers, browser automation, and `just run`. Exact next action: add a bridge-level source guard that renders source-policy and rollback/refusal metadata in the bottom panel before the governed live WebView proof.
- Added DX Studio bridge policy receipt rows: the injected WebView bottom panel now merges receipt-level `source_policy` with `source_edit_plan.source_policy` and renders real source kind, policy allow/refuse state, generated/materialized fallback state, failed-before-confirmed-write, rollback guard, and trusted stale-snapshot guard rows when the receipt carries them. Verification passed: red/green Node source guard, injected bridge JavaScript parse, and targeted rustfmt check. Skipped: Cargo tests/builds, live WebView proof, local servers, browser automation, and `just run`. Exact next action: add a compact bridge guard for hot-reload refresh-needed/no-version states so successful edits without DX-WWW version polling show an honest source-updated-refresh-needed panel row.
- Added DX Studio bridge hot-reload status rows: the injected WebView bridge now records source-updated hot-reload polling state, clears stale reload state on each new edit, and renders panel rows for polling, version endpoint unavailable, version poll timeout/failure, and version seen outcomes from the actual `/_dx/hot-reload/version` polling path. Verification passed: red/green Node source guard, injected bridge JavaScript parse, and targeted rustfmt check. Skipped: Cargo tests/builds, live WebView proof, local servers, browser automation, and `just run`. Exact next action: add a tiny guard that stale/non-source-updated edit receipts clear any previous hot-reload panel state while preserving refusal policy/style rows.
- Added DX Studio bridge failed-receipt reload clearing: the injected WebView `afterSourceEdit` path now funnels every receipt through a focused renderer that clears stale hot-reload panel state, stores the new receipt, redraws refusal policy/style rows from that receipt, and only starts hot-reload polling for `source_updated` receipts. Verification passed: red/green Node source guard, injected bridge JavaScript parse, and targeted rustfmt check. Skipped: Cargo tests/builds, live WebView proof, local servers, browser automation, and `just run`. Exact next action: add a compact source guard for selection-restore status so failed receipts keep the selected dashed overlay attached without showing a misleading "selection restored" success state.
- Refined the source-backed DX Studio Web Preview editing slice: the bridge now has a general Edit DX Studio Surface path, captures `data-dx-source-file`, insert slot, reorder group, design token, token scope, media slot, class tokens, and responsive tokens from the selected real DOM surface, and records a pre-write hot-reload version baseline before source mutations.
- Added real non-text source write coverage for declared contracts: `update_design_token` can replace selected source token references or class tokens, and `move_reorder_section` can move a selected source-owned section within its declared `data-dx-reorder-group`; both keep generated/runtime file refusal, stale/ambiguous-source refusal, and rollback-on-write-error behavior.
- Kept insert component and icon/media source edits honest: the operation IPC and plan states are wired, but actual writes require a manifest-declared source snippet/template so Zed does not create dummy UI or random inline source.
- Implemented the source-backed DX Studio Web Preview editing slice: WebView2 now installs a DX Studio bridge for nearest editable `data-dx-*` selection, selected/parent/route dashed overlays, responsive breakpoint panel metadata, editable-parent target choice, source-edit IPC, source-owned text update receipts, hot-reload version polling, and selection restoration; the Rust side resolves owning files and refuses ambiguous, stale, readonly, generated/runtime, non-DX, or unsupported operation writes.
- Added DX Studio source-edit honesty: direct `update_text_content` writes are real but limited to direct source literals under declared `data-dx-editable-text` markers, `update_design_token` and `move_reorder_section` now have guarded source transformers, and insert component/icon/media writes stay template-gated until DX-WWW manifests provide safe source snippets.
- Added DX launch UX/manifest polish: Web Preview's DX Studio parser now accepts DX-WWW preview routes from top-level routes, preview route arrays, or page arrays, and extracts route/source/package/asset/marker surfaces from both string rows and object rows.
- Added parsed DX Studio session target metadata: Web Preview session snapshots now include the default preview target and up to six preview targets with source file, Forge package, asset, `data-dx` marker, and hot-reload counts for easier route/source/package inspection.
- Polished the DX AI workspace rail organization: the left rail now shows a source-backed Chat/Tasks/Sources/Agent block from current Agent thread state, retained background tasks, source-set counts, and DX Agents bridge state; the right rail separates DX Agents Automations from Social Accounts.
- Polished DX onboarding preview target selection: first-run onboarding now honors manifest-declared static preview file paths before falling back to conventional `public`, `out`, and `dist` route files, while keeping explicit credential import and Skip behavior unchanged.
- Added first-class DX Agents launch rail treatment for Automations: the right rail now renders an Automations section with `dx agents automate list --json` as the explicit entrypoint, while Social Accounts stays focused on account and QR/connect state.
- Added DX Agents run-receipt hardening in the GPUI reader: receipt rows can now show task lifecycle state, duration state, provider/model readiness, and safe retry/cancel posture from the metadata-only `dx agents run --json` contract, while unsafe or redacted rows stay constrained to safe summary fields.
- Added DX Agents receipt-row social/automation counters in the GPUI reader: background task rows can now show social connected/needs-auth and automation enabled/warning counts from the runtime-owned receipt index without opening run receipt bodies.
- Added DX Agents `needs_auth` summary parsing in the GPUI reader while preserving fallback to older `needs_connection` receipts, so the Social Accounts rail can render launch-native auth-needed state without guessing.
- Recorded the DX Agents task-state review contract: runtime and CLI receipt indexes now flag unknown non-redacted run `task_state` values as unsafe or malformed metadata, and the existing receipt rail warning path should render those rows for operator review.
- Recorded the DX Agents contract-audit receipt classification: runtime and cached CLI receipt indexes now expose `contract-audit-latest.json` as bridge contract-audit metadata, so the receipt rail can distinguish audit evidence from generic cached rows.
- Hardened DX Agents bridge-contract redaction review: Zed now treats task payload, transcript, and provider credential export flags as review-triggering leak classes alongside secrets, account targets, automation bodies, and tool payloads.
- Tightened DX Studio edit-contract parsing so Web Preview requires explicit manifest or operation metadata before marking an edit surface as write-capable or source-owned; missing fields now stay conservative while normal Web Preview behavior and the no-node-modules default remain unchanged.
- Tightened DX Studio Web Preview session metadata so missing source-owned edit contracts no longer advertise write-file readiness; the session snapshot now marks edit contract loaded state explicitly and keeps mutation fields false until a real manifest is present.
- Aligned DX Agents row-action parsing with the latest bridge contract: Zed now carries and validates `public_command` / `public_refresh_command` metadata for social and automation rows, requires those fields to advertise the public root `dx agents ... --json` bridge, uses the public command metadata in prompts and rail summaries, and still executes only typed fixed command variants.
- Tightened DX Agents social row-action metadata validation: Zed now only accepts rendered `dx agents social connect/disconnect --platform ... --json` action descriptors when the platform token passes the same safe platform rule as executable bridge actions.
- Hardened the DX Agents action bridge: Agent Configuration now builds public `dx agents ... --json` actions from typed command variants and metadata command variants instead of raw argument vectors, keeping social connect/disconnect platform validation, receipt-cache writes for import-summary/release-gate, and fixed public command routing centralized in `dx_agent_bridge`.
- Added DX Agents receipt-index rendering: Zed now reads `G:\Dx\.dx\receipts\agents\receipts-list-latest.json`, exposes returned/known receipt counts, active task count, latest receipt path, redaction/unsafe-row state, and typed background receipt rows in both Agent Configuration and the DX launch right rail while keeping raw receipt payloads and unsafe rows out of the UI.
- Routed the DX Agents receipt-index refresh through the public root CLI command `dx agents receipts list --json`, so Zed can refresh the runtime-owned `receipts-list-latest.json` contract without calling the raw `dx-agents` binary directly.
- Aligned Agent Configuration controls with the public `dx agents ... --json` command surface for status, social, automation, and run receipt actions while keeping `dx-agents` runtime receipt metadata accepted and requiring safe explicit platform arguments for connect/disconnect.
- Hardened DX Agents receipt scalar filtering so Zed redacts bearer headers, authorization labels, bare `sk-` values, provider-key labels, password markers, private-token strings, and OAuth token names from cached metadata before rendering receipt rows or bridge action labels.
- Added crowded coding layout activity-bar behavior: when the active workspace has code editor and Web Preview items open with the Agent panel visible, the far-left AI sidebar now renders as a 48px icon-first activity bar for New Chat, Search, Agents, Sources, Plugins, Automations, Background Tasks, Settings, project switching, and sidebar toggle controls; full sidebar/AI modes remain available outside that tri-pane layout or when explicitly expanded.
- Tightened crowded coding layout detection so the compact activity bar only activates when editor and Web Preview/browser items are active in visible center panes with the Agent panel visible, avoiding collapse from hidden background preview tabs.
- Split the compact coding activity bar shell into a dedicated sidebar module while keeping the action wiring in the existing sidebar owner, reducing GPUI layout bulk without changing behavior.
- Hardened the compact activity bar for short windows by keeping secondary controls fixed and letting the primary action stack scroll inside the 48px rail instead of overflowing into the editor/Web Preview layout.
- Retargeted the compact activity bar Background Tasks action to the live thread list: it now expands the sidebar and selects the first running, waiting, or retained Agent task row when one is visible, with normal sidebar search focus as the empty state.
- Added compact Agent dock sizing for crowded coding layouts: when visible center panes include both editor and Web Preview/browser items, the Agent panel uses a fixed compact coding width instead of flexible proportional width, while no-editor/full-width AI mode and explicit zoom behavior remain available.
- Wired the compact activity bar Sources action to draft a real DX source attachment/review prompt from the current source-set snapshot in the Agent panel, instead of expanding generic thread chrome in crowded coding layouts.
- Added a dock-level optional max-size hook and capped the Agent dock in crowded editor + Web Preview layouts so persisted wide Agent widths cannot crush the center coding/Web Preview surface; the cap is inactive outside the compact coding posture.
- Wired the compact activity bar Automations action to draft a DX Agents automation receipt review from the Agent bridge snapshot, keeping compact coding layouts in place while avoiding direct automation execution, shell calls, or secret/social-login flows.
- Consolidated the compact activity bar AgentPanel update path so Sources and Automations share one focused draft-routing helper, keeping the coding layout behavior consistent while preserving the existing expanded sidebar task controls.
- Removed an unused screen-carousel width persistence experiment from `workspace.rs`, leaving the active edge-dwell screen switching path intact and reducing pre-test dead-code surface.
- Polished the expanded sidebar screen grid so it renders real files, folders, and site shortcuts plus one honest Add Folder action instead of repeated filler tiles, and removed dead current-space heading helpers from the sidebar source.
- Split the DX receipt-root scanner out of the large launch workspace view into `dx_receipts.rs`, keeping `G:\Dx\.dx\receipts` bucket counting, latest receipt labels, and cache behavior unchanged while reducing GPUI view ownership.
- Replaced bare token/tool meter placeholder values with receipt-root-aware pending, missing, or ready states so the AI workspace meter rows stay honest until governed token receipts exist.
- Added DX onboarding static preview manifest selection: the Web Preview hero now reads `public/preview-manifest.json`, prefers `/launch` then `/`, resolves real static/export HTML targets such as `.dx\vercel-landing\index.html`, `public\launch.html`, `out\launch\index.html`, or `dist\launch\index.html`, and scans bounded `G:\WWW\www\examples` roots when they expose a manifest or renderable preview.
- Added the DX Studio Web Preview contract slice: Zed Web Preview now detects DX-WWW workspaces from the root `dx`/route/Forge shape, opens the DX route origin by default for DX projects, exposes the `dx www preview-manifest --json`, `dx www routes --json`, and `dx forge packages --json` command contract, and collects `data-dx-*` route/source/Forge/hot-reload markers in page diagnostics without affecting normal websites.
- Completed the DX Studio edit/drag manifest slice: Web Preview session snapshots now expose source-owned Studio/Edit manifest candidates, real DX-WWW read contracts, and a metadata-only drag-to-preview contract while page diagnostics collect manifest links plus `data-dx-edit-target` / drag / drop markers without executing CLI commands or changing non-DX sites.
- Added DX Studio public manifest/edit-operation discovery: Web Preview now treats `public/preview-manifest.json` and source-owned `dx.studio.launch_edit_contract.v1` manifests as first-class candidates, advertises the explicit edit operation/marker contract in session and page diagnostics, and keeps all mutation commands disabled until a governed operator action exists.
- Added DX Studio manifest field compatibility: Web Preview now reads the current DX-WWW public manifest's camelCase route fields (`sourceFile`, `forgePackages`, `dataDxMarkers`, `hotReloadTarget`) and loads the source-owned edit contract summary with source, route, operation IDs, marker attributes, surface count, write policy, node_modules posture, and absolute-positioning posture.
- Split the DX Studio session metadata builder into a focused Web Preview module so the large GPUI view delegates manifest/edit/drag contract snapshot assembly while preserving the existing Web Preview behavior and DX-only detection gates.
- Added the DX launch onboarding Web Preview hero slice: onboarding now creates a real Web Preview child for the hero canvas, prefers a detected launchable `G:\WWW\www` DX WWW page on first paint, supports explicit `DX_ONBOARDING_PREVIEW_URL`, and falls back to a bundled original animated DX page without local servers or secret import.
- Added DX onboarding provider readiness: the launch hero now shows native authenticated provider readiness, DX Agents provider/model/contract receipt presence, and DX catalog artifact presence from source-owned paths/environment hooks, while keeping all credential setup behind the existing Provider Settings explicit-consent action.
- Added DX onboarding WWW target discovery: the launch hero now supports `DX_WWW_WORKSPACE` / `DX_WWW_ROOT`, scans bounded recent `G:\WWW\www\.dx\cache\*\launch-app` and `G:\WWW\.dx\*` evidence roots for renderable Forge/Web Preview HTML, keeps `DX_ONBOARDING_PREVIEW_URL` as the exact override, and only uses a `dx dev` route URL when the operator explicitly selects a DX WWW root.
- Added DX onboarding preview-contract visibility: the launch hero now renders source-backed Preview Contract rows for the selected target, hook, and DX WWW command contract (`dx www preview-manifest --json`, `dx www routes --json`, `dx forge packages --json`) or a graceful bundled fallback state without running the CLI.
- Completed DX onboarding demo action coverage: the launch hero quick actions now route to the selected Web Preview target, bundled 3D scene, Agent panel, Automations surface, Provider Settings, and Skip without adding secret import, social login, CLI execution, or dummy screens.
- Added DX launch status aggregate visibility: the AI workspace now reads the Zed-facing `dx.launch.status.v1` receipt from `G:\Dx\.dx\receipts\launch\status-latest.json`, renders agent/token/discovery readiness plus `dx www templates --json` and `dx forge packages --json` bridge commands in the right rail, and reports the exact missing receipt state without running the CLI.
- Added DX launch handoff visibility: the AI workspace now reads source-owned `G:\Dx\cli\fixtures\launch-examples\import-manifest.json` and `handoff.json` metadata, renders packet/action/polling/no-fanout posture in the right rail, and adds a Launch Handoff guided card that drafts the safe operator review without running CLI commands.
- Added DX launch gate visibility: the AI workspace now reads source-owned `G:\Dx\cli\fixtures\launch-examples\import-summary-*.json`, `release-gate-*.json`, and `fallback-drill-*.json` parser fixtures, renders import/release/fallback readiness and cached receipt fallback states in the right rail, and adds a Launch Gate guided card without running CLI commands.
- Added DX launch audit visibility: the AI workspace now reads source-owned `G:\Dx\cli\fixtures\launch-examples\schemas.json`, `fixtures.json`, `smoke.json`, and `status.json`, renders command matrix, startup polling, fixture render-state, smoke check, fanout, and redaction posture in the right rail, and adds a Launch Audit guided card without running CLI commands.
- Added DX-WWW launch evidence visibility: the AI workspace now reads the selected DX WWW project or `G:\WWW\www` fallback for `.dx\forge\template-readiness` and `.dx\forge\release` launch evidence handoff artifacts, renders release/restart/acceptance packet coverage in the right rail, and adds a WWW Evidence guided card that drafts the next safe DX-WWW operator sequence without running Forge or CLI commands.
- Added DX source audit visibility: the AI workspace now reads the hub-owned `G:\Dx\.dx\audit\launch-source\latest.json` and DX Studio QA audit receipt metadata, renders cross-repo coordination, worker-output ledger, template trust, risk-review blockers, and next Friday action in the right rail, and adds a Source Audit guided card without touching `G:\Dx\www` package work or running audit helpers.
- Added DX launch receipt review visibility: the AI workspace now scans `G:\Dx\.dx\receipts\launch` for the protected `status-latest.json` receipt and retained `launch-status-*.json` snapshots, renders freshness/malformed/stale/expired metadata in the right rail, and routes missing or stale states to the documented `dx launch status --json` / `dx launch receipts --json` contracts without reading receipt bodies or running the CLI.
- Added DX metadata binary-cache bridge visibility: `dx_catalog` now has a stable metadata-only receipt cache artifact contract with rkyv serialization and a memmap read path, and the AI workspace right rail reports provider catalog, launch receipt, global receipt-index, and token/tool meter cache readiness from real paths while keeping JSON receipts authoritative until a governed producer writes the binary artifacts.
- Completed the DX receipt-cache producer bridge: `dx_catalog` now includes a bounded metadata-only JSON receipt-folder builder plus an explicit artifact writer so governed tools can materialize `DXRCP001` caches for launch/agent/token/Forge/source/media/RLM/serializer/deploy/runtime-proof receipts without changing Zed startup or JSON receipt authority.
- Added the standard DX receipt-cache root contract so governed producers can materialize the same launch/agent/token/Forge/source/media/RLM/serializer/deploy/runtime-proof buckets that Zed rails already surface, without duplicating the mapping outside `dx_catalog`.
- Added DX receipt-cache summary helpers so panels and governed producers can read root counts, missing roots, freshness buckets, malformed entries, latest receipt timestamps, root-filtered entries, kind-filtered entries, and latest-per-root records from the stable manifest contract without reparsing receipt folders.
- Added DX receipt-cache per-kind summaries so Agent/token/Forge/source/media/RLM/serializer/deploy/runtime-proof rail buckets can read entry counts, freshness counts, malformed counts, latest timestamps, and total metadata size from the binary-cache manifest contract without bespoke panel logic.
- Added DX receipt-cache health classification so manifest and per-kind summary consumers share one stable ready/partial/stale/expired/malformed/missing/empty/unknown interpretation without duplicating rail-specific status rules.
- Wired the AI workspace Binary Cache rail to read DX receipt-cache artifacts through the shared `dx_catalog` rkyv/memmap contract when present, show manifest/bucket health for launch receipts and token/RLM/serializer meter buckets, gracefully fall back to the existing JSON-authoritative states when artifacts are missing, and surface malformed artifact states without treating them as ready.
- Added Binary Cache rail artifact-review status so malformed, expired, partial, missing-roots, or unknown receipt-cache artifact states route operators to regenerate metadata-only artifacts while keeping JSON receipts authoritative.
- Added the DX Agents GPUI bridge surface: Zed now reads redacted Agent status/social/automation/provider/model receipts from `G:\Dx\.dx\receipts\agents`, shows DX Agents status in Agent Configuration and the launch right rail, honors settings gates for CLI actions/managed providers/rail visibility, and runs only fixed public `dx agents ... --json` actions for contract, status, social list/actions, automations, runs, receipt list, provider list, model list, and provider catalog regeneration without importing `dx-agents` crates, secrets, or social-login flows into Zed.
- Added DX Agents social action controls: Agent Configuration now exposes per-account connect/disconnect receipt actions through fixed `dx agents social connect/disconnect --platform <platform> --json` command vectors, and Agent Configuration plus the right rail render redacted QR/link/revoke readiness from the latest social action receipts without storing payloads.
- Completed DX Agents row-action consumption: Zed now parses `accounts[].actions[]` and `automations[].actions[]`, validates action ids, receipt filenames, refresh commands, and `secrets_exposed=false`, renders action readiness in Agent Configuration and the launch right rail, and still executes only fixed safe bridge command vectors.
- Added DX Agents bridge-contract visibility so Agent Configuration and the right rail can show `dx-agents agents contract --json` status, fixed command coverage, receipt contracts, provider-catalog recovery guidance, and redaction posture from metadata-only receipts.
- Added gated provider/model/catalog refresh actions on the managed provider catalog row so the Zed surface exposes the public `dx agents providers list --json`, `dx agents models list --json`, and `dx agents providers catalog regenerate --json` bridge commands without credential import.
- Added DX Agents import-summary visibility so Agent Configuration and the launch right rail can run the fixed `dx agents import-summary --json` metadata action, cache validated stdout as the latest agents import-summary receipt, and surface release gates, action maps, recovery controls, command-fanout posture, warnings/blockers, and the next safe recovery command without starting `dx-agents`.
- Added DX Agents release-gate visibility so Agent Configuration and the launch right rail can run the fixed `dx agents release-gate --json` metadata action, cache validated stdout as the latest agents release-gate receipt, and surface final acceptance counts, receipt inbox state, action-map readiness, command-fanout posture, warnings/blockers, and first acceptance row without starting `dx-agents`.
- Implemented the DX launch workspace chrome around existing Agent thread rendering when the panel is zoomed/full-workspace: left Sources/receipts rail, right Progress/Git/Background Tasks rail, and token/tool meter slots.
- Added receipt hooks for `G:\Dx\.dx\receipts` with graceful missing/empty states and no CLI execution from Zed.
- Added an Agent action beside the existing Write/Ask mode and model controls while preserving the current model picker, profile selector, token usage, and thread behavior.
- Added New Chat, Search, Plugins, and Automations sidebar actions plus Pinned/All Chats group headers; the compact crowded-layout Automations action now drafts from DX Agents automation receipt metadata while expanded sidebar task controls remain available through existing Zed project task behavior.
- Made the Agent panel default to full-width/zoomed when the workspace has no active editor item; user zoom toggles still override that default.
- Added `execute_dx_media_tool`, a permissioned Agent tool that consumes approved media runner gates, executes ffmpeg/ffprobe via no-shell argument vectors, refuses overwrites and path traversal, records stdout/stderr previews, hashes produced files, and writes managed DX media execution receipts.
- Added a cached workspace tool-history scanner for `tools/dx-forge` and `tools/dx-media/executions`, then rendered Forge History and Media Executions in the DX right rail with missing/empty states.
- Added the first durable Sources rail model for workspace roots, DX metasearch source-pack receipts, produced media files, and Forge restore previews.
- Added `prepare_dx_source_attachment`, a permissioned Agent tool that packages selected workspace roots, metasearch source-pack receipts, produced media files, and Forge restore previews into managed source attachment receipts for later Agent context.
- Wired `prepare_dx_metasearch_context` to accept source attachment manifests/receipts, embed metasearch source-pack receipt text into compact cited context, and warn on path-only media/restore references instead of embedding binary payloads.
- Added `list_dx_launch_demo_recipes`, a permissioned read-only Agent tool that lists metasearch-to-context, media-output-to-sources, and Forge-restore-preview-to-sources demo flows with required tool chains, receipt gates, safety notes, and workspace receipt-root status.
- Added `gate_dx_serializer_rlm_runner`, a permissioned Agent tool that validates serializer/RLM execution plans or receipts, requires explicit runner approval, requires persisted execution receipts by default, separates RLM model-call approval, and can write managed runner-gate receipts without running serializer/RLM code.
- Added `write_dx_serializer_rlm_reduced_context`, a permissioned Agent tool that consumes runner-gate receipts and context bundles, writes deterministic reduced-context receipts with cited source summaries, and does not run external serializer/RLM crates, Cargo, network, browser input, or model calls.
- Surfaced reduced-context receipts in the launch workspace: the Sources rail now has a Reduced Context set, and the right Tool History rail scans workspace `tools/dx-serializer-rlm` receipts.
- Added guided DX sidebar actions for Demo Recipe and Review Receipts; they prepare permission-safe Agent drafts for the flagship metasearch-to-reduced-context receipt chain without auto-running tools.
- Added right-rail Media Proof and Forge Proof cards that prepare permission-safe Agent drafts for proof flows without running tools automatically.
- Added source attachment readiness to the Sources rail and a typed read-only Check score to the right rail, both backed by actual workspace/source/receipt/tool-history state.
- Added restore-preview warning labels to Forge source rows and a read-only Deploy rail backed by detected workspace deploy config files.
- Added source-derived action prompt cards and deploy readiness receipt counts/latest entries, still drafting only permission-safe Agent follow-ups.
- Added Deploy rail receipt buckets for readiness, env, logs, and rollback under `tools/dx-deploy`, with compact missing/fresh/stale/old states and safer deploy-readiness prompts that include the bucket summary.
- Added validation and visual proof freshness rows backed by managed workspace proof roots, plus produced-file proof lines for media outputs from execution receipts.
- Added URL/status deploy receipt buckets plus a Runtime Proof handoff card that drafts the next safe validation plan from Check score, proof freshness, deploy target, and receipt state without running tools.
- Added Restore Approval and Reducer Guard handoff cards plus sharper source prompts for restore-preview and reduced-context receipt rows.
- Added runtime proof freshness/status buckets for managed runtime proof, import, and runtime-status receipt roots.
- Added `plan_dx_runtime_proof`, a permissioned Agent tool that prepares governed manual validation checklists and managed plan receipts without running `just run`, Cargo, local servers, browser automation, deploys, external reducers, or restore-to-target actions.
- Split runtime proof planning from imported runtime proof in the Proof Freshness rail and Check score so plan receipts show readiness without counting as runtime-green proof evidence.
- Added compact Proof Freshness drilldowns with bucket descriptions and up to two latest receipt paths, making runtime plan/import state visible without opening raw receipt folders.
- Updated the Runtime Proof handoff card so drafted Agent prompts include current Proof Freshness bucket counts, statuses, descriptions, and latest receipt paths.
- Updated the Review Receipts action so drafted Agent prompts include live DX receipt buckets, latest receipt paths, tool-history buckets, proof freshness, and deploy receipt state.
- Expanded the Runtime Proof handoff card so drafted Agent prompts include live Check score items, blockers, receipt root/latest receipts, deploy targets, and deploy receipt buckets.
- Added a dedicated Import Proof guided card that drafts the exact operator-evidence contract for `import_dx_runtime_proof` without running validation from Zed.
- Added content-aware runtime proof status scanning so the right rail and Check score distinguish missing evidence, blocked imports, unpaired status copies, and paired runtime-green candidates.
- Added content-aware runtime proof plan drilldowns so the right rail and Agent handoff prompts show the latest governed plan command, checklist step counts, requirements, and blockers without treating plan receipts as runtime-green proof.
- Added row-level source controls so visible Sources rail rows can draft real Attach or Review Agent prompts without relying only on the separate Source Actions cards.
- Added row-level receipt drilldowns so Sources rail rows show the managed source-pack, reduced-context, media execution, or restore receipt that produced each visible source.
- Expanded source receipt drilldowns with receipt type/size metadata and added row-level Receipt handoff buttons that draft source-specific receipt review prompts.
- Added `import_dx_runtime_proof`, a permissioned Agent tool that captures operator-supplied runtime proof summaries, evidence, blockers, import receipts, and operator status copy receipts under managed runtime-proof roots.
- Added `capture_dx_forge_restore_approval`, a permissioned Agent tool that captures operator restore approval evidence, target path, rollback status, overwrite posture, blockers, and managed approval receipts under `tools/dx-forge/restore-approvals` without mutating target paths.
- Added `plan_dx_forge_restore_target`, a permissioned Agent tool that consumes restore approval receipts, inspects target/previews read-only, writes managed restore-target dry-run plan receipts, and keeps live target mutation in a separate governed future window.
- Added `preview_dx_serializer_rlm_reducer_execution`, a permissioned Agent tool that captures dry-run external reducer execution previews under `tools/dx-serializer-rlm/execution-previews` without running external reducers, Cargo, model calls, shell commands, network, or unmanaged writes.
- Added `execute_dx_serializer_rlm_reducer`, a permissioned Agent tool that runs only approved no-shell absolute serializer/RLM reducer command vectors under approved DX roots, feeds deterministic reduced-context text by stdin when requested, captures stdout/stderr previews and hashes, and writes managed external-execution receipts.
- Added content-aware Deploy receipt drilldowns so deploy buckets can surface latest receipt headline, status, target, URL, and blocker counts in the right rail and Agent handoff prompts.
- Added restore approval visibility to the read-only Forge history contract so `inspect_dx_forge_history` reports `restore_approval` entries, approval readiness, evidence counts, blockers, target paths, and preview roots.
- Added content-aware Forge history drilldowns so the right rail and Forge/Restore Approval prompts surface latest restore-target plan status, approval readiness, target path, preview root, and blockers without opening raw receipt JSON.
- Added a Runtime Evidence Form guided card plus plan-derived evidence metadata in the Runtime Proof rail, so operators can draft the exact `import_dx_runtime_proof` fields from the latest plan before any proof import.
- Added content-aware runtime proof import drilldowns so the Runtime Proof rail and Agent handoff prompts show imported proof summary, final command, source, and evidence samples from managed import receipts.
- Advanced the current non-mutating DX Native Tool Execution/Restore/Panels/Demos coding lane and the editor-lane launch UI to 100/100 coding-complete before governed runtime proof; the remaining launch gate is runtime evidence/import plus upstream source-audit reconciliation in the DX-WWW lane and any future restore-to-target mutation after preview/rollback evidence is ready.
- Split the DX launch binary-cache scanner into focused artifact, meter, path, row, state, and summary helpers with a new no-build source guard so receipt-cache artifact IO, fallback row construction, health mapping, and operator next-action logic stay source-owned and small.
- Split the DX receipt-history scanner into focused bucket, receipt-file, bounded JSON IO, Forge summary, Forge field, and shared field helpers with a new no-build source guard so Tool History receipt visibility stays maintainable without changing runtime behavior.
- Split the DX launch source-audit scanner into focused path, packet IO, field, row, snapshot, and status helpers with a new no-build source guard so cross-repo launch audit receipt parsing stays small and source-owned.
- Split the DX launch Source Audit rail renderer into a focused workspace module with a source guard so the right rail stays maintainable while preserving honest missing/schema/risk/template/QA warning states.
- Split the DX launch Audit rail renderer into a focused workspace module with a source guard so command/safety/fixture/smoke/example audit rows stay out of the workspace coordinator.
- Split the DX-WWW Evidence rail renderer into a focused workspace module with a source guard so release-evidence rows and warning states stay out of the workspace coordinator.
- Split the DX Launch Receipts rail renderer into a focused workspace module with a source guard so latest/snapshot receipt rows and stale/malformed/schema warning states stay out of the workspace coordinator.
- Split the DX Agent Receipts rail renderer into a focused agents submodule with a source guard so receipt root/inbox, safe-row, provider/model/social, and retry/cancel receipt details stay out of the broad Agent rail coordinator.
- Split the DX Agent social/action/automation rail renderers into focused agents submodules with a source guard so social account rows, connect/disconnect receipts, automation rows, and shared action-line labels stay out of the bridge coordinator.
- Split the DX Agent bridge summary renderer into a focused agents submodule with a source guard so contract/import/release-gate rows and bridge warning states stay out of the Agent module index.
- Split the DX Proof freshness renderer into a focused proof submodule with a source guard so freshness bucket rows and missing-root fallback labels stay out of the runtime proof status module.
- Split the DX runtime proof plan/import/status row renderers into a focused proof submodule with a source guard so runtime proof row details stay out of the runtime proof status shell.
- Split the DX Sources rail row renderer into a focused sources submodule with a source guard so source item rows, receipt drilldowns, proof signals, warnings, and source-kind icons stay out of the source-set orchestrator.
- Split the DX Agent receipt row renderer into a focused receipts submodule with a source guard so receipt detail rows, retry/cancel labels, social/auth labels, and automation labels stay out of the receipt index/inbox shell.
- Split the DX Agent bridge summary row renderer into a focused bridge submodule with a source guard so summary metrics, recovery labels, fanout labels, and receipt-index counts stay out of the bridge warning/next-action shell.
- Split the DX Runtime Proof row renderer into focused plan and receipt submodules with a source guard so plan requirements/evidence labels and import/status receipt detail labels stay out of the runtime-row coordinator.
- Split the DX Check label helpers into a tiny facade plus focused count/run/test modules with a source guard so checked-path, skipped-check, outcome, duration, and last-run policies stay independently owned.
- Split the DX Check rail row renderer into a focused Check row module with a source guard so section score, warning/blocker, and quick-fix detail rows stay out of the Check rail shell.
- Split the DX Launch Receipts row renderer into a focused row module with a source guard so receipt card identity, display-state, age, next-action, and themed row ownership stay out of the Launch Receipts shell.
- Split the DX Launch Status schema-valid detail rows and warning selection into focused submodules with a source guard so next-action/redaction-summary labels and invalid/redaction/error warnings stay out of the Launch Status shell.
- Split the DX Source Audit warning selection into a focused submodule with a source guard so first-issue, risk-review, template-trust, and DX Studio QA warning policy stays out of the Source Audit shell.
- Split the DX Launch Audit warning selection into a focused submodule with a source guard so first-issue, redaction-review, and command-fanout warning policy stays out of the Launch Audit shell.
- Split the DX Agent provider label helper into a small facade plus detail/state/text/test modules with a source guard so provider/model label policy and tests stay out of one near-budget file.
- Split the DX Agent bridge summary rows into overview/contract/import/gate modules with a source guard so the bridge summary coordinator no longer owns every metric domain.
- Split the DX Agent receipt label policy into a focused helper module with a source guard so receipt row rendering stays layout-only for provider/model/action/social/automation details.
- Split the DX Agent bridge review row decision tree into a focused helper with a source guard so action-error/redaction, release-gate/import, bridge-error, and next-action fallback policy stay out of the bridge shell.

## Remaining Proof

- The DX Agent bridge review split has red/green source-guard validation, targeted rustfmt, and line-count validation only so far; live Agent bridge review visual proof against real action-error, contract, import-summary, release-gate, and receipt-index receipts, Cargo checks, local servers, provider calls, and runtime native Zed proof remain deferred under the repo's launch validation rules.
- The DX Agent receipt label split has red/green source-guard validation, targeted rustfmt, and line-count validation only so far; live Agent receipt row visual proof against real DX Agents receipt-index and inbox receipts, Cargo checks, local servers, provider calls, and runtime native Zed proof remain deferred under the repo's launch validation rules.
- The DX Agent bridge summary section split has red/green source-guard validation, targeted rustfmt, and line-count validation only so far; live Agent bridge summary visual proof against real DX Agents contract/import/release-gate/receipt-index receipts, Cargo checks, local servers, provider calls, and runtime native Zed proof remain deferred under the repo's launch validation rules.
- The DX Agent provider label split has red/green source-guard validation, direct pure Rust unit coverage, targeted rustfmt, and line-count validation only so far; live Agent provider/model label visual proof against real provider catalog receipts, Cargo checks, local servers, provider calls, and runtime native Zed proof remain deferred under the repo's launch validation rules.
- The DX Launch Audit warning split has red/green source-guard validation, targeted rustfmt, and line-count validation only so far; live Launch Audit warning visual proof against real `G:\Dx\cli\fixtures\launch-examples` audit packets, Cargo checks, local servers, provider calls, and runtime native Zed proof remain deferred under the repo's launch validation rules.
- The DX Source Audit warning split has red/green source-guard validation, targeted rustfmt, and line-count validation only so far; live Source Audit warning visual proof against real `G:\Dx\.dx\audit\launch-source\latest.json` and DX Studio QA receipts, Cargo checks, local servers, provider calls, and runtime native Zed proof remain deferred under the repo's launch validation rules.
- The DX Launch Status row/warning split has red/green source-guard validation, targeted rustfmt, and line-count validation only so far; live Launch Status warning/detail visual proof against real `G:\Dx\.dx\receipts\launch\status-latest.json` receipts, Cargo checks, local servers, provider calls, and runtime native Zed proof remain deferred under the repo's launch validation rules.
- The DX Launch Receipts row split has red/green source-guard validation, targeted rustfmt, and line-count validation only so far; live Launch Receipts latest/snapshot card visual proof against real `G:\Dx\.dx\receipts\launch` receipts, Cargo checks, local servers, provider calls, and runtime native Zed proof remain deferred under the repo's launch validation rules.
- The DX Check row split has red/green source-guard validation, adjacent agent/deploy/source guards, targeted rustfmt, and line-count validation only so far; live Check rail section/warning/quick-fix visual proof against real `check-latest.json` receipts, Cargo checks, local servers, provider calls, and runtime native Zed proof remain deferred under the repo's launch validation rules.
- The DX Check label facade split has red/green source-guard validation, direct Rust label unit tests, adjacent agent/deploy/source guards, targeted rustfmt, and line-count validation only so far; live Check rail label proof against real `check-latest.json` receipts, Cargo checks, local servers, provider calls, and runtime native Zed proof remain deferred under the repo's launch validation rules.
- The DX Runtime Proof plan/receipt row split has red/green source-guard validation, adjacent agent/deploy/source guards, targeted rustfmt, and line-count validation only so far; live Runtime Proof plan/import/status visual proof against real runtime proof receipts, Cargo checks, local servers, provider calls, and runtime native Zed proof remain deferred under the repo's launch validation rules.
- The DX Agent bridge summary split has red/green source-guard validation, adjacent agent/deploy/source guards, targeted rustfmt, and line-count validation only so far; live Agent bridge summary visual proof against real contract/import/release-gate/receipt-index receipts, Cargo checks, local servers, provider calls, and runtime native Zed proof remain deferred under the repo's launch validation rules.
- The DX Agent receipt row split has red/green source-guard validation, adjacent agent/deploy/source guards, targeted rustfmt, diff hygiene, and conflict-marker validation only so far; live Agent receipt row visual proof against real DX Agents receipt-index and inbox receipts, Cargo checks, local servers, provider calls, and runtime native Zed proof remain deferred under the repo's launch validation rules.
- The DX Sources row split has red/green source-guard validation, adjacent agent/deploy/source guards, targeted rustfmt, diff hygiene, and conflict-marker validation only so far; live Sources rail visual proof against real workspace/source-pack/media/restore receipts, Cargo checks, local servers, provider calls, and runtime native Zed proof remain deferred under the repo's launch validation rules.
- The DX runtime proof row split has red/green source-guard validation, adjacent agent/deploy/source guards, targeted rustfmt, diff hygiene, and conflict-marker validation only so far; live Runtime Proof row visual proof against real runtime proof plan/import/status receipts, Cargo checks, local servers, provider calls, and runtime native Zed proof remain deferred under the repo's launch validation rules.
- The DX Proof freshness split has red/green source-guard validation, adjacent agent/deploy/source guards, targeted rustfmt, diff hygiene, and conflict-marker validation only so far; live Proof freshness visual proof against real DX proof freshness receipts, Cargo checks, local servers, provider calls, and runtime native Zed proof remain deferred under the repo's launch validation rules.
- The DX Agent bridge summary split has red/green source-guard validation, adjacent agent/deploy/source guards, targeted rustfmt, diff hygiene, and conflict-marker validation only so far; live Agent bridge summary visual proof against real DX Agents contract/import/release-gate/action-error receipts, Cargo checks, local servers, provider calls, and runtime native Zed proof remain deferred under the repo's launch validation rules.
- The DX Agent social/action/automation rail split has red/green source-guard validation, adjacent agent/deploy/source guards, targeted rustfmt, diff hygiene, and conflict-marker validation only so far; live Agent social/action/automation rail visual proof against real DX Agents receipts, Cargo checks, local servers, provider calls, and runtime native Zed proof remain deferred under the repo's launch validation rules.
- The DX Agent Receipts rail split has red/green source-guard validation, adjacent agent/deploy/source guards, targeted rustfmt, diff hygiene, and conflict-marker validation only so far; live Agent Receipts rail visual proof against real DX Agents receipt-index and inbox receipts, Cargo checks, local servers, provider calls, and runtime native Zed proof remain deferred under the repo's launch validation rules.
- The DX Launch Receipts rail split has red/green source-guard validation, adjacent launch/deploy guards, targeted rustfmt, diff hygiene, and conflict-marker validation only so far; live Launch Receipts rail visual proof against real `G:\Dx\.dx\receipts\launch` receipts, Cargo checks, local servers, and runtime native Zed proof remain deferred under the repo's launch validation rules.
- The DX-WWW Evidence rail split has red/green source-guard validation, adjacent DX-WWW/launch/deploy guards, targeted rustfmt, diff hygiene, and conflict-marker validation only so far; live WWW Evidence rail visual proof against real DX-WWW release evidence packets, Cargo checks, local servers, and runtime native Zed proof remain deferred under the repo's launch validation rules.
- The DX launch Audit rail split has red/green source-guard validation, adjacent launch/deploy/source-audit/prompt guards, fixture-contract validation, targeted rustfmt, diff hygiene, and conflict-marker validation only so far; live Launch Audit rail visual proof against the real `G:\Dx\cli\fixtures\launch-examples` packets, Cargo checks, local servers, and runtime native Zed proof remain deferred under the repo's launch validation rules.
- The DX launch Source Audit rail split has red/green source-guard validation, adjacent launch/deploy/source/receipt guards, targeted rustfmt, diff hygiene, and conflict-marker validation only so far; live Source Audit rail visual proof against `G:\Dx\.dx\audit\launch-source\latest.json`, Cargo checks, local servers, and runtime native Zed proof remain deferred under the repo's launch validation rules.
- The DX receipt-history helper split has red/green source-guard validation, adjacent launch/deploy/source-set/agent guards, targeted rustfmt, diff hygiene, and conflict-marker validation only so far; live Forge/media/serializer receipt refresh, Cargo checks, local servers, and runtime Tool History rail visual proof remain deferred under the repo's launch validation rules.
- The DX launch binary-cache helper split has red/green source-guard validation, adjacent launch source guards, targeted rustfmt, diff hygiene, and conflict-marker validation only so far; live receipt-cache artifact producer execution, artifact round-trip proof, Cargo checks, local servers, and runtime Binary Cache rail visual proof remain deferred under the repo's launch validation rules.
- The source-backed DX Studio Web Preview editing slice has rustfmt, targeted rustfmt check, targeted diff hygiene, conflict-marker validation, ASCII scan, source review, DX-WWW JSON/TypeScript manifest-contract inspection, and injected-bridge JS parse validation only so far; live WebView overlay behavior, prompt/edit flow, hot-reload timing, selection restoration after an actual DX-WWW reload, Cargo checks, local servers, and `just run` remain deferred under the launch validation rules.
- The May 22 DX launch UX/manifest polish has rustfmt, targeted rustfmt check, targeted diff hygiene, and conflict-marker validation only so far; live WebView route/source/package inspection, onboarding visual proof, Cargo checks, local servers, and `just run` remain deferred under the repo's launch validation rules.
- The DX Studio edit-contract parser hardening has formatting/diff/conflict/source-reference validation only so far; live WebView diagnostics and DX-WWW manifest producer proof remain deferred under the repo's launch validation rules.
- The DX onboarding and DX Studio Web Preview contracts have diff/conflict/source-level validation only so far; live route opening, hot-reload timing, Cargo checks, `just run`, and local server proof remain deferred under the repo's launch validation rules.
- The DX Studio edit/drag manifest, public edit-operation contract, and public-manifest field compatibility slice has diff/conflict/source-level validation only so far; live page marker proof, drag/drop runtime behavior, Cargo checks, `just run`, and DX-WWW CLI producer proof remain deferred under the repo's launch validation rules.
- The DX Studio session module split has formatting/diff/conflict/source-reference validation only so far; Cargo checks, `just run`, and runtime WebView proof remain deferred under the repo's launch validation rules.
- The DX Studio write-contract metadata hardening has formatting/diff/conflict/source-reference validation only so far; live WebView diagnostics and DX-WWW manifest producer proof remain deferred under the repo's launch validation rules.
- The DX onboarding Web Preview hero has formatting, diff, conflict-marker, and source-reference validation only so far; `just run` visual proof and native WebView2 runtime proof remain deferred under the repo's launch validation rules.
- The DX onboarding provider readiness slice has formatting/source-reference validation only so far; live onboarding visual proof, provider settings click proof, Cargo checks, local servers, and credential import proof remain deferred under the repo's launch validation rules.
- The DX onboarding WWW target discovery slice has formatting/source-reference validation only so far; live WebView2 navigation proof, `DX_WWW_WORKSPACE` route proof, Cargo checks, local servers, and `just run` remain deferred under the repo's launch validation rules.
- The DX onboarding preview-contract visibility slice has formatting/source-reference validation only so far; live visual proof, command-contract refresh proof, Cargo checks, local servers, and `just run` remain deferred under the repo's launch validation rules.
- The DX onboarding demo-action coverage slice has formatting/diff/conflict/source-reference validation only so far; live click proof, Cargo checks, local servers, and `just run` remain deferred under the repo's launch validation rules.
- The DX Agents bridge has formatting/diff/conflict/source-reference validation only so far; live `dx-agents ... --json` command proof, Cargo checks, provider calls, local servers, and runtime rail visual proof remain deferred under the repo's launch validation rules.
- The DX Agents public-command bridge has formatting/diff/conflict/source-review validation only so far; live `dx agents ... --json` command proof, Cargo checks, and runtime UI proof remain deferred under the repo's launch validation rules.
- The DX Agents receipt-index row slice has rustfmt/diff/conflict/source-reference validation only so far; live `dx agents receipts list --json` proof, Cargo checks, local servers, and runtime rail visual proof remain deferred under the repo's launch validation rules.
- The DX Agents social action receipt slice has formatting/diff/conflict/source-reference validation only so far; live social connect/disconnect command proof, Cargo checks, and runtime visual proof remain deferred under the repo's launch validation rules.
- The DX Agents import-summary slice has formatting/diff/conflict/source-reference validation only so far; live `dx agents import-summary --json` proof, Cargo checks, local servers, and runtime rail visual proof remain deferred under the repo's launch validation rules.
- The DX Agents release-gate slice has formatting/diff/conflict/source-reference validation only so far; live `dx agents release-gate --json` proof, Cargo checks, local servers, and runtime rail visual proof remain deferred under the repo's launch validation rules.
- The DX launch status aggregate slice has formatting/diff/conflict/source-reference validation only so far; live `dx launch status --json` proof, Cargo checks, local servers, and runtime rail visual proof remain deferred under the repo's launch validation rules.
- The DX launch handoff slice has formatting/diff/conflict/source-reference validation only so far; live `dx launch import-manifest --json`, `dx launch handoff --json`, Cargo checks, local servers, and runtime rail visual proof remain deferred under the repo's launch validation rules.
- The DX launch gate slice has formatting/source-reference validation only so far; live `dx launch import-summary --json`, `dx launch release-gate --json`, `dx launch fallback-drill --json`, Cargo checks, local servers, and runtime rail visual proof remain deferred under the repo's launch validation rules.
- The DX launch audit slice has formatting/source-reference validation only so far; live `dx launch schemas --json`, `dx launch fixtures --json`, `dx launch smoke --json`, Cargo checks, local servers, and runtime rail visual proof remain deferred under the repo's launch validation rules.
- The DX-WWW launch evidence slice has formatting/source-reference validation only so far; live `dx forge launch-readiness-bundle`, release packet, restart, acceptance, Friday baton commands, Cargo checks, local servers, and runtime rail visual proof remain deferred under the repo's launch validation rules.
- The DX launch source-audit helper split has red/green source-guard validation, adjacent launch/source/receipt guards, targeted rustfmt, diff hygiene, and conflict-marker validation only so far; live source-audit helper execution, cross-repo risk reconciliation, Cargo checks, local servers, and runtime rail visual proof remain deferred under the repo's launch validation rules.
- The DX launch receipt review slice has formatting/diff/conflict/source-reference validation only so far; live `dx launch receipts --json` proof, Cargo checks, local servers, and runtime rail visual proof remain deferred under the repo's launch validation rules.
- The DX metadata binary-cache bridge slice has formatting/source-level validation only so far; live receipt-cache producer proof, Cargo checks, local servers, and runtime rail visual proof remain deferred under the repo's launch validation rules.
- The DX receipt-cache producer bridge has formatting/diff/conflict/source validation only so far; governed CLI producer execution, artifact round-trip proof, Cargo checks, `just run`, and performance measurements remain deferred under the repo's launch validation rules.
- The standard DX receipt-cache root/summary/kind-summary/health contract and Binary Cache rail consumption/review path have formatting/diff/conflict/source-reference validation only so far; governed producer execution, artifact round-trip proof, Cargo checks, `just run`, and performance measurements remain deferred under the repo's launch validation rules.
- Runtime visual proof is still pending because this launch lane forbids local servers, `just run`, full Cargo builds, and heavy validation without explicit permission.
- The crowded coding layout activity bar has formatting/diff/conflict/source-reference validation only so far; live tri-pane visual proof, panel resize behavior, Cargo checks, and `just run` remain deferred under the repo's launch validation rules.
- The visible-pane activity-bar trigger refinement has formatting/diff/conflict/source-review validation only so far; live split-pane proof remains deferred under the repo's launch validation rules.
- The compact activity-bar module extraction has formatting/diff/conflict/source-reference validation only so far; Cargo checks and live visual proof remain deferred under the repo's launch validation rules.
- The compact activity-bar overflow hardening has formatting/diff/conflict/source-review validation only so far; live small-window proof remains deferred under the repo's launch validation rules.
- The compact activity-bar Background Tasks retargeting has formatting/diff/conflict/source-review validation only so far; live retained-thread selection proof remains deferred under the repo's launch validation rules.
- The compact activity-bar Sources/Automations shared routing cleanup has formatting/diff/conflict/source-review validation only so far; live click proof remains deferred under the repo's launch validation rules.
- The screen-carousel dead-code cleanup has formatting/diff/conflict/source-reference validation only so far; live screen switching proof remains deferred under the repo's launch validation rules.
- The expanded sidebar screen-grid polish has formatting/diff/conflict/source-reference validation only so far; live click and layout proof remains deferred under the repo's launch validation rules.
- The DX receipt scanner module split has formatting/diff/conflict/source-reference validation only so far; live receipt refresh and rail visual proof remain deferred under the repo's launch validation rules.
- The token/tool meter state cleanup has formatting/diff/conflict/source-reference validation only so far; live receipt visual proof and governed token/tool receipt producer proof remain deferred under the repo's launch validation rules.
- The DX CLI receipt producers remain external to Zed; this slice reads receipt files and reports missing or empty receipt states only.
- The media runner source slice has rustfmt/diff/conflict validation only so far; Cargo check/test and runtime ffmpeg proof remain deferred under the repo's launch validation rules.
- The reduced-context writer has formatting/diff/conflict validation only so far; Cargo check/test and runtime Agent proof remain deferred under the repo's launch validation rules.
- The reduced-context rail slice has formatting/diff/conflict validation only so far; runtime visual proof remains deferred under the repo's launch validation rules.
- The guided action slice has formatting/diff/conflict validation only so far; runtime click proof remains deferred under the repo's launch validation rules.
- The guided proof card slice has formatting/diff/conflict validation only so far; runtime click proof remains deferred under the repo's launch validation rules.
- The source attachment and Check score slice has formatting/diff/conflict validation only so far; runtime visual proof remains deferred under the repo's launch validation rules.
- The restore-warning and Deploy registry slice has formatting/diff/conflict validation only so far; runtime visual proof remains deferred under the repo's launch validation rules.
- The source action and deploy readiness receipt slice has formatting/diff/conflict validation only so far; runtime click proof remains deferred under the repo's launch validation rules.
- The deploy receipt bucket slice has formatting/diff/conflict validation only so far; runtime visual proof remains deferred under the repo's launch validation rules.
- The deploy receipt drilldown slice has formatting/diff/conflict validation only so far; runtime visual proof remains deferred under the repo's launch validation rules.
- The proof freshness and produced-file card slice has formatting/diff/conflict validation only so far; runtime visual proof remains deferred under the repo's launch validation rules.
- The deploy URL/status and runtime-proof handoff slice has formatting/diff/conflict validation only so far; runtime visual proof remains deferred under the repo's launch validation rules.
- The restore approval and reducer guard handoff slice has formatting/diff/conflict validation only so far; runtime visual proof remains deferred under the repo's launch validation rules.
- The runtime proof status bucket slice has formatting/diff/conflict validation only so far; runtime visual proof remains deferred under the repo's launch validation rules.
- The runtime proof plan slice has formatting/diff/conflict validation only so far; actual `just run` runtime evidence and proof import remain deferred under the repo's launch validation rules.
- The runtime proof plan visibility slice has formatting/diff/conflict validation only so far; runtime visual proof and proof import remain deferred under the repo's launch validation rules.
- The proof freshness drilldown slice has formatting/diff/conflict validation only so far; runtime visual proof remains deferred under the repo's launch validation rules.
- The runtime proof handoff-context slice has formatting/diff/conflict validation only so far; runtime visual proof remains deferred under the repo's launch validation rules.
- The live receipt-review draft slice has formatting/diff/conflict validation only so far; runtime click proof remains deferred under the repo's launch validation rules.
- The runtime proof live-context slice has formatting/diff/conflict validation only so far; runtime click proof remains deferred under the repo's launch validation rules.
- The runtime proof import handoff slice has formatting/diff/conflict validation only so far; runtime click proof and actual operator evidence import remain deferred under the repo's launch validation rules.
- The runtime proof status scanner slice has formatting/diff/conflict validation only so far; runtime visual proof and actual operator evidence import remain deferred under the repo's launch validation rules.
- The runtime proof plan drilldown slice has formatting/diff/conflict validation only so far; runtime visual proof and actual operator evidence import remain deferred under the repo's launch validation rules.
- The runtime proof evidence-form slice has formatting/diff/conflict validation only so far; runtime visual proof and actual operator evidence import remain deferred under the repo's launch validation rules.
- The runtime proof import drilldown slice has formatting/diff/conflict validation only so far; runtime visual proof and actual operator evidence import remain deferred under the repo's launch validation rules.
- The row-level source control slice has formatting/diff/conflict validation only so far; runtime click proof remains deferred under the repo's launch validation rules.
- The row-level receipt drilldown slice has formatting/diff/conflict validation only so far; runtime visual proof remains deferred under the repo's launch validation rules.
- The source receipt review handoff slice has formatting/diff/conflict validation only so far; runtime click proof remains deferred under the repo's launch validation rules.
- The runtime proof import capture slice has formatting/diff/conflict validation only so far; Cargo check/test and runtime proof remain deferred under the repo's launch validation rules.
- The restore approval capture slice has formatting/diff/conflict validation only so far; Cargo check/test and restore-to-target runtime proof remain deferred under the repo's launch validation rules.
- The restore approval history visibility slice has formatting/diff/conflict validation only so far; Cargo check/test and restore-to-target runtime proof remain deferred under the repo's launch validation rules.
- The restore-target dry-run plan slice has formatting/diff/conflict validation only so far; Cargo check/test and restore-to-target mutation remain deferred under the repo's launch validation rules.
- The Forge history drilldown slice has formatting/diff/conflict validation only so far; Cargo check/test and restore-to-target runtime proof remain deferred under the repo's launch validation rules.
- The serializer/RLM execution preview slice has formatting/diff/conflict validation only so far; Cargo check/test and external reducer runtime proof remain deferred under the repo's launch validation rules.
- The serializer/RLM external execution slice has formatting/diff/conflict validation only so far; Cargo check/test and actual reducer runtime proof remain deferred under the repo's launch validation rules.

## dx-check Quick-Fix Risk Metadata - 2026-05-22

- Native Zed dx-check snapshots now carry quick-fix `risk_level`, `requires_user_approval`, and `writes_receipts` fields from the shared receipt contract.
- Missing older metadata is derived from the command shape so `dx check ...` actions render as receipt-writing actions and opt-in runner/probe actions stay visibly approval-sensitive.
- The launch rail now shows risk, approval, and receipt-write state next to each dx-check quick fix instead of rendering command text with no operator risk context.
- Verification: `rustfmt --check crates/agent_ui/src/dx_check_panel.rs crates/agent_ui/src/dx_launch_workspace.rs` passed; targeted `cargo test -p agent_ui dx_check_panel --lib` was attempted twice but exceeded the timeout on this busy machine.

## DX Style Web Preview Sidebar - 2026-05-30

- The DX Style sidebar architecture is GPUI shell plus Web Preview canvas: native Zed owns panel status, active editor context, and actions; Web Preview owns the visual CSS generator UI.
- The next hardening pass resolves the active editor project path to an absolute workspace file path before receipt lookup, group context, source-review handoff, and dry-run matching. This keeps project-local `.dx\receipts\style` roots tied to the actual open project instead of relative process-cwd paths.
- Shared DX Style receipt root discovery now walks active source ancestors only for absolute source paths inside the active workspace root, with Windows-friendly de-duplication and no global fallback roots for active-source decisions.
- Active Style context now checks the editor buffer length before cloning full editor text, preserving the large-file refusal state while reducing sidebar work on oversized files.
- `G:\Dx\style` now exposes a read-only grouped-class cursor context contract so editor consumers can identify the active static style token, preserve grouped calls with spaces, and refuse dynamic/non-literal class expressions honestly.
- The Zed right-dock Style panel now reads the active editor cursor for static class/className context and dispatches the Web Preview generator surface with read-only source-context JSON.
- DX Style now defines `dx.style.grouped-class-dry-run-receipt` for source digest/span trust and ready/blocked patch previews.
- The Zed Style panel now computes an apply-gate snapshot from the active token and bounded receipt scans. It keeps apply disabled unless a trusted dry-run receipt and editor write bridge are both present.
- The Web Preview generator page now displays active context status/token/span, promotes the most relevant generator, and shows a disabled Apply gate with the real trust reason when launched from the Style panel.
- The Web Preview generator page now renders its first-pass controls from source-owned DX Style control metadata and reports whether it used the live DX Style fixture or Zed's embedded generated fallback.
- The Web Preview generator page now reads labels, categories, and cursor token hints from source-owned DX Style generator catalog metadata instead of a Zed-only browser list.
- DX Style now defines `dx.style.grouped-class-reverse-css-delta-contract`, and Zed Web Preview ingests that fixture before falling back to an embedded mirror, so generator edits to supported generated CSS declarations can be reviewed as source-owned atomic utility deltas while mutation remains disabled.
- Native Web Preview source-apply review receipts now include the reverse CSS delta contract summary and require the source-apply contract to name reverse-map and declaration-delta guards before a request can move past review.
- Reverse CSS delta review now covers `padding-inline`, `padding-block`, and `gap` spacing declarations as `px-*`, `py-*`, and `gap-*` targets, which makes the grouped button/layout path less color-only.
- The Web Preview generator surface now computes a live reverse CSS delta preview from current generated CSS plus active grouped utilities and includes that read-only preview in native source-apply review receipts.
- Trusted dry-run receipts can now flow into read-only patch review fields: intent, status, edit count, and message. No source mutation is enabled by this slice.
- CSS declaration dry-run preview diagnostics now gate the Web Preview's native review/apply path before IPC, so the visual generator cockpit can show oversized declaration blockers while Zed stays protected from invalid review packets.
- Copied Web Preview review packets now include the CSS declaration dry-run diagnostics, preview diagnostics, and declaration byte cap as source-only handoff evidence.
- Native source-apply receipts now preserve the same CSS declaration diagnostic arrays and declaration byte cap, and refuse native review payloads that carry non-empty CSS dry-run diagnostics.
- Native review also validates that the source-owned source-apply contract names those CSS declaration diagnostic and preview receipt fields before accepting the request as contract-aligned.
- DX Style's CSS declaration dry-run contract now owns the diagnostic and preview-diagnostic receipt fields too, and the Zed embedded fixture mirror is kept in sync for Web Preview review.
- Web Preview now surfaces CSS declaration dry-run receipt fields in the contract review UI and copied review packet, and native receipts preserve the CSS dry-run receipt schema plus field list.
- DX Style now also owns CSS declaration dry-run diagnostic caps (`max_diagnostic_count`, `max_diagnostic_bytes`); Web Preview surfaces those limits and native review validates them before preserving diagnostic arrays.
- Source-only verification so far: targeted `rustfmt`, `node --test script/dx-style-panel-source.test.ts script/dx-launch-workspace-source.test.ts script/dx-handoff-source-guard-registry.test.ts`, fixture mirror checks, `git diff --check`, and conflict-marker scans as recorded by the source-only lane. Cargo, `just run`, local servers, browser/WebView runtime proof, live Zed launch, and source mutation proof remain intentionally unrun by instruction. The current next step is governed runtime/WebView/build proof plus a mutation-capable editor write bridge.
