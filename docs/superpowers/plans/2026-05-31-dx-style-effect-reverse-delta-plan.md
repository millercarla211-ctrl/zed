# DX Style Effect Reverse Delta Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans or keep this checklist current while implementing the source-only effect reverse-delta slice.

**Goal:** Expand review-only generated CSS to grouped atomic utility projection for the Style sidebar effect generators without enabling source writes.

**Boundary:** This is source-only contract and Web Preview review work. It must not enable `Apply`, change `source_mutation_enabled`, introduce a file writer, or claim runtime/WebView/build proof.

## Tasks

- [x] Add source-owned reverse-delta value strategies for token suffixes, arbitrary bracket values, `drop-shadow(...)`, and `backdrop-filter: blur(...)`.
- [x] Extend the DX Style reverse-delta contract fixture with `box-shadow`, `text-shadow`, `filter` drop-shadow, and `backdrop-filter` blur mappings.
- [x] Teach Zed Web Preview reverse-delta preview logic to consume `value_strategy` and preserve color utilities when replacing shadow effect utilities.
- [x] Sync the generated Zed reverse-delta contract fallback from `G:\Dx\style`.
- [x] Guard the source-owned contract, fixture mirror, browser parser, and effect mapping helpers.

## Verification

- [x] `rustfmt --edition 2024 --check` on touched Rust files.
- [x] `node --test script\dx-style-panel-source.test.ts script\dx-handoff-source-guard-registry.test.ts script\dx-launch-workspace-source.test.ts`.
- [x] `node G:\Dx\style\scripts\sync_zed_visual_generator_fixtures.mjs --check`.
- [x] `git diff --check`.
- [x] Conflict-marker scan.
- [x] Focused commit in `G:\Dx\zed`.
