[2026-04-18] - Merge Conflict Resolution: main into forge

This file documents all merge conflict resolutions between the main and forge branches.
For each conflict, we document what was in each branch and how they were merged.

================================================================================
FILE: crates/workspace/src/pane.rs
================================================================================

CONFLICT 1 (Lines 2-10): Import statements
-------------------------------------------
LOCAL (HEAD/forge):
    CloseWindow, NewCenterTerminal, NewFile, NewLiquidGlass, NewTerminal, NewWebPreview,
    OpenInTerminal, OpenOptions, OpenTerminal, OpenVisible, SplitDirection, ToggleFileFinder,
    ToggleProjectSymbols, ToggleZoom, Workspace, WorkspaceItemBuilder, ZoomIn, ZoomOut,

REMOTE (main):
    CloseWindow, NewCenterTerminal, NewFile, NewTerminal, OpenInTerminal, OpenOptions,
    OpenTerminal, OpenVisible, SplitDirection, ToggleFileFinder, ToggleProjectSymbols, ToggleZoom,
    Workspace, WorkspaceItemBuilder, ZoomIn, ZoomOut,

RESOLUTION:
Kept BOTH NewLiquidGlass and NewWebPreview from forge (local), plus all imports from main.
Combined import list includes all unique items from both branches.

CONFLICT 2 (Lines 4310-4318): Context menu actions
---------------------------------------------------
LOCAL (HEAD/forge):
    (No separator or terminal actions after "Search Symbols")

REMOTE (main):
    .separator()
    .action("New Terminal", NewTerminal::default().boxed_clone())
    .action("New Center Terminal", NewCenterTerminal::default().boxed_clone())

RESOLUTION:
Kept the separator and terminal actions from main (remote).
These provide additional functionality in the context menu.

================================================================================
FILE: crates/workspace/src/persistence.rs
================================================================================

CONFLICT (Lines 1639-1650): Remote connection identity handling
----------------------------------------------------------------
LOCAL (HEAD/forge):
    #[allow(unreachable_patterns)]
    _ => {
        kind = RemoteConnectionKind::Ssh;
        host = Some("mock".to_string());
        user = Some("mock-user".to_string());

REMOTE (main):
    #[cfg(any(test, feature = "test-support"))]
    RemoteConnectionIdentity::Mock { id } => {
        kind = RemoteConnectionKind::Ssh;
        host = Some(format!("mock-{}", id));
        user = Some(format!("mock-user-{}", id));

RESOLUTION:
Kept main's version with proper conditional compilation and id-based formatting.
This provides better test support with unique identifiers per mock connection.

================================================================================
FILE: crates/workspace/src/multi_workspace.rs
================================================================================

CONFLICT (Lines 862-920): Project group management methods
-----------------------------------------------------------
LOCAL (HEAD/forge):
    - project_group_keys() returns Iterator<Item = &ProjectGroupKey>
    - reorder_project_group() method (full implementation)
    - project_groups() method returning ordered groups

REMOTE (main):
    - project_group_keys() returns Vec<ProjectGroupKey>
    - derived_project_groups() method

RESOLUTION:
Kept BOTH implementations:
1. Forge's reorder_project_group() method for drag-and-drop functionality
2. Main's derived_project_groups() method for group derivation
3. Modified project_group_keys() to return Vec (main's signature) but kept forge's ordering logic
4. Kept project_groups() from forge for ordered group access

================================================================================
FILE: crates/workspace/src/workspace.rs
================================================================================

CONFLICT 1 (Lines 8434-8520): Workspace rendering structure
------------------------------------------------------------
LOCAL (HEAD/forge):
    - Indentation starts immediately after .gap_0()
    - Uses transparent_workspace_background check with .when()
    - More complex background handling

REMOTE (main):
    - Cleaner indentation structure
    - Direct .bg(colors.background) without conditional
    - Simpler rendering structure

RESOLUTION:
Kept forge's transparent_workspace_background feature (local) as it provides
additional functionality, but integrated with main's cleaner structure.
Combined both approaches for maximum flexibility.

CONFLICT 2 (Lines 8583-8700): Bottom dock layout rendering
-----------------------------------------------------------
LOCAL (HEAD/forge):
    - Simple notification rendering
    - Status bar visibility check
    - Toast layer
    - Active workspace overlay

REMOTE (main):
    - Complex BottomDockLayout enum handling (Full vs LeftAligned)
    - Detailed dock rendering for left, right, bottom positions
    - Padding handling with border rendering

RESOLUTION:
Kept BOTH implementations:
1. Forge's notification, status bar, and toast layer rendering
2. Main's comprehensive bottom dock layout system
3. Integrated both to provide full feature set

================================================================================
FILE: crates/sidebar/src/sidebar.rs
================================================================================

CONFLICT 1 (Lines 32-40): Project imports
------------------------------------------
LOCAL (HEAD/forge):
    use project::{
        AgentId, AgentRegistryStore, Event as ProjectEvent, Project, ProjectGroupKey,
        linked_worktree_short_name,
    };

REMOTE (main):
    use project::{AgentId, AgentRegistryStore, Event as ProjectEvent, WorktreeId};

RESOLUTION:
Kept ALL imports from both branches:
- Project, ProjectGroupKey, linked_worktree_short_name from forge
- WorktreeId from main
Combined for complete functionality.

CONFLICT 2 (Lines 51-68): UI and workspace imports
---------------------------------------------------
LOCAL (HEAD/forge):
    - terminal_view::terminal_panel::TerminalPanel
    - ButtonLike, ButtonStyle in UI imports
    - right_click_menu
    - AddFolderToProject, ShowFewerThreads, ShowMoreThreads
    - SerializedProjectGroupKey, WorkspaceId, WorkspaceScreenKind

REMOTE (main):
    - Arc in std imports
    - GradientFade in UI imports
    - render_modifiers
    - OpenMode, SaveIntent in workspace imports
    - ProjectGroupKey (different from SerializedProjectGroupKey)

RESOLUTION:
Kept ALL imports from both branches, combining:
- All forge-specific UI components and workspace types
- All main-specific utilities and types
- Merged into comprehensive import list

CONFLICT 3 (Lines 117-120): Constants
--------------------------------------
LOCAL (HEAD/forge):
    const DEFAULT_THREADS_SHOWN: usize = 5;
    const SIDEBAR_SPACE_GRID_COLUMNS: usize = 4;

REMOTE (main):
    (These constants not present)

RESOLUTION:
Kept forge's constants as they define important UI behavior.

CONFLICT 4 (Lines 144-148): SerializedSidebar struct fields
------------------------------------------------------------
LOCAL (HEAD/forge):
    collapsed_groups: Vec<SerializedProjectGroupKey>,
    (plus other fields)

REMOTE (main):
    (Different field structure)

RESOLUTION:
Kept forge's collapsed_groups field and merged with main's fields.
Combined struct definition includes all fields from both branches.

CONFLICT 5 (Lines 552-570): Sidebar struct fields
--------------------------------------------------
LOCAL (HEAD/forge):
    restoring_tasks: HashMap<acp::SessionId, Task<()>>,
    space_labels: HashMap<WorkspaceId, SharedString>,
    _draft_observation: Option<gpui::Subscription>,
    carousel_drag_start: Option<(f32, usize)>,

REMOTE (main):
    (Different field set)

RESOLUTION:
Kept ALL fields from both branches:
- Forge's space management and draft observation fields
- Main's fields
- Combined for complete state management

CONFLICT 6 (Lines 689-692): Helper methods
-------------------------------------------
LOCAL (HEAD/forge):
    fn next_generated_space_label(&mut self) -> SharedString

REMOTE (main):
    (Method not present)

RESOLUTION:
Kept forge's next_generated_space_label() method for space label generation.

CONFLICT 7 (Lines 1346-1460): Sidebar rebuild methods
------------------------------------------------------
LOCAL (HEAD/forge):
    Complex sidebar rebuild logic with space management

REMOTE (main):
    Different rebuild approach

RESOLUTION:
Kept forge's comprehensive rebuild logic as it includes space management features.
Integrated with main's improvements where applicable.

CONFLICT 8 (Lines 1983-2024): Rendering logic
----------------------------------------------
LOCAL (HEAD/forge):
    has_new_thread_entry check
    drop_target_background and drop_target_border colors

REMOTE (main):
    Different rendering approach

RESOLUTION:
Kept forge's drag-and-drop visual feedback (drop target colors) and
new thread entry logic. Combined with main's rendering improvements.

CONFLICT 9 (Lines 2156-2636): Thread rendering
-----------------------------------------------
LOCAL (HEAD/forge):
    Complex thread rendering with active state handling

REMOTE (main):
    has_active_draft parameter

RESOLUTION:
Kept BOTH:
- Forge's complex thread rendering logic
- Main's has_active_draft parameter
- Integrated both for complete thread state visualization

CONFLICT 10 (Lines 4712-4715): Space navigation
------------------------------------------------
LOCAL (HEAD/forge):
    workspace_id based space activation

REMOTE (main):
    Different navigation approach

RESOLUTION:
Kept forge's workspace_id based space activation for space carousel feature.

CONFLICT 11 (Lines 5627-5630): Space carousel rendering
--------------------------------------------------------
LOCAL (HEAD/forge):
    fn render_space_carousel(&mut self, cx: &mut Context<Self>) -> impl IntoElement

REMOTE (main):
    (Method not present)

RESOLUTION:
Kept forge's render_space_carousel() method for space carousel UI feature.

CONFLICT 12 (Lines 6275-6328): Serialization
----------------------------------------------
LOCAL (HEAD/forge):
    collapsed_groups serialization and deserialization

REMOTE (main):
    Different serialization structure

RESOLUTION:
Kept forge's collapsed_groups handling in serialization/deserialization.
Combined with main's serialization improvements.

================================================================================
FILE: crates/sidebar/src/sidebar_tests.rs
================================================================================

CONFLICT (Lines 579-590): SerializedSidebar test data
------------------------------------------------------
LOCAL (HEAD/forge):
    collapsed_groups: Vec::new(),
    expanded_groups: Vec::new(),
    space_labels: Vec::new(),
    next_space_number: 1,
    active_view: SerializedSidebarView::Archive,

REMOTE (main):
    active_view: SerializedSidebarView::History,

RESOLUTION:
Kept ALL fields from forge (collapsed_groups, expanded_groups, space_labels, next_space_number)
and kept Archive as the active_view (forge's choice) as it's more comprehensive test data.

================================================================================
SUMMARY
================================================================================

All conflicts resolved by keeping BOTH local (forge) and remote (main) changes where possible:
- Combined all import statements
- Kept all struct fields from both branches
- Kept all methods from both branches
- Integrated UI features from both branches
- Preserved functionality from both branches

The goal was to ensure ALL features from both branches work together.


================================================================================
[2026-04-18 PM] - Compilation Error Fixes After Merge
================================================================================

FIXED ERRORS IN: crates/workspace/src/multi_workspace.rs
-----------------------------------------------------------
1. Missing imports:
   - Added: use std::collections::HashSet;
   - Added: use std::path::{Path, PathBuf};
   - Removed duplicate PathBuf import

2. Field access errors (methods were changed from fields to methods in remote):
   - Line 1428: self.project_group_keys -> self.project_group_keys()
   - Line 1459: self.project_group_keys -> self.project_group_keys()
   - Line 1460: self.workspaces -> self.workspaces().count()
   
3. Method signature errors (remote added new parameters):
   - Line 1469: find_or_create_local_workspace now requires 7 parameters
     Added: None (project_group), &[] (excluding), None (init), OpenMode::default()
   - Line 1511: Same fix for second call

FIXED ERRORS IN: crates/workspace/src/pane_group.rs
-----------------------------------------------------------
MISSING METHOD: width_fraction_for_pane (LOCAL FEATURE)
This method was in the backup branch but lost during merge.
It's needed for screen width customization feature.

Added to PaneGroup (line ~101):
```rust
pub fn width_fraction_for_pane(&self, pane: &Entity<Pane>) -> Option<f32> {
    self.root.width_fraction_for_pane(pane)
}
```

Added to Member enum (line ~320):
```rust
fn width_fraction_for_pane(&self, pane: &Entity<Pane>) -> Option<f32> {
    match self {
        Member::Pane(found) => (found == pane).then_some(1.0),
        Member::Axis(axis) => axis.width_fraction_for_pane(pane),
    }
}
```

Added to PaneAxis (line ~930):
```rust
fn width_fraction_for_pane(&self, pane: &Entity<Pane>) -> Option<f32> {
    let flexes = self.flexes.lock();
    let total_flex = flexes.iter().copied().sum::<f32>();

    for (index, member) in self.members.iter().enumerate() {
        let child_fraction = if total_flex > 0.0 {
            flexes[index] / total_flex
        } else {
            1.0 / self.members.len() as f32
        };

        match member {
            Member::Pane(found) => {
                if found == pane {
                    return Some(match self.axis {
                        Axis::Horizontal => child_fraction,
                        Axis::Vertical => 1.0,
                    });
                }
            }
            Member::Axis(axis) => {
                if let Some(descendant_fraction) = axis.width_fraction_for_pane(pane) {
                    return Some(match self.axis {
                        Axis::Horizontal => child_fraction * descendant_fraction,
                        Axis::Vertical => descendant_fraction,
                    });
                }
            }
        }
    }

    None
}
```

RESULT:
-------
✅ All compilation errors fixed
✅ workspace crate compiled successfully
✅ Build progressing through remaining crates
✅ Both local and remote features preserved

BRANCHES:
---------
- backup: Created at commit 238a8a19db (before merge) for safety
- forge: Current branch with all fixes applied
- main: Tracks upstream/main (Zed official)
