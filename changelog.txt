[2026-05-14] - Icon SVG Rendering And Remote Media Fetch

ADDED:
- `crates/icon_picker/src/icon_representative_bodies.tsv` now embeds representative SVG bodies for hardcoded external icon packs so the default All sets grid can warm real previews without opening every source Iconify JSON file.
- `crates/icon_picker` now warms visible external Iconify SVG previews on a background task and caches the generated SVG files before repainting them into the grid.
- `crates/icon_picker` now indexes Iconify aliases in addition to base icons so alternate icon names from each pack appear in panel search and selected-pack grids.
- `crates/editor` now resolves Iconify aliases when inserting dragged or selected external icons, so alias entries create real SVG assets instead of empty files.
- `crates/icon_picker` and `crates/editor` now apply Iconify alias hFlip, vFlip, and rotate metadata when generating previews or inserted SVG assets.
- `crates/shadcn_ui_panel` now writes the initial shadcn rkyv catalog cache on a background task when the cache is missing, keeping first panel construction responsive.
- `crates/media_panel` now fetches live no-key remote media rows from Openverse, Wikimedia Commons, NASA, Library of Congress, and Art Institute of Chicago for the active media query/filter.
- `crates/media_panel` now uses shared Zed HTTP client plumbing plus JSON parsing for direct provider results instead of relying only on static sample rows.
- `crates/media_panel` now caches remote media results per active query and media tab so repeated searches and tab switches can repaint immediately.
- `crates/media_panel` now debounces remote provider searches and guards them by generation, preventing stale or partial keystroke searches from repainting the panel.
- `crates/shadcn_ui_panel` now reuses available local shadcn screenshot assets as panel row thumbnails, with a small in-memory lookup cache.
- `crates/media_panel` now fetches The Met public-domain image results directly through its no-key collection API.
- `crates/media_panel` now fetches direct NASA video and audio assets by resolving NASA collection manifests into playable media files.
- `crates/media_panel` generated remote-browser pages now search Art Institute and The Met no-key APIs and resolve NASA audio/video manifests to playable files.
- `crates/media_panel` remote media rows now carry optional thumbnail URLs so provider preview images can render in native image, video, and audio rows.
- `crates/media_panel` now runs no-key provider fetches concurrently for first-time remote searches while keeping provider-specific error reporting.
- `crates/media_panel` now overlays compact Zed-themed video/audio badges on remote provider thumbnails so playable media does not read as a plain image row.
- `crates/media_panel` now fetches direct Internet Archive image, video, and audio rows by combining no-key advanced search with item metadata/download URLs.
- `crates/media_panel` now caps each remote provider fetch at four seconds so one slow public API cannot block the whole remote media result batch.
- `crates/media_panel` now fetches direct Cleveland Museum of Art CC0 image rows through the no-key open access API.

CHANGED:
- `crates/media_panel` now reuses the active remote-results buffer on cache hits and preallocates remote provider, dedupe, and error buffers for remote media fetches.
- `crates/icon_picker` now keeps selected-pack icon loads scoped to `external_icons_by_pack` instead of extending, sorting, and deduping the broad all-icons list on each individual pack open.
- `crates/icon_picker` now preallocates full external Iconify catalog vectors, pack maps, representative rows, per-pack rows, and SVG body maps from known pack sizes, reducing allocator churn during broad icon search and preview generation.
- `crates/icon_picker`, `crates/font_panel`, `crates/media_panel`, and `crates/shadcn_ui_panel` now preallocate bounded visible result buffers before collecting search/filter rows, reducing small allocation churn while typing or switching tabs.
- `crates/icon_picker` now caches built-in Zed icon search text so typed searches do not rebuild `DraggedIconAsset` labels for every Zed icon on each repaint.
- `crates/font_panel` now checks cached lowercase font search text in place instead of cloning the cached `SharedString` for every typed-search font match.
- `crates/shadcn_ui_panel` now checks cached catalog search text in place instead of cloning the cached `SharedString` for every typed-search row match.
- `crates/icon_picker` now filters cached or already-warming visible external icons before cloning them for preview warm queues.
- `crates/font_panel` now caches lowercase font search text and only builds visible `FontEntry` rows after a font matches a typed query.
- `crates/media_panel` now matches remote media query terms directly against asset fields instead of formatting and lowercasing a combined searchable string for every remote row.
- `crates/icon_picker` now batches external preview-cache checks with one preview-cache borrow and skips icons that are already warming before queuing another SVG preview batch.
- `crates/shadcn_ui_panel` now consumes per-render cached thumbnail URLs when building visible rows, avoiding another thumbnail URL clone per row.
- `crates/shadcn_ui_panel` now snapshots visible shadcn preview-image cache hits once per render and passes them into row rendering instead of locking the global thumbnail cache for every visible row.
- `crates/icon_picker` now skips render-path preview-file checks for external icons that are already queued for background SVG preview warming.
- `crates/icon_picker` now limits the empty All Sets representative icon collection to the visible result window instead of extending the full external representative catalog and truncating it afterward.
- `crates/icon_picker`, `crates/font_panel`, `crates/media_panel`, and `crates/shadcn_ui_panel` now consume the already-filtered visible row vectors when rendering tiles/rows instead of cloning each visible item again.
- `crates/media_panel` now computes media-kind counts once per render and shares them with the tab strip and status label, avoiding repeated local/static/remote asset scans.
- `crates/media_panel` now caches local media-kind counts when the media index loads and reuses them for empty-search local totals.
- `crates/media_panel` now caches static direct remote media-kind counts once instead of scanning static remote rows on every render.
- `crates/media_panel` now caches live remote media-kind counts when provider results load or are reused from the remote cache.
- `crates/media_panel` now matches remote media rows by reference and clones only rows that are actually shown.
- `crates/media_panel` now skips remote media search-string construction when the query is empty, reducing idle render work while browsing live provider rows.
- `crates/media_panel` now skips local media search matching when the query is empty, reducing idle render work while browsing large project media indexes.
- `crates/media_panel` now reads and normalizes the panel search query once per render before remote loading and local/remote matching.
- `crates/media_panel` now passes an empty query slice for empty searches instead of allocating split query terms.
- `crates/font_panel` now skips per-font lowercase search text construction when the font query is empty, making idle browsing of system and Google fonts cheaper.
- `crates/font_panel` now uses a bounded empty-search row collection path and derives totals from source counts instead of walking every Google font on idle renders.
- `crates/font_panel` now computes source counts once per render and shares them between the tab strip and header total label.
- `crates/font_panel` now reuses split search terms across font rows instead of splitting the same query for every font candidate.
- `crates/icon_picker`, `crates/media_panel`, and `crates/shadcn_ui_panel` now reuse split search terms across candidate rows instead of splitting the same query for every visible/searchable item.
- `crates/icon_picker` now reads the panel search query once per render before data loading, filtering, and count-label formatting.
- `crates/icon_picker` now uses a direct selected-set fast path for empty searches in Zed or external icon packs instead of running generic search matching.
- `crates/icon_picker` now returns from empty-search fast paths before allocating split query terms.
- `crates/shadcn_ui_panel` now computes UI filter counts once per render and shares them with the tab strip and status label, avoiding repeated per-tab catalog scans.
- `crates/shadcn_ui_panel` now caches catalog source counts with the loaded catalog, avoiding a full catalog count scan on every render.
- `crates/shadcn_ui_panel` now uses a bounded empty-search row collection path and cached filter counts instead of scanning the full UI catalog on idle renders.
- `crates/shadcn_ui_panel` now builds drag payloads only for draggable source-owned rows, avoiding wasted row-render work for registry and 21st.dev docs entries.
- `crates/shadcn_ui_panel` now skips search-text construction when the UI query is empty and caches per-entry lowercase search text for non-empty searches, reducing render work while the panel is open.
- `crates/shadcn_ui_panel` now reads the panel search query once per render before matching catalog entries.
- `crates/shadcn_ui_panel` now looks up cached catalog search text by borrowed item id and only allocates cache keys when a row is first cached.
- `crates/font_panel` now reads the panel search query once per render before matching system and Google fonts.
- `crates/icon_picker` now checks the embedded representative icon body index before loading full pack body JSON, reducing cold icon preview work for the default All sets grid.
- `crates/media_panel` now uses larger bounded result windows for live no-key provider searches and the generated remote media browser, while keeping Wikimedia at a safe anonymous API limit and switching native Openverse requests to the current `api.openverse.org` endpoint.
- `crates/icon_picker` no longer parses Iconify pack JSON from the tile render path; uncached external previews show the lightweight pack badge until the background preview warmer writes real SVGs.
- `crates/icon_picker` now uses a new external SVG preview cache version so stale pre-transform Iconify alias previews are bypassed automatically.
- `crates/icon_picker` now starts warming representative external icon previews on first render, tracks in-flight preview keys, and reuses disk hits in the preview cache instead of checking them repeatedly.
- `crates/icon_picker` now prewarms the full visible All Sets representative icon grid on first open instead of stopping after the first 120 entries.
- `crates/shadcn_ui_panel` now warms local screenshot thumbnail lookups on a background task instead of checking the filesystem from each row render.
- `crates/font_panel` now shows source-aware selected-row actions so native system fonts can change Zed editor/UI settings while Google/web fonts focus on preview, CSS, and project installation.
- `crates/shadcn_ui_panel` no longer performs the initial static catalog cache write synchronously from `initial_shadcn_catalog`.
- `crates/icon_picker` now renders real external SVG previews in the default All sets grid instead of falling back to pack-letter badges.
- `crates/icon_picker` now caches missing external SVG previews as misses so broken entries do not repeatedly reload.
- `crates/icon_picker` now reuses generated external SVG preview files from `target/icon-picker-icons` before reparsing source Iconify pack JSON.
- `crates/icon_picker` now wraps generated Iconify SVG bodies with inherited `currentColor` fill and writes them under a versioned preview cache path, making packs with implicit black fills visible on dark panels.
- `crates/media_panel` now prioritizes remote provider assets before local media rows so large provider catalogs are visible in the panel immediately.
- `crates/media_panel` remote browser pages now include Internet Archive and Cleveland Museum of Art in direct in-page searches and the provider chip list.

NOTES:
- Preserved the existing local media index, media preview insertion flow, and WebPreview-backed media browser fallback.
- Validation stayed lightweight: rustfmt and `git diff --check`; no `just run` was started because the current Zed process is already open for manual QA.

[2026-05-14] - Right Panel Polish Completion

CHANGED:
- `todo.txt` now marks the right-side Icon, Font, Media, and UI panel polish loop as 100/100 code-complete.

NOTES:
- Ran final `just run`; the debug binary at `G:\Zed\target\debug\zed.exe` was rebuilt and Zed launched successfully.
- The Codex command timed out because the launched Zed app remained open for manual QA, with process `zed` still running afterward.
- Existing untracked local logs, inspiration clones, `models`, and `tools` were preserved.

[2026-05-14] - UI Panel Search And Counts

CHANGED:
- `crates/shadcn_ui_panel` now matches UI catalog searches by all query words across id, title, category, and description, so searches like `login form` and `magic button` do not require exact phrase order.
- `crates/shadcn_ui_panel` now reports clean active-filter match totals instead of the previous shown/matched/all triple count.

NOTES:
- Preserved the static/rkyv catalog load path, install actions, generated previews, and WebPreview input behavior.

[2026-05-14] - Icon Panel Search And Counts

CHANGED:
- `crates/icon_picker` now matches icon searches by all query words across Zed icons and external icon metadata, so searches like `arrow left` and `github logo` do not require exact phrase order.
- `crates/icon_picker` now reports search result totals against the active icon set selection and uses static hardcoded pack totals for the All sets tab before the full external catalog is loaded.

NOTES:
- Preserved lazy external icon loading and SVG preview generation; this pass only changes search/count behavior and static count presentation.

[2026-05-14] - Font Panel Search And Counts

CHANGED:
- `crates/font_panel` now matches font searches by all query words across system and Google fonts, so searches like `inter tight` and `source code` do not require exact phrase order.
- `crates/font_panel` now reports total matches against the active All, System, or Web tab instead of showing only the capped visible row count against the all-font denominator.

NOTES:
- Preserved font preview/apply/project insertion behavior; this pass only changes panel search and count presentation.

[2026-05-14] - Media Panel Search And Counts

CHANGED:
- `crates/media_panel` now matches media searches by all query words across local assets and direct remote assets, so searches like `space background` and `open movie video` do not require exact phrase order.
- `crates/media_panel` now reports count totals against the active Images, Videos, Audio, or All tab instead of using the full unfiltered media denominator.

NOTES:
- Preserved media insertion, WebPreview preview generation, and the stable WebPreview input path.

[2026-05-14] - Native UI Preview Polish

CHANGED:
- `crates/shadcn_ui_panel` no longer shows implementation/status copy inside screenshot-backed UI previews.
- `crates/shadcn_ui_panel` now renders screenshot-backed previews with contain-fit sizing so larger dashboard/login screenshots are not cropped.

NOTES:
- Preserved the panel catalog loading path and WebPreview input behavior; this pass only changes generated UI preview HTML.

[2026-05-14] - Screenshot-Backed UI Panel Previews

CHANGED:
- `crates/shadcn_ui_panel` now uses local shadcn public/registry screenshots for common dashboard, login, button, card, sidebar, calendar, authentication, playground, and task-style preview pages when those assets are available.

NOTES:
- This keeps the UI panel preview upgrade on the existing static/local asset path without adding registry scans to the panel-open hot path or touching WebPreview input routing.

[2026-05-14] - Right Panel Tab Paging And Fast UI Catalog

CHANGED:
- `crates/media_panel` now ships more direct remote media rows across open images, videos, and audio samples before the provider-browser handoff.
- `crates/media_panel` now uses an ASCII metadata separator in remote media rows and generated remote-browser cards.
- `crates/shadcn_ui_panel/src/shadcn_catalog_index.tsv` now embeds the generated local shadcn/Magic catalog so the UI panel can show local entries immediately without scanning registry files on open.
- `crates/shadcn_ui_panel` now treats the static or cached catalog as loaded at startup, keeping heavier registry discovery out of the normal panel-open path.
- `crates/icon_picker/src/icon_pack_index.tsv` now includes representative icon names per pack.
- `crates/icon_picker` now loads external icon metadata by selected pack or broad search instead of scanning every external icon pack when the panel opens.
- `crates/icon_picker`, `crates/font_panel`, `crates/media_panel`, and `crates/shadcn_ui_panel` now page their horizontal tab strips with explicit previous/next icon buttons.
- `crates/icon_picker` no longer renders a bottom loaded/status strip; status now appears in the top count/status area.
- `crates/font_panel` no longer renders the sample text editor or per-row sample sentence in the panel body.
- `crates/font_panel`, `crates/media_panel`, and `crates/shadcn_ui_panel` now also keep status in the top count/status area instead of a bottom footer.
- `crates/shadcn_ui_panel` now opens from the rkyv/memmap cache when available and falls back to a static catalog before any heavier local catalog discovery.

NOTES:
- Preserved the current working WebPreview input path and normal editor input/caret behavior; this pass only touches the new right-panel UI.
- Ran `just run`; Cargo finished the debug build and launched `G:\Zed\target\debug\zed.exe` once. The Codex tool timed out afterward because the Zed app remained open for manual QA.

[2026-05-14] - Right Panel Dock And Preview Follow-Up

CHANGED:
- `crates/web_preview` no longer draws the WebPreview tab-content bottom border, and its document file handler now covers common Word, Excel, PowerPoint, OpenDocument, Apple iWork, ebook, PDF, CSV, and TSV formats.
- `crates/title_bar` now marks top-right panel buttons with the accent selected style when the matching right panel is open.
- `crates/icon_picker`, `crates/font_panel`, `crates/media_panel`, and `crates/shadcn_ui_panel` now hide their bottom-right dock buttons so the new panels are opened from the top-right titlebar only.
- `crates/icon_picker` no longer renders the selected-icon Copy/Insert strip above the icon grid.
- `crates/font_panel` now keeps action buttons inside the selected font row instead of a separate top preview/action block.
- `crates/media_panel` now renders direct remote media rows in the panel content and keeps the remote provider browser available as a content row instead of a top source section.

NOTES:
- Preserved the existing WebPreview input/focus routing and normal editor input path.

[2026-05-14] - Right Panel Polish And Viewer Cleanup

ADDED:
- `crates/icon_picker/src/icon_pack_index.tsv` stores the external icon-pack names/counts/sizes as compile-time data so pack tabs render immediately without directory discovery.
- `crates/shadcn_ui_panel` now writes and reads a compact rkyv catalog cache through memmap2 under `target/shadcn-ui-panel`.

CHANGED:
- `crates/title_bar` now uses an icon glyph for the icon picker action and labels the shadcn panel action as UI.
- `crates/icon_picker` now starts with static pack metadata, keeps external pack filters scrollable, and uses a denser icon tile grid.
- `crates/media_panel` and `crates/web_preview` generated viewer pages now fill the tab for video/image/document previews and remove the header/source cards and dotted-grid style.
- `crates/font_panel`, `crates/media_panel`, and `crates/shadcn_ui_panel` now use horizontally scrollable tab strips to avoid right-panel tab overflow.
- `crates/shadcn_ui_panel` now presents registry-directory and 21st.dev entries with concise source labels instead of generated-looking descriptions.

NOTES:
- Preserved the current working WebPreview input path and normal editor input/caret behavior; this pass only touches right-panel chrome, static catalog loading, and generated preview pages.
- Ran `just run`; it rebuilt and launched `G:\Zed\target\debug\zed.exe`. The command timed out afterward because the Zed app remained open for manual QA.

[2026-05-14] - WebPreview File Viewers And Right Panel Follow-Up

ADDED:
- `crates/web_preview` now registers media/document project items so video, audio, PDF, EPUB, and Office-style files open in WebPreview-backed editor tabs.
- `crates/web_preview` now generates theme-matched local viewer pages under `target/web-preview-files` for media/document files opened from the project tree.
- `crates/media_panel` now has a Browse Remote action that opens a WebPreview remote-media browser backed by no-key Openverse, Wikimedia Commons, and NASA API searches plus all configured provider links.
- `crates/font_panel` now includes an editable sample text field that drives panel row previews and generated WebPreview font specimens.

CHANGED:
- `crates/icon_picker` now caches the external Iconify catalog for the process, uses a compact tab strip for icon packs, renders real external SVGs when a pack or query is active, and uses a denser icon grid.
- `crates/shadcn_ui_panel` now caches the discovered shadcn/ui catalog for the process so reopening the panel avoids repeated local registry scans.
- `crates/media_panel` generated media preview pages now use a shadcn-style scroll area instead of the browser's default body scroll.
- `crates/workspace/src/pane.rs` exposes the owning workspace handle for project-item integrations that need workspace context.

NOTES:
- Preserved the current working Windows WebPreview input/focus path; this pass adds file-preview integration without changing native WebView event routing.

[2026-05-13] - Right Panel Quality Pass

CHANGED:
- `crates/shadcn_ui_panel` now includes shadcn registry examples in the native catalog, allowing chart/example entries to install through the same manifest-backed flow as components and blocks.
- `crates/shadcn_ui_panel` now treats shadcn public registry manifests as valid install sources, so entries without mirrored source files can still install through the native manifest installer.
- `crates/editor/src/items.rs` now creates unique destination filenames when copying media into a project, preventing different files with the same basename from silently reusing an old asset.
- `crates/media_panel` Refresh now re-reads the current workspace media roots before indexing, so new worktrees or media folders are discovered without reopening Zed.
- `crates/shadcn_ui_panel` now has a catalog Refresh action for reloading updated local shadcn/ui, block, and Magic UI inspiration sources.
- `crates/icon_picker` now shows filtered/total result counts and a polished no-match state instead of an empty grid.
- `crates/icon_picker` now has a selected-icon preview strip with explicit Insert and Copy Name actions backed by the system clipboard.
- `crates/icon_picker` can now copy the selected icon SVG markup directly to the clipboard.
- `crates/shadcn_ui_panel` rows now include a Copy action that places the import plus JSX snippet on the clipboard.
- `crates/shadcn_ui_panel` rows now include a Docs action separate from local Preview.
- `crates/font_panel` can now copy CSS snippets for selected system or web fonts to the clipboard.
- `crates/media_panel` can now copy local media paths and direct media URLs from the panel.
- `crates/editor/src/items.rs` now supports direct media URL insertion with React/Markdown/HTML/CSS-aware output.
- `crates/media_panel` now detects image, video, and audio URLs in the search box and offers direct URL insertion into the active editor.
- `crates/media_panel` URL detection now falls back to the active Images/Videos/Audio filter for extensionless CDN URLs.
- `crates/media_panel` now generates local WebPreview media pages under `target/media-previews` for local assets and direct URLs before insertion.
- `crates/media_panel` now has a Refresh action so workspace media can be re-indexed after files are added.
- `crates/font_panel` now shows loading, filtered/total counts, source filters for System/Web fonts, and a no-match state while keeping font preview/apply behavior on the existing settings path.
- `crates/font_panel` now supports custom web-font names from the search box, generating Google Fonts family queries and CSS variable names without requiring a new code entry.
- `crates/font_panel` now creates local browser-rendered font specimen pages under `target/font-previews` and opens them through WebPreview for true web-font testing.
- `crates/font_panel` can now add curated web fonts to the active project's global CSS by writing a Google Fonts import and a reusable `--font-*` CSS variable.
- `crates/media_panel` now has native All/Image/Video/Audio filters with live counts, plus loading and no-match states for the indexed local media list.
- `crates/shadcn_ui_panel` now reports filtered/total catalog counts consistently with the other right dock panels.
- `crates/shadcn_ui_panel` generated preview pages now include theme-matched visual component/block specimens before the insert snippet and source code.

NOTES:
- Ran targeted `rustfmt` on the touched right-panel Rust files.
- Did not run `just run`; this pass avoids expensive runtime validation until the full panel batch is ready.

[2026-05-13] - Native shadcn/ui Panel First Pass

ADDED:
- `crates/shadcn_ui` adds a native installer for source-backed shadcn/ui assets, import rewriting, project layout, `components.json`, `lib/utils.ts`, and common package dependencies.
- `crates/shadcn_ui_panel` adds a native right-dock shadcn/ui catalog panel with searchable official components, shadcn blocks, and Magic UI entries.
- `crates/shadcn_ui_panel` now discovers cloned shadcn registry manifests and Magic UI `.tsx` entries so the panel includes all available local registry UI/block assets beyond the curated starter list.
- `crates/shadcn_ui_panel` adds source filters with live counts for All, UI components, Blocks, and Magic UI plus an empty state for no-match searches.
- `crates/shadcn_ui_panel` now generates local source-backed WebPreview pages under `target/shadcn-previews` for Preview actions, with docs fallback when source is unavailable.
- The top-right titlebar hidden-feature actions now include a real shadcn/ui button that toggles the new panel.
- `crates/workspace/src/pane.rs` adds a `DraggedShadcnAsset` payload and pane-level drag/drop routing into active editor items.
- `crates/editor/src/items.rs` can insert shadcn assets into an active project through the new installer crate.
- `G:\Zed\inspirations` now includes cloned reference sources for shadcn/ui, Magic UI, and 21st.dev Magic MCP.

CHANGED:
- shadcn/ui previews open through the Windows WebPreview path when available, with external URL fallback on other platforms.
- shadcn registry imports are rewritten from the upstream registry paths into normal project paths like `@/components/ui`, `@/components/blocks`, and `@/hooks`.
- shadcn installs now prefer official `public/r/styles/new-york-v4/*.json` manifests, recursively installing registry dependencies and adding package dependencies from the manifest instead of copying every UI component blindly.
- Auto-discovered shadcn/ui, chart/block, and Magic UI catalog entries now derive their primary import component from real source exports, so entries like `input-otp`, `button-group`, and `rainbow-button` use the correct React identifiers.
- shadcn chart, example, and internal registry files now map into project `components/blocks` paths instead of being skipped by the installer.
- The shadcn/ui panel header is now more work-surface-like and avoids instructional filler text.
- shadcn installs now detect or create a global CSS file and append a marked shadcn theme block only when the target project does not already define shadcn variables.
- Existing `components.json` files now get missing `tailwind.css`, `cssVariables`, and alias fields filled without overwriting explicit user configuration.
- `crates/shadcn_ui` now returns an install report with the installed path, added dependency count, and theme-CSS write state for better panel/editor feedback.
- shadcn install reports now detect the target project's package manager from lockfiles and include the right install command when dependencies were added.

FIXED:
- Fixed scoped npm package parsing during dependency insertion so specs like `@radix-ui/react-slot@1.2.3` do not become malformed package names.

NOTES:
- Ran targeted `rustfmt` on the touched Rust files.
- Did not run `just run`; this pass stayed within the repo rule to avoid runtime validation until the full change set is ready for user testing.

[2026-05-13] - Icon Picker Sidebar Work

ADDED:
- Added `crates/icon_picker`, a right-dock icon picker panel backed by the existing `crates/icons` icon registry and bundled SVG assets.
- Added workspace/editor drag payload routing so icon tiles can be dropped into the active editor.
- Added React/JSX icon insertion that writes selected SVG assets into the current project and inserts a component import/usage.
- Added `crates/font_panel`, a right-dock font panel backed by Zed's cached system font family list.
- Added top-right title bar buttons for opening the icon picker and font panel directly.
- Added font search, per-font preview rows, and actions to apply the selected font to editor buffers or the Zed UI.
- Added `crates/media_panel`, a right-dock media search panel that indexes real media assets while excluding tool/plugin media folders.
- Added free media source launchers for Openverse, Wikimedia Commons, Unsplash, Pexels, and Pixabay using the panel query.
- Added editor drag/click insertion for media assets, copying selected media into project `assets/media` folders before inserting the right reference for React, Markdown, HTML, CSS, or plain text.

NOTES:
- Non-React buffers receive the raw SVG at the editor cursor.
- React `.tsx`/`.jsx` buffers generate project SVG files under `src/assets/icons` when a `src` folder exists, otherwise under `assets/icons`.
- The font panel writes through the existing user settings update path instead of maintaining a separate font preference store, and warms Zed's font cache in the background.
- The media panel indexes `inspirations/media`, `media`, `assets/media`, and `public/media` roots from visible worktrees, while skipping tool-ish folders such as `mcp`, `tools`, `target`, and cache/runtime folders.
- The implementation does not touch the WebPreview input path.

[2026-05-13] - Inspiration Source Import

ADDED:
- `inspirations/` now contains source/reference folders copied from `G:\Workspaces\hexed\archeive\integrations\tool-tui` and `G:\Workspaces\flow`.
- `inspirations/metasearch` now preserves the newer Flow metasearch workspace, including `crates/metasearch-core`.

CHANGED:
- Duplicate top-level inspiration folders such as `crates`, `docs`, and `extensions` now use the Flow versions instead of the older tool-tui versions.
- The import excludes generated/runtime-heavy folders and files such as `.git`, `target`, `tmp`, `trash`, `models`, logs, local `.env` files, audio recordings, and the oversized 98 MB `fluent-emoji.json` icon data asset.

NOTES:
- Committed and pushed the local llama.cpp model switching/speed checkpoint before copying inspiration sources.
- Verified Flow `inspirations/crates/flow-browser-core` exists and the older tool-tui duplicate `inspirations/crates/agent` is absent.
- Verified unique tool-tui folders such as `inspirations/apps` and `inspirations/mcp` exist.

[2026-05-13] - Local llama.cpp Speed and Model Switching

ADDED:
- `assets/settings/default.json` now includes Qwen 3.5 4B Q4_K_M and official Qwen 3 4B Q4_K_M local selector entries with G-drive model paths and Hugging Face auto-download metadata.

CHANGED:
- `assets/settings/default.json` now tunes the default Qwen 3.5 0.8B model to a 4096-token context and 384-token output cap so the tiny local model is not fed huge editor context by default.
- `crates/language_models/src/provider/llama_cpp.rs` now warms a freshly-started llama-server with a one-token completion before the first user request.
- `crates/language_models/src/provider/llama_cpp.rs` keeps exact llama.cpp `/tokenize` checks before completion so the smaller fast Qwen context still trims oversized Zed prompts safely.

FIXED:
- `crates/language_models/src/provider/llama_cpp.rs` now stops a Zed-managed local llama-server from `G:\Zed\tools\llama.cpp` when the selected AI panel model changes, so choosing Gemma no longer fails just because Qwen is already running on port 8080.

NOTES:
- Scanned G: for Qwen GGUF files; only the existing Qwen 3.5 0.8B GGUF and Flow's Qwen 3 0.6B GGUF were found, so the new 4B selector entries are configured to download into `G:\Zed\models\llama.cpp` when selected.
- Measured Qwen 3.5 0.8B via llama.cpp CLI/server at 4096 context: warmed tiny prompt completion was about 0.5s with roughly 38 output tokens/sec.
- Started Gemma 4 E4B directly with llama-server at 8192 context and verified a native `/apply-template` plus `/completion` request returned `gemma-ok`.

[2026-05-13] - Local Qwen llama.cpp Reliability

CHANGED:
- `crates/language_models/src/provider/llama_cpp.rs` now reuses an already-running same-model llama.cpp server instead of failing when the running server was launched with different context or slot settings.
- `crates/language_models/src/provider/llama_cpp.rs` now reads the actual running llama-server context window from `/props` before sending a completion.
- `crates/language_models/src/provider/llama_cpp.rs` now token-counts rendered prompts through llama.cpp `/tokenize` and trims older chat/context history before sending prompts that would exceed the local model window.

FIXED:
- Prevented local Qwen requests from failing in the code editor when a stale llama-server is already running with a smaller context than Zed's configured default.
- Prevented oversized local prompts from surfacing as hard llama.cpp context errors when Qwen can still answer with the newest relevant context.

NOTES:
- Verified the running Qwen server at `http://127.0.0.1:8080` has `Qwen3.5-0.8B-GGUF` loaded from `G:/Zed/models/llama.cpp/Qwen3.5-0.8B-Q4_0.gguf`.
- Smoke-tested the running server directly with `/apply-template`, `/completion`, and `/tokenize`; it returned `qwen-ok`.
- Ran `rustfmt` on the touched Rust file.

[2026-05-13] - Checkpointed Editor Speed and WebPreview Loading

ADDED:
- Pushed checkpoint branch `codex/checkpoint-working-2026-05-13` from the confirmed working `dev` state before making new edits.

CHANGED:
- Renamed stale branch `web-preivew` to `depricated-web-preview` locally and on `origin`, preserving the branch history under the deprecated naming pattern.
- `crates/editor/src/selection.rs` now throttles deferred selection-dependent refresh work by 75ms instead of running it on the next frame after typing.
- `crates/web_preview/src/web_preview_view.rs` now treats initial/open/reload/navigation as a real loading state and only removes the loading placeholder after navigation completes.
- `crates/web_preview/src/web_preview_view.rs` now starts Windows native preview mounting hidden during workspace attachment so the browser can warm up before the tab is shown.
- `crates/web_preview/src/windows_visual_webview.rs` now emits navigation-started events and respects the requested initial visibility when creating the WebView2 surface.

FIXED:
- Reduced editor typing and large input latency risk by moving noncritical code actions, highlights, outline, prediction, and blame refresh work out of the immediate paint path.
- Prevented the WebPreview transparent gap by keeping an opaque loading layer up until the embedded browser reports that navigation completed.

NOTES:
- WebPreview input/focus/passthrough routing was intentionally left unchanged.
- Ran `rustfmt` on the touched Rust files.
- Ran `git diff --check` on the touched files; only the repository's normal LF/CRLF checkout warnings were reported.
- Ran `just run`; Zed launched and exited successfully.

[2026-05-13] - Text Input Hot Path Speed Pass

CHANGED:
- Created and pushed `depricated-2` from the existing `web-preivew` branch without deleting the original branch name.
- `crates/editor/src/input.rs` no longer refreshes linked-edit ranges immediately after normal text input because the deferred selection refresh path already performs that work.
- `crates/editor/src/editor.rs` now treats paste/bulk text insertion like a paint-first edit path, deferring noncritical selection refreshes and skipping immediate completion/edit-prediction refresh work.
- `crates/editor/src/editor.rs` now avoids splitting clipboard text into lines unless there are multiple selections that could use line-distributed paste.

FIXED:
- Reduced duplicate work on every typed character after the previous deferred-refresh editor changes.
- Reduced latency risk when pasting or inserting larger text chunks into code files.

NOTES:
- No branches were deleted in this pass.
- WebPreview code was intentionally left unchanged.
- Ran `rustfmt` on the touched editor Rust files.
- Ran `git diff --check` on the touched files; only the repository's normal LF/CRLF checkout warnings were reported.
- Ran `just run`; Cargo rebuilt the affected crates successfully in 4m 52s and launched Zed once.
- The known post-launch `zed-cli` lookup message remains.

[2026-05-13] - Dx Selection and Bulk Editor Input

CHANGED:
- `assets/themes/dx/dx.json` now uses the Dx green primary color for local editor text selection in both dark and light variants.
- `crates/editor/src/editor.rs` now keeps the always-visible cursor fallback to full code editors while requiring real focus for single-line input-box carets.
- `crates/editor/src/input.rs` now treats multi-character/newline text insertion as bulk input and skips single-keystroke-only bracket, emoji, linked-edit, completion, on-type-formatting, and JSX auto-close work.

FIXED:
- Prevented unfocused input boxes from all showing a text caret after the previous code-editor caret visibility fix.
- Reduced latency risk when inserting or pasting larger chunks of text into code files.

NOTES:
- Pushed the current working WebPreview/editor baseline to `origin/dev` at `87831af7cb` before these new scoped edits.
- WebPreview code was intentionally left unchanged in this pass.
- Ran `rustfmt` on the touched editor Rust files.
- Parsed `assets/themes/dx/dx.json` successfully with PowerShell `ConvertFrom-Json`.
- Ran `git diff --check` on the touched files; only the repository's normal LF/CRLF checkout warnings were reported.
- Ran `just run`; Cargo rebuilt the affected crates successfully in 3m 03s and launched Zed once.
- The known post-launch `zed-cli` lookup message remains.

[2026-05-13] - Editor Typing Paint-First Fix

CHANGED:
- `assets/settings/default.json` now disables editor caret blinking by default so the code caret stays visible while typing.
- `assets/settings/default.json` now defaults `hide_mouse` to `never` so the mouse pointer does not disappear during code typing.
- `crates/editor/src/editor.rs` now paints local cursors for normal editable editors without depending on blink/focus overlay state.
- `crates/editor/src/selection.rs` now makes the caret visible before selection-dependent refresh work and can defer noncritical local selection refreshes to the next frame.
- `crates/editor/src/input.rs` now uses the deferred editor refresh path for normal text insertion and newline selection updates.
- `crates/editor/src/completions.rs` now coalesces completion/edit-prediction refreshes after text input onto the next frame.

FIXED:
- Reduced code-editor typing latency by letting buffer edits and caret painting complete before follow-up completion, prediction, highlight, outline, and code-action refresh work.

NOTES:
- WebPreview code was not changed in this editor-only pass.
- Ran `rustfmt` on the touched editor Rust files.
- Ran `git diff --check` on the touched editor files; only the repository's normal LF/CRLF checkout warnings were reported.
- Ran `just run`; Cargo rebuilt the affected crates successfully in 4m 20s and launched Zed once.
- Ran `just run` again after the stronger cursor visibility path; Cargo rebuilt the affected crates successfully in 2m 45s and launched Zed once.
- The known post-launch `zed-cli` lookup message remains.

[2026-05-13] - Topbar Trim and WebPreview Native Bounds Input

CHANGED:
- `crates/ui/src/utils/constants.rs` now uses a 40px Windows titlebar height instead of the previous oversized 44px value.
- `crates/title_bar/src/title_bar.rs` trims the screen dock body from 33px to 31px so it fits the slimmer topbar.
- `crates/gpui_windows/src/events.rs` now routes WebView2 hover, click, right-click, wheel, and captured pointer input by the registered native preview bounds.
- `crates/gpui_windows/src/window.rs` now uses the same native preview bounds for WebView2 drag/drop targeting.

FIXED:
- Restored WebPreview interactivity when GPUI pane/container hitboxes blocked the generic passthrough snapshot even though the native WebView2 preview was visible.

NOTES:
- Ran `rustfmt` on the touched Rust files.
- Ran `git diff --check` on the touched files; only the repository's normal LF/CRLF checkout warnings were reported.
- Ran `just run`; Cargo rebuilt the affected crates successfully in 2m 13s and launched Zed once.
- The known post-launch `zed-cli` lookup message remains.

[2026-05-13] - Restore WebPreview as Dev

CHANGED:
- Renamed the fresh upstream-only `dev` branch to `depricated-1`.
- Pushed `depricated-1` to `origin/depricated-1`.
- Recreated local `dev` from the saved `web-preivew` branch so WebPreview work is current again.

NOTES:
- The previous WebPreview branch is still present as `web-preivew` for safety until the restored `dev` is fully confirmed.

[2026-05-13] - Editor Tab Hot Path and WebPreview Event Pump

CHANGED:
- `crates/workspace/src/pane.rs` now only asks for a tab's `WorkspaceScreenKind` when that tab is active, so normal code tabs do not pay for WebPreview-specific active-tab styling during every tab render.
- `crates/title_bar/src/title_bar.rs` now reuses the already-computed active screen kind for the screen-dock add action instead of querying active workspace state again in the same render.
- `crates/web_preview/src/web_preview_view.rs` now keeps a separate native mount task and a lightweight browser-event pump, so queued WebView2 URL/title/navigation events wake the preview promptly without replacing the pump during mount.

FIXED:
- Reduced the custom tab/titlebar work that could make code-editor tab rendering and typing feedback feel delayed.
- Fixed a WebPreview focus recovery gap where navigation-completed events could remain queued until an unrelated render, making page inputs feel unfocused after moving to a new page until reload.

NOTES:
- Ran `rustfmt` on the touched Rust files.
- Ran `git diff --check` on the touched files; only the repository's LF/CRLF checkout warnings were reported.
- Ran `just run`; Cargo rebuilt the affected crates in 3m 12s and launched Zed once.
- The latest runtime log shows only the recurring AMD AGS driver-info lookup, the known Windows events line, and close-time `window not found` messages after the app exited.

[2026-05-09] - Branch and Build Environment Setup

ADDED:
- `todo.txt` to track the current Zed branch/setup state.
- `changelog.txt` to document branch, merge, and local build-environment changes.

CHANGED:
- `.cargo/config.toml` now pins Cargo build output to `G:/Zed/target`.
- `.cargo/config.toml` now uses 4 Cargo jobs for the verified 12-logical-CPU, 24GB RAM machine.
- `justfile` now describes the upgraded G-drive build profile instead of the old <8GB low-memory profile.
- Local `main` now matches `upstream/main` at `de14e3fcad`.
- Local `dev` is now the active working branch and includes a merge from refreshed `main`.
- `origin/main` was fast-forwarded to match upstream `main`.
- `origin/dev` was created from local `dev` and local `dev` now tracks it.

FIXED:
- Resolved merge conflicts in `Cargo.lock`, agent UI, GPUI, Windows event handling, sidebar, title bar, REPL remote kernels, and workspace files.

NOTES:
- No full local build/test was run because this repo's instructions reserve that for `just run`, and the user will run it when ready.
- The pre-existing `hexed/CHESS.md` working-tree edit was preserved and restored after the branch merge.

[2026-05-09] - Local Zed Run Fixes

CHANGED:
- Updated GPUI/title bar/sidebar/web preview compatibility code paths after merging current upstream `main`.
- Updated REPL remote kernel websocket imports for the current async-tungstenite API.
- Added the missing sidebar dependency on `vim_mode_setting`.
- Updated `todo.txt` to reflect the successful local run.

FIXED:
- Fixed compile errors from stale `Corner` anchors by moving affected call sites to `Anchor`.
- Fixed ACP content references in web preview crates by using the protocol schema module.
- Fixed sidebar activation calls for the current `MultiWorkspace::activate` signature.
- Fixed sidebar event matching for the current multi-workspace and agent-panel event shapes.
- Fixed the sidebar render tree around the list container and scrollbars.

NOTES:
- `cargo check -p zed --bin zed --keep-going` finished successfully after the batch fixes, with warnings only.
- `just run` built and launched the Zed editor successfully on the upgraded G-drive machine.
- The run still prints non-blocking warnings, including a missing `zed-cli` lookup message after launch.
- The pre-existing `hexed/CHESS.md` edit remains preserved and intentionally unstaged.

[2026-05-09] - Titlebar Polish

CHANGED:
- `crates/platform_title_bar/src/platforms/platform_windows.rs` now stretches Windows caption buttons to the full titlebar height instead of a fixed 32px strip.
- `crates/title_bar/src/title_bar.rs` no longer renders the duplicate screen-dock separator before the add/list action group.
- `todo.txt` now records the successful topbar polish run.

NOTES:
- `just run` completed and launched Zed again after the titlebar changes.
- Existing non-blocking warnings and the post-launch `zed-cli` lookup message remain.

[2026-05-09] - Faster Single-Launch Local Run

CHANGED:
- `justfile` now builds `zed.exe` with `cargo build -p zed --bin zed` before launching the debug binary once.
- `.cargo/config.toml` now uses 6 Cargo jobs for the upgraded 12-logical-CPU, 24GB RAM machine.
- `justfile` help/status text now reports the 6-worker local build profile.

FIXED:
- Fixed the `just run` double-launch behavior caused by `cargo run` launching Zed once and the recipe launching `target/debug/zed.exe` again after the first close.

NOTES:
- This is a local build/run workflow change only; no Zed source code was changed.
- First `just run` after the fix built and launched Zed once, then exited after the window was closed.
- Second no-change `just run` finished Cargo's build step in 2.53 seconds and launched Zed once.
- The existing non-blocking `zed-cli` lookup message and sidebar/agent UI warnings still remain.

[2026-05-09] - Web Preview Input and Tab Chrome Hardening

CHANGED:
- `crates/ui/src/components/tab.rs` now supports keeping a bottom border on selected tabs when a caller requests it.
- `crates/workspace/src/pane.rs` enables that selected bottom border for active Browser-screen tabs so web preview tabs stay visually separated from colorful pages.
- `crates/web_preview/src/web_preview_view.rs` now tracks URL-editor focus intent separately from native WebView keyboard focus so tab URL input does not get stuck mixing with WebView input.
- `crates/gpui_windows/src/events.rs` now forwards client-area double-clicks into the WebView2 composition controller and focuses WebView2 on mouse down.

FIXED:
- Reduced cursor/hover lag by removing redundant WebView2 mouse-move forwarding from `WM_SETCURSOR`.
- Prevented browser right-click context messages over the preview from falling back into Zed chrome handling.

NOTES:
- This is the first reliability slice from the larger web preview plan; DevTools, profiles, responsive mode, and agent browser tools remain future work.
- `just run` initially caught one Windows API result-handling typo in the new context-menu path; that was fixed before final verification.
- `just run` then completed successfully and launched Zed after rebuilding the affected crates.

[2026-05-09] - Project Panel Hover and WebView Focus Responsiveness

CHANGED:
- `crates/project_panel/src/project_panel.rs` now shows direct folder file counts from the in-memory worktree snapshot in the hover badge.
- `crates/project_panel/src/project_panel.rs` now keeps the hover badge lighter and offset to the right while still showing file sizes for files.
- `crates/web_preview/src/windows_visual_webview.rs` now skips redundant WebView2 visibility and bounds sync calls when the native preview has not changed.

FIXED:
- `crates/gpui_windows/src/events.rs` now clears stale WebView keyboard focus when GPUI has a real text input handler, preventing browser focus from intercepting normal editor typing.

NOTES:
- The folder hover path remains instant and does not perform recursive filesystem scans.
- `just run` completed successfully and launched Zed after rebuilding `gpui_windows`, `project_panel`, `web_preview`, and dependents.
- Existing non-blocking sidebar/agent UI warnings and the known post-launch `zed-cli` lookup message remain.

[2026-05-09] - Warning Cleanup and Web Preview Passthrough Fix

CHANGED:
- `crates/agent_ui/src/agent_panel.rs` now renders the workspace trust message path again so its field and renderer are not dead code.
- `crates/sidebar/src/sidebar.rs` now removes unused helper code, uses the header new-thread visibility flag, and handles the multi-workspace update result.
- `crates/project_panel/src/project_panel.rs` now caches direct folder file counts and clears that cache on worktree or relevant visibility-setting changes.
- `crates/web_preview/src/web_preview_view.rs` now keeps the native WebView mouse passthrough region registered while the preview is ready instead of depending on GPUI active-window state.
- `crates/web_preview/src/web_preview_view.rs` now hides the native preview on workspace deactivation and removes temporary mount debug logging.
- `crates/web_preview/src/windows_visual_webview.rs` now keeps the passthrough registry consistent even when visibility did not change.

FIXED:
- Removed the Rust warnings that were printed by `agent_ui` and `sidebar` during `just run`.
- Fixed a stale/fragile WebView2 mouse passthrough path that could leave hover, click, right-click, and wheel events routed to GPUI instead of the native page.
- Reduced editor typing lag risk by caching folder count badges and keeping hidden/inactive native preview state from lingering.

NOTES:
- `just run` completed successfully with no Rust warnings in the Cargo output.
- The known post-launch `zed-cli` lookup message remains, but it is not a Rust warning and did not stop Zed from launching.

[2026-05-10] - Editor Typing Hot Path and Caret Visibility

CHANGED:
- `crates/editor/src/editor.rs` now keeps local editor cursors visible when focus is inside an editor descendant, matching how input can be mediated through child focus handles.
- `crates/gpui_windows/src/platform.rs` now sends keydown/syskeydown messages for GPUI-owned windows through the GPUI accelerator first, even if a WebView focus flag is set.
- `crates/gpui_windows/src/events.rs` now treats WebView keyboard focus as inactive whenever GPUI currently has a text input handler.
- `crates/title_bar/src/title_bar.rs` now avoids collecting active pane screen entries during normal Editor, Browser, and Terminal renders; it only builds that list for non-standard screen kinds or when the screen-list menu opens.
- `crates/editor/src/blink_manager.rs` now skips blink-resume timer scheduling while `cursor_blink` is disabled.
- `assets/settings/default.json` now disables caret blinking by default so the insertion caret remains visible during active typing without a custom blink scheduler.
- `assets/settings/default.json` keeps the main-Zed mouse-pointer hiding behavior while disabling hosted edit predictions by default for this local development fork.

FIXED:
- Fixed a caret visibility edge case where a focused editor descendant could make local cursors disappear even though the user was still interacting with the editor.
- Fixed a stale WebView-focus path that could force normal editor keystrokes off GPUI's fast accelerated route, causing delayed text paint and missing local caret feedback.
- Reverted the custom `crates/editor/src/blink_manager.rs` scheduling experiment back to the upstream/main implementation after it made text input feel slower.
- Removed unnecessary caret blink timer work from the no-blink local default, keeping typing paint work closer to the fast path.
- Removed the default per-keystroke hosted edit-prediction path that was producing rate-limited prediction requests during local typing.

NOTES:
- The caret fix now uses settings plus the upstream blink manager instead of adding new editor render timers.
- Ran `just run`; Cargo rebuilt the editor dependency chain in 2m 58s and launched Zed once.
- The fresh log after this run did not show new edit-prediction rate-limit errors; only the existing AMD GPU-driver-info lookup warning and close-time window-not-found noise appeared.
- Ran `just run` again after the Windows keyboard fast-path fix; Cargo rebuilt `editor`, `gpui_windows`, and dependents in 3m 22s and launched Zed once.
- The fresh log after this run still showed no new edit-prediction rate-limit errors after the older 06:17 entries.

[2026-05-10] - Dev Editor Input Lag Investigation

CHANGED:
- `crates/gpui_windows/src/platform.rs` now only runs the synthetic GPUI accelerator bridge for HWNDs owned by GPUI, so WebView2 internal/child key messages are dispatched normally.
- `crates/web_preview/src/windows_visual_webview.rs` now focuses the parent Zed HWND before moving focus from the URL editor back into the WebView2 page.
- `crates/web_preview/src/web_preview_view.rs` no longer creates a hidden startup WebView2 warmup surface for every workspace.
- `crates/gpui_windows/src/events.rs` now treats the native WebView passthrough registry as the source of truth for WebView keyboard focus, instead of cancelling it from stale GPUI text-input state.
- `crates/gpui_windows/src/events.rs` now focuses the parent HWND before moving focus into the WebView2 controller in composition mode.
- `crates/gpui/src/window.rs` now exposes whether the current frame actually painted a mouse-passthrough region.
- `crates/gpui/src/elements/div.rs` now skips interactive mouse-passthrough hitbox bookkeeping on ordinary editor frames that have no native passthrough surface.
- `crates/gpui_windows/src/events.rs` now forwards mouse, wheel, and context-menu input directly to a registered native WebView target whenever the pointer is inside its bounds.
- `crates/gpui/src/window.rs` now lets the preview body's own passthrough hitbox win unless a top hitbox was explicitly marked as an interactive overlay.
- `crates/workspace/src/workspace.rs` now only asks active items for workspace overlays when the active screen kind is `LiquidGlass`, avoiding a mutable no-op item update on every editor frame.
- `crates/gpui/src/window.rs` now sends an empty mouse-passthrough snapshot when no passthrough region was painted, avoiding a full hitbox clone on ordinary editor frames.
- `crates/gpui_windows/src/window.rs` no longer calls `SetWindowRgn(hwnd, None, true)` when updating the mouse-passthrough snapshot.
- `crates/gpui_windows/src/window.rs` now skips redundant empty mouse-passthrough snapshot writes during normal editor frames.
- `crates/gpui_windows/src/window.rs` no longer toggles `WS_EX_LAYERED` for transparent backgrounds and now skips repeated same-value background appearance updates.
- `crates/gpui_windows/src/platform.rs` now always sends keydown messages through the normal GPUI accelerator bridge before the lower-level window handlers decide whether WebView owns keyboard focus.
- `crates/gpui_windows/src/events.rs` no longer handles `WM_KEYDOWN` and `WM_SYSKEYDOWN` directly; those messages now follow the same single accelerator route as the fast main/speed branch.
- `crates/gpui_windows/src/events.rs` returns an unhandled result from the synthetic accelerator when WebView2 truly owns keyboard focus, allowing the original Windows key message to reach the native preview.

FIXED:
- Fixed the deeper WebPreview keyboard failure where the global accelerator loop could swallow WebView2 key messages before WebView2 received them.
- Removed a startup-time editor lag source caused by creating a hidden native WebView2 composition controller shortly after every workspace opened.
- Fixed the remaining WebPreview keyboard-input handoff where a stale editor input handler could clear browser focus right after a preview click.
- Removed another normal editor-frame cost by avoiding per-hitbox passthrough overlay tracking unless a native preview passthrough region exists in that frame.
- Fixed the dead WebPreview interaction path where ordinary GPUI pane/container hitboxes could prevent hover, click, right-click, and wheel messages from ever reaching WebView2.
- Made the native WebView registered bounds the source of truth for Windows mouse forwarding so the browser surface is not visible-but-dead.
- Removed another ordinary editor-frame overhead path where code-edit renders were querying an overlay hook that only Liquid Glass uses.
- Fixed another Windows-only editor lag and invisible-cursor risk by restoring main-style background composition instead of using layered-window alpha for the Zed window.
- Avoided reapplying background composition from web preview renders when the requested background appearance did not change.
- Fixed the deeper per-edit cursor flashing and typing lag risk caused by forcing Windows to refresh the native window region after every rendered frame.
- Removed unnecessary per-frame hitbox snapshot cloning for normal code editor frames.
- Fixed a dev-only editor typing lag path where a stale native WebView focus flag could bypass GPUI's fast editor key handling even after focus had moved back to code files.
- Fixed the remaining dev-only typing lag/caret flashing path caused by duplicate editor keydown dispatch on normal text input.

NOTES:
- The fast `speed` branch did not have this platform-level WebView focus shortcut; the dev branch did.
- The web preview still receives passthrough hit-testing when a preview actually paints a passthrough region.
- WebView keyboard ownership now follows the native preview registry; outside-preview mouse input still clears the registry focus before GPUI receives the event.
- `just run` rebuilt `gpui_windows`, `web_preview`, `gpui_platform`, `sidebar`, and `zed` in 1m 15s and launched Zed after the deeper keyboard/startup-lag fix; the recipe still returned the known post-close `zed-cli` lookup exit code.
- `just run` rebuilt `gpui_windows`, `web_preview`, `gpui_platform`, `sidebar`, and `zed` in 1m 13s and launched Zed successfully with no Rust warnings after the keyboard/editor-frame fix.
- `just run` rebuilt the affected crates in 22.60 seconds and launched Zed successfully after the duplicate-keydown fix.
- `just run` rebuilt the broader GPUI dependency chain in 3m 30s and launched Zed successfully after the frame-time fix.
- `just run` rebuilt the Windows/WebView side in 2m 10s and launched Zed successfully after restoring main-style background composition.
- `just run` rebuilt the workspace dependency chain in 2m 22s and launched Zed successfully after the editor-frame overlay gate.

[2026-05-10] - Temporary Worktree Cleanup

CHANGED:
- Removed the temporary `G:\Zed-speed` worktree.
- Removed the temporary `G:\Zed-forge-run` worktree folder using a long-path-safe Windows path after Git hit a filename-too-long deletion error.
- Pruned Git worktree metadata so only the active `G:\Zed` checkout remains.

NOTES:
- The lightweight `speed` and `forge` branch refs were not deleted; only the disk-heavy worktree folders were removed.

[2026-05-10] - Local Llama.cpp AI Provider

ADDED:
- `crates/language_models/src/provider/llama_cpp.rs` adds a built-in `Llama.cpp` provider for Zed's AI panel.
- `crates/settings_content/src/language_model.rs` adds `language_models.llama_cpp` settings for server paths, G-drive model/cache/tool directories, runtime toggles, and local GGUF model metadata.
- `assets/settings/default.json` configures Gemma 4 E4B Q4_K_M as the normal local default and Qwen 3.5 0.8B Q4_0 as the low-spec default.

CHANGED:
- `crates/language_models/src/language_models.rs` registers the local llama.cpp provider immediately after Zed Cloud so default signed-out setups can choose local AI before remote API providers.
- `crates/language_models/Cargo.toml` now includes the lightweight runtime dependencies needed for local server startup and system-memory detection.
- `todo.txt` now tracks first manual local-agent feedback for the llama.cpp provider.

FIXED:
- Local model selection no longer waits on a remote provider or model list; the AI panel can show the configured local model immediately.
- The local runtime uses `G:/Zed/models/huggingface`, `G:/Zed/models/llama.cpp`, and `G:/Zed/tools/llama.cpp` by default on this Windows/G-drive setup.

NOTES:
- The provider starts `llama-server` on first use, finding it from settings, G-drive tools, PATH, or downloading the latest matching llama.cpp release asset.
- The provider starts models through llama.cpp's Hugging Face `-hf` flow, so model downloads are handled by llama.cpp into the G-drive cache instead of buffering large GGUF files in Zed.
- `just run` rebuilt successfully and launched Zed; the only nonzero exit was the known post-close `zed-cli` lookup message from the local recipe.

[2026-05-10] - Direct Llama.cpp Qwen Default

ADDED:
- Downloaded `G:\Zed\models\llama.cpp\Qwen3.5-0.8B-Q4_0.gguf` for fast local default testing.

CHANGED:
- `assets/settings/default.json` now makes Qwen 3.5 0.8B Q4_0 the normal and low-spec local default, with Gemma 4 E4B retained as an optional configured model.
- `assets/settings/default.json` raises the default local llama.cpp context window to 32768 tokens for Qwen.
- `crates/language_models/src/provider/llama_cpp.rs` now sends local requests through llama.cpp native `/apply-template` and `/completion` endpoints instead of the OpenAI-compatible chat-completions wrapper.
- `crates/language_models/src/provider/llama_cpp.rs` disables local tool-schema support by default so the agent does not send huge tool definitions to small local models.
- `crates/language_models/src/provider/llama_cpp.rs` disables Qwen thinking in the llama.cpp chat template kwargs for faster direct answers.

FIXED:
- Avoided the 8192-token prompt overflow path caused by sending full agent/tool-heavy requests to a small local model.
- Avoided the external-looking OpenAI-compatible local request path; the local provider now uses llama.cpp's native server API.

NOTES:
- Verified `llama-server` running Qwen 3.5 0.8B on `127.0.0.1:8080` with `/v1/health`, `/v1/models`, `/apply-template`, and `/completion`.

[2026-05-10] - Local Model Discovery and Switching

ADDED:
- `crates/language_models/src/provider/llama_cpp.rs` now auto-discovers local `.gguf` files from the configured model directory and G-drive model folders for the AI panel.
- `crates/settings_content/src/language_model.rs` and `crates/language_models/src/settings.rs` add `auto_discover_local_models`.

CHANGED:
- `crates/language_models/src/provider/llama_cpp.rs` now checks the loaded llama.cpp model list before reusing a running server.
- `crates/language_models/src/provider/llama_cpp.rs` restarts its owned llama.cpp child when the selected model/signature changes.
- `assets/settings/default.json` enables local GGUF discovery by default and sets Gemma 4 E4B to a 32768-token configured context.

FIXED:
- Fixed the Gemma selection bug where a healthy Qwen server on port 8080 could be reused even though Gemma was selected in the AI panel.

NOTES:
- Verified Gemma 4 E4B Q4_K_M from `G:\Zed\models\llama.cpp\gemma-4-E4B-it-Q4_K_M.gguf` at 32768 context with llama.cpp native `/apply-template` and `/completion`.

[2026-05-10] - Gemma Local Runtime Handoff

CHANGED:
- `crates/language_models/src/provider/llama_cpp.rs` now treats provider authentication as a no-op because local llama.cpp models do not have credentials.
- `crates/language_models/src/provider/llama_cpp.rs` now launches the exact selected model only when a completion request starts, instead of preloading the provider fallback model during background authentication.
- `C:\Users\Computer\AppData\Roaming\Zed\settings.json` now defaults the agent back to `Qwen3.5-0.8B-GGUF` as requested, while Gemma remains available in the picker.

FIXED:
- Avoided the startup path where background provider authentication could load Qwen on port 8080 before a Gemma-selected thread sent its request.
- Added llama.cpp startup/request log lines so future Qwen/Gemma switching issues show which model the provider is actually launching.

NOTES:
- Direct smoke testing confirmed Gemma 4 E4B Q4_K_M accepts `/apply-template` and streamed `/completion` requests at 32768 context.

[2026-05-10] - Drop-In Local GGUF Models

CHANGED:
- `crates/language_models/src/provider/llama_cpp.rs` now caches discovered local GGUF models on provider state and refreshes that cache when llama.cpp settings change, instead of rescanning model folders on every model-picker render.
- `crates/language_models/src/provider/llama_cpp.rs` now launches `llama-server` with the selected model's `max_tokens` as the context window, so configured and discovered models can use their own limits.
- `crates/language_models/src/provider/llama_cpp.rs` now performs a quick GGUF metadata sniff for discovered model context length, capped by the default local context window unless a sidecar override says otherwise.

ADDED:
- Optional sidecar overrides next to a local model file: `<model>.gguf.json` or `<model>.json` can set `name`, `display_name`, `max_tokens`, `max_output_tokens`, `supports_tools`, `supports_images`, and Hugging Face metadata without editing Rust source.
- The Llama.cpp configuration view now tells users to drop GGUF files into the configured G-drive model folder and optionally add a sidecar for custom names or limits.

FIXED:
- New local models no longer require source edits just to appear in the AI panel or launch with a model-specific context window.

[2026-05-10] - WebPreview Keyboard and Editor Typing Lag Follow-up

CHANGED:
- `crates/gpui_windows/src/platform.rs` now leaves keydown/syskeydown messages addressed to non-GPUI HWNDs alone, so WebView2-owned child/internal windows do not get routed through GPUI's synthetic accelerator.
- `crates/web_preview/src/web_preview_view.rs` now avoids repeating `MoveFocus` into the native WebView2 preview when the preview already owns keyboard focus.
- `crates/sidebar/src/sidebar.rs` now caches sidebar screen-grid entries by screen kind and root path, and clears that cache when sidebar entries refresh.

FIXED:
- WebPreview page inputs should now receive keyboard input after the user clicks into the native preview instead of having those key messages swallowed by the editor window accelerator path.
- Normal editor typing should no longer reread and sort the workspace root through the sidebar grid on every render, which was a plausible cause of delayed text/caret paint in a large `G:\Zed` checkout.

NOTES:
- Microsoft WebView2 composition hosting exposes mouse input forwarding but not a direct `SendKeyEvent` path in the current bindings; keyboard delivery depends on `MoveFocus` plus normal Windows key-message routing.
- Ran `rustfmt` on the touched Rust files and `git diff --check`; only existing LF/CRLF checkout warnings were reported.
- Did not run `just run` in this pass because the repo instruction says to reserve it for final/manual validation unless needed.

[2026-05-10] - Sidebar Cache Import Fix

FIXED:
- `crates/sidebar/src/sidebar.rs` now imports `std::cell::RefCell`, fixing the compile error from the sidebar screen-grid cache field.

NOTES:
- Ran `rustfmt` on `crates/sidebar/src/sidebar.rs` and `git diff --check -- crates/sidebar/src/sidebar.rs todo.txt changelog.txt`; only LF/CRLF checkout warnings were reported.
- Ran `just run` after the import fix; the build finished in 1m 32s and launched `target/debug/zed.exe`.

[2026-05-10] - Titlebar Screen Dock Render Hot Path Fix

CHANGED:
- `crates/title_bar/src/title_bar.rs` now collects active screen-dock entries once per titlebar render instead of scanning the active pane twice.
- `crates/title_bar/src/title_bar.rs` now builds title/tooltip metadata only for the first item of each screen kind and the selected item, instead of materializing metadata for every editor tab on every keystroke-driven render.
- `crates/title_bar/src/title_bar.rs` now skips active-pane screen entry collection entirely for normal Editor, Browser, and Terminal renders; the full list is collected only for non-standard screen buttons or when the list menu is opened.
- `assets/settings/default.json` disables caret blinking by default so the text caret stays visible without adding custom blink scheduling.
- `assets/settings/default.json` keeps main-Zed's `hide_mouse` default so the mouse pointer can still be hidden during typing for performance.
- `assets/settings/default.json` disables automatic edit predictions by default for this local dev fork so typing does not enqueue hosted/rate-limited prediction work on every keystroke.

FIXED:
- Reduced the remaining normal editor typing/caret lag from the custom screen dock doing unnecessary per-tab metadata work during ordinary code editor refreshes.
- Fixed the remaining cursor-visibility complaint through default settings while keeping the editor blink manager on the upstream/main path.
- Removed a per-keystroke AI prediction source from the default local setup; the AI panel/local llama provider remains available, but normal text input now stays editor-first.
- Removed the new unused-parameter warning in the optimized titlebar menu helper.

NOTES:
- Ran `just run`; the build completed in 49.63s and launched `target/debug/zed.exe` for manual typing validation.
- The warning fix was applied immediately after that successful run and validated with `rustfmt` plus `git diff --check -- crates/title_bar/src/title_bar.rs todo.txt changelog.txt`.

[2026-05-10] - Editor Caret Paint Follow-up

CHANGED:
- `assets/settings/default.json` now sets `hide_mouse` to `never` for this local dev fork, so the mouse pointer is not hidden during typing while caret behavior is being stabilized.
- `crates/editor/src/editor.rs` now treats disabled cursor blinking as an always-visible local caret state while editor focus is inside the editor.
- `crates/gpui/src/window.rs` now treats only explicitly marked interactive overlays as blockers for WebPreview mouse passthrough.
- `crates/gpui_windows/src/events.rs` now checks the native WebView focus registry before querying the GPUI text input handler, keeping normal editor key/char/IME paths cheap unless a WebView is actually focused.
- `crates/title_bar/src/title_bar.rs` now keeps hidden-feature actions in the More menu instead of rendering a long row of icon buttons on every titlebar repaint.
- `crates/workspace/src/pane.rs` now only asks Browser items for custom tab-bar controls, avoiding a mutable active-item update during normal editor tab-bar renders.

FIXED:
- Removed the remaining default setting that could make the pointer appear to disappear during code typing.
- Fixed the path where a stale blink-manager `visible=false` state could hide the caret even though local settings had cursor blinking disabled.
- Fixed a WebPreview passthrough fragility where ordinary pane/container hitboxes could still block hover, click, right-click, and wheel even when they were not overlay controls.
- Removed an unnecessary editor-input hot-path query that ran on normal code typing even when no native WebView owned keyboard focus.
- Reduced custom titlebar work during normal editor typing without removing the hidden-feature commands.
- Removed an editor hot-path cost where every code-editor redraw still called the WebPreview custom-tab-control hook on the active editor item.

NOTES:
- Ran `just run` after these fixes. The tool timed out because the Zed GUI remained open.
- Ran `just run` again after the WebPreview passthrough/titlebar-cost fixes. Zed launched and later exited; no Rust compile errors were reported through the run result.
- The current log shows the normal AMD AGS driver-info lookup warning and no new edit-prediction rate-limit errors after this launch.

[2026-05-10] - WebPreview Keyboard Focus and Qwen Local Speed Fix

CHANGED:
- `crates/web_preview/src/windows_visual_webview.rs` now treats `SetFocus` as best-effort and always still calls WebView2 `MoveFocus`, avoiding a Windows null-previous-focus return from blocking browser keyboard ownership.
- `crates/gpui_windows/src/window.rs` now adds `WS_EX_CONTROLPARENT` to normal/dialog GPUI windows, matching the WebView2 Win32 focus pattern needed around embedded controllers.
- `assets/settings/default.json` now launches local Qwen with a 16k context, 768 output tokens, and 8 CPU threads instead of the previous 32k/default slot-heavy setup.
- `crates/language_models/src/provider/llama_cpp.rs` now launches llama-server with one parallel slot and matching generation/batch thread counts for the local single-user AI panel.
- `crates/language_models/src/provider/llama_cpp.rs` now includes context, thread, GPU-layer, and parallel-slot settings in the local server signature.

FIXED:
- WebPreview keyboard focus should no longer fail just because Windows reports no previous focus handle when moving focus from GPUI into WebView2.
- Zed no longer silently reuses a same-model llama-server if it was launched with stale settings like `32768` context and `4` slots.
- Qwen 3.5 0.8B should spend less CPU/RAM on oversized server slots and long default generations.

NOTES:
- Stopped the stale `G:\Zed\tools\llama.cpp\b9090\llama-server.exe` that was still using the old `32768` context and `4` slots.
- Smoke-tested the tuned Qwen launch directly: `/props` reported `16384` context and `1` slot, and a tiny native `/apply-template` + `/completion` request answered `OK` in 165 ms.
- Did not run `just run` yet; this pass used code inspection, local WebView2 docs/source research, `rustfmt`, `git diff --check`, and targeted llama.cpp smoke testing.

[2026-05-11] - WebPreview Keyboard Ownership and Editor Speed Fix

CHANGED:
- `assets/settings/default.json` restores main-style cursor blinking and mouse hiding defaults, while leaving hosted edit predictions disabled for local typing speed.
- `crates/gpui_windows/src/window.rs` now tracks WebView passthrough keyboard focus by the active WebView2 composition controller instead of inheriting a sticky per-window focus flag.
- `crates/gpui_windows/src/platform.rs` now skips the GPUI synthetic key accelerator only when the focused native WebView currently owns keyboard input.
- `crates/web_preview/src/windows_visual_webview.rs` now updates focus, cursor, and target cleanup through controller-specific registry helpers so stale hidden previews cannot overwrite the active target.
- `crates/web_preview/src/web_preview_view.rs` now tracks focus on the preview body without blocking passthrough, so page clicks update GPUI focus and then move focus into WebView2.
- `crates/web_preview/src/windows_visual_webview.rs` now emits a navigation-completed event that lets the active preview refocus the WebView2 page after navigation commits.

FIXED:
- WebPreview page inputs should receive keyboard messages again after clicking inside the native preview.
- WebPreview page inputs should keep keyboard focus after link/search navigation instead of only recovering after reload.
- Normal code editor typing should stay on GPUI's fast key path after switching away from WebPreview, because stale WebView focus is cleared or ignored unless it belongs to the active controller.
- Browser overlay controls, URL input, tabs, hover, click, right-click, and wheel routing keep GPUI/WebView priority separated through the passthrough snapshot.

NOTES:
- Ran `rustfmt` on the touched Windows/WebPreview files.
- Ran `git diff --check` on the touched files; only the repository's existing LF/CRLF checkout warnings were reported.
- Ran `just run`; Cargo built successfully in 4m 57s and Zed launched once, with only the known close-time `zed-cli` lookup message after the app exited.
- Ran `just run` again after the navigation refocus and cursor default follow-up; Cargo rebuilt `web_preview`, `sidebar`, and `zed` in 4m 44s and launched Zed once. The recipe returned the known post-launch `zed-cli` lookup exit code.
- The latest runtime log did not show a new Zed-side panic during the user test; system CPU was very high during follow-up inspection, led by the external `flow`/Codex processes.

[2026-05-14] - Native Right Panel Runtime Integration Pass

CHANGED:
- `crates/icon_picker/src/icon_picker.rs`, `crates/font_panel/src/font_panel.rs`, `crates/media_panel/src/media_panel.rs`, and `crates/shadcn_ui_panel/src/shadcn_ui_panel.rs` now use Zed's current compact button sizing, `IconSize::XLarge`, stable string element IDs, and direct tooltip builders.
- The new panel scroll containers now use stateful element IDs before vertical scrolling, matching GPUI's scroll API expectations.
- Font, media, and shadcn preview buttons now route through the live click-event `Window` and update the workspace directly, instead of trying to call `update_in` from a panel context.
- `crates/media_panel/src/media_panel.rs` now separates first-load indexing from manual refresh so startup can index the already-known media roots without rereading `Workspace` during workspace construction.

FIXED:
- Fixed compile errors in the new icon, font, media, and shadcn/ui panel crates caused by API mismatches with local Zed UI types.
- Fixed the startup panic where the Media panel tried to read `Workspace` while the workspace was already being updated.
- Fixed row rendering borrow issues by materializing panel rows into concrete elements before inserting them into the element tree.

NOTES:
- Ran `just run`; Cargo rebuilt the touched crates successfully in 1m 12s and launched `target/debug/zed.exe` once. The app exited cleanly after closing, with only the known `zed-cli` lookup message in terminal output.

[2026-05-14] - Expanded Native Right Panel Catalogs

ADDED:
- `crates/font_panel/src/google_fonts.rs` now contains the generated Google Fonts family catalog from the public fonts metadata endpoint, so the font panel can list all current families without requiring an API key at runtime.
- `crates/shadcn_ui_panel/src/registry_directory.rs` now contains the public shadcn registry directory, including community registries such as Magic UI, AI Elements, shadcnblocks, and related shadcn-compatible libraries.

CHANGED:
- `crates/icon_picker/src/icon_picker.rs` now loads the external `G:\Assets\icon\data` Iconify-style packs lazily, samples all icon sets in the default grid, and keeps pack filters compact/scrollable so the icon grid can use the panel space.
- `crates/workspace/src/pane.rs` and `crates/editor/src/items.rs` now carry optional external SVG payloads for icon drag/drop and insertion, while preserving built-in Zed icons.
- `crates/font_panel/src/font_panel.rs` now searches the generated Google Fonts list and still supports typed custom Google-style family names.
- `crates/media_panel/src/media_panel.rs` now starts on the Images filter and exposes a larger set of free media source shortcuts.
- `crates/shadcn_ui_panel/src/shadcn_ui_panel.rs` now exposes registry-directory and 21st.dev Magic entries as docs/preview sources, keeps source-backed entries draggable/installable, and renders generated preview pages with a ScrollArea-style source pane plus TypeScript highlighting.

FIXED:
- Fixed the icon pack filter strip so it no longer consumes the panel or fails GPUI scroll API requirements.
- Prevented non-local shadcn registry rows from advertising broken drag-install behavior.

NOTES:
- Verified Openverse, Wikimedia Commons, The Met, and NASA image APIs separately for no-key image discovery, and kept API-key providers as web-search shortcuts.
- Ran `git diff --check` over the touched panel/editor/workspace files; only existing LF/CRLF checkout warnings were reported.
- Ran `just run`; Cargo rebuilt the new panel crates successfully in 3m 40s, launched Zed once, and returned with only the known `zed-cli` lookup message.

[2026-05-14] - Right Panel Catalog Polish Follow-up

CHANGED:
- `crates/font_panel/src/font_panel.rs` now renders a compact `Aa Bb Cc 123` specimen inside each font row, so users can judge system and Google fonts directly from the list without restoring the removed top sample block.
- `crates/workspace/src/pane.rs`, `crates/icon_picker/src/icon_picker.rs`, and `crates/editor/src/items.rs` now carry external Iconify width as well as height through icon drag/click insertion so generated SVG files keep correct non-square viewBoxes.
- `crates/icon_picker/src/icon_picker.rs` now shows compact pack-initial badges for unloaded external icon-pack representatives instead of a generic placeholder, keeping all hardcoded icon sets visually distinct without loading every large pack body.
- `crates/media_panel/src/media_panel.rs` now renders direct remote video rows as compact player cards and audio rows as waveform cards instead of using one generic icon placeholder.
- `crates/media_panel/src/media_panel.rs` now includes additional verified direct remote rows from NASA, Blender Open Movie, and SoundHelix for immediate image, video, and audio coverage.
- `crates/media_panel/src/media_panel.rs` now gives direct remote media assets explicit search tags so built-in open images, videos, and audio samples match natural media/design queries immediately.
- `crates/media_panel/src/media_panel.rs` now routes remote provider links through image, video, or audio search templates based on the active media tab and hides providers that do not support the selected category.
- `crates/media_panel/src/media_panel.rs` now shows the number of remote providers available for the active media tab in the provider browser row.
- `crates/shadcn_ui_panel/src/shadcn_ui_panel.rs` now seeds the rkyv/memmap catalog cache from the embedded static catalog when no cache exists, so first open remains instant and later opens can use the memory-mapped cache path.
- `crates/shadcn_ui_panel/src/shadcn_ui_panel.rs` now has distinct generated thumbnails for more common shadcn components, including badges, avatars, tabs, tables, checkboxes, dialogs, menus, accordions, and sidebars.
- `crates/shadcn_ui_panel/src/shadcn_ui_panel.rs` now has richer generated thumbnails and WebPreview demo shells for form, menu, feedback, loading, calendar, navigation, scroll, switch, slider, progress, and tooltip-style components.
- `crates/shadcn_ui_panel/src/shadcn_ui_panel.rs` now labels each row by source family (`shadcn`, `block`, `magic-ui`, `registry`, or `21st`) beside the category, keeping rows informative without long marketing descriptions.

NOTES:
- Ran `rustfmt` on the touched icon, media, and UI panel files.
- Ran `git diff --check -- crates\media_panel\src\media_panel.rs crates\shadcn_ui_panel\src\shadcn_ui_panel.rs`; no whitespace errors were reported.
