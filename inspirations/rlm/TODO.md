# TODO

These are future expansion items, not current release blockers.

- Add first-class JSON or typed structured output helpers on top of `FINAL(...)`.
- Add provider-specific adapters for non-OpenAI-compatible APIs if needed.
- Add richer evidence capture from executed Rhai snippets.
- Add benchmark fixtures for codebases, transcripts, and API reference docs.
- Add host-level integration fixtures for Zed, Codex, ZeroClaw, and DX adapters.
